openapi: 3.1.0
info:
  title: Voice AI Platform API
  description: Production-ready Voice AI Platform for building intelligent voice assistants
  version: 1.0.0
tags:
  - name: Assistant
  - name: Tool
  - name: File
  - name: Knowledge Base
  - name: Phone Number
  - name: SIP Trunk
  - name: Call
  - name: Monitor
  - name: Messaging - Channels
  - name: Messaging - Templates
  - name: Messaging - Messages
  - name: Messaging - Batches
  - name: Messaging - Conversations
  - name: Messaging - Contacts
  - name: Messaging - Quick Replies
  - name: Messaging - Opt-outs
  - name: Messaging - Consent
  - name: Messaging - Webhook Deliveries
  - name: Messaging - Media
  - name: Widget - Admin
  - name: Folders
  - name: Webhooks
  - name: Billing - Invoices
  - name: Chat
  - name: Messaging
paths:
  /assistant:
    get:
      tags:
        - Assistant
      summary: List Assistants
      description: |-
        List assistants in your workspace.

        Returns the assistants associated with your API key, with
        pagination, name search, multi-field filtering, and sort.
        Soft-deleted assistants are excluded.

        The ``language`` filter spans both modes — it matches an assistant
        whose pipeline ``transcriber.language`` *or* realtime ``model.language``
        equals the value, so a single filter call works regardless of whether
        you're searching pipeline or realtime configs. ``folderId="null"`` (the
        literal string) lists only unfiled assistants; omitting the parameter
        lists everything regardless of folder.
      operationId: listAssistants
      parameters:
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            description: Number of assistants to skip — pair with ``limit`` to paginate.
            default: 0
            title: Skip
          description: Number of assistants to skip — pair with ``limit`` to paginate.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 1000
            minimum: 1
            description: Maximum number of assistants to return per page (1–1000).
            default: 100
            title: Limit
          description: Maximum number of assistants to return per page (1–1000).
        - name: search
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                maxLength: 200
              - type: 'null'
            description: Case-insensitive substring match on the assistant name.
            title: Search
          description: Case-insensitive substring match on the assistant name.
        - name: type
          in: query
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/AssistantType'
              - type: 'null'
            description: >-
              Filter by mode — ``pipeline`` (separate STT/LLM/TTS) or
              ``realtime`` (end-to-end audio model).
            title: Type
          description: >-
            Filter by mode — ``pipeline`` (separate STT/LLM/TTS) or ``realtime``
            (end-to-end audio model).
        - name: llmProvider
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Filter by ``model.provider`` (e.g. ``openai``, ``anthropic``,
              ``google-realtime``).
            title: Llmprovider
          description: >-
            Filter by ``model.provider`` (e.g. ``openai``, ``anthropic``,
            ``google-realtime``).
        - name: sttProvider
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by ``transcriber.provider`` (pipeline assistants only).
            title: Sttprovider
          description: Filter by ``transcriber.provider`` (pipeline assistants only).
        - name: ttsProvider
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by ``voice.provider`` (pipeline assistants only).
            title: Ttsprovider
          description: Filter by ``voice.provider`` (pipeline assistants only).
        - name: language
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Filter by language code. Matches either ``transcriber.language``
              (pipeline) or ``model.language`` (realtime) so a single filter
              value works across both modes.
            title: Language
          description: >-
            Filter by language code. Matches either ``transcriber.language``
            (pipeline) or ``model.language`` (realtime) so a single filter value
            works across both modes.
        - name: folderId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Filter by folder membership. Pass a folder id to list only its
              contents. Pass the sentinel ``null`` to list assistants that are
              not in any folder. Omit the parameter to list all assistants
              regardless of folder.
            title: Folderid
          description: >-
            Filter by folder membership. Pass a folder id to list only its
            contents. Pass the sentinel ``null`` to list assistants that are not
            in any folder. Omit the parameter to list all assistants regardless
            of folder.
        - name: sortBy
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/AssistantSortBy'
            description: 'Sort field: ``createdAt``, ``updatedAt``, or ``name``.'
            default: createdAt
          description: 'Sort field: ``createdAt``, ``updatedAt``, or ``name``.'
        - name: sortOrder
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/SortOrder'
            description: >-
              Sort direction — ``asc`` (oldest / A→Z first) or ``desc`` (newest
              / Z→A first).
            default: desc
          description: >-
            Sort direction — ``asc`` (oldest / A→Z first) or ``desc`` (newest /
            Z→A first).
        - name: createdAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only assistants created **after** this timestamp (ISO 8601).
            title: Createdatgt
          description: Only assistants created **after** this timestamp (ISO 8601).
        - name: createdAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only assistants created **before** this timestamp (ISO 8601).
            title: Createdatlt
          description: Only assistants created **before** this timestamp (ISO 8601).
        - name: createdAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only assistants created at or after this timestamp (ISO 8601).
            title: Createdatge
          description: Only assistants created at or after this timestamp (ISO 8601).
        - name: createdAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only assistants created at or before this timestamp (ISO 8601).
            title: Createdatle
          description: Only assistants created at or before this timestamp (ISO 8601).
        - name: updatedAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only assistants last updated **after** this timestamp (ISO 8601).
            title: Updatedatgt
          description: Only assistants last updated **after** this timestamp (ISO 8601).
        - name: updatedAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only assistants last updated **before** this timestamp (ISO 8601).
            title: Updatedatlt
          description: Only assistants last updated **before** this timestamp (ISO 8601).
        - name: updatedAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only assistants last updated at or after this timestamp (ISO
              8601).
            title: Updatedatge
          description: Only assistants last updated at or after this timestamp (ISO 8601).
        - name: updatedAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only assistants last updated at or before this timestamp (ISO
              8601).
            title: Updatedatle
          description: Only assistants last updated at or before this timestamp (ISO 8601).
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_PaginatedAssistantResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Assistant
      summary: Create Assistant
      description: |-
        Create a new assistant.

        The request body is a discriminated union on ``type``: pick
        ``pipeline`` for separate STT → LLM → TTS components, or
        ``realtime`` for an end-to-end audio model. Each variant exposes
        only the fields valid for that mode — pipeline requires ``voice``
        and ``transcriber``; realtime accepts neither (the realtime model
        handles audio I/O directly).

        Tool, knowledge-base, and folder references are validated against
        your workspace before the assistant is persisted —
        referencing a deleted or out-of-scope id fails with ``400`` rather
        than silently dropping the link. ``tools`` accepts string ids
        (existing tools created via ``POST /tool``) and / or inline tool
        objects (one-off definitions that are attached to this assistant
        only and aren't written to the tools collection).

        Returns the full assistant with the server-assigned ``id`` and
        audit timestamps, ready to be referenced by a phone number, an
        inbound dispatch rule, or an outbound ``POST /call``.
      operationId: createAssistant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/PipelineAssistantCreate-Input'
                - $ref: '#/components/schemas/RealtimeAssistantCreate-Input'
              discriminator:
                propertyName: type
                mapping:
                  pipeline: '#/components/schemas/PipelineAssistantCreate-Input'
                  realtime: '#/components/schemas/RealtimeAssistantCreate-Input'
              title: Data
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_Annotated_Union_PipelineAssistantResponse__RealtimeAssistantResponse___FieldInfo_annotation_NoneType__required_True__discriminator__type____
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /assistant/{assistantId}:
    get:
      tags:
        - Assistant
      summary: Get Assistant
      description: |-
        Retrieve a single assistant by id.

        Returns the full assistant configuration. The response is a
        discriminated union on ``type`` — ``PipelineAssistantResponse`` or
        ``RealtimeAssistantResponse`` — so each shape contains only the
        fields relevant to its mode (pipeline includes ``voice`` and
        ``transcriber``; realtime omits them).

        Returns ``404`` if the id doesn't exist, has been soft-deleted, or
        isn't accessible to your API key.
      operationId: getAssistant
      parameters:
        - name: assistantId
          in: path
          required: true
          schema:
            type: string
            description: >-
              The assistant's unique id, returned from ``POST /assistant`` or
              ``GET /assistant``.
            title: Assistantid
          description: >-
            The assistant's unique id, returned from ``POST /assistant`` or
            ``GET /assistant``.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_Annotated_Union_PipelineAssistantResponse__RealtimeAssistantResponse___FieldInfo_annotation_NoneType__required_True__discriminator__type____
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Assistant
      summary: Update Assistant
      description: |-
        Update an assistant — partial PATCH semantics.

        Only fields included in the request body are written; omitted
        fields are left unchanged. Pass an explicit ``null`` to clear a
        nullable field. Tool, knowledge-base, and folder references are
        re-validated for any field actually present in the body.

        **Mode change.** Setting ``type`` to a different mode triggers
        auto-cleanup so the persisted document stays consistent:
        pipeline → realtime drops ``voice`` and ``transcriber``;
        realtime → pipeline strips realtime-only options from the stored
        ``model``. After the merge, mode-specific invariants are re-checked
        (model provider matches ``type``, pipeline requires
        ``voice`` + ``transcriber``, realtime caps the system prompt at
        8 000 characters) — violations return ``400``.

        **Webhook secret.** Patching ``server`` while omitting ``secret``
        preserves the existing signing key, so a UI doing read-modify-write
        can submit without round-tripping the secret. Send an explicit
        ``"secret": null`` to clear it.

        **Move-only updates.** A PATCH that changes only ``folderId`` is
        treated as a reshelving operation — ``updatedAt`` and ``updatedBy``
        are deliberately not bumped, so "recently updated" sort and the
        relative-time label in the UI keep reflecting the last real config
        change.
      operationId: updateAssistant
      parameters:
        - name: assistantId
          in: path
          required: true
          schema:
            type: string
            description: The assistant's unique id.
            title: Assistantid
          description: The assistant's unique id.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssistantUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_Annotated_Union_PipelineAssistantResponse__RealtimeAssistantResponse___FieldInfo_annotation_NoneType__required_True__discriminator__type____
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Assistant
      summary: Delete Assistant
      description: |-
        Soft-delete an assistant.

        Marks the assistant as deleted (``isDeleted=true``). It stops
        appearing in list / get responses, and new calls cannot be routed
        to it. The underlying document is retained so the deletion can be
        reversed by a platform operator if needed.

        **Cascade.** Phone numbers whose ``assistantId`` pointed at this
        assistant have that link cleared, so inbound calls on those numbers
        no longer try to load a non-existent assistant at ringing time.
        The phone numbers themselves are not deleted — re-link them to a
        different assistant via ``PATCH /phone-number/{id}`` to receive
        inbound calls again.

        Returns ``404`` if the id doesn't exist, has already been
        soft-deleted, or isn't accessible to your API key.
      operationId: deleteAssistant
      parameters:
        - name: assistantId
          in: path
          required: true
          schema:
            type: string
            description: The assistant's unique id.
            title: Assistantid
          description: The assistant's unique id.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_NoneType_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /assistant/{assistantId}/versions:
    get:
      tags:
        - Assistant
      summary: List versions
      description: |-
        List version history for an assistant, newest first.

        Returns version metadata only (id, number, label, pin, author,
        timestamp). The full snapshot of any individual version is fetched
        separately via ``GET /assistant/{id}/versions/{versionId}`` — this
        keeps list responses small even for resources with hundreds of
        versions.

        Version docs are preserved indefinitely for both active and
        soft-deleted assistants; this endpoint exposes only the active
        case (404 when the parent is soft-deleted).
      operationId: listAssistantVersions
      parameters:
        - name: assistantId
          in: path
          required: true
          schema:
            type: string
            description: The assistant's unique id.
            title: Assistantid
          description: The assistant's unique id.
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
            title: Skip
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
        - name: pinnedOnly
          in: query
          required: false
          schema:
            type: boolean
            description: When ``true``, return only versions the user has pinned.
            default: false
            title: Pinnedonly
          description: When ``true``, return only versions the user has pinned.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedResourceVersionsResponse_
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /assistant/{assistantId}/versions/{versionId}:
    get:
      tags:
        - Assistant
      summary: Get Assistant Version
      description: |-
        Get a single assistant version with its full snapshot payload.

        Used by the diff view and the restore preview — the snapshot is
        the assistant doc as it existed at the moment the version was
        saved, suitable for direct comparison against the current state.
      operationId: getAssistantVersion
      parameters:
        - name: assistantId
          in: path
          required: true
          schema:
            type: string
            title: Assistantid
        - name: versionId
          in: path
          required: true
          schema:
            type: string
            description: The version's unique id.
            title: Versionid
          description: The version's unique id.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ResourceVersionResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Assistant
      summary: Update version
      description: |-
        Update the editable metadata on a version.

        Only ``label`` / ``note`` / ``pinned`` are mutable — the snapshot
        itself is immutable. To "restore" a previous state, the UI loads
        the snapshot back into the editor form (``GET /versions/{vId}``)
        and the user saves normally; the existing PATCH path's
        auto-version hook records the new state as the next version.
      operationId: updateAssistantVersion
      parameters:
        - name: assistantId
          in: path
          required: true
          schema:
            type: string
            title: Assistantid
        - name: versionId
          in: path
          required: true
          schema:
            type: string
            title: Versionid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VersionUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ResourceVersionResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /folders:
    get:
      tags:
        - Folders
      summary: List Folders
      description: |-
        List every folder of ``resourceType`` for the caller's workspace.

        Folder counts are computed live via aggregation so a caller can
        rely on ``itemCount`` for a badge without a second call. Results
        are sorted newest first — matching the default sort on the
        underlying resource list pages.

        Permission gating is derived from ``resourceType``: a caller needs
        the matching ``<resourceType>.view`` permission (e.g. ``tool.view``
        for ``resourceType=tool``) — folders do not have a dedicated
        permission domain.
      operationId: listFolders
      parameters:
        - name: resourceType
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/FolderResourceType'
            description: Resource family to list folders for.
          description: Resource family to list folders for.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_FolderListResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Folders
      summary: Create Folder
      description: Create a new folder. Returns the fresh folder with ``itemCount=0``.
      operationId: createFolder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FolderCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_FolderResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /folders/{folderId}:
    get:
      tags:
        - Folders
      summary: Get Folder
      description: Fetch a single folder by id, including its live item count.
      operationId: getFolder
      parameters:
        - name: folderId
          in: path
          required: true
          schema:
            type: string
            description: ID of the folder.
            title: Folderid
          description: ID of the folder.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_FolderResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Folders
      summary: Update Folder
      description: |-
        Update a folder's ``name`` in place.

        ``resourceType`` is deliberately immutable — changing it would
        orphan every child that still points at this folder. Callers that
        want to move contents between resource types should create a new
        folder and reassign the children explicitly.
      operationId: updateFolder
      parameters:
        - name: folderId
          in: path
          required: true
          schema:
            type: string
            description: ID of the folder to update.
            title: Folderid
          description: ID of the folder to update.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FolderUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_FolderResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Folders
      summary: Delete Folder
      description: |-
        Delete a folder, optionally cascading to its contents.

        The response carries counts for both paths so the caller can show
        the right wording ("3 assistants moved to root" vs "3 assistants
        deleted along with the folder") without a second round-trip.
      operationId: deleteFolder
      parameters:
        - name: folderId
          in: path
          required: true
          schema:
            type: string
            description: ID of the folder to delete.
            title: Folderid
          description: ID of the folder to delete.
        - name: cascade
          in: query
          required: false
          schema:
            type: boolean
            description: >-
              If ``true``, every child resource in the folder is also deleted.
              If ``false`` (default), children are detached (their ``folderId``
              is cleared) and otherwise left alone.
            default: false
            title: Cascade
          description: >-
            If ``true``, every child resource in the folder is also deleted. If
            ``false`` (default), children are detached (their ``folderId`` is
            cleared) and otherwise left alone.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_FolderDeleteResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /phone-number:
    get:
      tags:
        - Phone Number
      summary: List Phone Numbers
      description: |-
        List phone numbers in your workspace.

        Supports name / number search, provider filter, assistant filter,
        SIP-trunk filter (BYO numbers only), folder scoping, sort by
        ``createdAt`` / ``updatedAt`` / ``name`` / ``number``, and
        inclusive or exclusive date-range bounds. Use ``assigned`` to
        narrow to numbers with (or without) a default assistant or a
        dynamic-selection webhook. Soft-deleted numbers are excluded.
      operationId: listPhoneNumbers
      parameters:
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            description: Number of phone numbers to skip for pagination.
            default: 0
            title: Skip
          description: Number of phone numbers to skip for pagination.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 1000
            minimum: 1
            description: Maximum phone numbers to return per page.
            default: 100
            title: Limit
          description: Maximum phone numbers to return per page.
        - name: search
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                maxLength: 200
              - type: 'null'
            description: Case-insensitive substring match on phone number or display name.
            title: Search
          description: Case-insensitive substring match on phone number or display name.
        - name: provider
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - twilio
                  - byo-sip
                type: string
              - type: 'null'
            description: Filter by provider.
            title: Provider
          description: Filter by provider.
        - name: assistantId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by default inbound assistant.
            title: Assistantid
          description: Filter by default inbound assistant.
        - name: assigned
          in: query
          required: false
          schema:
            anyOf:
              - type: boolean
              - type: 'null'
            description: >-
              Filter by assignment. ``true`` returns numbers with either a
              default ``assistantId`` or a dynamic-selection webhook. ``false``
              returns numbers with neither.
            title: Assigned
          description: >-
            Filter by assignment. ``true`` returns numbers with either a default
            ``assistantId`` or a dynamic-selection webhook. ``false`` returns
            numbers with neither.
        - name: sipTrunkId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by BYO SIP trunk (ignored for Twilio numbers).
            title: Siptrunkid
          description: Filter by BYO SIP trunk (ignored for Twilio numbers).
        - name: folderId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Filter by folder membership. Pass a folder id to list only its
              contents. Pass the sentinel ``null`` to list numbers that are not
              in any folder. Omit to include numbers from every folder.
            title: Folderid
          description: >-
            Filter by folder membership. Pass a folder id to list only its
            contents. Pass the sentinel ``null`` to list numbers that are not in
            any folder. Omit to include numbers from every folder.
        - name: sortBy
          in: query
          required: false
          schema:
            enum:
              - createdAt
              - updatedAt
              - name
              - number
            type: string
            description: Field to sort by.
            default: createdAt
            title: Sortby
          description: Field to sort by.
        - name: sortOrder
          in: query
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            description: Sort direction.
            default: desc
            title: Sortorder
          description: Sort direction.
        - name: createdAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return phone numbers created strictly after this timestamp
              (exclusive).
            title: Createdatgt
          description: >-
            Only return phone numbers created strictly after this timestamp
            (exclusive).
        - name: createdAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return phone numbers created strictly before this timestamp
              (exclusive).
            title: Createdatlt
          description: >-
            Only return phone numbers created strictly before this timestamp
            (exclusive).
        - name: createdAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return phone numbers created on or after this timestamp
              (inclusive).
            title: Createdatge
          description: >-
            Only return phone numbers created on or after this timestamp
            (inclusive).
        - name: createdAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return phone numbers created on or before this timestamp
              (inclusive).
            title: Createdatle
          description: >-
            Only return phone numbers created on or before this timestamp
            (inclusive).
        - name: updatedAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return phone numbers last updated strictly after this
              timestamp (exclusive).
            title: Updatedatgt
          description: >-
            Only return phone numbers last updated strictly after this timestamp
            (exclusive).
        - name: updatedAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return phone numbers last updated strictly before this
              timestamp (exclusive).
            title: Updatedatlt
          description: >-
            Only return phone numbers last updated strictly before this
            timestamp (exclusive).
        - name: updatedAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return phone numbers last updated on or after this timestamp
              (inclusive).
            title: Updatedatge
          description: >-
            Only return phone numbers last updated on or after this timestamp
            (inclusive).
        - name: updatedAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return phone numbers last updated on or before this timestamp
              (inclusive).
            title: Updatedatle
          description: >-
            Only return phone numbers last updated on or before this timestamp
            (inclusive).
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_PaginatedPhoneNumberResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Phone Number
      summary: Create Phone Number
      description: |-
        Provision a phone number for your workspace.

        Two provider subtypes are accepted, discriminated by ``provider``:

        - ``twilio`` — caller supplies their Twilio account SID + auth
          token. The backend provisions a Twilio Elastic SIP Trunk +
          credential list and creates the matching inbound / outbound
          routing.
        - ``byo-sip`` — caller references an existing ``SipTrunk``
          document; the backend creates only the inbound / outbound
          routing on top of that trunk.

        Returns ``status: "active"`` once provisioning succeeds. Returns
        409 if the number is already imported into this workspace.
      operationId: createPhoneNumber
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/TwilioPhoneNumber'
                - $ref: '#/components/schemas/ByoSipPhoneNumber'
              discriminator:
                propertyName: provider
                mapping:
                  twilio: '#/components/schemas/TwilioPhoneNumber'
                  byo-sip: '#/components/schemas/ByoSipPhoneNumber'
              title: Data
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_Annotated_Union_TwilioPhoneNumberResponse__ByoSipPhoneNumberResponse___FieldInfo_annotation_NoneType__required_True__discriminator__provider____
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /phone-number/{phoneNumberId}:
    get:
      tags:
        - Phone Number
      summary: Get Phone Number
      description: |-
        Return a single phone number with its provider variant, routing
        config, and lifecycle status. Twilio auth tokens are masked.
      operationId: getPhoneNumber
      parameters:
        - name: phoneNumberId
          in: path
          required: true
          schema:
            type: string
            description: ID of the phone number.
            title: Phonenumberid
          description: ID of the phone number.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_Annotated_Union_TwilioPhoneNumberResponse__ByoSipPhoneNumberResponse___FieldInfo_annotation_NoneType__required_True__discriminator__provider____
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Phone Number
      summary: Update Phone Number
      description: |-
        Update a phone number.

        Partial PATCH — only the fields you send are applied. The
        ``provider`` is immutable; switch trunks by deleting and recreating.
        Twilio credential rotation (``twilioAccountSid`` /
        ``twilioAuthToken``) is silently ignored on BYO numbers. Changes
        that affect routing — ``assistantId`` or ``server`` — take effect
        on the next inbound call.
      operationId: updatePhoneNumber
      parameters:
        - name: phoneNumberId
          in: path
          required: true
          schema:
            type: string
            description: ID of the phone number to update.
            title: Phonenumberid
          description: ID of the phone number to update.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PhoneNumberUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_Annotated_Union_TwilioPhoneNumberResponse__ByoSipPhoneNumberResponse___FieldInfo_annotation_NoneType__required_True__discriminator__provider____
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Phone Number
      summary: Delete Phone Number
      description: |-
        Delete a phone number and release every provisioned resource.

        Cleans up in reverse provisioning order: the inbound / outbound
        routing rules first, then the Twilio Elastic SIP Trunk and
        credential list (for Twilio-provider numbers). The number is
        marked deleted; any calls that still reference its id keep
        working for audit.
      operationId: deletePhoneNumber
      parameters:
        - name: phoneNumberId
          in: path
          required: true
          schema:
            type: string
            description: ID of the phone number to delete.
            title: Phonenumberid
          description: ID of the phone number to delete.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /sip-trunk:
    get:
      tags:
        - SIP Trunk
      summary: List SIP Trunks
      description: |-
        List SIP trunks in your workspace.

        Supports name search, registration filters (enabled flag and live
        status reported by the SIP register sidecar), connectivity-scope
        filter, sort by ``createdAt`` / ``updatedAt`` / ``name``, and
        inclusive or exclusive date-range bounds. Soft-deleted trunks are
        excluded.
      operationId: listSipTrunks
      parameters:
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            description: Number of trunks to skip for pagination.
            default: 0
            title: Skip
          description: Number of trunks to skip for pagination.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 1000
            minimum: 1
            description: Maximum trunks to return per page.
            default: 100
            title: Limit
          description: Maximum trunks to return per page.
        - name: search
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Case-insensitive substring match on the trunk name.
            title: Search
          description: Case-insensitive substring match on the trunk name.
        - name: registrationEnabled
          in: query
          required: false
          schema:
            anyOf:
              - type: boolean
              - type: 'null'
            description: Filter by whether SIP REGISTER is enabled on the trunk.
            title: Registrationenabled
          description: Filter by whether SIP REGISTER is enabled on the trunk.
        - name: registrationStatus
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - registered
                  - failed
                  - unregistered
                  - pending
                type: string
              - type: 'null'
            description: >-
              Filter by current REGISTER status reported by the SIP register
              sidecar.
            title: Registrationstatus
          description: >-
            Filter by current REGISTER status reported by the SIP register
            sidecar.
        - name: scope
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - public
                  - vpn
                type: string
              - type: 'null'
            description: >-
              Filter by connectivity scope. ``public`` — trunks routed via the
              public media endpoint. ``vpn`` — trunks routed through your
              dedicated IPSec tunnel.
            title: Scope
          description: >-
            Filter by connectivity scope. ``public`` — trunks routed via the
            public media endpoint. ``vpn`` — trunks routed through your
            dedicated IPSec tunnel.
        - name: sortBy
          in: query
          required: false
          schema:
            enum:
              - createdAt
              - updatedAt
              - name
            type: string
            description: Field to sort by.
            default: createdAt
            title: Sortby
          description: Field to sort by.
        - name: sortOrder
          in: query
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            description: Sort direction.
            default: desc
            title: Sortorder
          description: Sort direction.
        - name: createdAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return trunks created strictly after this timestamp
              (exclusive).
            title: Createdatgt
          description: >-
            Only return trunks created strictly after this timestamp
            (exclusive).
        - name: createdAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return trunks created strictly before this timestamp
              (exclusive).
            title: Createdatlt
          description: >-
            Only return trunks created strictly before this timestamp
            (exclusive).
        - name: createdAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only return trunks created on or after this timestamp (inclusive).
            title: Createdatge
          description: Only return trunks created on or after this timestamp (inclusive).
        - name: createdAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return trunks created on or before this timestamp
              (inclusive).
            title: Createdatle
          description: Only return trunks created on or before this timestamp (inclusive).
        - name: updatedAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return trunks last updated strictly after this timestamp
              (exclusive).
            title: Updatedatgt
          description: >-
            Only return trunks last updated strictly after this timestamp
            (exclusive).
        - name: updatedAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return trunks last updated strictly before this timestamp
              (exclusive).
            title: Updatedatlt
          description: >-
            Only return trunks last updated strictly before this timestamp
            (exclusive).
        - name: updatedAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return trunks last updated on or after this timestamp
              (inclusive).
            title: Updatedatge
          description: >-
            Only return trunks last updated on or after this timestamp
            (inclusive).
        - name: updatedAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return trunks last updated on or before this timestamp
              (inclusive).
            title: Updatedatle
          description: >-
            Only return trunks last updated on or before this timestamp
            (inclusive).
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_PaginatedSipTrunkResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - SIP Trunk
      summary: Create a SIP Trunk
      description: |-
        Create a new SIP trunk.

        Stores the gateway list, authentication, and policy. The actual
        inbound / outbound routing is wired up when a phone number is
        linked to this trunk. Cross-field rules (TLS gating, REGISTER
        requires auth) are enforced by the schema.

        When ``scope=vpn``: your workspace must be entitled AND
        provisioned for private IPSec VPN — otherwise the trunk would
        have no bridge that can serve it.
      operationId: createSipTrunk
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SipTrunkCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_SipTrunkResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /sip-trunk/{trunkId}:
    get:
      tags:
        - SIP Trunk
      summary: Get SIP Trunk
      description: |-
        Return a single SIP trunk with its gateway list, header policy,
        REGISTER mode, and live registration status. The encrypted
        ``authPassword`` is never returned.
      operationId: getSipTrunk
      parameters:
        - name: trunkId
          in: path
          required: true
          schema:
            type: string
            description: ID of the SIP trunk.
            title: Trunkid
          description: ID of the SIP trunk.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_SipTrunkResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - SIP Trunk
      summary: Update a SIP Trunk
      description: |-
        Update a SIP trunk and propagate the edit to every linked phone number.

        Partial PATCH — only the fields you send are applied. Schema
        validators enforce transport enums, TLS gating, ``includeHeaders``
        values, and ``registrationServer`` format. The merged document is
        re-checked for the REGISTER-requires-auth rule.

        The response carries a ``sync`` block with per-phone-number
        outcomes — the trunk's current desired state is re-pushed to each
        linked phone number's inbound + outbound routing so changes to
        gateways, auth, header policy, or REGISTER mode reach traffic
        immediately. Cosmetic edits (name only) return ``sync: null`` and
        do not touch any phone-number routing.
      operationId: updateSipTrunk
      parameters:
        - name: trunkId
          in: path
          required: true
          schema:
            type: string
            description: ID of the SIP trunk to update.
            title: Trunkid
          description: ID of the SIP trunk to update.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SipTrunkUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_SipTrunkUpdateResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - SIP Trunk
      summary: Delete a SIP Trunk
      description: |-
        Delete a SIP trunk.

        Refuses with 409 if any phone numbers still reference the trunk —
        detach or delete those phone numbers first. The action is not
        reversible.
      operationId: deleteSipTrunk
      parameters:
        - name: trunkId
          in: path
          required: true
          schema:
            type: string
            description: ID of the SIP trunk to delete.
            title: Trunkid
          description: ID of the SIP trunk to delete.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /sip-trunk/{trunkId}/credentials:
    delete:
      tags:
        - SIP Trunk
      summary: Clear credentials
      description: >-
        Clear the digest-auth credentials (``authUsername`` and
        ``authPassword``).


        Dedicated endpoint so credentials can be wiped explicitly without

        the ambiguity of a PATCH with an empty string. Refuses with 400

        when SIP REGISTER is enabled — disable REGISTER first, since it

        cannot authenticate without a credential pair.


        The change is propagated to every linked phone number's routing

        in the same response (under ``sync``), so callers see immediate

        confirmation that the credential rotation took effect.
      operationId: clearSipTrunkCredentials
      parameters:
        - name: trunkId
          in: path
          required: true
          schema:
            type: string
            description: ID of the SIP trunk to clear credentials on.
            title: Trunkid
          description: ID of the SIP trunk to clear credentials on.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_SipTrunkUpdateResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /tool:
    get:
      tags:
        - Tool
      summary: List Tools
      description: List tools with pagination, search, folder scoping, and sort.
      operationId: listTools
      parameters:
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
            title: Skip
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 1000
            minimum: 1
            default: 100
            title: Limit
        - name: type
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Type
        - name: search
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Case-insensitive substring match on the tool name.
            title: Search
          description: Case-insensitive substring match on the tool name.
        - name: folderId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Filter by folder. Pass ``"null"`` to return only unfiled tools
              (list root). Omit to include tools from every folder.
            title: Folderid
          description: >-
            Filter by folder. Pass ``"null"`` to return only unfiled tools (list
            root). Omit to include tools from every folder.
        - name: sortBy
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: 'Field to sort by: ``createdAt``, ``updatedAt``, or ``name``.'
            default: createdAt
            title: Sortby
          description: 'Field to sort by: ``createdAt``, ``updatedAt``, or ``name``.'
        - name: sortOrder
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: '``asc`` or ``desc``.'
            default: desc
            title: Sortorder
          description: '``asc`` or ``desc``.'
        - name: createdAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Createdatgt
        - name: createdAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Createdatlt
        - name: createdAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Createdatge
        - name: createdAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Createdatle
        - name: updatedAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Updatedatgt
        - name: updatedAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Updatedatlt
        - name: updatedAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Updatedatge
        - name: updatedAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Updatedatle
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_PaginatedToolResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Tool
      summary: Create Tool
      description: |-
        Create a tool of any supported type.

        The request body is one of: ``FunctionTool``, ``EndCallTool``,
        ``TransferCallTool``, ``VoicemailTool``, ``LiveEscalateTool`` —
        discriminated by the ``type`` field.
      operationId: createTool
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/FunctionTool-Input'
                - $ref: '#/components/schemas/EndCallTool-Input'
                - $ref: '#/components/schemas/TransferCallTool-Input'
                - $ref: '#/components/schemas/VoicemailTool-Input'
                - $ref: '#/components/schemas/LiveEscalateTool-Input'
              discriminator:
                propertyName: type
                mapping:
                  function: '#/components/schemas/FunctionTool-Input'
                  endCall: '#/components/schemas/EndCallTool-Input'
                  transferCall: '#/components/schemas/TransferCallTool-Input'
                  voicemail: '#/components/schemas/VoicemailTool-Input'
                  liveEscalate: '#/components/schemas/LiveEscalateTool-Input'
              title: Data
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_Annotated_Union_FunctionToolResponse__EndCallToolResponse__TransferCallToolResponse__VoicemailToolResponse__LiveEscalateToolResponse___FieldInfo_annotation_NoneType__required_True__discriminator__type____
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /tool/{toolId}:
    get:
      tags:
        - Tool
      summary: Get Tool
      description: Get a tool by id.
      operationId: getTool
      parameters:
        - name: toolId
          in: path
          required: true
          schema:
            type: string
            title: Toolid
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_Annotated_Union_FunctionToolResponse__EndCallToolResponse__TransferCallToolResponse__VoicemailToolResponse__LiveEscalateToolResponse___FieldInfo_annotation_NoneType__required_True__discriminator__type____
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Tool
      summary: Update Tool
      description: |-
        Update a tool — patch fields on an existing tool of the same type.

        The body must carry the same ``type`` as the stored tool — it pins
        the right variant for partial validation. Cross-type edits aren't
        supported; delete and recreate instead.
      operationId: updateTool
      parameters:
        - name: toolId
          in: path
          required: true
          schema:
            type: string
            title: Toolid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/FunctionToolUpdate'
                - $ref: '#/components/schemas/EndCallToolUpdate'
                - $ref: '#/components/schemas/TransferCallToolUpdate'
                - $ref: '#/components/schemas/VoicemailToolUpdate'
                - $ref: '#/components/schemas/LiveEscalateToolUpdate'
              discriminator:
                propertyName: type
                mapping:
                  function: '#/components/schemas/FunctionToolUpdate'
                  endCall: '#/components/schemas/EndCallToolUpdate'
                  transferCall: '#/components/schemas/TransferCallToolUpdate'
                  voicemail: '#/components/schemas/VoicemailToolUpdate'
                  liveEscalate: '#/components/schemas/LiveEscalateToolUpdate'
              title: Data
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_Annotated_Union_FunctionToolResponse__EndCallToolResponse__TransferCallToolResponse__VoicemailToolResponse__LiveEscalateToolResponse___FieldInfo_annotation_NoneType__required_True__discriminator__type____
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Tool
      summary: Delete Tool
      description: >-
        Soft-delete a tool and detach it from every assistant that references
        it.


        The agent's tool loader skips soft-deleted tools on read, but leaving

        the dangling id in ``assistant.tools`` keeps clutter around forever

        and confuses the UI. We ``$pull`` the id from every affected

        assistant and invalidate each assistant's config cache so the next

        call picks up the pruned list immediately.
      operationId: deleteTool
      parameters:
        - name: toolId
          in: path
          required: true
          schema:
            type: string
            title: Toolid
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /tool/{toolId}/versions:
    get:
      tags:
        - Tool
      summary: List Tool Versions
      description: |-
        List version history for a tool, newest first.

        Returns version metadata only — fetch the full snapshot of any
        individual version via ``GET /tool/{id}/versions/{versionId}``.
      operationId: listToolVersions
      parameters:
        - name: toolId
          in: path
          required: true
          schema:
            type: string
            description: The tool's unique id.
            title: Toolid
          description: The tool's unique id.
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
            title: Skip
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
        - name: pinnedOnly
          in: query
          required: false
          schema:
            type: boolean
            description: When ``true``, return only versions the user has pinned.
            default: false
            title: Pinnedonly
          description: When ``true``, return only versions the user has pinned.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedResourceVersionsResponse_
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /tool/{toolId}/versions/{versionId}:
    get:
      tags:
        - Tool
      summary: Get Tool Version
      description: Get a single tool version with its full snapshot payload.
      operationId: getToolVersion
      parameters:
        - name: toolId
          in: path
          required: true
          schema:
            type: string
            title: Toolid
        - name: versionId
          in: path
          required: true
          schema:
            type: string
            description: The version's unique id.
            title: Versionid
          description: The version's unique id.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ResourceVersionResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Tool
      summary: Update version
      description: Update label / note / pinned on a tool version. Snapshot is immutable.
      operationId: updateToolVersion
      parameters:
        - name: toolId
          in: path
          required: true
          schema:
            type: string
            title: Toolid
        - name: versionId
          in: path
          required: true
          schema:
            type: string
            title: Versionid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VersionUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ResourceVersionResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /call:
    get:
      tags:
        - Call
      summary: List Calls
      description: |-
        List calls in your workspace.

        Supports text search (id / name / customer name / customer number /
        transcript), filters by type, lifecycle status, ended reason,
        assistant, phone number, success bucket, recording presence,
        duration range, and inclusive or exclusive date-range bounds on
        both ``startedAt`` and ``createdAt``. Sort by ``createdAt`` /
        ``startedAt`` / ``durationSeconds``. Soft-deleted calls are
        excluded. All filters compose with AND semantics.
      operationId: listCalls
      parameters:
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            description: Number of calls to skip for pagination.
            default: 0
            title: Skip
          description: Number of calls to skip for pagination.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 100
            minimum: 1
            description: Maximum calls to return per page.
            default: 10
            title: Limit
          description: Maximum calls to return per page.
        - name: search
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                maxLength: 200
              - type: 'null'
            description: >-
              Case-insensitive substring match on customer name/number, id,
              name, or transcript.
            title: Search
          description: >-
            Case-insensitive substring match on customer name/number, id, name,
            or transcript.
        - name: type
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - inboundPhoneCall
                  - outboundPhoneCall
                  - webCall
                type: string
              - type: 'null'
            description: Filter by call type.
            title: Type
          description: Filter by call type.
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - queued
                  - ringing
                  - in-progress
                  - forwarding
                  - ended
                type: string
              - type: 'null'
            description: Filter by lifecycle status.
            title: Status
          description: Filter by lifecycle status.
        - name: endedReason
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - assistant-ended-call
                  - customer-ended-call
                  - silence-timed-out
                  - max-duration-reached
                  - assistant-error
                  - voicemail
                  - pipeline-error-llm-failed
                  - system-error
                  - agent-init-failed
                  - assistant-request-failed
                  - fallback-destination
                  - ringing-timeout
                  - no-answer
                  - call-forwarded
                  - human-takeover
                  - human-ended-call
                  - outbound-trunk-misconfigured
                  - outbound-sip-unavailable
                  - outbound-sip-rejected
                  - outbound-init-failed
                  - insufficient-balance
                  - balance-exhausted
                  - billing-suspended
                  - billing-misconfigured
                  - concurrency-cap-exceeded
                  - tier-not-allowed
                  - unknown
                type: string
              - type: 'null'
            description: Filter by why the call ended.
            title: Endedreason
          description: Filter by why the call ended.
        - name: assistantId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by assistant id.
            title: Assistantid
          description: Filter by assistant id.
        - name: phoneNumberId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by phone number id.
            title: Phonenumberid
          description: Filter by phone number id.
        - name: successEvaluation
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - positive
                  - negative
                type: string
              - type: 'null'
            description: >-
              Filter by post-call success bucket. ``positive`` matches
              success/pass/true/yes/positive; ``negative`` matches
              fail/false/no/negative/error.
            title: Successevaluation
          description: >-
            Filter by post-call success bucket. ``positive`` matches
            success/pass/true/yes/positive; ``negative`` matches
            fail/false/no/negative/error.
        - name: hasRecording
          in: query
          required: false
          schema:
            anyOf:
              - type: boolean
              - type: 'null'
            description: >-
              When true, only calls with at least one recording track. When
              false, only calls with no recording.
            title: Hasrecording
          description: >-
            When true, only calls with at least one recording track. When false,
            only calls with no recording.
        - name: minDurationSeconds
          in: query
          required: false
          schema:
            anyOf:
              - type: number
                minimum: 0
              - type: 'null'
            description: Minimum call duration in seconds (inclusive).
            title: Mindurationseconds
          description: Minimum call duration in seconds (inclusive).
        - name: maxDurationSeconds
          in: query
          required: false
          schema:
            anyOf:
              - type: number
                minimum: 0
              - type: 'null'
            description: Maximum call duration in seconds (inclusive).
            title: Maxdurationseconds
          description: Maximum call duration in seconds (inclusive).
        - name: startedAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return calls that started strictly after this timestamp
              (exclusive).
            title: Startedatgt
          description: >-
            Only return calls that started strictly after this timestamp
            (exclusive).
        - name: startedAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return calls that started strictly before this timestamp
              (exclusive).
            title: Startedatlt
          description: >-
            Only return calls that started strictly before this timestamp
            (exclusive).
        - name: startedAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return calls that started on or after this timestamp
              (inclusive).
            title: Startedatge
          description: >-
            Only return calls that started on or after this timestamp
            (inclusive).
        - name: startedAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return calls that started on or before this timestamp
              (inclusive).
            title: Startedatle
          description: >-
            Only return calls that started on or before this timestamp
            (inclusive).
        - name: createdAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return calls created strictly after this timestamp
              (exclusive).
            title: Createdatgt
          description: Only return calls created strictly after this timestamp (exclusive).
        - name: createdAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return calls created strictly before this timestamp
              (exclusive).
            title: Createdatlt
          description: >-
            Only return calls created strictly before this timestamp
            (exclusive).
        - name: createdAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only return calls created on or after this timestamp (inclusive).
            title: Createdatge
          description: Only return calls created on or after this timestamp (inclusive).
        - name: createdAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only return calls created on or before this timestamp (inclusive).
            title: Createdatle
          description: Only return calls created on or before this timestamp (inclusive).
        - name: sortBy
          in: query
          required: false
          schema:
            enum:
              - createdAt
              - startedAt
              - durationSeconds
              - billedAmountCents
            type: string
            description: Field to sort by.
            default: createdAt
            title: Sortby
          description: Field to sort by.
        - name: sortOrder
          in: query
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            description: Sort direction.
            default: desc
            title: Sortorder
          description: Sort direction.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_PaginatedCallResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Call
      summary: Create Call
      description: |-
        Create a new call.

        Every call needs an assistant — supply exactly one of
        ``assistantId`` or inline ``assistant``. Inline assistant bodies
        are ephemeral (not persisted) and apply to this call only.

        Two call types, both initiated by this endpoint:

        - **Outbound phone call** — set ``customer.number`` to the
          destination and supply either ``phoneNumberId`` or inline
          ``phoneNumber`` (the trunk to dial out from). The platform
          places the call; status starts at ``queued`` and progresses
          through ``ringing`` → ``in-progress`` → ``ended``.
        - **Web call** — omit ``customer.number`` (and any phone-number
          reference). The response carries ``transport.token`` +
          ``transport.url`` (WebRTC access credentials) that the client
          uses to connect to the call.

        ``overrides`` carries per-call deep-merge patches to the resolved
        assistant — handy for A/B testing or per-customer prompt tweaks
        without forking the assistant.
      operationId: createCall
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CallCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_CallResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /call/{callId}:
    get:
      tags:
        - Call
      summary: Get Call
      description: |-
        Return a single call.

        Includes the full transcript, timeline, post-call analysis,
        usage counts (Custom mode only), and freshly-resolved presigned
        download URLs for every recording track. Side-channel fields
        populated by the post-call worker (analysis, recording URLs,
        model usage) are present once the call has ended and processing
        has run.
      operationId: getCall
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the call.
            title: Callid
          description: ID of the call.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_CallResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Call
      summary: Update Call
      description: |-
        Update a call's display name or metadata.

        Only ``name`` and ``metadata`` are mutable through this endpoint —
        everything else (status, transcript, analysis, costs, recordings)
        is set by the platform. Passing ``metadata: null`` is rejected;
        send ``{}`` to clear instead.
      operationId: updateCall
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the call to update.
            title: Callid
          description: ID of the call to update.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CallUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_CallResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Call
      summary: Delete Call
      description: |-
        Delete a call from the visible workspace.

        The underlying record is retained for audit; once deleted, the
        call is excluded from list and detail reads. The action is not
        reversible from the API.
      operationId: deleteCall
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the call to delete.
            title: Callid
          description: ID of the call to delete.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_CallResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /call/{callId}/logs:
    get:
      tags:
        - Call
      summary: Get Call Log Archive
      description: |-
        Return the sanitized log stream for a call.

        The same feed the dashboard's Logs tab renders, as JSON.

        Archive-only: records are available once the call has ended and
        the post-call worker has flushed the permanent archive. The
        response carries one of four ``source`` states:

          * ``pending`` — call not yet ended or archive still being
            prepared; clients poll until it flips.
          * ``archive`` — records loaded from the archive and sanitized.
          * ``expired`` — archive deleted per the assistant's data
            retention policy.
          * ``unavailable`` — archive is missing or unreadable.

        Logs are stored raw for forensic integrity; sanitization runs on
        read. Provider SDK spam and HTTP client internals are dropped;
        internal scope ids, private IPs, storage URLs, bearer tokens, and
        vendor names are redacted. Filters can only narrow the query,
        never broaden it.

        Rate limited per API key / user — see the ``X-RateLimit-*``
        response headers.
      operationId: getCallLogs
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the call.
            title: Callid
          description: ID of the call.
        - name: category
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - API
                  - Agent
                  - Tools
                  - Knowledge Base
                  - Lifecycle
                  - Post-Processing
                  - Media Server
                  - Telephony
                  - System
                type: string
              - type: 'null'
            description: Filter by logical category.
            title: Category
          description: Filter by logical category.
        - name: level
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - debug
                  - info
                  - warning
                  - error
                  - critical
                type: string
              - type: 'null'
            description: Filter by log severity.
            title: Level
          description: Filter by log severity.
        - name: event_type
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by event type (e.g. ``agent_state_changed``).
            title: Event Type
          description: Filter by event type (e.g. ``agent_state_changed``).
        - name: search
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                maxLength: 200
              - type: 'null'
            description: Free-text substring match against the log line.
            title: Search
          description: Free-text substring match against the log line.
        - name: limit
          in: query
          required: false
          schema:
            anyOf:
              - type: integer
                maximum: 10000
                minimum: 1
              - type: 'null'
            description: Maximum log lines to return (1-10000, default 5000).
            title: Limit
          description: Maximum log lines to return (1-10000, default 5000).
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_CallLogsResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '429':
          description: Too many requests — rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /call/{callId}/agent:
    patch:
      tags:
        - Call
      summary: Update a Live Call's Instructions or Tools
      description: |-
        Change a running assistant's system instructions and/or tools mid-call.

        The call continues on the same connection with the same conversation —
        this is **not** a transfer and not a handoff to another assistant. The
        new behaviour applies from the assistant's next turn.

        Typical use is a language or mode switch: keep a lean base prompt and
        push a specialised instruction block only when the caller needs it,
        instead of carrying every variant in one long prompt.

        Both fields are optional but at least one is required. ``tools``
        **replaces** the active set — resend any tools you want to keep.
        Knowledge-base search tools are always retained.

        Returns ``200`` once the running agent confirms the change, or ``202``
        if it was dispatched but not confirmed within the ack window (it may
        still land — check the call timeline). Returns ``409`` when the call
        is not in progress, or when the assistant's model does not support
        live updates.
      operationId: updateLiveCallAgent
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the in-progress call to update.
            title: Callid
          description: ID of the in-progress call to update.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LiveAgentUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_LiveAgentUpdateResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /chat/session:
    post:
      tags:
        - Chat
      summary: Create a chat session
      description: >-
        Create a new chat session bound to a saved or inline assistant.


        Realtime-mode assistants are rejected with 422 (use a pipeline

        assistant). The session is billed at create time and stays active

        for 24 hours or until you close it via ``POST
        /chat/session/{id}/close``.

        Returns the session with its embedded recent-messages list (empty

        on a just-created session).


        **Idempotency:** include ``Idempotency-Key`` to dedupe retries

        over a 24-hour window — same key returns the original session.


        **Rate limit:** 600 requests/minute per API key (default).

        Responses carry ``X-RateLimit-Limit`` / ``-Remaining`` / ``-Reset``

        headers so you can pace your sender. Exceeding the limit returns

        HTTP 429 with a ``Retry-After`` header. Contact support if you

        need a higher ceiling.
      operationId: createChatSession
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatSessionCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChatSessionResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '402':
          description: Error 402.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          description: Too many requests — rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      tags:
        - Chat
      summary: List chat sessions
      description: |-
        Cursor-paginated list of chat sessions in the caller's scope.

        Sorted newest-first. Use the ``nextCursor`` from the response to
        page forward; ``null`` means no more pages.
      operationId: listChatSessions
      parameters:
        - name: assistantId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Assistantid
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/ChatSessionStatus'
              - type: 'null'
            title: Status
        - name: createdAfter
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Createdafter
        - name: createdBefore
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            title: Createdbefore
        - name: cursor
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Cursor
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 100
            minimum: 1
            default: 20
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChatSessionListResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /chat/session/{sessionId}:
    get:
      tags:
        - Chat
      summary: Get a chat session
      description: |-
        Fetch a chat session along with its most recent N messages
        (where N is the session's sliding-window setting). For full
        paginated history use ``GET /chat/session/{sessionId}/message``.
      operationId: getChatSession
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
            description: Chat session ID
            title: Sessionid
          description: Chat session ID
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChatSessionResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
        - Chat
      summary: Delete session
      description: |-
        Soft-delete a chat session and cascade-soft-delete its messages.

        Cannot be reversed by the API. The session record stays in the
        database (so audit + billing reconciliation still see it) but
        becomes invisible to list / get / message endpoints.
      operationId: deleteChatSession
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
            description: Chat session ID
            title: Sessionid
          description: Chat session ID
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /chat/session/{sessionId}/close:
    post:
      tags:
        - Chat
      summary: Close a chat session
      description: |-
        End an active chat session immediately.

        Idempotent — closing an already-ended session returns its current
        state. Triggers the post-chat worker (Phase 5) which delivers the
        ``chat-session-end-report`` webhook + runs analysis +
        anonymization on the session transcript.
      operationId: closeChatSession
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
            description: Chat session ID
            title: Sessionid
          description: Chat session ID
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
                - $ref: '#/components/schemas/ChatSessionCloseRequest'
                - type: 'null'
              title: Data
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChatSessionResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /chat/session/{sessionId}/inject:
    post:
      tags:
        - Chat
      summary: Inject message
      description: |-
        Append a hidden message into a chat session's history.

        Used by support / takeover flows to push out-of-band context the
        model will reference on the next user turn. The injected message
        is recorded as ``source="operator-inject"`` and surfaces on
        subsequent ``GET /chat/session/{id}`` reads + the per-turn
        ``chat-message`` webhook so downstream receivers can flag
        operator-injected entries.

        Permission: either ``chat.manage`` (Chat-API admin) OR
        ``monitor.manage`` (live-call/voice operator) — mirrors the
        monitor router's ``add-message`` precedent on the voice surface.
        Active sessions only (409 on ended / expired). 422 on a bad
        role / oversized content body (32 KB max).
      operationId: injectChatMessage
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
            description: Chat session ID
            title: Sessionid
          description: Chat session ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatInjectRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChatMessageResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /chat/message:
    post:
      tags:
        - Chat
      summary: Send message
      description: |-
        Send a user message into a chat session and receive the assistant reply.

        Two transports:

        - ``Accept: application/json`` (default): buffered. Server waits
          for the full assistant reply (including tool execution) before
          returning the standard ``ApiEnvelope[ChatMessageSendResponse]``
          body.
        - ``Accept: text/event-stream``: SSE stream of ``delta`` /
          ``usage`` / ``done`` events. Tool execution stays silent in
          the stream — the connection pauses while a tool runs and
          resumes streaming the model's continuation. Client disconnect
          cancels the in-flight LLM call; whatever content was generated
          up to that point is persisted normally so the customer doesn't
          lose work.

        Two session modes (apply to BOTH transports):

        - ``sessionId`` provided: posts into the existing session.
        - ``sessionId`` omitted + ``assistantId`` / ``assistant`` provided:
          implicitly creates a new session (billed) and immediately posts
          the user message into it. The response carries the assigned
          ``sessionId`` and ``sessionCreated=true``.

        **Idempotency:** include ``Idempotency-Key`` to dedupe buffered
        retries over a 24-hour window. Streaming retries with the same
        key return 409 (streams can't replay).

        **Rate limit:** 600 requests/minute per API key (default). See
        ``X-RateLimit-*`` response headers for the current budget; 429
        + ``Retry-After`` on exceedance. Contact support to raise the cap.
      operationId: sendChatMessage
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatMessageCreate'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChatMessageSendResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '402':
          description: Error 402.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '429':
          description: Too many requests — rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /chat/session/{sessionId}/message:
    get:
      tags:
        - Chat
      summary: List messages
      description: |-
        Cursor-paginated message history for a chat session.

        Default ordering is chronological (ascending ``createdAt``) so a
        UI rendering the conversation top-to-bottom reads naturally.
        Pass ``?ascending=false`` to page newest-first.
      operationId: listChatMessages
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
            description: Chat session ID
            title: Sessionid
          description: Chat session ID
        - name: cursor
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Cursor
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
        - name: ascending
          in: query
          required: false
          schema:
            type: boolean
            description: Chronological ordering
            default: true
            title: Ascending
          description: Chronological ordering
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChatMessageListResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /chat/session/{sessionId}/logs:
    get:
      tags:
        - Chat
      summary: Get Chat Session Log Archive
      description: |-
        Return the sanitized log stream for a chat session.

        Sibling of ``GET /call/{id}/logs``. Same archive-only contract,
        same response shape (``CallLogsResponse``), same source-state
        machine — so the frontend can reuse the call-logs panel component
        against either endpoint. Categories ``Media Server`` and
        ``Telephony`` never appear on chat session logs (voice-only) but
        are accepted in the filter Literal for shape consistency.

        Source states:

          * ``pending`` — session is still active OR the post-chat worker
            hasn't finished writing the archive yet. Frontend polls every
            5 s until it flips.
          * ``archive`` — records loaded from the archive and sanitized.
          * ``expired`` — archive was deleted by the assistant's
            ``chatLogArchiveDays`` retention policy (distinct from
            unavailable so the UI can render a dedicated banner).
          * ``unavailable`` — archive is missing or unreadable (transient).

        Logs are stored raw for forensic integrity; sanitization runs on
        read so future tightening of the redaction rules retroactively
        improves every existing archive without a rewrite. Provider SDK
        spam and HTTP client internals are dropped; internal account
        IDs, private IPs, storage URLs, bearer tokens, and vendor names
        are redacted. Tier-mode sessions get an additional vendor-name
        layer that hides the underlying model + provider.
      operationId: getChatSessionLogs
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
            description: Chat session ID
            title: Sessionid
          description: Chat session ID
        - name: category
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - API
                  - Agent
                  - Tools
                  - Knowledge Base
                  - Lifecycle
                  - Post-Processing
                  - Media Server
                  - Telephony
                  - System
                type: string
              - type: 'null'
            description: Filter by logical category.
            title: Category
          description: Filter by logical category.
        - name: level
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - debug
                  - info
                  - warning
                  - error
                  - critical
                type: string
              - type: 'null'
            description: Filter by log severity.
            title: Level
          description: Filter by log severity.
        - name: event_type
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by event type (e.g. ``chat_turn_complete``).
            title: Event Type
          description: Filter by event type (e.g. ``chat_turn_complete``).
        - name: search
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                maxLength: 200
              - type: 'null'
            description: Free-text substring match against the log line.
            title: Search
          description: Free-text substring match against the log line.
        - name: limit
          in: query
          required: false
          schema:
            anyOf:
              - type: integer
                maximum: 10000
                minimum: 1
              - type: 'null'
            description: Maximum log lines to return (1-10000, default 5000).
            title: Limit
          description: Maximum log lines to return (1-10000, default 5000).
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_CallLogsResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '429':
          description: Too many requests — rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /call/{callId}/monitor:
    get:
      tags:
        - Monitor
        - Monitor
      summary: Get monitor URLs
      description: |-
        Return live-call monitoring info — capability flags + pre-built URLs.

        The response carries ``listenUrl`` (a WebRTC URL with an embedded
        listen-only token, ready to feed into a WebRTC client) and
        ``controlUrl`` (the REST endpoint for sending control messages).
        Each URL is only populated when the assistant's monitoring config
        permits the corresponding capability.

        Only callable while the call is ``ringing`` or ``in-progress``.
      operationId: getCallMonitor
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the live call to monitor.
            title: Callid
          description: ID of the live call to monitor.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_MonitorInfo_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /call/{callId}/monitor/listen-token:
    get:
      tags:
        - Monitor
        - Monitor
      summary: Get Listen-Only Token
      description: |-
        Generate a listen-only access token for a live call.

        The token grants subscribe-only WebRTC access — the supervisor
        can hear the agent and the customer but cannot publish audio,
        send data, or appear as a visible participant. Use the returned
        ``url`` + ``token`` with any standard WebRTC client to connect.

        Only callable while the call is ``ringing`` or ``in-progress``,
        and only when the assistant's monitoring config permits live
        listening (otherwise 403).
      operationId: getCallListenToken
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the live call to listen to.
            title: Callid
          description: ID of the live call to listen to.
        - name: identity
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Optional supervisor identity to bake into the token.
              Auto-generated when omitted.
            title: Identity
          description: >-
            Optional supervisor identity to bake into the token. Auto-generated
            when omitted.
        - name: ttl
          in: query
          required: false
          schema:
            type: integer
            maximum: 86400
            minimum: 60
            description: Token lifetime in seconds (60-86400). Defaults to 1 hour.
            default: 3600
            title: Ttl
          description: Token lifetime in seconds (60-86400). Defaults to 1 hour.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ListenTokenResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /call/{callId}/monitor/control:
    post:
      tags:
        - Monitor
        - Monitor
      summary: Send control
      description: |-
        Send a control message to the live agent.

        The message is dispatched over the call's data channel and the
        agent acts on it on the next turn. Supported types:

        - ``say`` — agent speaks ``content`` verbatim.
        - ``add-message`` — append a hidden context message; ``role``
          selects sender (``system`` / ``user`` / ``assistant``).
        - ``end-call`` — ends the call; if ``content`` is set the agent
          speaks it before hanging up.
        - ``transfer`` — transfer the call. Target carried in ``metadata``.
        - ``mute`` — mute or unmute the agent.
        - ``agent-leave`` — agent leaves the room while keeping it alive
          so a human supervisor can take over (use with the takeover
          token endpoint).

        Only callable while the call is ``ringing`` or ``in-progress``.
        Returns 403 when the assistant's monitoring config disables
        control (or, for ``agent-leave``, both control and takeover).
      operationId: sendCallControl
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the live call.
            title: Callid
          description: ID of the live call.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ControlMessage'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ControlResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /call/{callId}/monitor/end-takeover:
    post:
      tags:
        - Monitor
        - Monitor
      summary: End Takeover Session
      description: |-
        End a human takeover session.

        Disconnects every participant (including SIP / phone), tears down
        the call's media room, and marks the call as ``ended``. Use this
        once the human is done with the takeover so resources are cleaned
        up immediately — a stale takeover room continues to consume media
        capacity until it expires on its own.
      operationId: endCallTakeover
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the live call whose takeover is ending.
            title: Callid
          description: ID of the live call whose takeover is ending.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EndTakeoverRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_EndTakeoverResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /call/{callId}/monitor/takeover-token:
    get:
      tags:
        - Monitor
        - Monitor
      summary: Get Takeover Token
      description: |-
        Generate a takeover access token for a live call.

        The returned token grants full WebRTC participant rights — the
        human supervisor can speak to the customer, hear the customer,
        and is visible as a participant. Typical flow:

        1. Call this endpoint, get ``token`` + ``url``.
        2. Connect to the room from a WebRTC client with the microphone
           enabled.
        3. ``POST /call/{callId}/monitor/control`` with
           ``{"type": "agent-leave"}`` so the AI agent leaves; the room
           stays alive.
        4. The human supervisor is now talking directly to the customer.
        5. When done, ``POST /call/{callId}/monitor/end-takeover`` to
           disconnect everyone and end the call.

        Only callable while the call is ``ringing`` or ``in-progress``,
        and only when the assistant's monitoring config permits takeover
        (otherwise 403).
      operationId: getCallTakeoverToken
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            description: ID of the live call to take over.
            title: Callid
          description: ID of the live call to take over.
        - name: identity
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Optional supervisor identity to bake into the token.
              Auto-generated when omitted.
            title: Identity
          description: >-
            Optional supervisor identity to bake into the token. Auto-generated
            when omitted.
        - name: ttl
          in: query
          required: false
          schema:
            type: integer
            maximum: 86400
            minimum: 60
            description: Token lifetime in seconds (60-86400). Defaults to 1 hour.
            default: 3600
            title: Ttl
          description: Token lifetime in seconds (60-86400). Defaults to 1 hour.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_TakeoverTokenResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /file:
    get:
      tags:
        - File
      summary: List Files
      description: |-
        List files in your workspace.

        Supports name search, MIME-type filter, folder scoping (pass
        ``folderId="null"`` to narrow to unfiled root entries), sort by
        ``createdAt`` / ``updatedAt`` / ``name`` / ``size``, and inclusive
        or exclusive date-range bounds.

        Every entry carries a freshly-generated, time-limited presigned
        download URL — no extra round-trip needed to fetch each one
        individually.
      operationId: listFiles
      parameters:
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            description: Number of files to skip for pagination.
            default: 0
            title: Skip
          description: Number of files to skip for pagination.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 1000
            minimum: 1
            description: Maximum files to return per page.
            default: 100
            title: Limit
          description: Maximum files to return per page.
        - name: search
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Case-insensitive substring match on the file's name.
            title: Search
          description: Case-insensitive substring match on the file's name.
        - name: mimeType
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - application/pdf
                  - >-
                    application/vnd.openxmlformats-officedocument.wordprocessingml.document
                  - application/msword
                  - text/plain
                  - text/markdown
                  - text/csv
                  - application/json
                type: string
              - type: 'null'
            description: >-
              Filter by MIME type. Restricted to the knowledge-base-ingestible
              whitelist used at upload.
            title: Mimetype
          description: >-
            Filter by MIME type. Restricted to the knowledge-base-ingestible
            whitelist used at upload.
        - name: folderId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Filter by folder. Pass ``"null"`` to return only unfiled files
              (list root). Omit to include files from every folder.
            title: Folderid
          description: >-
            Filter by folder. Pass ``"null"`` to return only unfiled files (list
            root). Omit to include files from every folder.
        - name: sortBy
          in: query
          required: false
          schema:
            enum:
              - createdAt
              - updatedAt
              - name
              - size
            type: string
            description: Field to sort by.
            default: createdAt
            title: Sortby
          description: Field to sort by.
        - name: sortOrder
          in: query
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            description: Sort direction.
            default: desc
            title: Sortorder
          description: Sort direction.
        - name: createdAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return files uploaded strictly after this timestamp
              (exclusive).
            title: Createdatgt
          description: >-
            Only return files uploaded strictly after this timestamp
            (exclusive).
        - name: createdAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return files uploaded strictly before this timestamp
              (exclusive).
            title: Createdatlt
          description: >-
            Only return files uploaded strictly before this timestamp
            (exclusive).
        - name: createdAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: Only return files uploaded on or after this timestamp (inclusive).
            title: Createdatge
          description: Only return files uploaded on or after this timestamp (inclusive).
        - name: createdAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return files uploaded on or before this timestamp
              (inclusive).
            title: Createdatle
          description: Only return files uploaded on or before this timestamp (inclusive).
        - name: updatedAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return files last updated strictly after this timestamp
              (exclusive).
            title: Updatedatgt
          description: >-
            Only return files last updated strictly after this timestamp
            (exclusive).
        - name: updatedAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return files last updated strictly before this timestamp
              (exclusive).
            title: Updatedatlt
          description: >-
            Only return files last updated strictly before this timestamp
            (exclusive).
        - name: updatedAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return files last updated on or after this timestamp
              (inclusive).
            title: Updatedatge
          description: >-
            Only return files last updated on or after this timestamp
            (inclusive).
        - name: updatedAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return files last updated on or before this timestamp
              (inclusive).
            title: Updatedatle
          description: >-
            Only return files last updated on or before this timestamp
            (inclusive).
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_PaginatedFileResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - File
      summary: Upload a File
      description: |-
        Upload a document for use as a knowledge-base source.

        Accepted formats: PDF, DOCX, DOC, TXT, MD, CSV, JSON. Maximum
        upload size is 50 MB. Returns the stored record with a fresh
        presigned download URL on success.
      operationId: uploadFile
      parameters:
        - name: folderId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Drop the new file into this folder. Omit for the root list.
            title: Folderid
          description: Drop the new file into this folder. Omit for the root list.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_uploadFile'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_FileResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '413':
          description: Error 413.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /file/{fileId}:
    get:
      tags:
        - File
      summary: Get File
      description: Return a single file with a freshly-generated presigned download URL.
      operationId: getFile
      parameters:
        - name: fileId
          in: path
          required: true
          schema:
            type: string
            description: ID of the file.
            title: Fileid
          description: ID of the file.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_FileResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
        - File
      summary: Delete a File
      description: |-
        Delete a file.

        Cascades into knowledge bases: any source that referenced this
        file is marked as ``failed`` with an explanatory error so callers
        can see why ingestion stopped. The action is not reversible.
      operationId: deleteFile
      parameters:
        - name: fileId
          in: path
          required: true
          schema:
            type: string
            description: ID of the file to delete.
            title: Fileid
          description: ID of the file to delete.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /knowledge-base:
    get:
      tags:
        - Knowledge Base
      summary: List Knowledge Bases
      description: |-
        List knowledge bases in your workspace.

        Supports name search, status filtering, sort by ``createdAt`` /
        ``updatedAt`` / ``name``, and inclusive / exclusive date-range
        bounds on both ``createdAt`` and ``updatedAt``. Soft-deleted
        knowledge bases are excluded.
      operationId: listKnowledgeBases
      parameters:
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            description: Number of knowledge bases to skip for pagination.
            default: 0
            title: Skip
          description: Number of knowledge bases to skip for pagination.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 1000
            minimum: 1
            description: Maximum knowledge bases to return per page.
            default: 100
            title: Limit
          description: Maximum knowledge bases to return per page.
        - name: search
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Case-insensitive substring match on the knowledge base name.
            title: Search
          description: Case-insensitive substring match on the knowledge base name.
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - enum:
                  - pending
                  - processing
                  - ready
                  - partial
                  - failed
                type: string
              - type: 'null'
            description: >-
              Filter by rolled-up status. ``pending`` — no sources yet.
              ``processing`` — at least one source is still ingesting. ``ready``
              — every source ingested successfully. ``partial`` — some
              succeeded, some failed. ``failed`` — every source failed.
            title: Status
          description: >-
            Filter by rolled-up status. ``pending`` — no sources yet.
            ``processing`` — at least one source is still ingesting. ``ready`` —
            every source ingested successfully. ``partial`` — some succeeded,
            some failed. ``failed`` — every source failed.
        - name: sortBy
          in: query
          required: false
          schema:
            enum:
              - createdAt
              - updatedAt
              - name
            type: string
            description: Field to sort by.
            default: createdAt
            title: Sortby
          description: Field to sort by.
        - name: sortOrder
          in: query
          required: false
          schema:
            enum:
              - asc
              - desc
            type: string
            description: Sort direction.
            default: desc
            title: Sortorder
          description: Sort direction.
        - name: createdAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return knowledge bases created strictly after this timestamp
              (exclusive).
            title: Createdatgt
          description: >-
            Only return knowledge bases created strictly after this timestamp
            (exclusive).
        - name: createdAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return knowledge bases created strictly before this timestamp
              (exclusive).
            title: Createdatlt
          description: >-
            Only return knowledge bases created strictly before this timestamp
            (exclusive).
        - name: createdAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return knowledge bases created on or after this timestamp
              (inclusive).
            title: Createdatge
          description: >-
            Only return knowledge bases created on or after this timestamp
            (inclusive).
        - name: createdAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return knowledge bases created on or before this timestamp
              (inclusive).
            title: Createdatle
          description: >-
            Only return knowledge bases created on or before this timestamp
            (inclusive).
        - name: updatedAtGt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return knowledge bases last updated strictly after this
              timestamp (exclusive).
            title: Updatedatgt
          description: >-
            Only return knowledge bases last updated strictly after this
            timestamp (exclusive).
        - name: updatedAtLt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return knowledge bases last updated strictly before this
              timestamp (exclusive).
            title: Updatedatlt
          description: >-
            Only return knowledge bases last updated strictly before this
            timestamp (exclusive).
        - name: updatedAtGe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return knowledge bases last updated on or after this
              timestamp (inclusive).
            title: Updatedatge
          description: >-
            Only return knowledge bases last updated on or after this timestamp
            (inclusive).
        - name: updatedAtLe
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: date-time
              - type: 'null'
            description: >-
              Only return knowledge bases last updated on or before this
              timestamp (inclusive).
            title: Updatedatle
          description: >-
            Only return knowledge bases last updated on or before this timestamp
            (inclusive).
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedKnowledgeBaseResponse_
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Knowledge Base
      summary: Create knowledge base
      description: |-
        Create a new knowledge base.

        Returns immediately with ``status: "pending"``. Sources are attached
        separately via ``POST /knowledge-base/{kbId}/sources`` and ingest
        asynchronously — poll ``GET /knowledge-base/{kbId}`` (or read the
        per-source ``status`` field) to track progress.

        ``chunkingConfig`` and ``searchConfig`` accept platform defaults
        when omitted (1000 / 200 character chunks, top-K 5, score
        threshold 0.3). Embedding model and dimensions are platform-pinned
        and not configurable.
      operationId: createKnowledgeBase
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KnowledgeBaseCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_KnowledgeBaseResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /knowledge-base/{kbId}:
    get:
      tags:
        - Knowledge Base
      summary: Get Knowledge Base
      description: >-
        Return a single knowledge base with its sources, configs, and rolled-up
        stats.
      operationId: getKnowledgeBase
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            description: ID of the knowledge base.
            title: Kbid
          description: ID of the knowledge base.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_KnowledgeBaseResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Knowledge Base
      summary: Update knowledge base
      description: |-
        Update a knowledge base.

        Partial PATCH — only the fields you send are applied; omitted
        fields are left unchanged. Sources are managed through the
        dedicated ``/sources`` endpoints and are not affected by this call.

        Changes to ``chunkingConfig`` only take effect the next time a
        source is ingested or re-synced (existing chunks were produced
        with the old settings). Changes to ``searchConfig`` apply
        immediately to the next retrieval query.
      operationId: updateKnowledgeBase
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            description: ID of the knowledge base to update.
            title: Kbid
          description: ID of the knowledge base to update.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KnowledgeBaseUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_KnowledgeBaseResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Knowledge Base
      summary: Delete knowledge base
      description: |-
        Soft-delete a knowledge base.

        Full cascade: drops the underlying vector index and every chunk,
        soft-deletes the knowledge base document, and detaches the
        knowledge base id from every assistant that referenced it. Each
        affected assistant's configuration cache is invalidated so the
        next call picks up the change immediately. The action is not
        reversible.
      operationId: deleteKnowledgeBase
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            description: ID of the knowledge base to delete.
            title: Kbid
          description: ID of the knowledge base to delete.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /knowledge-base/{kbId}/sources:
    post:
      tags:
        - Knowledge Base
      summary: Add source
      description: |-
        Attach a new source to a knowledge base.

        The request body is one of two shapes, discriminated on ``type``:

        - ``{"type": "file", "fileId": "..."}`` — attach an already-uploaded
          file (PDF, DOCX, TXT, MD, CSV, or JSON). The file must belong to
          the same workspace.
        - ``{"type": "url", "url": "..."}`` — crawl an HTTP / HTTPS URL.
          JavaScript-rendered pages and sites behind authentication are
          not supported. Private, loopback, and cloud-metadata addresses
          are rejected.

        Returns the updated knowledge base immediately with the new source
        in ``status: "pending"``. Ingestion runs asynchronously — chunks
        appear once parsing, chunking, and embedding complete. Adding the
        same file or URL twice returns 409.
      operationId: addKnowledgeBaseSource
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            description: ID of the knowledge base to attach the source to.
            title: Kbid
          description: ID of the knowledge base to attach the source to.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/AddFileSource'
                - $ref: '#/components/schemas/AddUrlSource'
              discriminator:
                propertyName: type
                mapping:
                  file: '#/components/schemas/AddFileSource'
                  url: '#/components/schemas/AddUrlSource'
              title: Data
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_KnowledgeBaseResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /knowledge-base/{kbId}/sources/{sourceId}:
    delete:
      tags:
        - Knowledge Base
      summary: Remove source
      description: |-
        Remove a source from a knowledge base.

        Drops every embedding and stored chunk produced from this source
        and recomputes the knowledge base's rolled-up status. The action
        is not reversible — to restore a removed source, attach it again
        via ``POST /knowledge-base/{kbId}/sources``.
      operationId: removeKnowledgeBaseSource
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            description: ID of the knowledge base.
            title: Kbid
          description: ID of the knowledge base.
        - name: sourceId
          in: path
          required: true
          schema:
            type: string
            description: ID of the source to remove.
            title: Sourceid
          description: ID of the source to remove.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_KnowledgeBaseResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /knowledge-base/{kbId}/sync:
    post:
      tags:
        - Knowledge Base
      summary: Re-Sync All Sources
      description: |-
        Re-process every source in a knowledge base.

        Use this after changing ``chunkingConfig`` so existing sources are
        re-chunked under the new settings, or after the underlying URL
        content changes and you want to refresh the index. Each source
        transitions back to ``pending`` and is then re-ingested
        asynchronously.

        Returns 422 if the knowledge base has no sources, and 502 if the
        ingestion queue is unreachable — sources are not reset until the
        queue write succeeds.
      operationId: syncKnowledgeBase
      parameters:
        - name: kbId
          in: path
          required: true
          schema:
            type: string
            description: ID of the knowledge base to re-sync.
            title: Kbid
          description: ID of the knowledge base to re-sync.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_KnowledgeBaseResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/channels:
    get:
      tags:
        - Messaging
        - Messaging - Channels
      summary: List channels
      operationId: listMessagingChannels
      parameters:
        - name: channelType
          in: query
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/ChannelType'
              - type: 'null'
            title: Channeltype
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/ChannelStatus'
              - type: 'null'
            title: Status
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedResponse_ChannelResponse__
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/channels/{channel_id}:
    get:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Get a channel by id
      operationId: getMessagingChannel
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChannelResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Update channel
      operationId: patchMessagingChannel
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChannelPatchRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChannelResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Soft-delete a channel
      operationId: deleteMessagingChannel
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/channels/{channel_id}/disconnect:
    post:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Disconnect channel
      operationId: disconnectMessagingChannel
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChannelResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/channels/{channel_id}/reconnect:
    post:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Reconnect channel
      operationId: reconnectMessagingChannel
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChannelResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/channels/{channel_id}/whatsapp/register:
    post:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Register number
      description: >-
        Complete Cloud API registration for a WhatsApp number.


        We manage the 2-step-verification PIN automatically; ``pin`` is only

        needed when the number already had a PIN and Meta asks for it (the error

        response sets ``pinRequired: true`` so the UI can prompt). On success
        the

        channel's status is reconciled against Meta and returned.
      operationId: registerWhatsAppChannel
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
                - $ref: '#/components/schemas/WhatsAppRegisterRequest'
                - type: 'null'
              title: Body
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChannelResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/channels/{channel_id}/whatsapp/refresh-status:
    post:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Refresh WhatsApp status
      description: |-
        Re-read the number's live status from Meta and mirror it locally.

        Lets the dashboard reconcile on demand so it never shows a stale status.
        Upstream blips leave the existing status untouched (logged server-side).
      operationId: refreshWhatsAppChannelStatus
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChannelResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/channels/{channel_id}/whatsapp/resubscribe:
    post:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Resubscribe WABA
      description: |-
        Repair a channel whose per-WABA subscription landed with empty
        ``subscribed_fields`` — Meta accepted onboarding but registered no
        fields, so inbound webhooks were silently suppressed.
      operationId: resubscribeWhatsAppChannel
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/channels/{channel_id}/whatsapp/diagnose:
    post:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Diagnose channel
      description: |-
        Pull WABA details, subscribed apps + their fields, and full phone state
        from Meta's Graph API using the customer's access token. Surfaces the
        likely root cause when inbound webhooks aren't arriving (our app not
        subscribed / messages field missing / per-WABA callback override /
        business verification incomplete).
      operationId: diagnoseWhatsAppChannel
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/channels/{channel_id}/email/refresh-status:
    post:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Refresh email status
      description: |-
        Re-read the Resend domain status and mirror it locally.

        Lets a pending domain flip to active once DNS verification completes,
        without re-onboarding. Upstream blips leave the status untouched.
      operationId: refreshEmailChannelStatus
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChannelResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/channels/{channel_id}/capabilities:
    get:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Get capabilities
      operationId: getMessagingChannelCapabilities
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/channels/{channel_id}/health:
    get:
      tags:
        - Messaging
        - Messaging - Channels
      summary: Get health
      operationId: getMessagingChannelHealth
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ChannelHealthResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/messages/can-send:
    get:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Check send window
      description: |-
        Pre-flight check before sending a free-form (non-template) message.

        Returns whether the send will succeed given the channel + recipient's
        current WhatsApp customer-care window, plus a stable
        ``recommendation`` field (``"send_freeform"`` / ``"use_template"``)
        so workflows and dashboards can branch without recomputing the
        24h clock.

        Read-only — no message is sent and no billing is debited.
      operationId: canSendMessagingMessage
      parameters:
        - name: channelId
          in: query
          required: true
          schema:
            type: string
            description: Connected channel id
            title: Channelid
          description: Connected channel id
        - name: to
          in: query
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 512
            description: >-
              Recipient identifier — E.164 phone for WhatsApp/SMS, Telegram chat
              id, or email address. Same value you would use in ``POST
              /messaging/messages`` ``to`` field.
            title: To
          description: >-
            Recipient identifier — E.164 phone for WhatsApp/SMS, Telegram chat
            id, or email address. Same value you would use in ``POST
            /messaging/messages`` ``to`` field.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_CanSendResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/messages:
    post:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Send message
      operationId: sendMessagingMessage
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Idempotency-Key
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendMessageRequest'
            examples:
              text:
                summary: Text message
                value:
                  channelId: 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
                  to: '+4915112345678'
                  type: text
                  text:
                    body: Your order ORD-42 is ready for pickup 🎉
              template:
                summary: WhatsApp template (outside the 24h window)
                value:
                  channelId: 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
                  to: '+4915112345678'
                  type: template
                  template:
                    name: order_confirmation
                    language: de
                    components:
                      - type: body
                        parameters:
                          - type: text
                            text: '{{ orderId }}'
                  templateVariables:
                    orderId: ORD-42
              image:
                summary: Image with caption
                value:
                  channelId: 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
                  to: '+4915112345678'
                  type: image
                  image:
                    url: https://files.example.com/photo-042.jpg
                    caption: Here's the photo you asked for
              document:
                summary: Document (PDF)
                value:
                  channelId: 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
                  to: '+4915112345678'
                  type: document
                  document:
                    url: https://files.example.com/invoice-042.pdf
                    filename: invoice-042.pdf
                    caption: Your invoice
              location:
                summary: Location pin
                value:
                  channelId: 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
                  to: '+4915112345678'
                  type: location
                  location:
                    latitude: 52.520008
                    longitude: 13.404954
                    name: Berlin Office
                    address: Unter den Linden 1, 10117 Berlin
              interactive:
                summary: Interactive buttons
                value:
                  channelId: 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
                  to: '+4915112345678'
                  type: interactive
                  interactive:
                    type: button
                    body:
                      text: Confirm your appointment?
                    action:
                      buttons:
                        - type: reply
                          reply:
                            id: 'yes'
                            title: 'Yes'
                        - type: reply
                          reply:
                            id: 'no'
                            title: 'No'
              reaction:
                summary: Emoji reaction
                value:
                  channelId: 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
                  to: '+4915112345678'
                  type: reaction
                  reaction:
                    messageId: b4c8e2a1-6f3d-4e79-8c25-91a0d7f3e6b2
                    emoji: 👍
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_SendMessageResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '402':
          description: Error 402.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '503':
          description: Error 503.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      tags:
        - Messaging
        - Messaging - Messages
      summary: List messages
      operationId: listMessagingMessages
      parameters:
        - name: channelId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Channelid
        - name: conversationId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Conversationid
        - name: contactId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Contactid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedResponse_MessageResponse__
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/messages/{message_id}:
    get:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Get message
      operationId: getMessagingMessage
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            title: Message Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_MessageResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/messages/{message_id}/read:
    post:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Mark as read
      operationId: markMessagingMessageRead
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            title: Message Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/messages/{message_id}/reactions:
    post:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Add reaction
      operationId: addMessagingReaction
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            title: Message Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReactionRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Remove reaction
      operationId: removeMessagingReaction
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            title: Message Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/messages/{message_id}/retract:
    post:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Retract message
      operationId: retractMessagingMessage
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            title: Message Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/messages/{message_id}/status-history:
    get:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Get status history
      operationId: getMessagingMessageStatusHistory
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            title: Message Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_MessageStatusHistoryResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/messages/bulk:
    post:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Bulk send
      operationId: bulkSendMessages
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkSendRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_BulkSendResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/messages/schedule:
    post:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Schedule message
      operationId: scheduleMessagingMessage
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleMessageRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_MessageResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/messages/{message_id}/cancel-scheduled:
    post:
      tags:
        - Messaging
        - Messaging - Messages
      summary: Cancel scheduled
      operationId: cancelScheduledMessagingMessage
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
            title: Message Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_MessageResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/conversations:
    get:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: List conversations
      operationId: listMessagingConversations
      parameters:
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/ConversationStatus'
              - type: 'null'
            title: Status
        - name: channelId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Channelid
        - name: assignedToUserId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Assignedtouserid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedResponse_ConversationResponse__
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/conversations/{conversation_id}:
    get:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Get conversation
      operationId: getMessagingConversation
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ConversationResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Update conversation
      operationId: patchMessagingConversation
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConversationPatchRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ConversationResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/conversations/{conversation_id}/close:
    post:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Close a conversation
      operationId: closeMessagingConversation
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ConversationResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/conversations/{conversation_id}/reopen:
    post:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Reopen conversation
      operationId: reopenMessagingConversation
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ConversationResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/conversations/{conversation_id}/typing:
    post:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Send typing
      operationId: sendMessagingTypingIndicator
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TypingRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/conversations/{conversation_id}/read-all:
    post:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Mark all read
      operationId: readAllMessagingConversation
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ConversationResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/conversations/{conversation_id}/assign:
    post:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Assign conversation
      operationId: assignMessagingConversation
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignConversationRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ConversationResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/conversations/{conversation_id}/unassign:
    post:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Unassign conversation
      operationId: unassignMessagingConversation
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ConversationResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/conversations/{conversation_id}/notes:
    get:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: List notes
      operationId: listMessagingConversationNotes
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_NoteResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Add note
      operationId: createMessagingConversationNote
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNoteRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_NoteResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/conversations/{conversation_id}/notes/{note_id}:
    delete:
      tags:
        - Messaging
        - Messaging - Conversations
      summary: Delete note
      operationId: deleteMessagingConversationNote
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema:
            type: string
            title: Conversation Id
        - name: note_id
          in: path
          required: true
          schema:
            type: string
            title: Note Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/contacts:
    get:
      tags:
        - Messaging
        - Messaging - Contacts
      summary: List contacts
      operationId: listMessagingContacts
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
        - name: ids
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Comma-separated contact IDs to filter by. Used by the inbox to
              fetch only the contacts it needs to display.
            title: Ids
          description: >-
            Comma-separated contact IDs to filter by. Used by the inbox to fetch
            only the contacts it needs to display.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedResponse_ContactResponse__
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Messaging
        - Messaging - Contacts
      summary: Create a contact
      operationId: createMessagingContact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContactCreateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ContactResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/contacts/{contact_id}:
    get:
      tags:
        - Messaging
        - Messaging - Contacts
      summary: Get a contact by id
      operationId: getMessagingContact
      parameters:
        - name: contact_id
          in: path
          required: true
          schema:
            type: string
            title: Contact Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ContactResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Messaging
        - Messaging - Contacts
      summary: Update contact fields
      operationId: patchMessagingContact
      parameters:
        - name: contact_id
          in: path
          required: true
          schema:
            type: string
            title: Contact Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContactPatchRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ContactResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/batches:
    get:
      tags:
        - Messaging
        - Messaging - Batches
      summary: List batches
      operationId: listMessagingBatches
      parameters:
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/BatchStatus'
              - type: 'null'
            title: Status
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedResponse_BatchResponse__
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/batches/{batch_id}:
    get:
      tags:
        - Messaging
        - Messaging - Batches
      summary: Get batch
      operationId: getMessagingBatch
      parameters:
        - name: batch_id
          in: path
          required: true
          schema:
            type: string
            title: Batch Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_BatchResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/batches/{batch_id}/items:
    get:
      tags:
        - Messaging
        - Messaging - Batches
      summary: List batch items
      operationId: listMessagingBatchItems
      parameters:
        - name: batch_id
          in: path
          required: true
          schema:
            type: string
            title: Batch Id
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 1000
            minimum: 1
            default: 200
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedResponse_MessageResponse__
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/batches/{batch_id}/cancel:
    post:
      tags:
        - Messaging
        - Messaging - Batches
      summary: Cancel batch
      operationId: cancelMessagingBatch
      parameters:
        - name: batch_id
          in: path
          required: true
          schema:
            type: string
            title: Batch Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_BatchResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/opt-outs:
    get:
      tags:
        - Messaging
        - Messaging - Opt-outs
      summary: List opt-outs
      operationId: listMessagingOptOuts
      parameters:
        - name: channelType
          in: query
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/ChannelType'
              - type: 'null'
            title: Channeltype
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 500
            minimum: 1
            default: 100
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_OptOutResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Messaging
        - Messaging - Opt-outs
      summary: Add opt-out
      operationId: createMessagingOptOut
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOptOutRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_OptOutResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/opt-outs/{optout_id}:
    delete:
      tags:
        - Messaging
        - Messaging - Opt-outs
      summary: Remove opt-out
      operationId: deleteMessagingOptOut
      parameters:
        - name: optout_id
          in: path
          required: true
          schema:
            type: string
            title: Optout Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/opt-outs/check:
    get:
      tags:
        - Messaging
        - Messaging - Opt-outs
      summary: Check opt-out
      operationId: checkMessagingOptOut
      parameters:
        - name: channel
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/ChannelType'
        - name: recipient
          in: query
          required: true
          schema:
            type: string
            minLength: 1
            title: Recipient
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_OptOutCheckResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/consent:
    get:
      tags:
        - Messaging
        - Messaging - Consent
      summary: List consent records
      operationId: listMessagingConsent
      parameters:
        - name: contactId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Contactid
        - name: channelType
          in: query
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/ChannelType'
              - type: 'null'
            title: Channeltype
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 500
            minimum: 1
            default: 100
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_ConsentRecordResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Messaging
        - Messaging - Consent
      summary: Record opt-in
      operationId: createMessagingConsent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateConsentRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ConsentRecordResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/consent/{contact_id}:
    get:
      tags:
        - Messaging
        - Messaging - Consent
      summary: Get contact consent
      operationId: getMessagingConsentForContact
      parameters:
        - name: contact_id
          in: path
          required: true
          schema:
            type: string
            title: Contact Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_ConsentRecordResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/quick-replies:
    get:
      tags:
        - Messaging
        - Messaging - Quick Replies
      summary: List quick replies
      operationId: listMessagingQuickReplies
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_QuickReplyResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      tags:
        - Messaging
        - Messaging - Quick Replies
      summary: Create a quick reply
      operationId: createMessagingQuickReply
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuickReplyCreateRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_QuickReplyResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/quick-replies/{quick_reply_id}:
    patch:
      tags:
        - Messaging
        - Messaging - Quick Replies
      summary: Update a quick reply
      operationId: patchMessagingQuickReply
      parameters:
        - name: quick_reply_id
          in: path
          required: true
          schema:
            type: string
            title: Quick Reply Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuickReplyPatchRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_QuickReplyResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
        - Messaging
        - Messaging - Quick Replies
      summary: Delete a quick reply
      operationId: deleteMessagingQuickReply
      parameters:
        - name: quick_reply_id
          in: path
          required: true
          schema:
            type: string
            title: Quick Reply Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/quick-replies/{quick_reply_id}/use:
    post:
      tags:
        - Messaging
        - Messaging - Quick Replies
      summary: Record quick-reply use
      operationId: useMessagingQuickReply
      parameters:
        - name: quick_reply_id
          in: path
          required: true
          schema:
            type: string
            title: Quick Reply Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_QuickReplyResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/webhook-deliveries:
    get:
      tags:
        - Messaging
        - Messaging - Webhook Deliveries
      summary: List deliveries
      operationId: listMessagingWebhookDeliveries
      parameters:
        - name: channelId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Channelid
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - $ref: '#/components/schemas/DeliveryStatus'
              - type: 'null'
            title: Status
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_list_WebhookDeliveryResponse__
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/webhook-deliveries/{delivery_id}:
    get:
      tags:
        - Messaging
        - Messaging - Webhook Deliveries
      summary: Get delivery
      operationId: getMessagingWebhookDelivery
      parameters:
        - name: delivery_id
          in: path
          required: true
          schema:
            type: string
            title: Delivery Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WebhookDeliveryResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/webhook-deliveries/{delivery_id}/retry:
    post:
      tags:
        - Messaging
        - Messaging - Webhook Deliveries
      summary: Retry delivery
      operationId: retryMessagingWebhookDelivery
      parameters:
        - name: delivery_id
          in: path
          required: true
          schema:
            type: string
            title: Delivery Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WebhookDeliveryResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/media:
    post:
      tags:
        - Messaging
        - Messaging - Media
      summary: Upload media
      operationId: uploadMessagingMedia
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_uploadMessagingMedia'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_MediaUploadResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '413':
          description: Error 413.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /messaging/channels/{channel_id}/templates:
    get:
      tags:
        - Messaging
        - Messaging - Templates
      summary: List templates
      operationId: listMessagingTemplates
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_TemplateResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
        - Messaging
        - Messaging - Templates
      summary: Create template
      description: |-
        Create a WhatsApp message template and submit it to Meta for review.

        The new template lands locally with ``PENDING`` status; the status
        worker flips it to ``approved`` / ``rejected`` once Meta decides.
        Meta-side rejections (policy, naming, structure) surface as a 502 with
        a friendly message.
      operationId: createMessagingTemplate
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TemplateCreateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_TemplateResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/channels/{channel_id}/templates/{template_id}:
    get:
      tags:
        - Messaging
        - Messaging - Templates
      summary: Get a single template
      operationId: getMessagingTemplate
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
        - name: template_id
          in: path
          required: true
          schema:
            type: string
            title: Template Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_TemplateResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
        - Messaging
        - Messaging - Templates
      summary: Delete template
      description: Delete a template on Meta and soft-delete it locally. Irreversible.
      operationId: deleteMessagingTemplate
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
        - name: template_id
          in: path
          required: true
          schema:
            type: string
            title: Template Id
      responses:
        '204':
          description: Successful Response
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/channels/{channel_id}/templates/sync:
    post:
      tags:
        - Messaging
        - Messaging - Templates
      summary: Sync templates
      operationId: syncMessagingTemplates
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_TemplateSyncResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '502':
          description: Upstream service returned an error or was unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /messaging/channels/{channel_id}/templates/{template_id}/preview:
    post:
      tags:
        - Messaging
        - Messaging - Templates
      summary: Preview template
      operationId: previewMessagingTemplate
      parameters:
        - name: channel_id
          in: path
          required: true
          schema:
            type: string
            title: Channel Id
        - name: template_id
          in: path
          required: true
          schema:
            type: string
            title: Template Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TemplatePreviewRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_TemplatePreviewResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets:
    post:
      tags:
        - Widget - Admin
      summary: Create widget
      description: |-
        Create a new widget under the caller's current organization.

        Returns the widget config PLUS the two plaintext secrets (identity
        HMAC + inbound-webhook signing) ONCE. After this call the secrets
        are encrypted at rest; to see them again, the admin must rotate.
      operationId: createWidget
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WidgetCreateRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetCreateResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      tags:
        - Widget - Admin
      summary: List widgets
      description: |-
        Return the current org's widgets, newest-first. Excludes
        soft-deleted rows.
      operationId: listWidgets
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
            title: Skip
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_WidgetResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}:
    get:
      tags:
        - Widget - Admin
      summary: Get one widget by id
      operationId: getWidget
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
        - Widget - Admin
      summary: Update a widget config
      operationId: updateWidget
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WidgetUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Widget - Admin
      summary: Soft-delete a widget
      operationId: deleteWidget
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetDeletedResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/rotate-identity-secret:
    post:
      tags:
        - Widget - Admin
      summary: Rotate identity secret
      operationId: rotateWidgetIdentitySecret
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetSecretRotationResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/rotate-webhook-secret:
    post:
      tags:
        - Widget - Admin
      summary: Rotate webhook secret
      operationId: rotateWidgetWebhookSecret
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetSecretRotationResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/upload-logo:
    post:
      tags:
        - Widget - Admin
      summary: Upload logo
      operationId: widgetAdminUploadLogo
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BrandAssetUploadRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_BrandAssetUploadResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '413':
          description: Error 413.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '415':
          description: Error 415.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/upload-header-bg:
    post:
      tags:
        - Widget - Admin
      summary: Upload header image
      operationId: widgetAdminUploadHeaderBg
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BrandAssetUploadRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_BrandAssetUploadResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '413':
          description: Error 413.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '415':
          description: Error 415.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/articles:
    post:
      tags:
        - Widget - Admin
      summary: Create article
      operationId: widgetCreateArticle
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleCreateRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ArticleAdminResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      tags:
        - Widget - Admin
      summary: List articles
      operationId: widgetListArticlesAdmin
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_ArticleAdminResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/articles/{article_id}:
    patch:
      tags:
        - Widget - Admin
      summary: Update an article
      operationId: widgetUpdateArticle
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: article_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Article Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ArticleAdminResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Widget - Admin
      summary: Soft-delete an article
      operationId: widgetDeleteArticle
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: article_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Article Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetDeletedResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/articles/categories:
    post:
      tags:
        - Widget - Admin
      summary: Create category
      operationId: widgetCreateArticleCategory
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleCategoryCreateRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ArticleCategoryResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      tags:
        - Widget - Admin
      summary: List categories
      operationId: widgetListArticleCategories
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_list_ArticleCategoryResponse__
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/articles/categories/{category_id}:
    patch:
      tags:
        - Widget - Admin
      summary: Update category
      operationId: widgetUpdateArticleCategory
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: category_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Category Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleCategoryUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_ArticleCategoryResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Widget - Admin
      summary: Delete category
      operationId: widgetDeleteArticleCategory
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: category_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Category Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetDeletedResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/news:
    post:
      tags:
        - Widget - Admin
      summary: Create a news post
      operationId: widgetCreateNews
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NewsCreateRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_NewsAdminResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      tags:
        - Widget - Admin
      summary: List news posts
      operationId: widgetListNewsAdmin
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_NewsAdminResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/news/{news_id}:
    patch:
      tags:
        - Widget - Admin
      summary: Update a news post
      operationId: widgetUpdateNews
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: news_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: News Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NewsUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_NewsAdminResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Widget - Admin
      summary: Delete news post
      operationId: widgetDeleteNews
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: news_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: News Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetDeletedResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/gdpr-requests:
    get:
      tags:
        - Widget - Admin
      summary: List GDPR requests
      operationId: widgetListGdprRequests
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Status
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_GdprRequestResponse__'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/surveys:
    post:
      tags:
        - Widget - Admin
      summary: Create survey
      operationId: widgetCreateSurvey
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SurveyCreateRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_SurveyResponseSchema_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      tags:
        - Widget - Admin
      summary: List surveys
      operationId: widgetListSurveys
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_SurveyResponseSchema__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/surveys/{survey_id}:
    patch:
      tags:
        - Widget - Admin
      summary: Update a survey config
      operationId: widgetUpdateSurvey
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: survey_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Survey Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SurveyUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_SurveyResponseSchema_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Widget - Admin
      summary: Soft-delete a survey
      operationId: widgetDeleteSurvey
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: survey_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Survey Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetDeletedResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/surveys/{survey_id}/responses:
    get:
      tags:
        - Widget - Admin
      summary: Get survey stats
      operationId: widgetGetSurveyResponses
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: survey_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Survey Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_SurveyAggregateResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/audit-webhook-endpoints:
    post:
      tags:
        - Widget - Admin
      summary: Create endpoint
      operationId: widgetCreateAuditEndpoint
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EndpointCreateRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_EndpointCreateResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      tags:
        - Widget - Admin
      summary: List endpoints
      operationId: widgetListAuditEndpoints
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_EndpointResponse__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/audit-webhook-endpoints/{endpoint_id}:
    patch:
      tags:
        - Widget - Admin
      summary: Update endpoint
      operationId: widgetUpdateAuditEndpoint
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: endpoint_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Endpoint Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EndpointUpdateRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_EndpointResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Unprocessable entity — semantic validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags:
        - Widget - Admin
      summary: Delete endpoint
      operationId: widgetDeleteAuditEndpoint
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: endpoint_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Endpoint Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_WidgetDeletedResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/audit-webhook-endpoints/{endpoint_id}/test:
    post:
      tags:
        - Widget - Admin
      summary: Test endpoint
      operationId: widgetTestAuditEndpoint
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: endpoint_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Endpoint Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/audit-webhook-deliveries:
    get:
      tags:
        - Widget - Admin
      summary: List deliveries
      operationId: widgetListAuditDeliveries
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Status
        - name: eventType
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            title: Eventtype
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_DeliveryListItem__'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widgets/{widget_id}/audit-webhook-deliveries/{delivery_id}/replay:
    post:
      tags:
        - Widget - Admin
      summary: Replay delivery
      operationId: widgetReplayAuditDelivery
      parameters:
        - name: widget_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Widget Id
        - name: delivery_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            title: Delivery Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_dict_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /widget/admin/widget-audit-event-catalog:
    get:
      tags:
        - Widget - Admin
      summary: List event names
      operationId: widgetGetAuditEventCatalog
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_list_str__'
  /billing/invoices:
    get:
      tags:
        - Billing - Invoices
      summary: List invoices visible to the caller
      operationId: listBillingInvoices
      parameters:
        - name: skip
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
            title: Skip
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
            minimum: 1
            default: 50
            title: Limit
        - name: ownerType
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: >-
              Filter to invoices owned by this entity type ('tenant' or
              'organization').
            title: Ownertype
          description: >-
            Filter to invoices owned by this entity type ('tenant' or
            'organization').
        - name: ownerId
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter to invoices owned by this id.
            title: Ownerid
          description: Filter to invoices owned by this id.
        - name: status
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by status ('open', 'paid', 'void', 'uncollectible').
            title: Status
          description: Filter by status ('open', 'paid', 'void', 'uncollectible').
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: >-
                  #/components/schemas/ApiEnvelope_PaginatedBillingInvoiceResponse_
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /billing/invoices/{invoice_id}:
    get:
      tags:
        - Billing - Invoices
      summary: Retrieve a single invoice
      operationId: getBillingInvoice
      parameters:
        - name: invoice_id
          in: path
          required: true
          schema:
            type: string
            title: Invoice Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_BillingInvoiceResponse_'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /billing/invoices/{invoice_id}/json:
    get:
      tags:
        - Billing - Invoices
      summary: Download an invoice as a JSON package
      description: |-
        Machine-readable twin of the invoice PDF.

        Self-contained: totals, line items, dates and the FROZEN issuer and
        bill-to party snapshots, so an invoice re-downloaded years later
        reconciles against the parties as they were when it was issued.

        Returned unwrapped — no ``{success, data}`` envelope — because the
        body is a document a customer saves to a file or feeds to their
        accounting system, and a wrapper would force every consumer to
        unwrap before use. ``Content-Disposition`` names the file after the
        invoice number.
      operationId: downloadBillingInvoiceJson
      parameters:
        - name: invoice_id
          in: path
          required: true
          schema:
            type: string
            title: Invoice Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceJsonPackage'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /billing/invoices/{invoice_id}/mark-paid:
    post:
      tags:
        - Billing - Invoices
      summary: Confirm an offline payment landed → credits the MC ledger
      description: |-
        Mark a manual-contract invoice paid → credits the MC ledger.
        Idempotent — calling on an already-paid invoice returns the
        unchanged record.

        Auth: platform admins can mark any invoice on their platform.
        Tenant admins can mark invoices owned by their tenant directly OR
        by an organization under their tenant — they're the merchant of
        record for org manual-contract billing and need to record the
        off-Stripe payment when it lands. Org admins are blocked
        (service-layer ``_enforce_invoice_write_auth``).
      operationId: markInvoicePaid
      parameters:
        - name: invoice_id
          in: path
          required: true
          schema:
            type: string
            title: Invoice Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MarkInvoicePaidRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiEnvelope_BillingInvoiceResponse_'
        '400':
          description: Invalid request — malformed body or failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          description: Authentication required — missing or invalid access token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Forbidden — caller lacks the required permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Resource not found in the caller's workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Conflict — resource state prevents the requested change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
webhooks:
  assistant-request:
    post:
      tags:
        - Webhooks
      summary: Assistant Request
      description: >-
        Fires before the call is connected when the receiving phone number has
        ``server.url`` configured. Your endpoint **must** respond within
        ``server.timeoutSeconds`` (default 7s, max 7s) with one of the supported
        response shapes — see the response schema for the union of valid
        replies. The body is signed with HMAC-SHA256 in the
        ``X-Webhook-Signature`` header when a signing secret is configured on
        the phone number.
      operationId: webhookAssistantRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssistantRequestEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssistantRequestResponse'
  status-update:
    post:
      tags:
        - Webhooks
      summary: Status Update
      description: >-
        Call lifecycle transition (queued → ringing → in-progress → ended). When
        the call is in-progress, ``call.monitor`` carries live-monitoring URLs.
        Fire-and-forget; one retry on 5xx with 200ms backoff. Subscribe via
        ``assistant.webhookEvents`` containing ``status-update``.
      operationId: webhookStatusUpdate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StatusUpdateEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  transcript:
    post:
      tags:
        - Webhooks
      summary: Transcript
      description: >-
        Final or partial transcript chunk for a single utterance. Use
        ``isFinal`` to distinguish stable transcripts from streaming partials.
        Subscribe via ``assistant.webhookEvents``.
      operationId: webhookTranscript
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TranscriptEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  end-of-call-report:
    post:
      tags:
        - Webhooks
      summary: End of Call Report
      description: >-
        Post-call snapshot fired once per call after analysis and recording
        split finish. The most important event for downstream systems — persist
        transcripts, archive recordings, push CRM updates, run analytics.
        Subscribe via ``assistant.webhookEvents`` containing
        ``end-of-call-report``.
      operationId: webhookEndOfCallReport
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EndOfCallReportEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  tool-function-call:
    post:
      tags:
        - Webhooks
      summary: Tool Function Call
      description: >-
        Per-invocation HTTP call to the **tool's own** ``server.url`` — fires
        every time the LLM picks a function tool. **Sync mode** (``async =
        false``): the LLM blocks on your response and forwards your reply as the
        tool result. **Async mode** (``async = true``): fire-and-forget — the
        LLM sees ``{"async": true}`` immediately; your reply (if any) is
        discarded. Custom headers configured on ``tool.server.headers`` are
        merged into every request.


        The same payload shape is also used by the *transfer* tool's optional
        dynamic-destination resolver: when a transfer tool has ``server.url``
        configured and the requested destination doesn't match a static
        destination on the tool, the platform calls your endpoint with
        ``function.name = "get_transfer_destination"`` and ``function.arguments
        = {"requested_destination": ..., "reason": ...}``. Reply with
        ``{"destination": {...}}`` to forward the call to the resolved
        destination.
      operationId: webhookToolFunctionCall
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ToolCallEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolCallResponse'
  chat-session-status-update:
    post:
      tags:
        - Webhooks
      summary: Chat Session Status Update
      description: >-
        Fires when a chat session is created and again when it ends (closed,
        expired, or errored) — ``event`` says which. Delivered to the
        assistant's ``server.url``; subscribe via ``assistant.webhookEvents``
        containing ``chat-session-status-update``. Signed with HMAC-SHA256 in
        ``X-Webhook-Signature`` (``sha256=<hex>`` over the raw body) when
        ``server.secret`` is set.
      operationId: webhookChatSessionStatusUpdate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatSessionStatusUpdateEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  chat-message:
    post:
      tags:
        - Webhooks
      summary: Chat Message
      description: >-
        Fires after every completed assistant turn — and when an operator
        injects a hidden message (``message.source = "operator-inject"``).
        Delivered to the assistant's ``server.url``; subscribe via
        ``assistant.webhookEvents`` containing ``chat-message``. Signed with
        HMAC-SHA256 in ``X-Webhook-Signature``.
      operationId: webhookChatMessage
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatMessageEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  chat-session-end-report:
    post:
      tags:
        - Webhooks
      summary: Chat Session End Report
      description: >-
        The canonical conversation-finished record — delivered once the
        post-session pipeline (analysis, anonymization) completes, and
        **before** any zero-day retention purge, so this payload is your durable
        copy. Subscribe via ``assistant.webhookEvents`` containing
        ``chat-session-end-report``. Signed with HMAC-SHA256 in
        ``X-Webhook-Signature``. Delivery failures re-queue the job (up to 3
        attempts) — dedupe on ``id``.
      operationId: webhookChatSessionEndReport
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatSessionEndReportEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  widget-message-incoming:
    post:
      tags:
        - Webhooks
      summary: Widget Message Incoming
      description: >-
        One POST per visitor message to the widget's
        ``incomingMessageWebhookUrl`` — your endpoint is the widget's brain.
        Headers: ``X-AgenticFlow-Signature-256`` (``sha256=<hex>`` HMAC-SHA256
        over the raw body with the widget's webhook secret),
        ``X-AgenticFlow-Event: widget.message.incoming``,
        ``X-AgenticFlow-Delivery`` (dedupe key), ``X-AgenticFlow-Timestamp``.


        **Respond within 30 seconds**: ``200`` with the response body below
        replies inline (sync mode); ``202`` with an empty body acknowledges and
        you reply later via ``POST /widget/reply`` with the ``replyToken``
        (``interstitial`` messages keep the visitor's widget polling, ``final``
        closes the turn; at most 20 messages per token). Timeouts and ``5xx``
        are retried; ``4xx`` is not.
      operationId: webhookWidgetMessageIncoming
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WidgetMessageIncomingEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WidgetMessageSyncResponse'
  widget-audit-event:
    post:
      tags:
        - Webhooks
      summary: Widget Audit Event
      description: >-
        Write-only event mirror delivered to each configured audit-webhook
        endpoint (separate from the reply webhook). An empty subscription list
        on the endpoint subscribes to **all** events. Signed per endpoint with
        ``X-AgenticFlow-Signature-256`` over the raw body using that endpoint's
        own ``whsec_…`` secret. Delivery is at-least-once — dedupe on ``id``.
      operationId: webhookWidgetAuditEvent
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WidgetAuditEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  messaging-message-received:
    post:
      tags:
        - Webhooks
      summary: Message Received
      description: >-
        An inbound message arrived on a channel. ``messageType`` selects which
        content fields are populated (``text``, ``media`` with a signed 24h
        ``downloadUrl``, ``location``, ``contactVcard``, ``interactive``,
        inbound-email ``subject``). WhatsApp events carry the 24h window state
        so you can branch without a follow-up call. Delivered to the channel's
        ``webhookUrl``. Headers: ``X-Webhook-Signature``
        (``t=<unix_ts>,v1=<hex>`` — HMAC-SHA256 over
        ``v1:{timestamp}:{raw_body}`` with the channel's signing secret; reject
        stale timestamps, ~5 min tolerance), ``X-Webhook-Event-Id`` (dedupe
        key), ``X-Webhook-Event-Type``, ``X-Webhook-Channel-Id``, ``User-Agent:
        MessagingWebhook/1.0``. Respond ``2xx`` within 10 seconds. Retries:
        +30s, +2m, +10m, +1h (5 attempts total); after 25 consecutive permanent
        failures the URL is auto-disabled. Delivery is at-least-once and
        unordered — dedupe on ``eventId``, order by ``occurredAt``. The envelope
        is identical for every event type; inapplicable fields are ``null``.
      operationId: webhookMessagingMessageReceived
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessagingWebhookEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  messaging-message-status-changed:
    post:
      tags:
        - Webhooks
      summary: Status Changed
      description: >-
        An outbound message advanced — ``newStatus`` is ``sent``, ``delivered``,
        or ``read`` (there are no separate per-status events, and statuses never
        downgrade). Delivered to the channel's ``webhookUrl``. Headers:
        ``X-Webhook-Signature`` (``t=<unix_ts>,v1=<hex>`` — HMAC-SHA256 over
        ``v1:{timestamp}:{raw_body}`` with the channel's signing secret; reject
        stale timestamps, ~5 min tolerance), ``X-Webhook-Event-Id`` (dedupe
        key), ``X-Webhook-Event-Type``, ``X-Webhook-Channel-Id``, ``User-Agent:
        MessagingWebhook/1.0``. Respond ``2xx`` within 10 seconds. Retries:
        +30s, +2m, +10m, +1h (5 attempts total); after 25 consecutive permanent
        failures the URL is auto-disabled. Delivery is at-least-once and
        unordered — dedupe on ``eventId``, order by ``occurredAt``. The envelope
        is identical for every event type; inapplicable fields are ``null``.
      operationId: webhookMessagingMessageStatusChanged
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessagingWebhookEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  messaging-message-failed:
    post:
      tags:
        - Webhooks
      summary: Message Failed
      description: >-
        An outbound message permanently failed — ``failureReason`` carries the
        provider's verbatim reason (e.g. "recipient not on WhatsApp", "template
        not approved"). Failed messages are not retried by the platform.
        Delivered to the channel's ``webhookUrl``. Headers:
        ``X-Webhook-Signature`` (``t=<unix_ts>,v1=<hex>`` — HMAC-SHA256 over
        ``v1:{timestamp}:{raw_body}`` with the channel's signing secret; reject
        stale timestamps, ~5 min tolerance), ``X-Webhook-Event-Id`` (dedupe
        key), ``X-Webhook-Event-Type``, ``X-Webhook-Channel-Id``, ``User-Agent:
        MessagingWebhook/1.0``. Respond ``2xx`` within 10 seconds. Retries:
        +30s, +2m, +10m, +1h (5 attempts total); after 25 consecutive permanent
        failures the URL is auto-disabled. Delivery is at-least-once and
        unordered — dedupe on ``eventId``, order by ``occurredAt``. The envelope
        is identical for every event type; inapplicable fields are ``null``.
      operationId: webhookMessagingMessageFailed
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessagingWebhookEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  messaging-reaction-added:
    post:
      tags:
        - Webhooks
      summary: Reaction Added
      description: >-
        Someone reacted to one of your messages — ``reactionEmoji`` plus
        ``reactionTargetMessageId`` (the provider id of the message that was
        reacted to). Delivered to the channel's ``webhookUrl``. Headers:
        ``X-Webhook-Signature`` (``t=<unix_ts>,v1=<hex>`` — HMAC-SHA256 over
        ``v1:{timestamp}:{raw_body}`` with the channel's signing secret; reject
        stale timestamps, ~5 min tolerance), ``X-Webhook-Event-Id`` (dedupe
        key), ``X-Webhook-Event-Type``, ``X-Webhook-Channel-Id``, ``User-Agent:
        MessagingWebhook/1.0``. Respond ``2xx`` within 10 seconds. Retries:
        +30s, +2m, +10m, +1h (5 attempts total); after 25 consecutive permanent
        failures the URL is auto-disabled. Delivery is at-least-once and
        unordered — dedupe on ``eventId``, order by ``occurredAt``. The envelope
        is identical for every event type; inapplicable fields are ``null``.
      operationId: webhookMessagingReactionAdded
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessagingWebhookEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  messaging-reaction-removed:
    post:
      tags:
        - Webhooks
      summary: Reaction Removed
      description: >-
        A reaction was removed. Delivered to the channel's ``webhookUrl``.
        Headers: ``X-Webhook-Signature`` (``t=<unix_ts>,v1=<hex>`` — HMAC-SHA256
        over ``v1:{timestamp}:{raw_body}`` with the channel's signing secret;
        reject stale timestamps, ~5 min tolerance), ``X-Webhook-Event-Id``
        (dedupe key), ``X-Webhook-Event-Type``, ``X-Webhook-Channel-Id``,
        ``User-Agent: MessagingWebhook/1.0``. Respond ``2xx`` within 10 seconds.
        Retries: +30s, +2m, +10m, +1h (5 attempts total); after 25 consecutive
        permanent failures the URL is auto-disabled. Delivery is at-least-once
        and unordered — dedupe on ``eventId``, order by ``occurredAt``. The
        envelope is identical for every event type; inapplicable fields are
        ``null``.
      operationId: webhookMessagingReactionRemoved
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessagingWebhookEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  messaging-contact-opted-out:
    post:
      tags:
        - Webhooks
      summary: Contact Opted Out
      description: >-
        The contact opted out — a STOP keyword, unsubscribe link, or provider
        opt-out signal. The platform records it in the opt-out registry
        automatically. Delivered to the channel's ``webhookUrl``. Headers:
        ``X-Webhook-Signature`` (``t=<unix_ts>,v1=<hex>`` — HMAC-SHA256 over
        ``v1:{timestamp}:{raw_body}`` with the channel's signing secret; reject
        stale timestamps, ~5 min tolerance), ``X-Webhook-Event-Id`` (dedupe
        key), ``X-Webhook-Event-Type``, ``X-Webhook-Channel-Id``, ``User-Agent:
        MessagingWebhook/1.0``. Respond ``2xx`` within 10 seconds. Retries:
        +30s, +2m, +10m, +1h (5 attempts total); after 25 consecutive permanent
        failures the URL is auto-disabled. Delivery is at-least-once and
        unordered — dedupe on ``eventId``, order by ``occurredAt``. The envelope
        is identical for every event type; inapplicable fields are ``null``.
      operationId: webhookMessagingContactOptedOut
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessagingWebhookEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  messaging-contact-opted-in:
    post:
      tags:
        - Webhooks
      summary: Contact Opted In
      description: >-
        The contact opted back in (START keyword or manual removal) — the
        opt-out registry entry is lifted. Delivered to the channel's
        ``webhookUrl``. Headers: ``X-Webhook-Signature``
        (``t=<unix_ts>,v1=<hex>`` — HMAC-SHA256 over
        ``v1:{timestamp}:{raw_body}`` with the channel's signing secret; reject
        stale timestamps, ~5 min tolerance), ``X-Webhook-Event-Id`` (dedupe
        key), ``X-Webhook-Event-Type``, ``X-Webhook-Channel-Id``, ``User-Agent:
        MessagingWebhook/1.0``. Respond ``2xx`` within 10 seconds. Retries:
        +30s, +2m, +10m, +1h (5 attempts total); after 25 consecutive permanent
        failures the URL is auto-disabled. Delivery is at-least-once and
        unordered — dedupe on ``eventId``, order by ``occurredAt``. The envelope
        is identical for every event type; inapplicable fields are ``null``.
      operationId: webhookMessagingContactOptedIn
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessagingWebhookEvent'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
components:
  schemas:
    AddFileSource:
      properties:
        type:
          type: string
          const: file
          title: Type
          default: file
        fileId:
          type: string
          minLength: 1
          title: Fileid
          description: ID of an already-uploaded File.
      additionalProperties: false
      type: object
      required:
        - fileId
      title: AddFileSource
      description: 'Add-source body variant: attach an uploaded file.'
    AddUrlSource:
      properties:
        type:
          type: string
          const: url
          title: Type
          default: url
        url:
          type: string
          title: Url
          description: HTTP/HTTPS URL to crawl and ingest.
      additionalProperties: false
      type: object
      required:
        - url
      title: AddUrlSource
      description: |-
        Add-source body variant: crawl a URL.

        The ``url`` validator rejects wildcards, loopback, RFC1918 and cloud
        metadata addresses so the crawler can't be weaponised for SSRF.
    AiHeader:
      properties:
        header:
          type: string
          title: Header
          description: >-
            SIP header name, e.g. 'X-RoutingGroup'. Letters, digits and hyphens
            only; an 'X-' prefix is recommended for custom headers.
        description:
          type: string
          title: Description
          description: >-
            Instruction telling the AI what value to put here, based on the
            conversation.
        enum:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Enum
          description: Optional allowed values. When set, the AI must pick one of them.
        required:
          type: boolean
          title: Required
          description: When true, the AI is always asked to supply this header.
          default: false
      additionalProperties: false
      type: object
      required:
        - header
        - description
      title: AiHeader
      description: >-
        A SIP header whose VALUE the AI fills at call time.


        Unlike static ``sipHeaders`` (fixed strings or ``{{variable}}``
        templates

        resolved from call context), the AI chooses each value from the live

        conversation — guided by ``description`` and optionally constrained to

        ``enum`` — exactly like a function-tool argument. The chosen value is

        sanitized and placed on the outbound SIP transfer/escalation request.

        Used by ``transferCall`` and ``liveEscalate`` tools.
    AnalysisConfigSchema-Input:
      properties:
        summary:
          anyOf:
            - $ref: '#/components/schemas/SummaryConfigSchema'
            - type: 'null'
        structuredData:
          anyOf:
            - $ref: '#/components/schemas/StructuredDataConfigSchema-Input'
            - type: 'null'
        evaluation:
          anyOf:
            - $ref: '#/components/schemas/EvaluationConfigSchema'
            - type: 'null'
        anonymization:
          anyOf:
            - $ref: '#/components/schemas/AnonymizationConfigSchema'
            - type: 'null'
      type: object
      title: AnalysisConfigSchema
      description: >-
        Post-conversation analysis — applies to BOTH voice calls and chat
        sessions.


        Configure this once on the assistant; the same summary / structured-

        data / success-evaluation / anonymization rules run on every

        conversation that ends, regardless of channel. The transcript fed

        into the engine is the same shape (role + content + timestamp) for

        both voice and chat, so a prompt tuned for one channel works for

        the other.
    AnalysisConfigSchema-Output:
      properties:
        summary:
          anyOf:
            - $ref: '#/components/schemas/SummaryConfigSchema'
            - type: 'null'
        structuredData:
          anyOf:
            - $ref: '#/components/schemas/StructuredDataConfigSchema-Output'
            - type: 'null'
        evaluation:
          anyOf:
            - $ref: '#/components/schemas/EvaluationConfigSchema'
            - type: 'null'
        anonymization:
          anyOf:
            - $ref: '#/components/schemas/AnonymizationConfigSchema'
            - type: 'null'
      type: object
      title: AnalysisConfigSchema
      description: >-
        Post-conversation analysis — applies to BOTH voice calls and chat
        sessions.


        Configure this once on the assistant; the same summary / structured-

        data / success-evaluation / anonymization rules run on every

        conversation that ends, regardless of channel. The transcript fed

        into the engine is the same shape (role + content + timestamp) for

        both voice and chat, so a prompt tuned for one channel works for

        the other.
    AnonymizationConfigSchema:
      properties:
        enabled:
          type: boolean
          title: Enabled
          default: false
        prompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Prompt
          description: >-
            Optional override for the redaction engine's instructions. Leave
            null to use the platform default — most users should never set this.
        timeoutSeconds:
          type: integer
          maximum: 120
          minimum: 1
          title: Timeoutseconds
          default: 30
        maxRetries:
          type: integer
          maximum: 5
          minimum: 1
          title: Maxretries
          default: 3
        rules:
          items:
            $ref: '#/components/schemas/AnonymizationRuleSchema'
          type: array
          maxItems: 50
          title: Rules
      type: object
      title: AnonymizationConfigSchema
      description: >-
        Transcript anonymization — applies to BOTH voice calls and chat
        sessions.


        Substitution rules are channel-agnostic — PII is PII regardless of

        whether the transcript came from a phone call or a chat session.

        Runs AFTER analysis (so structured-data extraction sees the raw

        transcript) and BEFORE the end-of-conversation webhook fires.
    AnonymizationRuleSchema:
      properties:
        label:
          type: string
          maxLength: 64
          minLength: 1
          title: Label
          description: >-
            The literal replacement string substituted for matches (e.g.
            ``[PERSON]``, ``<NAME_1>``, ``***``). Used verbatim — no wrapping is
            applied on our side.
        description:
          type: string
          maxLength: 500
          minLength: 1
          title: Description
          description: >-
            Plain-language description of what to find — e.g. ``any human first
            or last name including nicknames and initials``. Used to identify
            matches in the transcript.
      type: object
      required:
        - label
        - description
      title: AnonymizationRuleSchema
      description: >-
        One redaction rule — what to find + the literal label to replace it
        with.
    Anthropic:
      properties:
        provider:
          type: string
          const: anthropic
          title: Provider
        model:
          type: string
          enum:
            - claude-sonnet-4-6
            - claude-haiku-4-5
            - claude-opus-4-6
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topK:
          anyOf:
            - type: integer
              maximum: 500
              minimum: 1
            - type: 'null'
          title: Topk
          description: Sample from the K most likely tokens. Default unset.
        caching:
          anyOf:
            - type: string
              const: ephemeral
            - type: 'null'
          title: Caching
          description: >-
            Prompt caching mode. ``ephemeral`` enables the 5-minute default
            cache — major cost saver for repetitive system prompts. See
            https://docs.claude.com/en/build-with-claude/prompt-caching
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: Anthropic
      description: |-
        Anthropic Claude LLM (pipeline mode).

        Anthropic's Messages API does not accept ``top_p``,
        ``frequency_penalty`` or ``presence_penalty``. Per Anthropic
        docs, ``top_p`` and ``temperature`` are also mutually exclusive.
        Supported fields:

          - ``temperature`` — 0-1
          - ``maxTokens``   — positive integer
          - ``topK``        — sample from the top-K most likely tokens
          - ``caching``     — prompt caching mode (``"ephemeral"`` for the
            5-min default cache; cuts token cost on repetitive prompts)
    ApiEnvelope_Annotated_Union_FunctionToolResponse__EndCallToolResponse__TransferCallToolResponse__VoicemailToolResponse__LiveEscalateToolResponse___FieldInfo_annotation_NoneType__required_True__discriminator__type____:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/FunctionToolResponse'
                - $ref: '#/components/schemas/EndCallToolResponse'
                - $ref: '#/components/schemas/TransferCallToolResponse'
                - $ref: '#/components/schemas/VoicemailToolResponse'
                - $ref: '#/components/schemas/LiveEscalateToolResponse'
              discriminator:
                propertyName: type
                mapping:
                  endCall: '#/components/schemas/EndCallToolResponse'
                  function: '#/components/schemas/FunctionToolResponse'
                  liveEscalate: '#/components/schemas/LiveEscalateToolResponse'
                  transferCall: '#/components/schemas/TransferCallToolResponse'
                  voicemail: '#/components/schemas/VoicemailToolResponse'
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: >-
        ApiEnvelope[Annotated[Union[FunctionToolResponse, EndCallToolResponse,
        TransferCallToolResponse, VoicemailToolResponse,
        LiveEscalateToolResponse], FieldInfo(annotation=NoneType, required=True,
        discriminator='type')]]
    ApiEnvelope_Annotated_Union_PipelineAssistantResponse__RealtimeAssistantResponse___FieldInfo_annotation_NoneType__required_True__discriminator__type____:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/PipelineAssistantResponse'
                - $ref: '#/components/schemas/RealtimeAssistantResponse'
              discriminator:
                propertyName: type
                mapping:
                  pipeline: '#/components/schemas/PipelineAssistantResponse'
                  realtime: '#/components/schemas/RealtimeAssistantResponse'
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: >-
        ApiEnvelope[Annotated[Union[PipelineAssistantResponse,
        RealtimeAssistantResponse], FieldInfo(annotation=NoneType,
        required=True, discriminator='type')]]
    ApiEnvelope_Annotated_Union_TwilioPhoneNumberResponse__ByoSipPhoneNumberResponse___FieldInfo_annotation_NoneType__required_True__discriminator__provider____:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/TwilioPhoneNumberResponse'
                - $ref: '#/components/schemas/ByoSipPhoneNumberResponse'
              discriminator:
                propertyName: provider
                mapping:
                  byo-sip: '#/components/schemas/ByoSipPhoneNumberResponse'
                  twilio: '#/components/schemas/TwilioPhoneNumberResponse'
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: >-
        ApiEnvelope[Annotated[Union[TwilioPhoneNumberResponse,
        ByoSipPhoneNumberResponse], FieldInfo(annotation=NoneType,
        required=True, discriminator='provider')]]
    ApiEnvelope_ArticleAdminResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ArticleAdminResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ArticleAdminResponse]
    ApiEnvelope_ArticleCategoryResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ArticleCategoryResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ArticleCategoryResponse]
    ApiEnvelope_BatchResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/BatchResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[BatchResponse]
    ApiEnvelope_BillingInvoiceResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/BillingInvoiceResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[BillingInvoiceResponse]
    ApiEnvelope_BrandAssetUploadResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/BrandAssetUploadResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[BrandAssetUploadResponse]
    ApiEnvelope_BulkSendResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/BulkSendResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[BulkSendResponse]
    ApiEnvelope_CallLogsResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/CallLogsResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[CallLogsResponse]
    ApiEnvelope_CallResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/CallResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[CallResponse]
    ApiEnvelope_CanSendResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/CanSendResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[CanSendResponse]
    ApiEnvelope_ChannelHealthResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ChannelHealthResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ChannelHealthResponse]
    ApiEnvelope_ChannelResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ChannelResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ChannelResponse]
    ApiEnvelope_ChatMessageListResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ChatMessageListResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ChatMessageListResponse]
    ApiEnvelope_ChatMessageResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ChatMessageResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ChatMessageResponse]
    ApiEnvelope_ChatMessageSendResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ChatMessageSendResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ChatMessageSendResponse]
    ApiEnvelope_ChatSessionListResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ChatSessionListResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ChatSessionListResponse]
    ApiEnvelope_ChatSessionResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ChatSessionResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ChatSessionResponse]
    ApiEnvelope_ConsentRecordResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ConsentRecordResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ConsentRecordResponse]
    ApiEnvelope_ContactResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ContactResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ContactResponse]
    ApiEnvelope_ControlResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ControlResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ControlResponse]
    ApiEnvelope_ConversationResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ConversationResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ConversationResponse]
    ApiEnvelope_EndTakeoverResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/EndTakeoverResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[EndTakeoverResponse]
    ApiEnvelope_EndpointCreateResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/EndpointCreateResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[EndpointCreateResponse]
    ApiEnvelope_EndpointResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/EndpointResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[EndpointResponse]
    ApiEnvelope_FileResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/FileResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[FileResponse]
    ApiEnvelope_FolderDeleteResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/FolderDeleteResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[FolderDeleteResponse]
    ApiEnvelope_FolderListResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/FolderListResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[FolderListResponse]
    ApiEnvelope_FolderResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/FolderResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[FolderResponse]
    ApiEnvelope_KnowledgeBaseResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/KnowledgeBaseResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[KnowledgeBaseResponse]
    ApiEnvelope_ListenTokenResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ListenTokenResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ListenTokenResponse]
    ApiEnvelope_LiveAgentUpdateResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/LiveAgentUpdateResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[LiveAgentUpdateResponse]
    ApiEnvelope_MediaUploadResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/MediaUploadResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[MediaUploadResponse]
    ApiEnvelope_MessageResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/MessageResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[MessageResponse]
    ApiEnvelope_MessageStatusHistoryResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/MessageStatusHistoryResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[MessageStatusHistoryResponse]
    ApiEnvelope_MonitorInfo_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/MonitorInfo'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[MonitorInfo]
    ApiEnvelope_NewsAdminResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/NewsAdminResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[NewsAdminResponse]
    ApiEnvelope_NoneType_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[NoneType]
    ApiEnvelope_NoteResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/NoteResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[NoteResponse]
    ApiEnvelope_OptOutCheckResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/OptOutCheckResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[OptOutCheckResponse]
    ApiEnvelope_OptOutResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/OptOutResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[OptOutResponse]
    ApiEnvelope_PaginatedAssistantResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedAssistantResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedAssistantResponse]
    ApiEnvelope_PaginatedBillingInvoiceResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedBillingInvoiceResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedBillingInvoiceResponse]
    ApiEnvelope_PaginatedCallResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedCallResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedCallResponse]
    ApiEnvelope_PaginatedFileResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedFileResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedFileResponse]
    ApiEnvelope_PaginatedKnowledgeBaseResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedKnowledgeBaseResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedKnowledgeBaseResponse]
    ApiEnvelope_PaginatedPhoneNumberResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedPhoneNumberResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedPhoneNumberResponse]
    ApiEnvelope_PaginatedResourceVersionsResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedResourceVersionsResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedResourceVersionsResponse]
    ApiEnvelope_PaginatedResponse_BatchResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedResponse_BatchResponse_'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedResponse[BatchResponse]]
    ApiEnvelope_PaginatedResponse_ChannelResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedResponse_ChannelResponse_'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedResponse[ChannelResponse]]
    ApiEnvelope_PaginatedResponse_ContactResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedResponse_ContactResponse_'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedResponse[ContactResponse]]
    ApiEnvelope_PaginatedResponse_ConversationResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedResponse_ConversationResponse_'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedResponse[ConversationResponse]]
    ApiEnvelope_PaginatedResponse_MessageResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedResponse_MessageResponse_'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedResponse[MessageResponse]]
    ApiEnvelope_PaginatedSipTrunkResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedSipTrunkResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedSipTrunkResponse]
    ApiEnvelope_PaginatedToolResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/PaginatedToolResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[PaginatedToolResponse]
    ApiEnvelope_QuickReplyResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/QuickReplyResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[QuickReplyResponse]
    ApiEnvelope_ResourceVersionResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/ResourceVersionResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[ResourceVersionResponse]
    ApiEnvelope_SendMessageResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/SendMessageResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[SendMessageResponse]
    ApiEnvelope_SipTrunkResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/SipTrunkResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[SipTrunkResponse]
    ApiEnvelope_SipTrunkUpdateResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/SipTrunkUpdateResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[SipTrunkUpdateResponse]
    ApiEnvelope_SurveyAggregateResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/SurveyAggregateResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[SurveyAggregateResponse]
    ApiEnvelope_SurveyResponseSchema_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/SurveyResponseSchema'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[SurveyResponseSchema]
    ApiEnvelope_TakeoverTokenResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/TakeoverTokenResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[TakeoverTokenResponse]
    ApiEnvelope_TemplatePreviewResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/TemplatePreviewResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[TemplatePreviewResponse]
    ApiEnvelope_TemplateResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/TemplateResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[TemplateResponse]
    ApiEnvelope_TemplateSyncResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/TemplateSyncResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[TemplateSyncResponse]
    ApiEnvelope_WebhookDeliveryResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/WebhookDeliveryResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[WebhookDeliveryResponse]
    ApiEnvelope_WidgetCreateResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/WidgetCreateResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[WidgetCreateResponse]
    ApiEnvelope_WidgetDeletedResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/WidgetDeletedResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[WidgetDeletedResponse]
    ApiEnvelope_WidgetResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/WidgetResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[WidgetResponse]
    ApiEnvelope_WidgetSecretRotationResponse_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - $ref: '#/components/schemas/WidgetSecretRotationResponse'
            - type: 'null'
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[WidgetSecretRotationResponse]
    ApiEnvelope_dict_:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[dict]
    ApiEnvelope_list_ArticleAdminResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/ArticleAdminResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[ArticleAdminResponse]]
    ApiEnvelope_list_ArticleCategoryResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/ArticleCategoryResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[ArticleCategoryResponse]]
    ApiEnvelope_list_ConsentRecordResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/ConsentRecordResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[ConsentRecordResponse]]
    ApiEnvelope_list_DeliveryListItem__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/DeliveryListItem'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[DeliveryListItem]]
    ApiEnvelope_list_EndpointResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/EndpointResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[EndpointResponse]]
    ApiEnvelope_list_GdprRequestResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/GdprRequestResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[GdprRequestResponse]]
    ApiEnvelope_list_NewsAdminResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/NewsAdminResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[NewsAdminResponse]]
    ApiEnvelope_list_NoteResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/NoteResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[NoteResponse]]
    ApiEnvelope_list_OptOutResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/OptOutResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[OptOutResponse]]
    ApiEnvelope_list_QuickReplyResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/QuickReplyResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[QuickReplyResponse]]
    ApiEnvelope_list_SurveyResponseSchema__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/SurveyResponseSchema'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[SurveyResponseSchema]]
    ApiEnvelope_list_TemplateResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/TemplateResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[TemplateResponse]]
    ApiEnvelope_list_WebhookDeliveryResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/WebhookDeliveryResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[WebhookDeliveryResponse]]
    ApiEnvelope_list_WidgetResponse__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                $ref: '#/components/schemas/WidgetResponse'
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[WidgetResponse]]
    ApiEnvelope_list_str__:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code (mirrors the response status).
        data:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Data
          description: Response payload.
        message:
          type: string
          title: Message
          description: Human-readable status message.
        success:
          type: boolean
          title: Success
          description: Always ``true`` for this envelope.
          default: true
      type: object
      required:
        - statusCode
        - data
        - message
      title: ApiEnvelope[list[str]]
    ArticleAdminResponse:
      properties:
        id:
          type: string
          title: Id
        widgetId:
          type: string
          title: Widgetid
        title:
          additionalProperties:
            type: string
          type: object
          title: Title
        body:
          additionalProperties:
            type: string
          type: object
          title: Body
        excerpt:
          additionalProperties:
            type: string
          type: object
          title: Excerpt
        categoryId:
          anyOf:
            - type: string
            - type: 'null'
          title: Categoryid
        coverImageUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Coverimageurl
        slug:
          type: string
          title: Slug
        status:
          type: string
          title: Status
        publishedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Publishedat
        viewCount:
          type: integer
          title: Viewcount
          default: 0
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - widgetId
        - title
        - body
        - excerpt
        - slug
        - status
        - createdAt
        - updatedAt
      title: ArticleAdminResponse
    ArticleCategoryCreateRequest:
      properties:
        name:
          additionalProperties:
            type: string
          type: object
          title: Name
        description:
          additionalProperties:
            type: string
          type: object
          title: Description
        iconName:
          anyOf:
            - type: string
              maxLength: 64
            - type: 'null'
          title: Iconname
        order:
          type: integer
          minimum: 0
          title: Order
          default: 0
      additionalProperties: false
      type: object
      required:
        - name
      title: ArticleCategoryCreateRequest
    ArticleCategoryResponse:
      properties:
        id:
          type: string
          title: Id
        widgetId:
          type: string
          title: Widgetid
        name:
          additionalProperties:
            type: string
          type: object
          title: Name
        description:
          additionalProperties:
            type: string
          type: object
          title: Description
        iconName:
          anyOf:
            - type: string
            - type: 'null'
          title: Iconname
        order:
          type: integer
          title: Order
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - widgetId
        - name
        - description
        - order
        - createdAt
        - updatedAt
      title: ArticleCategoryResponse
    ArticleCategoryUpdateRequest:
      properties:
        name:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Name
        description:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Description
        iconName:
          anyOf:
            - type: string
              maxLength: 64
            - type: 'null'
          title: Iconname
        order:
          anyOf:
            - type: integer
              minimum: 0
            - type: 'null'
          title: Order
      additionalProperties: false
      type: object
      title: ArticleCategoryUpdateRequest
    ArticleCreateRequest:
      properties:
        title:
          additionalProperties:
            type: string
          type: object
          title: Title
        body:
          additionalProperties:
            type: string
          type: object
          title: Body
        excerpt:
          additionalProperties:
            type: string
          type: object
          title: Excerpt
        categoryId:
          anyOf:
            - type: string
            - type: 'null'
          title: Categoryid
        coverImageUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Coverimageurl
        slug:
          type: string
          maxLength: 256
          minLength: 1
          pattern: ^[a-z0-9-]+$
          title: Slug
      additionalProperties: false
      type: object
      required:
        - title
        - body
        - slug
      title: ArticleCreateRequest
    ArticleUpdateRequest:
      properties:
        title:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Title
        body:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Body
        excerpt:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Excerpt
        categoryId:
          anyOf:
            - type: string
            - type: 'null'
          title: Categoryid
        coverImageUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Coverimageurl
        slug:
          anyOf:
            - type: string
              maxLength: 256
              minLength: 1
              pattern: ^[a-z0-9-]+$
            - type: 'null'
          title: Slug
        status:
          anyOf:
            - type: string
              pattern: ^(draft|published)$
            - type: 'null'
          title: Status
      additionalProperties: false
      type: object
      title: ArticleUpdateRequest
    AssemblyAI:
      properties:
        provider:
          type: string
          const: assemblyai
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - u3-rt-pro
                - universal-streaming-multilingual
                - universal-streaming-english
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        keyterms:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Keyterms
        speakerLabels:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Speakerlabels
        prompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Prompt
      additionalProperties: false
      type: object
      required:
        - provider
      title: AssemblyAI
      description: |-
        AssemblyAI streaming STT variant.

        NOTE: the plugin does NOT accept a ``language`` parameter — multilingual
        models auto-detect language. The ``language`` field is kept for UI
        consistency but ignored at construction time.
    AssignConversationRequest:
      properties:
        userId:
          type: string
          minLength: 1
          title: Userid
      additionalProperties: false
      type: object
      required:
        - userId
      title: AssignConversationRequest
    AssistantCallObject:
      properties:
        id:
          type: string
          title: Id
          description: Call ID.
        type:
          anyOf:
            - type: string
              enum:
                - inboundPhoneCall
                - outboundPhoneCall
                - webCall
            - type: 'null'
          title: Type
          description: Call type — inbound phone, outbound phone, or web call.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: Assistant currently handling the call.
        status:
          anyOf:
            - type: string
              enum:
                - queued
                - ringing
                - in-progress
                - ended
            - type: 'null'
          title: Status
          description: Call lifecycle status at dispatch time.
        startedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Startedat
          description: Call start (UTC ISO-8601).
        endedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Endedat
          description: Call end (UTC ISO-8601).
        endedReason:
          anyOf:
            - type: string
              enum:
                - assistant-ended-call
                - customer-ended-call
                - silence-timed-out
                - max-duration-reached
                - assistant-error
                - voicemail
                - pipeline-error-llm-failed
                - system-error
                - agent-init-failed
                - assistant-request-failed
                - fallback-destination
                - ringing-timeout
                - no-answer
                - call-forwarded
                - human-takeover
                - human-ended-call
                - outbound-trunk-misconfigured
                - outbound-sip-unavailable
                - outbound-sip-rejected
                - outbound-init-failed
                - insufficient-balance
                - balance-exhausted
                - billing-suspended
                - billing-misconfigured
                - concurrency-cap-exceeded
                - tier-not-allowed
                - unknown
            - type: 'null'
          title: Endedreason
          description: Why the call ended.
        durationSeconds:
          anyOf:
            - type: integer
            - type: 'null'
          title: Durationseconds
          description: Whole-second call duration.
        conversationOccurred:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Conversationoccurred
          description: >-
            Whether a real interaction happened — the assistant spoke to the
            caller OR the caller spoke. ``false`` marks a connected-but-empty
            call (misdial / caller hung up during ring: the line connected but
            no audio ever flowed). Use this to filter out abandoned connections
            **independent of** ``endedReason`` (the same empty call may end as
            ``customer-ended-call`` / ``assistant-ended-call`` /
            ``system-error``). Only present on the ``end-of-call-report`` event.
        monitor:
          anyOf:
            - $ref: '#/components/schemas/CallMonitorURLs'
            - type: 'null'
          description: >-
            Live-monitoring endpoints. Present on ``status-update`` while the
            call is in-progress and listen / control are enabled on the
            assistant.
        phoneNumber:
          anyOf:
            - $ref: '#/components/schemas/CallPhoneNumber'
            - type: 'null'
          description: >-
            Phone number record the call is bound to (record id, E.164 number,
            configured name). Absent for web calls.
        sipHeaders:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Sipheaders
          description: >-
            SIP participant attribute headers captured at call start. Present on
            SIP calls; absent for other transports.
        customer:
          anyOf:
            - $ref: '#/components/schemas/CallCustomer'
            - type: 'null'
          description: >-
            Customer record echoed for receiver convenience. Only ``number`` is
            guaranteed; other fields populate when supplied at call creation.
        callerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Callerid
          description: >-
            Caller ID actually used on the SIP INVITE for this outbound call.
            Reflects the resolved per-call override / per-number default / SIP
            registration username / phone number's E.164. Absent for inbound and
            web calls.
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
          description: >-
            Productized tier the assistant was bound to at call start
            (``standard`` / ``premium`` / ``gold``). Absent on Custom-mode
            assistants — those legacy customers see the underlying
            ``model.provider`` etc. as before. When ``tier`` is set the
            per-provider model / voice / transcriber details are intentionally
            omitted from this payload to keep the white-label contract clean.
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
          description: >-
            Tier voice-card id selected for the call (e.g. ``"eva"``,
            ``"holly"``). Only present when ``tier`` is set.
        billedAmountCents:
          anyOf:
            - type: integer
            - type: 'null'
          title: Billedamountcents
          description: >-
            Cost the organization incurred for this call, in cents (EUR).
            Settled on call cleanup; only present on the ``end-of-call-report``
            event.
        surchargeApplied:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Surchargeapplied
          description: >-
            True if any portion of the call's debit hit the surcharge rate (call
            ran past the MC bucket onto topup). Only present on the
            ``end-of-call-report`` event.
        retentionStatus:
          anyOf:
            - $ref: '#/components/schemas/CallRetentionStatusMeta'
            - type: 'null'
          description: >-
            Per-surface data retention application status. Receivers use this to
            detect when post-call data has been deleted by the assistant's
            retention policy — e.g. an ``analysis`` of ``"applied"`` means a
            follow-up ``GET /call/{id}`` will return ``null`` analysis even if
            the receiver saw analysis data on this very webhook payload (the
            wire carries the data once; the database does not). Only present on
            the ``end-of-call-report`` event and only when the assistant has
            data retention configured.
      type: object
      required:
        - id
      title: AssistantCallObject
      description: |-
        Call snapshot embedded in every assistant webhook event.

        Field-presence semantics (driven by the dispatcher's
        ``model_dump(exclude_unset=True)`` — only fields the dispatcher
        explicitly assigns appear on the wire):

          - ``id``, ``assistantId``, ``status``, ``startedAt`` — present
            on every event.
          - ``type``, ``phoneNumber``, ``customer`` — present on every
            event when the agent has them in scope. ``type`` discriminates
            inbound / outbound / web; ``phoneNumber`` carries the record
            id, E.164 number, and configured name; ``customer`` echoes the
            call's stored customer record.
          - ``monitor`` — only on ``status-update`` while the call is
            in-progress and listen / control are enabled on the assistant.
          - ``endedAt``, ``endedReason``, ``durationSeconds`` — only on
            ``end-of-call-report`` (and on ``status-update`` when status
            transitions to ``ended``).
          - ``sipHeaders`` — only on SIP calls; surfaces the carrier's
            custom attribute headers.
    AssistantDestination:
      properties:
        type:
          type: string
          const: assistant
          title: Type
          default: assistant
        assistantId:
          type: string
          title: Assistantid
        transferMode:
          type: string
          enum:
            - rolling-history
            - swap-system-message-in-history
            - delete-history
          title: Transfermode
          description: >-
            History handling on handoff:

            - ``rolling-history``: pass the full chat history through.

            - ``swap-system-message-in-history``: keep user/assistant turns,
            replace only the system prompt.

            - ``delete-history``: start the next assistant with a clean slate.
          default: rolling-history
        message:
          anyOf:
            - type: string
            - type: 'null'
          title: Message
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
      additionalProperties: false
      type: object
      required:
        - assistantId
      title: AssistantDestination
      description: |-
        Transfer destination — hand off the active session to another assistant.

        This is an in-session hand-off (``update_agent()``), not a SIP transfer.
        ``transferMode`` controls how conversation history is passed to the
        next assistant.
    AssistantRequestCallContext:
      properties:
        type:
          type: string
          const: inboundPhoneCall
          title: Type
          description: Call type. Only ``inboundPhoneCall`` is currently supported.
          default: inboundPhoneCall
        timestamp:
          type: string
          format: date-time
          title: Timestamp
          description: Webhook dispatch time (UTC ISO-8601).
        roomName:
          type: string
          title: Roomname
          description: Identifier of the realtime room created for this call.
        sipHeaders:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Sipheaders
          description: >-
            SIP participant attribute headers captured from the inbound leg
            before the agent joins. Useful for routing decisions based on
            carrier-supplied custom headers (Diversion, P-Asserted-Identity,
            X-Twilio-CallSid, …). Present only for SIP calls; absent for other
            transports.
      type: object
      required:
        - timestamp
        - roomName
      title: AssistantRequestCallContext
      description: Minimal call context — the call hasn't been created yet.
    AssistantRequestCustomer:
      properties:
        number:
          anyOf:
            - type: string
            - type: 'null'
          title: Number
          description: Caller phone number in E.164 format.
      type: object
      title: AssistantRequestCustomer
      description: |-
        Caller details. Only ``number`` is provided at this point — the
        call has not yet been bound to a customer record.
    AssistantRequestDestination:
      properties:
        type:
          type: string
          const: number
          title: Type
          description: Destination kind. Only ``number`` is supported today.
        number:
          type: string
          title: Number
          description: Phone number to forward to (E.164).
      type: object
      required:
        - type
        - number
      title: AssistantRequestDestination
      description: |-
        Immediate-transfer destination. Currently only ``type=number`` is
        supported — the call is forwarded to the supplied phone number
        without engaging any AI assistant.
    AssistantRequestEvent:
      properties:
        id:
          type: string
          title: Id
          description: Unique event ID — use for idempotency keys.
        type:
          type: string
          const: assistant-request
          title: Type
          default: assistant-request
        timestamp:
          type: string
          format: date-time
          title: Timestamp
          description: Dispatch time (UTC ISO-8601).
        call:
          $ref: '#/components/schemas/AssistantRequestCallContext'
        phoneNumber:
          $ref: '#/components/schemas/AssistantRequestPhoneNumber'
        customer:
          $ref: '#/components/schemas/AssistantRequestCustomer'
      type: object
      required:
        - id
        - timestamp
        - call
        - phoneNumber
        - customer
      title: AssistantRequestEvent
      description: |-
        Synchronous routing webhook fired on every inbound call when the
        phone number's ``server.url`` is configured.

        Your endpoint **must** respond within ``server.timeoutSeconds``
        (default 7s, max 7s) with one of the response shapes documented in
        :class:`AssistantRequestResponse`.
    AssistantRequestPhoneNumber:
      properties:
        id:
          type: string
          title: Id
          description: Phone number record ID.
        number:
          anyOf:
            - type: string
            - type: 'null'
          title: Number
          description: Phone number in E.164 format.
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name configured on the number.
      type: object
      required:
        - id
      title: AssistantRequestPhoneNumber
      description: The phone number that received the inbound call.
    AssistantRequestResponse:
      properties:
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: Existing assistant ID to handle the call.
        id:
          anyOf:
            - type: string
            - type: 'null'
          title: Id
          description: Alias for ``assistantId`` accepted for convenience.
        assistant:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Assistant
          description: >-
            Inline transient assistant. Must validate against the
            ``AssistantCreate`` schema — same shape as ``POST /assistant``.
        overrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Overrides
          description: >-
            Per-call overrides applied on top of the resolved assistant. Any
            ``AssistantCreate`` field can be overridden. ``variables`` are
            deep-merged with the call's stored variables.
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: >-
            Per-call free-form metadata to attach to this inbound call.
            Persisted on the Call doc (``Call.metadata``) and echoed at
            top-level on every downstream Family-B event (``status-update`` /
            ``transcript`` / ``end-of-call-report``) and Family-C
            ``tool-function-call`` event. This is the inbound analog of
            ``CallCreate.metadata`` on ``POST /call``. Combine with any of the
            other response patterns above.
        destination:
          anyOf:
            - $ref: '#/components/schemas/AssistantRequestDestination'
            - type: 'null'
          description: Forward the call instead of engaging an assistant.
        error:
          anyOf:
            - type: string
            - type: 'null'
          title: Error
          description: >-
            Surface a routing error. The message is logged; the call falls back
            to the assigned assistant.
      type: object
      title: AssistantRequestResponse
      description: |-
        Response shape your endpoint returns to route the call.

        Exactly one of these patterns should be supplied:

          - **Empty object** (``{}``) — fall back to the phone number's
            assigned assistant.
          - ``{"assistantId": "..."}`` — use an existing assistant by ID.
            ``id`` is accepted as an alias for ``assistantId``.
          - ``{"assistantId": "...", "overrides": {...}}`` — existing
            assistant with per-call overrides (any
            ``AssistantCreate`` field is overridable; ``variables`` are
            deep-merged for prompt substitution).
          - ``{"assistant": {...}}`` — transient inline assistant. Body must
            validate against the ``AssistantCreate`` schema. The transient
            config is used for this call only.
          - ``{"destination": {...}}`` — immediate transfer (no AI).
          - ``{"error": "..."}`` — log the message and fall back to the
            assigned assistant.

        The optional ``metadata`` field can be combined with any of the
        above (it's the inbound analog of ``CallCreate.metadata`` —
        persisted on the Call doc and echoed on every Family-B + C event
        for the call).
    AssistantSortBy:
      type: string
      enum:
        - createdAt
        - updatedAt
        - name
      title: AssistantSortBy
    AssistantType:
      type: string
      enum:
        - pipeline
        - realtime
      title: AssistantType
      description: Assistant execution mode.
    AssistantUpdate:
      properties:
        type:
          anyOf:
            - type: string
            - type: 'null'
          title: Type
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
        firstMessage:
          anyOf:
            - type: string
            - type: 'null'
          title: Firstmessage
        firstMessageMode:
          anyOf:
            - type: string
              enum:
                - assistant-speaks-first
                - assistant-speaks-first-with-model-generated-message
                - assistant-waits-for-user
            - type: 'null'
          title: Firstmessagemode
        firstMessageFallbackSeconds:
          anyOf:
            - type: number
            - type: 'null'
          title: Firstmessagefallbackseconds
        model:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/Anthropic'
                - $ref: '#/components/schemas/AwsBedrock'
                - $ref: '#/components/schemas/AzureOpenai'
                - $ref: '#/components/schemas/Baseten'
                - $ref: '#/components/schemas/Cerebras'
                - $ref: '#/components/schemas/CustomOpenAI'
                - $ref: '#/components/schemas/DeepSeek'
                - $ref: '#/components/schemas/Google'
                - $ref: '#/components/schemas/Groq'
                - $ref: '#/components/schemas/Moonshot'
                - $ref: '#/components/schemas/OpenAI'
                - $ref: '#/components/schemas/OpenRouter'
                - $ref: '#/components/schemas/Perplexity'
                - $ref: '#/components/schemas/Xai'
                - $ref: '#/components/schemas/AwsNovaSonic'
                - $ref: '#/components/schemas/OpenAIRealtime'
                - $ref: '#/components/schemas/AzureOpenAIRealtime'
                - $ref: '#/components/schemas/DeepslateRealtime'
                - $ref: '#/components/schemas/GoogleRealtimeConfig-Input'
                - $ref: '#/components/schemas/PhonicRealtime'
                - $ref: '#/components/schemas/UltravoxRealtime'
                - $ref: '#/components/schemas/XaiRealtime'
              discriminator:
                propertyName: provider
                mapping:
                  anthropic: '#/components/schemas/Anthropic'
                  aws-bedrock: '#/components/schemas/AwsBedrock'
                  aws-nova-sonic: '#/components/schemas/AwsNovaSonic'
                  azure-openai: '#/components/schemas/AzureOpenai'
                  azure-openai-realtime: '#/components/schemas/AzureOpenAIRealtime'
                  baseten: '#/components/schemas/Baseten'
                  cerebras: '#/components/schemas/Cerebras'
                  custom-openai: '#/components/schemas/CustomOpenAI'
                  deepseek: '#/components/schemas/DeepSeek'
                  deepslate-realtime: '#/components/schemas/DeepslateRealtime'
                  google: '#/components/schemas/Google'
                  google-realtime: '#/components/schemas/GoogleRealtimeConfig-Input'
                  groq: '#/components/schemas/Groq'
                  moonshot: '#/components/schemas/Moonshot'
                  openai: '#/components/schemas/OpenAI'
                  openai-realtime: '#/components/schemas/OpenAIRealtime'
                  openrouter: '#/components/schemas/OpenRouter'
                  perplexity: '#/components/schemas/Perplexity'
                  phonic-realtime: '#/components/schemas/PhonicRealtime'
                  ultravox-realtime: '#/components/schemas/UltravoxRealtime'
                  xai: '#/components/schemas/Xai'
                  xai-realtime: '#/components/schemas/XaiRealtime'
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AsyncAI'
                - $ref: '#/components/schemas/AwsPolly'
                - $ref: '#/components/schemas/AzureTTS'
                - $ref: '#/components/schemas/AzureOpenAITTS'
                - $ref: '#/components/schemas/Cartesia'
                - $ref: '#/components/schemas/DeepgramTTS'
                - $ref: '#/components/schemas/ElevenLabs'
                - $ref: '#/components/schemas/Fruitly'
                - $ref: '#/components/schemas/GeminiTTS'
                - $ref: '#/components/schemas/GoogleTTS'
                - $ref: '#/components/schemas/GradiumTTS'
                - $ref: '#/components/schemas/GroqTTS'
                - $ref: '#/components/schemas/Hume'
                - $ref: '#/components/schemas/Inworld'
                - $ref: '#/components/schemas/Lmnt'
                - $ref: '#/components/schemas/MinimaxTTS'
                - $ref: '#/components/schemas/Munsit'
                - $ref: '#/components/schemas/Neuphonic'
                - $ref: '#/components/schemas/NvidiaTTS'
                - $ref: '#/components/schemas/OpenAITTS'
                - $ref: '#/components/schemas/Rime'
                - $ref: '#/components/schemas/SarvamTTS'
                - $ref: '#/components/schemas/Silma'
                - $ref: '#/components/schemas/SmallestAI'
                - $ref: '#/components/schemas/Speechify'
                - $ref: '#/components/schemas/SpeechmaticsTTS'
                - $ref: '#/components/schemas/XaiTTS'
              discriminator:
                propertyName: provider
                mapping:
                  asyncai: '#/components/schemas/AsyncAI'
                  aws-polly: '#/components/schemas/AwsPolly'
                  azure-openai-tts: '#/components/schemas/AzureOpenAITTS'
                  azure-tts: '#/components/schemas/AzureTTS'
                  cartesia: '#/components/schemas/Cartesia'
                  deepgram-tts: '#/components/schemas/DeepgramTTS'
                  elevenlabs: '#/components/schemas/ElevenLabs'
                  fruitly: '#/components/schemas/Fruitly'
                  gemini-tts: '#/components/schemas/GeminiTTS'
                  google-tts: '#/components/schemas/GoogleTTS'
                  gradium-tts: '#/components/schemas/GradiumTTS'
                  groq-tts: '#/components/schemas/GroqTTS'
                  hume: '#/components/schemas/Hume'
                  inworld: '#/components/schemas/Inworld'
                  lmnt: '#/components/schemas/Lmnt'
                  minimax-tts: '#/components/schemas/MinimaxTTS'
                  munsit: '#/components/schemas/Munsit'
                  neuphonic: '#/components/schemas/Neuphonic'
                  nvidia-tts: '#/components/schemas/NvidiaTTS'
                  openai-tts: '#/components/schemas/OpenAITTS'
                  rime: '#/components/schemas/Rime'
                  sarvam-tts: '#/components/schemas/SarvamTTS'
                  silma: '#/components/schemas/Silma'
                  smallestai: '#/components/schemas/SmallestAI'
                  speechify: '#/components/schemas/Speechify'
                  speechmatics-tts: '#/components/schemas/SpeechmaticsTTS'
                  xai-tts: '#/components/schemas/XaiTTS'
            - type: 'null'
          title: Voice
        transcriber:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AssemblyAI'
                - $ref: '#/components/schemas/AwsTranscribe'
                - $ref: '#/components/schemas/AzureSTT'
                - $ref: '#/components/schemas/AzureOpenAISTT'
                - $ref: '#/components/schemas/CartesiaSTT'
                - $ref: '#/components/schemas/DeepgramSTT'
                - $ref: '#/components/schemas/ElevenLabsSTT'
                - $ref: '#/components/schemas/GoogleSTT'
                - $ref: '#/components/schemas/GradiumSTT'
                - $ref: '#/components/schemas/GroqSTT'
                - $ref: '#/components/schemas/NvidiaSTT'
                - $ref: '#/components/schemas/OpenAISTT'
                - $ref: '#/components/schemas/SarvamSTT'
                - $ref: '#/components/schemas/SonioxSTT'
                - $ref: '#/components/schemas/SpeechmaticsSTT'
              discriminator:
                propertyName: provider
                mapping:
                  assemblyai: '#/components/schemas/AssemblyAI'
                  aws-transcribe: '#/components/schemas/AwsTranscribe'
                  azure-openai-stt: '#/components/schemas/AzureOpenAISTT'
                  azure-stt: '#/components/schemas/AzureSTT'
                  cartesia-stt: '#/components/schemas/CartesiaSTT'
                  deepgram: '#/components/schemas/DeepgramSTT'
                  elevenlabs-stt: '#/components/schemas/ElevenLabsSTT'
                  google-stt: '#/components/schemas/GoogleSTT'
                  gradium-stt: '#/components/schemas/GradiumSTT'
                  groq-stt: '#/components/schemas/GroqSTT'
                  nvidia-stt: '#/components/schemas/NvidiaSTT'
                  openai-stt: '#/components/schemas/OpenAISTT'
                  sarvam-stt: '#/components/schemas/SarvamSTT'
                  soniox: '#/components/schemas/SonioxSTT'
                  speechmatics-stt: '#/components/schemas/SpeechmaticsSTT'
            - type: 'null'
          title: Transcriber
        modelFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/Anthropic'
                - $ref: '#/components/schemas/AwsBedrock'
                - $ref: '#/components/schemas/AzureOpenai'
                - $ref: '#/components/schemas/Baseten'
                - $ref: '#/components/schemas/Cerebras'
                - $ref: '#/components/schemas/CustomOpenAI'
                - $ref: '#/components/schemas/DeepSeek'
                - $ref: '#/components/schemas/Google'
                - $ref: '#/components/schemas/Groq'
                - $ref: '#/components/schemas/Moonshot'
                - $ref: '#/components/schemas/OpenAI'
                - $ref: '#/components/schemas/OpenRouter'
                - $ref: '#/components/schemas/Perplexity'
                - $ref: '#/components/schemas/Xai'
              discriminator:
                propertyName: provider
                mapping:
                  anthropic: '#/components/schemas/Anthropic'
                  aws-bedrock: '#/components/schemas/AwsBedrock'
                  azure-openai: '#/components/schemas/AzureOpenai'
                  baseten: '#/components/schemas/Baseten'
                  cerebras: '#/components/schemas/Cerebras'
                  custom-openai: '#/components/schemas/CustomOpenAI'
                  deepseek: '#/components/schemas/DeepSeek'
                  google: '#/components/schemas/Google'
                  groq: '#/components/schemas/Groq'
                  moonshot: '#/components/schemas/Moonshot'
                  openai: '#/components/schemas/OpenAI'
                  openrouter: '#/components/schemas/OpenRouter'
                  perplexity: '#/components/schemas/Perplexity'
                  xai: '#/components/schemas/Xai'
            - type: 'null'
          title: Modelfallback
          description: >-
            Patch the fallback LLM. Pass an LLM config dict to set or replace;
            ``null`` clears (resets the stage to unprotected); omit the field to
            leave it unchanged. **Custom-mode only** and **pipeline-only** — the
            merged document is re-validated after the patch and tier-mode /
            realtime-mode assistants with a non-null ``modelFallback`` are
            rejected with 400.
        voiceFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AsyncAI'
                - $ref: '#/components/schemas/AwsPolly'
                - $ref: '#/components/schemas/AzureTTS'
                - $ref: '#/components/schemas/AzureOpenAITTS'
                - $ref: '#/components/schemas/Cartesia'
                - $ref: '#/components/schemas/DeepgramTTS'
                - $ref: '#/components/schemas/ElevenLabs'
                - $ref: '#/components/schemas/Fruitly'
                - $ref: '#/components/schemas/GeminiTTS'
                - $ref: '#/components/schemas/GoogleTTS'
                - $ref: '#/components/schemas/GradiumTTS'
                - $ref: '#/components/schemas/GroqTTS'
                - $ref: '#/components/schemas/Hume'
                - $ref: '#/components/schemas/Inworld'
                - $ref: '#/components/schemas/Lmnt'
                - $ref: '#/components/schemas/MinimaxTTS'
                - $ref: '#/components/schemas/Munsit'
                - $ref: '#/components/schemas/Neuphonic'
                - $ref: '#/components/schemas/NvidiaTTS'
                - $ref: '#/components/schemas/OpenAITTS'
                - $ref: '#/components/schemas/Rime'
                - $ref: '#/components/schemas/SarvamTTS'
                - $ref: '#/components/schemas/Silma'
                - $ref: '#/components/schemas/SmallestAI'
                - $ref: '#/components/schemas/Speechify'
                - $ref: '#/components/schemas/SpeechmaticsTTS'
                - $ref: '#/components/schemas/XaiTTS'
              discriminator:
                propertyName: provider
                mapping:
                  asyncai: '#/components/schemas/AsyncAI'
                  aws-polly: '#/components/schemas/AwsPolly'
                  azure-openai-tts: '#/components/schemas/AzureOpenAITTS'
                  azure-tts: '#/components/schemas/AzureTTS'
                  cartesia: '#/components/schemas/Cartesia'
                  deepgram-tts: '#/components/schemas/DeepgramTTS'
                  elevenlabs: '#/components/schemas/ElevenLabs'
                  fruitly: '#/components/schemas/Fruitly'
                  gemini-tts: '#/components/schemas/GeminiTTS'
                  google-tts: '#/components/schemas/GoogleTTS'
                  gradium-tts: '#/components/schemas/GradiumTTS'
                  groq-tts: '#/components/schemas/GroqTTS'
                  hume: '#/components/schemas/Hume'
                  inworld: '#/components/schemas/Inworld'
                  lmnt: '#/components/schemas/Lmnt'
                  minimax-tts: '#/components/schemas/MinimaxTTS'
                  munsit: '#/components/schemas/Munsit'
                  neuphonic: '#/components/schemas/Neuphonic'
                  nvidia-tts: '#/components/schemas/NvidiaTTS'
                  openai-tts: '#/components/schemas/OpenAITTS'
                  rime: '#/components/schemas/Rime'
                  sarvam-tts: '#/components/schemas/SarvamTTS'
                  silma: '#/components/schemas/Silma'
                  smallestai: '#/components/schemas/SmallestAI'
                  speechify: '#/components/schemas/Speechify'
                  speechmatics-tts: '#/components/schemas/SpeechmaticsTTS'
                  xai-tts: '#/components/schemas/XaiTTS'
            - type: 'null'
          title: Voicefallback
          description: >-
            Patch the fallback TTS voice. Pass a TTS config dict to set or
            replace; ``null`` clears; omit to leave unchanged. **Custom-mode
            only** and **pipeline-only**.
        transcriberFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AssemblyAI'
                - $ref: '#/components/schemas/AwsTranscribe'
                - $ref: '#/components/schemas/AzureSTT'
                - $ref: '#/components/schemas/AzureOpenAISTT'
                - $ref: '#/components/schemas/CartesiaSTT'
                - $ref: '#/components/schemas/DeepgramSTT'
                - $ref: '#/components/schemas/ElevenLabsSTT'
                - $ref: '#/components/schemas/GoogleSTT'
                - $ref: '#/components/schemas/GradiumSTT'
                - $ref: '#/components/schemas/GroqSTT'
                - $ref: '#/components/schemas/NvidiaSTT'
                - $ref: '#/components/schemas/OpenAISTT'
                - $ref: '#/components/schemas/SarvamSTT'
                - $ref: '#/components/schemas/SonioxSTT'
                - $ref: '#/components/schemas/SpeechmaticsSTT'
              discriminator:
                propertyName: provider
                mapping:
                  assemblyai: '#/components/schemas/AssemblyAI'
                  aws-transcribe: '#/components/schemas/AwsTranscribe'
                  azure-openai-stt: '#/components/schemas/AzureOpenAISTT'
                  azure-stt: '#/components/schemas/AzureSTT'
                  cartesia-stt: '#/components/schemas/CartesiaSTT'
                  deepgram: '#/components/schemas/DeepgramSTT'
                  elevenlabs-stt: '#/components/schemas/ElevenLabsSTT'
                  google-stt: '#/components/schemas/GoogleSTT'
                  gradium-stt: '#/components/schemas/GradiumSTT'
                  groq-stt: '#/components/schemas/GroqSTT'
                  nvidia-stt: '#/components/schemas/NvidiaSTT'
                  openai-stt: '#/components/schemas/OpenAISTT'
                  sarvam-stt: '#/components/schemas/SarvamSTT'
                  soniox: '#/components/schemas/SonioxSTT'
                  speechmatics-stt: '#/components/schemas/SpeechmaticsSTT'
            - type: 'null'
          title: Transcriberfallback
          description: >-
            Patch the fallback STT transcriber. Pass an STT config dict to set
            or replace; ``null`` clears; omit to leave unchanged. **Custom-mode
            only** and **pipeline-only**.
        tools:
          anyOf:
            - items:
                anyOf:
                  - type: string
                  - oneOf:
                      - $ref: '#/components/schemas/FunctionTool-Input'
                      - $ref: '#/components/schemas/EndCallTool-Input'
                      - $ref: '#/components/schemas/TransferCallTool-Input'
                      - $ref: '#/components/schemas/VoicemailTool-Input'
                      - $ref: '#/components/schemas/LiveEscalateTool-Input'
                    discriminator:
                      propertyName: type
                      mapping:
                        endCall: '#/components/schemas/EndCallTool-Input'
                        function: '#/components/schemas/FunctionTool-Input'
                        liveEscalate: '#/components/schemas/LiveEscalateTool-Input'
                        transferCall: '#/components/schemas/TransferCallTool-Input'
                        voicemail: '#/components/schemas/VoicemailTool-Input'
              type: array
            - type: 'null'
          title: Tools
          description: >-
            Replace the assistant's tool list. Each entry is either a tool id
            (string) or an inline tool object (``function``, ``endCall``,
            ``transferCall``, ``voicemail``, or ``liveEscalate``). Pass ``[]``
            to clear all tools; omit the field to leave them unchanged.
        knowledgeBaseIds:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Knowledgebaseids
        kbRetrievalMode:
          anyOf:
            - type: string
              enum:
                - auto
                - tool
            - type: 'null'
          title: Kbretrievalmode
          description: >-
            Update the KB retrieval mode. ``null`` resets to the platform
            default (pipeline → ``auto``, realtime → ``tool``). Tier-mode
            assistants ignore this field — the tier picker forces the value.
        silenceTimeoutSeconds:
          anyOf:
            - type: number
              maximum: 600
              minimum: 10
            - type: 'null'
          title: Silencetimeoutseconds
        maxDurationSeconds:
          anyOf:
            - type: integer
              maximum: 43200
              minimum: 5
            - type: 'null'
          title: Maxdurationseconds
        allowInterruptions:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Allowinterruptions
        preemptiveGeneration:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Preemptivegeneration
        maxToolSteps:
          anyOf:
            - type: integer
              maximum: 10
              minimum: 1
            - type: 'null'
          title: Maxtoolsteps
        chatMessagesInContext:
          anyOf:
            - type: integer
              maximum: 500
              minimum: 5
            - type: 'null'
          title: Chatmessagesincontext
        backgroundSound:
          anyOf:
            - $ref: '#/components/schemas/BackgroundSoundSchema'
            - type: 'null'
        webhookEvents:
          anyOf:
            - items:
                type: string
                enum:
                  - status-update
                  - transcript
                  - end-of-call-report
                  - chat-session-status-update
                  - chat-message
                  - chat-session-end-report
              type: array
            - type: 'null'
          title: Webhookevents
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfigSchema'
            - type: 'null'
        analysis:
          anyOf:
            - $ref: '#/components/schemas/AnalysisConfigSchema-Input'
            - type: 'null'
        dataRetention:
          anyOf:
            - $ref: '#/components/schemas/DataRetentionConfigSchema'
            - type: 'null'
        chatDataRetention:
          anyOf:
            - $ref: '#/components/schemas/ChatDataRetentionConfigSchema'
            - type: 'null'
        endpointing:
          anyOf:
            - $ref: '#/components/schemas/EndpointingConfigSchema'
            - type: 'null'
        interruption:
          anyOf:
            - $ref: '#/components/schemas/InterruptionConfigSchema'
            - type: 'null'
        idle:
          anyOf:
            - $ref: '#/components/schemas/IdleConfigSchema'
            - type: 'null'
        recording:
          anyOf:
            - $ref: '#/components/schemas/RecordingConfigSchema'
            - type: 'null'
        monitoring:
          anyOf:
            - $ref: '#/components/schemas/MonitoringConfigSchema'
            - type: 'null'
        textProcessing:
          anyOf:
            - $ref: '#/components/schemas/TextProcessingSchema'
            - type: 'null'
        performance:
          anyOf:
            - $ref: '#/components/schemas/PerformanceSchema'
            - type: 'null'
        telephony:
          anyOf:
            - $ref: '#/components/schemas/TelephonySchema'
            - type: 'null'
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
        tierLanguage:
          anyOf:
            - type: string
            - type: 'null'
          title: Tierlanguage
        tierSttOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiersttoverrides
        tierVoiceOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiervoiceoverrides
        tierRealtimeOverrides:
          anyOf:
            - $ref: '#/components/schemas/TierRealtimeOverrides'
            - type: 'null'
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
      type: object
      title: AssistantUpdate
      description: |-
        Update assistant request schema — all fields optional.

        Kept as a single class (not a discriminated union) because PATCH
        bodies legitimately omit ``type`` when the caller is changing
        something unrelated. The router merges this against the existing
        document and re-validates the merged result via
        ``_validate_post_update`` to catch any cross-mode invariant
        violation introduced by the partial patch.
    AsyncAI:
      properties:
        provider:
          type: string
          const: asyncai
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            AsyncAI voice id. See https://docs.async.com for the upstream voice
            catalog.
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: AsyncAI
      description: AsyncAI TTS variant — ultra-low latency, voice cloning.
    AuditWebhookEndpointResponse:
      properties:
        id:
          type: string
          title: Id
        url:
          type: string
          title: Url
        subscribedEvents:
          items:
            type: string
          type: array
          title: Subscribedevents
        enabled:
          type: boolean
          title: Enabled
        createdAt:
          type: string
          format: date-time
          title: Createdat
        hasRotationInProgress:
          type: boolean
          title: Hasrotationinprogress
          default: false
      additionalProperties: true
      type: object
      required:
        - id
        - url
        - subscribedEvents
        - enabled
        - createdAt
      title: AuditWebhookEndpointResponse
      description: |-
        Audit-webhook endpoint surfaced on widget GET responses.

        The signing secret is NEVER returned here — admin sees it once at
        endpoint creation, then must rotate to see a new one.
    AwsBedrock:
      properties:
        provider:
          type: string
          const: aws-bedrock
          title: Provider
        model:
          type: string
          title: Model
          description: >-
            Any Bedrock model id (e.g.
            ``anthropic.claude-sonnet-4-5-20250929-v1:0``,
            ``amazon.nova-2-pro-v1:0``). The full per-account list comes from
            AWS's ``bedrock.ListFoundationModels`` API — see
            https://docs.aws.amazon.com/bedrock/latest/userguide/models-supported.html
            for available models in your region.
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 64000
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        cacheSystem:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Cachesystem
        cacheTools:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Cachetools
        additionalRequestFields:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Additionalrequestfields
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: AwsBedrock
      description: |-
        AWS Bedrock LLM (pipeline mode, multi-credential).

        ``model`` is open-ended ``str`` — Bedrock's catalog is account-specific
        so the user submits any model id their account has access to. The
        static MODELS list is just a curated reference shown in the docs.

        No `frequencyPenalty` / `presencePenalty` — Bedrock doesn't accept them.
    AwsNovaSonic:
      properties:
        provider:
          type: string
          const: aws-nova-sonic
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - amazon.nova-2-sonic-v1:0
                - amazon.nova-sonic-v1:0
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
              enum:
                - tiffany
                - matthew
                - amy
                - olivia
                - lupe
                - carlos
                - ambre
                - florian
                - tina
                - lennart
                - greta
                - beatrice
                - lorenzo
                - carolina
                - leo
                - arjun
                - kiara
            - type: 'null'
          title: Voice
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Temperature
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        turnDetection:
          anyOf:
            - type: string
              enum:
                - HIGH
                - MEDIUM
                - LOW
            - type: 'null'
          title: Turndetection
      additionalProperties: false
      type: object
      required:
        - provider
      title: AwsNovaSonic
      description: AWS Nova Sonic — multi-cred (AWS access key + secret + region).
    AwsPolly:
      properties:
        provider:
          type: string
          const: aws-polly
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Polly voice name (e.g. `Ruth`, `Tiffany`, `Joanna`). See
            https://docs.aws.amazon.com/polly/latest/dg/voicelist.html for the
            upstream catalog.
        model:
          anyOf:
            - type: string
              enum:
                - generative
                - long-form
                - neural
                - standard
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        textType:
          anyOf:
            - type: string
              enum:
                - text
                - ssml
            - type: 'null'
          title: Texttype
        sampleRate:
          anyOf:
            - type: integer
              maximum: 24000
              minimum: 8000
            - type: 'null'
          title: Samplerate
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: AwsPolly
      description: AWS Polly TTS — engine in ``model``, voice in ``voiceId``.
    AwsTranscribe:
      properties:
        provider:
          type: string
          const: aws-transcribe
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        vocabularyName:
          anyOf:
            - type: string
            - type: 'null'
          title: Vocabularyname
        languageModelName:
          anyOf:
            - type: string
            - type: 'null'
          title: Languagemodelname
        vocabularyFilterName:
          anyOf:
            - type: string
            - type: 'null'
          title: Vocabularyfiltername
        vocabularyFilterMethod:
          anyOf:
            - type: string
              enum:
                - mask
                - remove
                - tag
            - type: 'null'
          title: Vocabularyfiltermethod
        diarization:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Diarization
        enablePartialResultsStabilization:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enablepartialresultsstabilization
        partialResultsStability:
          anyOf:
            - type: string
              enum:
                - high
                - medium
                - low
            - type: 'null'
          title: Partialresultsstability
        enableChannelIdentification:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enablechannelidentification
        numberOfChannels:
          anyOf:
            - type: integer
              maximum: 8
              minimum: 1
            - type: 'null'
          title: Numberofchannels
        sampleRate:
          anyOf:
            - type: integer
              maximum: 48000
              minimum: 8000
            - type: 'null'
          title: Samplerate
      additionalProperties: false
      type: object
      required:
        - provider
      title: AwsTranscribe
      description: AWS Transcribe streaming STT — multi-cred.
    AzureOpenAIRealtime:
      properties:
        provider:
          type: string
          const: azure-openai-realtime
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - gpt-realtime-2
                - gpt-realtime-1.5
                - gpt-realtime
                - gpt-realtime-mini
                - gpt-4o-realtime-preview
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
              enum:
                - alloy
                - ash
                - ballad
                - cedar
                - coral
                - echo
                - marin
                - sage
                - shimmer
                - verse
            - type: 'null'
          title: Voice
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        inputAudioTranscription:
          anyOf:
            - $ref: '#/components/schemas/RealtimeInputAudioTranscription'
            - type: 'null'
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        deployment:
          anyOf:
            - type: string
            - type: 'null'
          title: Deployment
          description: >-
            Azure deployment alias. Defaults to the ``model`` id when omitted —
            Azure deployments are commonly named after the model they back.
        apiVersion:
          anyOf:
            - type: string
            - type: 'null'
          title: Apiversion
        turnDetection:
          anyOf:
            - {}
            - type: 'null'
          title: Turndetection
        inputAudioNoiseReduction:
          anyOf:
            - type: string
              enum:
                - near_field
                - far_field
            - type: 'null'
          title: Inputaudionoisereduction
        speed:
          anyOf:
            - type: number
              maximum: 4
              minimum: 0.25
            - type: 'null'
          title: Speed
        reasoningEffort:
          anyOf:
            - type: string
              enum:
                - minimal
                - low
                - medium
                - high
                - xhigh
            - type: 'null'
          title: Reasoningeffort
          description: >-
            Reasoning effort for ``gpt-realtime-2`` (the only model that honours
            this field on Azure today). Same 5 levels as the direct OpenAI
            variant — start with ``low`` (default), tune up for harder tasks.
            Silently dropped on non-reasoning models.
      additionalProperties: false
      type: object
      required:
        - provider
      title: AzureOpenAIRealtime
      description: >-
        Azure-hosted OpenAI Realtime. Leave apiVersion blank for the GA API
        (required for gpt-realtime-2); a date-based apiVersion selects legacy
        preview.
    AzureOpenAISTT:
      properties:
        provider:
          type: string
          const: azure-openai-stt
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - gpt-4o-mini-transcribe
                - gpt-4o-transcribe
                - whisper-1
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        deployment:
          anyOf:
            - type: string
            - type: 'null'
          title: Deployment
          description: >-
            Azure deployment alias. Defaults to ``model`` (or
            ``gpt-4o-mini-transcribe``) when omitted — Azure deployments are
            commonly named after the model they back.
        apiVersion:
          anyOf:
            - type: string
            - type: 'null'
          title: Apiversion
        detectLanguage:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Detectlanguage
        prompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Prompt
        noiseReductionType:
          anyOf:
            - type: string
              enum:
                - near_field
                - far_field
            - type: 'null'
          title: Noisereductiontype
      additionalProperties: false
      type: object
      required:
        - provider
      title: AzureOpenAISTT
      description: Azure OpenAI STT — requires deployment + apiVersion.
    AzureOpenAITTS:
      properties:
        provider:
          type: string
          const: azure-openai-tts
          title: Provider
        voiceId:
          type: string
          enum:
            - alloy
            - ash
            - ballad
            - cedar
            - coral
            - echo
            - fable
            - marin
            - nova
            - onyx
            - sage
            - shimmer
            - verse
          title: Voiceid
        model:
          anyOf:
            - type: string
              enum:
                - gpt-4o-mini-tts
                - tts-1
                - tts-1-hd
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        deployment:
          anyOf:
            - type: string
            - type: 'null'
          title: Deployment
          description: >-
            Azure deployment alias. Defaults to ``model`` (or
            ``gpt-4o-mini-tts``) when omitted — Azure deployments are commonly
            named after the model they back.
        apiVersion:
          anyOf:
            - type: string
            - type: 'null'
          title: Apiversion
        instructions:
          anyOf:
            - type: string
            - type: 'null'
          title: Instructions
        speed:
          anyOf:
            - type: number
              maximum: 4
              minimum: 0.25
            - type: 'null'
          title: Speed
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: AzureOpenAITTS
      description: Azure-hosted OpenAI TTS — requires deployment + apiVersion.
    AzureOpenai:
      properties:
        provider:
          type: string
          const: azure-openai
          title: Provider
        model:
          type: string
          enum:
            - gpt-5.4-mini
            - gpt-5.4-nano
            - gpt-5.4
            - gpt-5.2
            - gpt-5.1
            - gpt-5-mini
            - gpt-5-nano
            - gpt-5
            - gpt-5-chat
            - gpt-4.1
            - gpt-4.1-mini
            - gpt-4.1-nano
            - gpt-4o
            - gpt-4o-mini
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        frequencyPenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Frequencypenalty
        presencePenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Presencepenalty
        deployment:
          anyOf:
            - type: string
            - type: 'null'
          title: Deployment
          description: >-
            Azure deployment alias. Defaults to the ``model`` id when omitted —
            Azure deployments are commonly named after the model they back.
        apiVersion:
          anyOf:
            - type: string
            - type: 'null'
          title: Apiversion
          description: Override the API version on the credential.
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: AzureOpenai
      description: |-
        Azure-hosted OpenAI LLM (pipeline mode).

        Requires a ``deployment`` name (Azure-deployed model alias). Optional
        ``apiVersion`` overrides the credential's default.
    AzureSTT:
      properties:
        provider:
          type: string
          const: azure-stt
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        multipleLanguages:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Multiplelanguages
        sampleRate:
          anyOf:
            - type: integer
              maximum: 48000
              minimum: 8000
            - type: 'null'
          title: Samplerate
        numberOfChannels:
          anyOf:
            - type: integer
              maximum: 8
              minimum: 1
            - type: 'null'
          title: Numberofchannels
        phraseList:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Phraselist
        profanity:
          anyOf:
            - type: string
              enum:
                - Masked
                - Removed
                - Raw
            - type: 'null'
          title: Profanity
        segmentationSilenceTimeoutMs:
          anyOf:
            - type: integer
              maximum: 5000
              minimum: 100
            - type: 'null'
          title: Segmentationsilencetimeoutms
        segmentationMaxTimeMs:
          anyOf:
            - type: integer
              maximum: 120000
              minimum: 1000
            - type: 'null'
          title: Segmentationmaxtimems
        segmentationStrategy:
          anyOf:
            - type: string
              enum:
                - Default
                - Time
                - Semantic
            - type: 'null'
          title: Segmentationstrategy
        explicitPunctuation:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Explicitpunctuation
        trueTextPostProcessing:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Truetextpostprocessing
        speechEndpoint:
          anyOf:
            - type: string
            - type: 'null'
          title: Speechendpoint
      additionalProperties: false
      type: object
      required:
        - provider
      title: AzureSTT
      description: Azure AI Speech STT — multi-cred.
    AzureTTS:
      properties:
        provider:
          type: string
          const: azure-tts
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Azure voice name (e.g. `en-US-JennyNeural`). See
            https://learn.microsoft.com/en-us/azure/ai-services/speech-service/language-support?tabs=tts
            for the upstream catalog.
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        sampleRate:
          anyOf:
            - type: integer
              maximum: 48000
              minimum: 8000
            - type: 'null'
          title: Samplerate
        prosodyRate:
          anyOf:
            - {}
            - type: 'null'
          title: Prosodyrate
        prosodyVolume:
          anyOf:
            - {}
            - type: 'null'
          title: Prosodyvolume
        prosodyPitch:
          anyOf:
            - type: string
              enum:
                - x-low
                - low
                - medium
                - high
                - x-high
            - type: 'null'
          title: Prosodypitch
        style:
          anyOf:
            - type: string
            - type: 'null'
          title: Style
        styleDegree:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.1
            - type: 'null'
          title: Styledegree
        lexiconUri:
          anyOf:
            - type: string
            - type: 'null'
          title: Lexiconuri
        deploymentId:
          anyOf:
            - type: string
            - type: 'null'
          title: Deploymentid
        speechEndpoint:
          anyOf:
            - type: string
            - type: 'null'
          title: Speechendpoint
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: AzureTTS
      description: Azure AI Speech TTS — all tiers (voice tier implicit in voice name).
    BackgroundSoundSchema:
      properties:
        url:
          type: string
          title: Url
        volume:
          type: number
          maximum: 1
          minimum: 0
          title: Volume
          default: 0.5
      type: object
      required:
        - url
      title: BackgroundSoundSchema
    Baseten:
      properties:
        provider:
          type: string
          const: baseten
          title: Provider
        model:
          type: string
          enum:
            - openai/gpt-oss-120b
            - deepseek-ai/DeepSeek-V3.1
            - nvidia/Nemotron-120B-A12B
            - MiniMaxAI/MiniMax-M2.5
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        frequencyPenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Frequencypenalty
        presencePenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Presencepenalty
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: Baseten
      description: Baseten LLM (pipeline mode).
    BatchResponse:
      properties:
        id:
          type: string
          title: Id
        tenantId:
          type: string
          title: Tenantid
        organizationId:
          type: string
          title: Organizationid
        channelId:
          type: string
          title: Channelid
        segmentId:
          anyOf:
            - type: string
            - type: 'null'
          title: Segmentid
        templateId:
          anyOf:
            - type: string
            - type: 'null'
          title: Templateid
        totalRecipients:
          type: integer
          title: Totalrecipients
        dispatchedCount:
          type: integer
          title: Dispatchedcount
        deliveredCount:
          type: integer
          title: Deliveredcount
        failedCount:
          type: integer
          title: Failedcount
        cancelledCount:
          type: integer
          title: Cancelledcount
        sendAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Sendat
        startedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Startedat
        completedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Completedat
        status:
          $ref: '#/components/schemas/BatchStatus'
        failureReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Failurereason
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - channelId
        - totalRecipients
        - dispatchedCount
        - deliveredCount
        - failedCount
        - cancelledCount
        - status
        - createdAt
        - updatedAt
      title: BatchResponse
      description: One batch row — created by bulk send, drained by scheduler worker.
    BatchStatus:
      type: string
      enum:
        - queued
        - processing
        - completed
        - cancelled
        - failed
      title: BatchStatus
    BillingInvoiceResponse:
      properties:
        id:
          type: string
          title: Id
        ownerType:
          type: string
          enum:
            - tenant
            - organization
          title: Ownertype
        ownerId:
          type: string
          title: Ownerid
        invoiceNumber:
          type: string
          title: Invoicenumber
        mode:
          type: string
          enum:
            - stripe
            - manual_contract
          title: Mode
        stripeInvoiceId:
          anyOf:
            - type: string
            - type: 'null'
          title: Stripeinvoiceid
        stripeHostedInvoiceUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Stripehostedinvoiceurl
          description: >-
            Stripe-hosted invoice page URL. Populated for ``mode='stripe'`` rows
            that came in via the ``invoice.finalized`` / ``invoice.paid``
            webhook. Use this for the user-facing 'View invoice' link.
        stripeInvoicePdfUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Stripeinvoicepdfurl
          description: >-
            Stripe-hosted PDF download URL (``invoice.invoice_pdf``). Populated
            for ``mode='stripe'`` subscription invoices.
        amountCents:
          type: integer
          title: Amountcents
        amountPaidCents:
          type: integer
          title: Amountpaidcents
        currency:
          type: string
          title: Currency
        subtotalExclTaxCents:
          type: integer
          title: Subtotalexcltaxcents
        taxCents:
          type: integer
          title: Taxcents
        reverseChargeApplied:
          type: boolean
          title: Reversechargeapplied
        status:
          type: string
          enum:
            - draft
            - open
            - paid
            - uncollectible
            - void
            - refunded
          title: Status
        issuedAt:
          type: string
          format: date-time
          title: Issuedat
        dueAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Dueat
        paidAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Paidat
        voidedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Voidedat
        refundedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Refundedat
        amountRefundedCents:
          type: integer
          title: Amountrefundedcents
          default: 0
        refundReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Refundreason
        periodStart:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Periodstart
        periodEnd:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Periodend
        lineItems:
          items:
            $ref: '#/components/schemas/InvoiceLineItemResponse'
          type: array
          title: Lineitems
        pdfPresignedUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Pdfpresignedurl
          description: >-
            Presigned S3 URL for the invoice PDF. Generated at read-time, ~1h
            TTL.
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - ownerType
        - ownerId
        - invoiceNumber
        - mode
        - amountCents
        - amountPaidCents
        - currency
        - subtotalExclTaxCents
        - taxCents
        - reverseChargeApplied
        - status
        - issuedAt
        - createdAt
        - updatedAt
      title: BillingInvoiceResponse
      description: '``GET /billing/invoices/{id}``.'
    BodyComponent:
      properties:
        type:
          type: string
          const: BODY
          title: Type
        text:
          type: string
          maxLength: 1024
          minLength: 1
          title: Text
        example:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Example
      additionalProperties: false
      type: object
      required:
        - type
        - text
      title: BodyComponent
    Body_uploadFile:
      properties:
        file:
          type: string
          format: binary
          title: File
          description: Multipart file to upload.
      type: object
      required:
        - file
      title: Body_uploadFile
    Body_uploadMessagingMedia:
      properties:
        file:
          type: string
          format: binary
          title: File
      type: object
      required:
        - file
      title: Body_uploadMessagingMedia
    BrandAssetUploadRequest:
      properties:
        filename:
          type: string
          maxLength: 512
          minLength: 1
          title: Filename
        mimeType:
          type: string
          maxLength: 256
          minLength: 1
          title: Mimetype
        sizeBytes:
          type: integer
          maximum: 10485760
          minimum: 0
          title: Sizebytes
      additionalProperties: false
      type: object
      required:
        - filename
        - mimeType
        - sizeBytes
      title: BrandAssetUploadRequest
      description: |-
        Body for POST /widget/admin/widgets/{id}/upload-logo and
        /upload-header-bg.

        Admin sends file metadata; server mints a presigned R2 PUT URL.
        Admin uploads directly to R2 with the URL, then PATCHes the
        returned ``publicUrl`` onto the widget's ``workspaceLogoUrl`` /
        ``headerBackgroundImageUrl`` via the existing PATCH endpoint.
    BrandAssetUploadResponse:
      properties:
        assetKind:
          type: string
          enum:
            - logo
            - header-bg
          title: Assetkind
        presignedPutUrl:
          type: string
          title: Presignedputurl
        presignedExpiresAt:
          type: string
          format: date-time
          title: Presignedexpiresat
        publicUrl:
          type: string
          title: Publicurl
      additionalProperties: false
      type: object
      required:
        - assetKind
        - presignedPutUrl
        - presignedExpiresAt
        - publicUrl
      title: BrandAssetUploadResponse
    BulkRecipient:
      properties:
        to:
          type: string
          minLength: 1
          title: To
        variables:
          additionalProperties:
            type: string
          type: object
          title: Variables
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
      additionalProperties: false
      type: object
      required:
        - to
      title: BulkRecipient
      description: |-
        One row in a bulk send. ``to`` is the channel identifier (phone, chat
        id, email). ``variables`` are merged into the template body at dispatch
        time — must include every variable the template references.
    BulkSendRequest:
      properties:
        channelId:
          type: string
          title: Channelid
        type:
          $ref: '#/components/schemas/MessageType'
        recipients:
          items:
            $ref: '#/components/schemas/BulkRecipient'
          type: array
          maxItems: 10000
          minItems: 1
          title: Recipients
        text:
          anyOf:
            - $ref: '#/components/schemas/TextPayload'
            - type: 'null'
        image:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        video:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        audio:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        document:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        sticker:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        location:
          anyOf:
            - $ref: '#/components/schemas/LocationPayload'
            - type: 'null'
        contact:
          anyOf:
            - $ref: '#/components/schemas/ContactPayload'
            - type: 'null'
        template:
          anyOf:
            - $ref: '#/components/schemas/TemplatePayload'
            - type: 'null'
        interactive:
          anyOf:
            - $ref: '#/components/schemas/InteractivePayload'
            - type: 'null'
        reaction:
          anyOf:
            - $ref: '#/components/schemas/ReactionPayload'
            - type: 'null'
        templateVariables:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Templatevariables
          description: >-
            Optional flat dict of placeholder values shared across every
            recipient in the batch. Merged with each recipient's ``variables``
            (per-recipient overrides batch-level on key collision). Same Jinja2
            syntax as the single-send API (``{{ name | upper }}`` etc.).
        sendAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Sendat
          description: >-
            When to dispatch the batch. Null = ASAP. If set in the past, the
            scheduler picks it up on the next poll tick. Must be UTC.
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
      additionalProperties: false
      type: object
      required:
        - channelId
        - type
        - recipients
      title: BulkSendRequest
      description: |-
        Polymorphic bulk-send body — same payload shape as send_message
        but with N recipients. Total recipients capped at 10_000 per call;
        larger lists must be split (operational safety against ledger storms).
    BulkSendResponse:
      properties:
        batch:
          $ref: '#/components/schemas/BatchResponse'
        queuedRecipients:
          type: integer
          title: Queuedrecipients
          description: Number of recipients actually queued (drops opted-out).
        droppedOptedOut:
          type: integer
          title: Droppedoptedout
          description: Recipients silently dropped because they are on the opt-out list.
          default: 0
      type: object
      required:
        - batch
        - queuedRecipients
      title: BulkSendResponse
      description: Echo of the created batch + a few summary stats.
    ButtonsComponent:
      properties:
        type:
          type: string
          const: BUTTONS
          title: Type
        buttons:
          items:
            oneOf:
              - $ref: '#/components/schemas/QuickReplyButton'
              - $ref: '#/components/schemas/UrlButton'
              - $ref: '#/components/schemas/PhoneButton'
            discriminator:
              propertyName: type
              mapping:
                PHONE_NUMBER: '#/components/schemas/PhoneButton'
                QUICK_REPLY: '#/components/schemas/QuickReplyButton'
                URL: '#/components/schemas/UrlButton'
          type: array
          maxItems: 10
          minItems: 1
          title: Buttons
      additionalProperties: false
      type: object
      required:
        - type
        - buttons
      title: ButtonsComponent
    ByoSipPhoneNumber:
      properties:
        number:
          type: string
          title: Number
          description: Phone number in E.164 format (e.g. ``+14155551234``).
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this number belongs to. Must reference a live folder with
            ``resourceType='phone_number'`` in the caller's workspace. Null
            places the number at the list root.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: >-
            Default assistant that handles inbound calls on this number. Used
            only when ``server`` is not set.
        server:
          anyOf:
            - $ref: '#/components/schemas/InboundServerConfig'
            - type: 'null'
          description: >-
            Dynamic-assistant selection webhook. Takes precedence over
            ``assistantId`` when both are set.
        fallbackDestination:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/FallbackNumberDestination'
                - $ref: '#/components/schemas/FallbackSipDestination'
              discriminator:
                propertyName: type
                mapping:
                  number: '#/components/schemas/FallbackNumberDestination'
                  sip: '#/components/schemas/FallbackSipDestination'
            - type: 'null'
          title: Fallbackdestination
          description: >-
            Inbound safety net. If the assistant can't be set up for a call (any
            init failure — webhook error, config load, session start), the
            caller is transferred to this ``number`` / ``sip`` destination
            instead of being dropped. Leave null to drop on failure (default).
        outboundCallerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundcallerid
          description: >-
            Number shown as the caller ID on outbound calls from this phone
            number. Twilio: must be a Twilio-provisioned number on the account
            or a Verified Caller ID — pre-validated against Twilio at
            create/PATCH time. BYO SIP: validated by the carrier at call time.
            Leave null to use this number's own E.164.
        provider:
          type: string
          const: byo-sip
          title: Provider
          default: byo-sip
        sipTrunkId:
          type: string
          title: Siptrunkid
          description: >-
            Id of the ``SipTrunk`` document that defines the gateways + auth for
            this number.
        numberE164CheckEnabled:
          type: boolean
          title: Numbere164Checkenabled
          description: >-
            Enforce E.164 format on the number. Set to ``false`` only for
            non-standard BYO SIP dial strings (e.g. registration trunks that use
            local-format numbers).
          default: true
      additionalProperties: false
      type: object
      required:
        - number
        - sipTrunkId
      title: ByoSipPhoneNumber
      description: |-
        Phone number attached to a customer-managed SIP trunk.

        References a ``SipTrunk`` document by id. The backend reads that
        trunk's gateways and auth to wire up the inbound / outbound
        routing. The trunk must belong to the same workspace.
    ByoSipPhoneNumberResponse:
      properties:
        id:
          type: string
          title: Id
        number:
          type: string
          title: Number
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: Folder this number belongs to, or ``null`` when unfiled.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
        server:
          anyOf:
            - $ref: '#/components/schemas/InboundServerConfigInfo'
            - type: 'null'
        fallbackDestination:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/FallbackNumberDestination'
                - $ref: '#/components/schemas/FallbackSipDestination'
              discriminator:
                propertyName: type
                mapping:
                  number: '#/components/schemas/FallbackNumberDestination'
                  sip: '#/components/schemas/FallbackSipDestination'
            - type: 'null'
          title: Fallbackdestination
        numberE164CheckEnabled:
          type: boolean
          title: Numbere164Checkenabled
          default: true
        status:
          type: string
          enum:
            - pending
            - active
            - failed
            - released
          title: Status
          default: active
        inboundTrunkId:
          anyOf:
            - type: string
            - type: 'null'
          title: Inboundtrunkid
        outboundTrunkId:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundtrunkid
        dispatchRuleId:
          anyOf:
            - type: string
            - type: 'null'
          title: Dispatchruleid
        sipCallerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Sipcallerid
          description: From-header override for registration-based SIP trunks.
        outboundCallerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundcallerid
          description: >-
            Number shown as the caller ID on outbound calls from this phone
            number. Twilio: must be a Twilio-provisioned number on the account
            or a Verified Caller ID — pre-validated against Twilio at
            create/PATCH time. BYO SIP: validated by the carrier at call time.
            Leave null to use this number's own E.164.
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        provider:
          type: string
          const: byo-sip
          title: Provider
          default: byo-sip
        sipTrunkId:
          anyOf:
            - type: string
            - type: 'null'
          title: Siptrunkid
      additionalProperties: false
      type: object
      required:
        - id
        - number
      title: ByoSipPhoneNumberResponse
    CallAnalysisSchema:
      properties:
        summary:
          anyOf:
            - type: string
            - type: 'null'
          title: Summary
        successEvaluation:
          anyOf:
            - type: string
            - type: 'null'
          title: Successevaluation
          description: >-
            Worker-generated evaluation of whether the call succeeded against
            its goals.
        structuredData:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Structureddata
          description: >-
            Structured data extracted from the call, following the assistant's
            ``analysis.structuredData.schema`` if configured.
      additionalProperties: false
      type: object
      title: CallAnalysisSchema
      description: Post-call analysis — only populated once the worker has run.
    CallAnonymizationMeta:
      properties:
        applied:
          type: boolean
          title: Applied
          description: >-
            True when the anonymization pass replaced at least one substring.
            False when the feature was disabled, no rules matched, or the pass
            failed.
        status:
          type: string
          enum:
            - applied
            - failed
            - skipped
          title: Status
          description: >-
            ``applied`` — substitutions written; ``failed`` — retries exhausted,
            content delivered RAW; ``skipped`` — feature disabled or already
            anonymized on a prior attempt.
        rulesAppliedCount:
          type: integer
          title: Rulesappliedcount
          description: Distinct substrings substituted by the pass.
          default: 0
      type: object
      required:
        - applied
        - status
      title: CallAnonymizationMeta
      description: |-
        Anonymization audit metadata attached to ``end-of-call-report``.

        Lets receivers detect at-source whether the transcript / messages /
        summary they're seeing have been redacted, what status the pass
        finished in, and how many substitutions landed. Always present on
        the event — ``applied=false`` + ``status="skipped"`` when the
        feature was disabled for the assistant.
    CallCreate:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Optional display name for the call.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: Id of a persisted assistant.
        assistant:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/PipelineAssistantCreate-Input'
                - $ref: '#/components/schemas/RealtimeAssistantCreate-Input'
              discriminator:
                propertyName: type
                mapping:
                  pipeline: '#/components/schemas/PipelineAssistantCreate-Input'
                  realtime: '#/components/schemas/RealtimeAssistantCreate-Input'
            - type: 'null'
          title: Assistant
          description: >-
            Full inline assistant body for a one-shot ephemeral assistant that
            is not persisted. Mutually exclusive with ``assistantId``.
        overrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Overrides
          description: >-
            Per-call patches applied over the resolved assistant via **deep
            merge**. Accepts any field on the assistant — partial nested objects
            are supported, so you can patch one sub-field (e.g.
            ``overrides.model.temperature``) without re-supplying the whole
            model config.


            **Common overrides:**


            - ``systemPrompt`` — top-level shortcut; copies into
            ``model.systemPrompt`` (use this for the simplest prompt swap).

            - ``firstMessage`` / ``firstMessageMode`` — opening greeting and
            whether the assistant or the user speaks first.

            - ``model`` — full model config or a partial. Set
            ``model.systemPrompt``, ``model.temperature``, ``model.maxTokens``,
            etc. independently.

            - ``voice`` — full TTS config or a partial. Set ``voice.voiceId``,
            ``voice.speed``, ``voice.stability``, etc.

            - ``transcriber`` — STT config patch.

            - ``tools`` — replace the tool list (ids or inline configs). ``[]``
            clears all tools for this call.

            - ``knowledgeBaseIds`` / ``kbRetrievalMode`` — per-call KB scope and
            ``auto`` vs ``tool`` retrieval mode.

            - ``silenceTimeoutSeconds`` / ``maxDurationSeconds`` /
            ``allowInterruptions`` / ``preemptiveGeneration`` / ``maxToolSteps``
            — behavioural knobs.

            - ``endpointing`` / ``interruption`` / ``idle`` — turn-detection
            tuning.

            - ``recording`` / ``monitoring`` — recording channels and
            live-monitor enablement.

            - ``analysis`` — per-call summary / structured-data / evaluation
            prompts.

            - ``server`` / ``webhookEvents`` — per-call assistant webhook
            URL/secret and event subscription set.

            - ``backgroundSound`` — hold-music / ambience override.

            - ``metadata`` — patches the **assistant's** metadata sub-doc only.
            NOT the same as the top-level ``metadata`` on this body — that one
            is per-call data and is the one echoed on outbound webhooks. If you
            want CRM ids / business data on the webhook, set the top-level
            ``metadata`` (and / or ``customer.externalId`` /
            ``customer.metadata``), not this.

            - ``variables`` — call-time template bag, available as
            ``{{variableName}}`` substitutions in any string field on the
            assistant. NOT merged into the assistant config; the agent pulls it
            out separately for substitution.


            **Tier-mode caveat:** assistants bound to a productized tier
            (``standard`` / ``premium`` / ``gold``) ignore ``model`` / ``voice``
            / ``transcriber`` overrides — those fields are platform-managed
            under the tier contract. Use Custom mode (``tier=null``) for full
            provider-stack control.


            **Validation:** unknown top-level keys are rejected (typo guard).
            Field shapes are not deeply validated here — the merged result is
            validated by the agent at session start.
          examples:
            - firstMessage: Hallo! Hier ist die SpringSale-Hotline.
              model:
                maxTokens: 800
                temperature: 0.3
              silenceTimeoutSeconds: 20
              systemPrompt: You are a German-speaking sales agent for SpringSale 2026.
              variables:
                campaignCode: SPRING-26
        phoneNumberId:
          anyOf:
            - type: string
            - type: 'null'
          title: Phonenumberid
          description: Id of a persisted phone number (outbound).
        phoneNumber:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/TwilioPhoneNumber'
                - $ref: '#/components/schemas/ByoSipPhoneNumber'
              discriminator:
                propertyName: provider
                mapping:
                  byo-sip: '#/components/schemas/ByoSipPhoneNumber'
                  twilio: '#/components/schemas/TwilioPhoneNumber'
            - type: 'null'
          title: Phonenumber
          description: >-
            Full inline phone number body for a one-shot ephemeral phone number.
            Mutually exclusive with ``phoneNumberId``.
        customer:
          anyOf:
            - $ref: '#/components/schemas/CustomerSchema'
            - type: 'null'
          description: >-
            The person being called (outbound) or caller identity (web).
            Top-level field — NOT a child of ``overrides``. Echoed back on every
            outbound webhook event as ``call.customer.{number, name, externalId,
            metadata}``. To carry CRM identifiers / per-call business data
            through to your webhook receiver, set them here
            (``customer.externalId`` / ``customer.metadata``), not under
            ``overrides``.


            **Required fields:** ``number`` is mandatory for outbound phone
            calls (the destination). All other sub-fields are optional and
            round-trip through the webhook unchanged.
        callerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Callerid
          description: >-
            Per-call caller-ID override (E.164). Overrides the phone number's
            configured ``outboundCallerId`` for THIS call only. Valid only on
            outbound phone calls (with ``customer.number`` and a phone number
            reference). For Twilio numbers the value must be a
            Twilio-provisioned number on the account or a Verified Caller ID —
            we do not pre-validate per call to keep outbound init fast; Twilio
            rejects unauthorized values at SIP level (surfaced as
            ``outbound-sip-rejected``).
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
          description: >-
            Free-form key/value metadata stored on this CALL document. Echoed
            back verbatim on every outbound webhook event
            (``end-of-call-report``, ``status-update``, etc.) under the
            top-level ``metadata`` field.


            **Don't confuse with** ``overrides.metadata`` — that one patches the
            *assistant's* metadata sub-doc and is NOT echoed on the webhook's
            top-level ``metadata`` field. If you want per-call business data
            (ticket id, campaign code, CRM row) to land on the webhook, put it
            here at the top level, not under overrides.
      type: object
      title: CallCreate
      description: |-
        Create a call.

        Every call needs an assistant — either by reference (``assistantId``) or
        as a one-shot inline body (``assistant``). Exactly one of the two.
        For outbound phone calls, supply ``customer.number`` plus either
        ``phoneNumberId`` or an inline ``phoneNumber`` body. Omit both and the
        call is a **web call** — the response carries a ``transport.token`` +
        ``transport.url`` (WebRTC access credentials) that the client joins.
    CallCustomer:
      properties:
        number:
          anyOf:
            - type: string
            - type: 'null'
          title: Number
          description: Customer phone number in E.164 format.
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name supplied at call creation.
        externalId:
          anyOf:
            - type: string
            - type: 'null'
          title: Externalid
          description: Customer-side identifier (e.g. CRM ID).
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: Free-form metadata supplied at call creation.
      additionalProperties: true
      type: object
      title: CallCustomer
      description: |-
        Customer details echoed on outbound webhook payloads.

        Populated from the call's stored customer object. All fields
        optional — only ``number`` is guaranteed for inbound calls.
        Extra keys are preserved verbatim so customer-supplied metadata
        keys round-trip cleanly.
    CallError:
      properties:
        type:
          type: string
          title: Type
          description: 'Component that failed: "llm", "stt", "tts", "system", ….'
        message:
          type: string
          title: Message
          description: Human-readable error message.
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
          description: Provider id if the failure originated in a vendor SDK.
        code:
          anyOf:
            - type: integer
            - type: 'null'
          title: Code
          description: Provider-specific status/error code.
      additionalProperties: true
      type: object
      required:
        - type
        - message
      title: CallError
      description: >-
        Error details populated when the call ended with a pipeline / system
        failure.


        ``extra="allow"`` so optional diagnostic fields (``count`` for
        consecutive

        errors, etc.) survive the serialization round-trip.
    CallLogRecord:
      properties:
        timestamp:
          type: string
          title: Timestamp
        level:
          type: string
          title: Level
        category:
          type: string
          title: Category
        message:
          type: string
          title: Message
        call_id:
          anyOf:
            - type: string
            - type: 'null'
          title: Call Id
        session_id:
          anyOf:
            - type: string
            - type: 'null'
          title: Session Id
        correlation_id:
          anyOf:
            - type: string
            - type: 'null'
          title: Correlation Id
        request_id:
          anyOf:
            - type: string
            - type: 'null'
          title: Request Id
        event_type:
          anyOf:
            - type: string
            - type: 'null'
          title: Event Type
        extra:
          additionalProperties: true
          type: object
          title: Extra
      type: object
      required:
        - timestamp
        - level
        - category
        - message
      title: CallLogRecord
      description: |-
        A single normalized, sanitized log record for the call detail
        Logs tab.

        Fields are intentionally minimal — every field here has been vetted
        as safe to expose to API consumers. New fields go in ``extra`` after
        passing through the same sanitization pipeline.

        Internal attribution and isolation markers (Python module paths,
        process metadata, scope ids) are stripped from the raw log stream
        before reaching this record. The user-facing ``category`` field
        replaces the underlying logger name with a stable, friendly label.
    CallLogsResponse:
      properties:
        logs:
          items:
            $ref: '#/components/schemas/CallLogRecord'
          type: array
          title: Logs
        total:
          type: integer
          title: Total
        truncated:
          type: boolean
          title: Truncated
          default: false
        source:
          type: string
          enum:
            - archive
            - pending
            - unavailable
            - expired
          title: Source
          default: archive
        message:
          anyOf:
            - type: string
            - type: 'null'
          title: Message
      type: object
      required:
        - logs
        - total
      title: CallLogsResponse
      description: |-
        Response body for ``GET /call/{id}/logs``.

        The endpoint is **archive-only** from the user's perspective: logs
        only exist for a call once it has ended and the post-call worker
        has flushed the log archive to permanent storage. Between call end
        and archive completion, the frontend sees ``source="pending"`` and
        polls until ``source="archive"`` flips on.
    CallMessageSchema:
      properties:
        role:
          type: string
          title: Role
          description: >-
            Entry kind. ``user`` / ``assistant`` / ``system`` for conversational
            turns; ``tool_call`` / ``tool_result`` for structured tool I/O;
            ``kb_retrieval`` for KB lookups. Legacy archives may carry
            ``function`` / ``tool``.
        content:
          type: string
          title: Content
        timestamp:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Timestamp
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Tool name. Present on ``tool_call`` / ``tool_result``.
        toolCallId:
          anyOf:
            - type: string
            - type: 'null'
          title: Toolcallid
          description: SDK-issued correlation id pairing call ↔ result.
        arguments:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Arguments
          description: Structured tool arguments. Present on ``tool_call``.
        isError:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Iserror
          description: True when the tool raised. Present on ``tool_result``.
        query:
          anyOf:
            - type: string
            - type: 'null'
          title: Query
          description: Retrieval query text. Present on ``kb_retrieval``.
        chunksCount:
          anyOf:
            - type: integer
            - type: 'null'
          title: Chunkscount
          description: Chunks the retrieval returned. Present on ``kb_retrieval``.
        topScore:
          anyOf:
            - type: number
            - type: 'null'
          title: Topscore
          description: Best similarity score across chunks. Present on ``kb_retrieval``.
        knowledgeBaseIds:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Knowledgebaseids
          description: >-
            Distinct KB ids whose chunks contributed. Present on
            ``kb_retrieval``.
        interrupted:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Interrupted
          description: Assistant turns only. True when TTS was interrupted.
      additionalProperties: true
      type: object
      required:
        - role
        - content
      title: CallMessageSchema
      description: |-
        One entry in the call's conversation log.

        Carries spoken / typed turns (``user`` / ``assistant`` / ``system``)
        interleaved chronologically with structured tool I/O and
        knowledge-base retrievals. Mirrors :class:`ConversationMessage`
        on the live tool-calls webhook so frontend + API consumers see
        the same shape the agent persisted.

        ``role`` is typed as ``str`` (rather than a closed ``Literal``)
        so future entry kinds round-trip via ``extra="allow"`` without
        requiring a schema bump. Documented values:

          - ``user`` / ``assistant`` / ``system`` — spoken / typed turn.
            Body in ``content``.
          - ``tool_call`` — LLM invoked a tool. ``name``, ``toolCallId``,
            ``arguments`` populated. ``content`` carries the canonical
            JSON dump of ``arguments``.
          - ``tool_result`` — tool returned its result. ``name``,
            ``toolCallId``, ``isError`` populated. ``content`` carries
            the exact string the tool returned.
          - ``kb_retrieval`` — knowledge-base retrieval. ``query``,
            ``chunksCount``, ``topScore``, ``knowledgeBaseIds`` populated.
            ``content`` carries the query text.
          - ``function`` / ``tool`` — legacy names from earlier agent
            builds; kept readable for back-compat with already-archived
            calls. New writes use ``tool_call`` / ``tool_result``.
    CallMonitorURLs:
      properties:
        controlUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Controlurl
          description: >-
            POST runtime commands to this URL with the workspace API key —
            ``say``, ``add-message``, ``end-call``, ``transfer``. See the
            Monitor API reference.
        listenTokenUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Listentokenurl
          description: >-
            GET this URL with the workspace API key to mint a short-lived listen
            token. Pair the token with the platform realtime endpoint to
            subscribe to the call's audio stream in read-only mode.
      type: object
      title: CallMonitorURLs
      description: |-
        Live-monitoring endpoints surfaced on ``status-update`` events.

        Only present while ``call.status == "in-progress"`` and the call's
        listen / control flags are enabled — the URLs are useless on a
        queued or ended call.
    CallPhoneNumber:
      properties:
        id:
          type: string
          title: Id
          description: Phone number record ID.
        number:
          anyOf:
            - type: string
            - type: 'null'
          title: Number
          description: Phone number in E.164 format.
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name configured on the number.
      type: object
      required:
        - id
      title: CallPhoneNumber
      description: |-
        Phone number record echoed on outbound webhook payloads.

        Identifies which configured phone number the call was placed on
        or routed to. Useful for multi-account routing on the receiver
        (e.g. one webhook endpoint handling several of your numbers).
    CallRecording-Input:
      properties:
        stereoUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Stereourl
          description: Mixed stereo recording (assistant + customer in separate channels).
        agentUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Agenturl
          description: Assistant-only mono recording.
        customerUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Customerurl
          description: Customer-only mono recording.
      type: object
      title: CallRecording
      description: |-
        Recording artifacts produced after the call ends.

        Audio is always encoded as Opus in an Ogg container
        (``audio/ogg; codecs=opus``). The URLs are stable permalinks that
        redirect to a freshly-signed S3 URL on each request, so they keep
        working until the recording's retention window elapses — at which
        point the audio is deleted and the link 404s. Download to your own
        storage if you need durable archives beyond that window.
    CallRecording-Output:
      properties:
        type:
          type: string
          title: Type
          description: MIME type of the recording.
          default: audio/ogg
        stereoUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Stereourl
          description: Room-composite track (agent + customer).
        agentUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Agenturl
          description: Agent-only audio track.
        customerUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Customerurl
          description: Customer-only audio track.
      additionalProperties: false
      type: object
      title: CallRecording
      description: >-
        Recording URLs for a completed call.


        Populated by the post-call worker once recording uploads finish.

        URLs are stable, login-free permalinks
        (``/call/{id}/recording/{track}``)

        that redirect to a freshly-signed S3 URL on each request, so they keep

        working until the recording is retention-deleted (then they 404). The

        single-call GET surfaces every available track; list endpoints surface

        the stereo track only (per-track resolution is skipped on list).
    CallResponse:
      properties:
        id:
          type: string
          title: Id
        type:
          type: string
          enum:
            - inboundPhoneCall
            - outboundPhoneCall
            - webCall
          title: Type
        status:
          type: string
          enum:
            - queued
            - ringing
            - in-progress
            - forwarding
            - ended
          title: Status
        endedReason:
          anyOf:
            - type: string
              enum:
                - assistant-ended-call
                - customer-ended-call
                - silence-timed-out
                - max-duration-reached
                - assistant-error
                - voicemail
                - pipeline-error-llm-failed
                - system-error
                - agent-init-failed
                - assistant-request-failed
                - fallback-destination
                - ringing-timeout
                - no-answer
                - call-forwarded
                - human-takeover
                - human-ended-call
                - outbound-trunk-misconfigured
                - outbound-sip-unavailable
                - outbound-sip-rejected
                - outbound-init-failed
                - insufficient-balance
                - balance-exhausted
                - billing-suspended
                - billing-misconfigured
                - concurrency-cap-exceeded
                - tier-not-allowed
                - unknown
            - type: 'null'
          title: Endedreason
        conversationOccurred:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Conversationoccurred
          description: >-
            Whether a real interaction happened — the assistant spoke to the
            caller OR the caller spoke. ``false`` marks a connected-but-empty
            call (misdial / caller hung up during ring). Distinguishes an
            abandoned connection from a real call **independent of**
            ``endedReason``. ``null`` only on legacy calls that predate
            phase-timestamp tracking.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
        assistant:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/PipelineAssistantCreate-Output'
                - $ref: '#/components/schemas/RealtimeAssistantCreate-Output'
              discriminator:
                propertyName: type
                mapping:
                  pipeline: '#/components/schemas/PipelineAssistantCreate-Output'
                  realtime: '#/components/schemas/RealtimeAssistantCreate-Output'
            - type: 'null'
          title: Assistant
          description: >-
            Inline assistant body that ran for this call, echoed back verbatim
            from the create request. Only set when the call was created with an
            inline ``assistant`` instead of ``assistantId``; otherwise ``null``.
        phoneNumberId:
          anyOf:
            - type: string
            - type: 'null'
          title: Phonenumberid
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
          description: >-
            Productized tier the assistant was bound to at call start. Absent
            for Custom-mode assistants.
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
          description: >-
            Tier voice-card id selected for the call (e.g. ``"eva"``,
            ``"holly"``). Only present when ``tier`` is set.
        customer:
          anyOf:
            - $ref: '#/components/schemas/CustomerSchema'
            - type: 'null'
        callerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Callerid
          description: >-
            Caller ID actually used on the SIP INVITE for this call. Snapshot at
            outbound dispatch from the per-call override > per-number
            ``outboundCallerId`` > SIP registration username > phone number's
            E.164 resolution chain. ``None`` for inbound and web calls.
        transport:
          $ref: '#/components/schemas/CallTransportSchema'
        phoneCallProvider:
          anyOf:
            - type: string
            - type: 'null'
          title: Phonecallprovider
          description: '"sip", "twilio", …'
        phoneCallProviderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Phonecallproviderid
          description: Provider-assigned SIP call id.
        messages:
          items:
            $ref: '#/components/schemas/CallMessageSchema'
          type: array
          title: Messages
        transcript:
          anyOf:
            - type: string
            - type: 'null'
          title: Transcript
        timeline:
          items:
            oneOf:
              - $ref: '#/components/schemas/TimelineMessageEvent'
              - $ref: '#/components/schemas/TimelineToolCallEvent'
              - $ref: '#/components/schemas/TimelineToolResultEvent'
              - $ref: '#/components/schemas/TimelineKbRetrievalEvent'
              - $ref: '#/components/schemas/TimelineHandoffEvent'
              - $ref: '#/components/schemas/TimelineErrorEvent'
            discriminator:
              propertyName: type
              mapping:
                agent_handoff: '#/components/schemas/TimelineHandoffEvent'
                error: '#/components/schemas/TimelineErrorEvent'
                handoff: '#/components/schemas/TimelineHandoffEvent'
                kb_retrieval: '#/components/schemas/TimelineKbRetrievalEvent'
                message: '#/components/schemas/TimelineMessageEvent'
                tool_call: '#/components/schemas/TimelineToolCallEvent'
                tool_result: '#/components/schemas/TimelineToolResultEvent'
          type: array
          title: Timeline
        modelUsage:
          items:
            oneOf:
              - $ref: '#/components/schemas/LlmUsageRecord'
              - $ref: '#/components/schemas/TtsUsageRecord'
              - $ref: '#/components/schemas/SttUsageRecord'
              - $ref: '#/components/schemas/RealtimeUsageRecord'
              - $ref: '#/components/schemas/InterruptionUsageRecord'
            discriminator:
              propertyName: type
              mapping:
                interruption_usage: '#/components/schemas/InterruptionUsageRecord'
                llm_usage: '#/components/schemas/LlmUsageRecord'
                realtime_usage: '#/components/schemas/RealtimeUsageRecord'
                stt_usage: '#/components/schemas/SttUsageRecord'
                tts_usage: '#/components/schemas/TtsUsageRecord'
          type: array
          title: Modelusage
        sessionConfig:
          anyOf:
            - $ref: '#/components/schemas/SessionConfigSnapshotSchema'
            - type: 'null'
        phaseTimestamps:
          anyOf:
            - $ref: '#/components/schemas/PhaseTimestampsSchema'
            - type: 'null'
        toolCallSummary:
          items:
            $ref: '#/components/schemas/ToolCallSummaryItemSchema'
          type: array
          title: Toolcallsummary
        kbRetrievalSummary:
          items:
            $ref: '#/components/schemas/KbRetrievalSummaryItemSchema'
          type: array
          title: Kbretrievalsummary
        analysis:
          anyOf:
            - $ref: '#/components/schemas/CallAnalysisSchema'
            - type: 'null'
        anonymizedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Anonymizedat
          description: >-
            Timestamp the post-call worker completed transcript anonymization.
            Absent when the feature was disabled or the redaction pass failed
            permanently.
        anonymizationStatus:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
                - skipped
            - type: 'null'
          title: Anonymizationstatus
          description: >-
            Outcome of the anonymization pass: ``applied`` (substitutions
            successful), ``failed`` (all retries exhausted — content left raw),
            ``skipped`` (rare, e.g. config disabled mid-flight).
        retentionStatus:
          anyOf:
            - $ref: '#/components/schemas/RetentionStatusSchema'
            - type: 'null'
          description: >-
            Per-surface retention application status. Surfaces that have been
            redacted per the assistant's retention policy are marked ``applied``
            here; the corresponding fields (transcript / recording URLs /
            analysis / log archive) are cleared.
        recording:
          anyOf:
            - $ref: '#/components/schemas/CallRecording-Output'
            - type: 'null'
        error:
          anyOf:
            - $ref: '#/components/schemas/CallError'
            - type: 'null'
        startedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Startedat
        callAnsweredAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Callansweredat
          description: >-
            When the customer audio path was established. The billing timer
            starts here (NOT at ``startedAt``) — pre-answer ringing time isn't
            billed.
        endedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Endedat
        durationSeconds:
          anyOf:
            - type: number
            - type: 'null'
          title: Durationseconds
        usage:
          additionalProperties: true
          type: object
          title: Usage
          description: >-
            Raw per-call usage metrics emitted by the agent session (token /
            character / STT-seconds counts). Surfaces in Custom mode so the
            customer can reconcile against their own provider invoices. Empty
            dict for tier-mode calls — tier billing is per-minute and
            per-component counts are irrelevant to the customer.
        billedAmountCents:
          anyOf:
            - type: integer
            - type: 'null'
          title: Billedamountcents
          description: >-
            Cost the organization incurred for this call, in cents (EUR).
            Computed at cleanup as ``ceil(answered_seconds/60) ×
            PricingPolicy.voiceRates[tier].baseCents`` (with surcharge applied
            past the MC bucket). ``None`` while the call is in flight.


            **Bucket coverage:** Reflects the call's total usage cost regardless
            of which pre-paid bucket absorbed it — trial credit, monthly
            commitment (MC), or top-up. A call fully covered by trial credit
            still reports its actual minute cost here (the trial bucket simply
            absorbed the charge); use the ``ledger_transactions`` feed if you
            need a per-bucket breakdown.
        tenantWholesaleCents:
          anyOf:
            - type: integer
            - type: 'null'
          title: Tenantwholesalecents
          description: >-
            Internal admin-only cost field used for partner-level margin
            analysis. Always ``null`` for API-key (org-scope) callers; this
            field is not part of your billing contract — use
            ``billedAmountCents`` for the call's actual charge. Same
            bucket-coverage rule applies: trial debits are included on the
            tenant-side total.
        surchargeApplied:
          type: boolean
          title: Surchargeapplied
          description: >-
            True if any portion of this call billed at the surcharge rate — i.e.
            it ran past the monthly-commitment bucket and consumed top-up
            balance.
          default: false
        overrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Overrides
          description: >-
            Per-call overrides snapshot — intentionally untyped (deep-merge
            patch shape).
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
          description: Free-form key/value metadata stored on the call.
        sipHeaders:
          additionalProperties: true
          type: object
          title: Sipheaders
          description: >-
            Custom SIP headers captured during the call, keyed by header name.
            Populated per the trunk's ``headersToAttributes`` policy when the
            call originated or terminated over SIP.
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
      type: object
      required:
        - id
        - type
        - status
      title: CallResponse
      description: The canonical call record returned by every ``/call`` endpoint.
    CallRetentionStatusMeta:
      properties:
        recordings:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
            - type: 'null'
          title: Recordings
        recordingsAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Recordingsappliedat
        conversation:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
            - type: 'null'
          title: Conversation
        conversationAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Conversationappliedat
        analysis:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
            - type: 'null'
          title: Analysis
        analysisAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Analysisappliedat
        logArchive:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
            - type: 'null'
          title: Logarchive
        logArchiveAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Logarchiveappliedat
      type: object
      title: CallRetentionStatusMeta
      description: |-
        Per-surface retention status block on the call object.

        Each surface mirrors the ``retentionStatus`` field on the Call
        document. ``"applied"`` means the post-call worker has removed the
        data; ``"failed"`` means the worker tried but couldn't fully
        complete (next sweep retries). Absent fields signify either:
          * no retention configured for that surface, or
          * retention configured but the cutoff hasn't been reached yet.
    CallTransportSchema:
      properties:
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
          description: '"sip", "twilio", "web", …'
        callSid:
          anyOf:
            - type: string
            - type: 'null'
          title: Callsid
          description: Provider-assigned SIP call id.
        roomName:
          anyOf:
            - type: string
            - type: 'null'
          title: Roomname
        roomId:
          anyOf:
            - type: string
            - type: 'null'
          title: Roomid
        token:
          anyOf:
            - type: string
            - type: 'null'
          title: Token
          description: WebRTC access token (web calls only).
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
          description: WebRTC WebSocket URL (web calls only).
        egressId:
          anyOf:
            - type: string
            - type: 'null'
          title: Egressid
          description: Recording egress id — join key for egress logs.
        sipCallID:
          anyOf:
            - type: string
            - type: 'null'
          title: Sipcallid
          description: SIP bridge call id — join key for SIP logs.
      type: object
      title: CallTransportSchema
      description: |-
        Transport / telephony details for the call.

        Web calls carry ``token`` + ``url`` (WebRTC access credentials).
        Phone calls carry ``callSid`` + ``roomName`` + ``egressId`` /
        ``sipCallID`` (join keys for multi-source log merging).
    CallUpdate:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: Replaces metadata wholesale. Pass ``{}`` to clear, not ``null``.
      additionalProperties: false
      type: object
      title: CallUpdate
      description: |-
        Patch a small whitelist of mutable fields on a live or ended call.

        Status transitions are handled by the internal lifecycle and are not
        exposed via PATCH.
    CanSendResponse:
      properties:
        canSendFreeform:
          type: boolean
          title: Cansendfreeform
          description: >-
            ``True`` iff a free-form text / media message can be sent now
            without a template. Non-WhatsApp channels are always true; WhatsApp
            channels are true only when the 24h customer-care window is
            currently open.
        windowOpenUntil:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Windowopenuntil
          description: >-
            ISO timestamp the WhatsApp 24h customer-care window closes at.
            ``None`` for non-WhatsApp channels and for WhatsApp contacts with no
            inbound history.
        windowState:
          type: string
          enum:
            - open
            - expired
            - not_applicable
          title: Windowstate
          description: >-
            Stable string form of the window status. ``not_applicable`` covers
            non-WhatsApp + WhatsApp-without-history.
        minutesRemaining:
          anyOf:
            - type: integer
            - type: 'null'
          title: Minutesremaining
          description: >-
            Whole minutes left on the window (rounded down). ``None`` when the
            window is closed, expired, or not applicable.
        channelType:
          type: string
          title: Channeltype
          description: Channel type for context (``whatsapp`` / ``telegram`` / etc.).
        recommendation:
          type: string
          enum:
            - send_freeform
            - use_template
          title: Recommendation
          description: >-
            What to do next. ``send_freeform`` = call ``POST
            /messaging/messages`` with the user's content. ``use_template`` =
            the free-form path will be rejected by Meta; use ``type:
            "template"`` with an approved template or set ``fallbackTemplate``
            on the send request.
        recommendedFallbackTemplate:
          anyOf:
            - type: string
            - type: 'null'
          title: Recommendedfallbacktemplate
          description: >-
            Name of the channel's configured default re-engagement template, if
            any. ``None`` when no default is configured. Populated by the
            channel's ``defaultReengagementTemplate`` field — Phase 4 of the
            WhatsApp window rollout adds the config surface.
      additionalProperties: false
      type: object
      required:
        - canSendFreeform
        - windowState
        - channelType
        - recommendation
      title: CanSendResponse
      description: |-
        Result of the ``GET /messaging/can-send`` pre-flight check.

        Tells the caller whether a free-form (non-template) message can be
        sent right now to a specific recipient on a specific channel — and
        if not, what to do instead. Designed so workflows (Activepieces,
        custom code) and dashboard UIs can branch on a stable string
        instead of recomputing the 24h-window clock comparison themselves.

        Non-WhatsApp channels always return ``canSendFreeform=True`` and
        ``windowState="not_applicable"`` — the customer-care window concept
        only exists on WhatsApp Cloud API.
    CartesiaSTT:
      properties:
        provider:
          type: string
          const: cartesia-stt
          title: Provider
        model:
          anyOf:
            - type: string
              const: ink-whisper
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
      additionalProperties: false
      type: object
      required:
        - provider
      title: CartesiaSTT
      description: Cartesia Ink Whisper STT variant.
    Cartesia:
      properties:
        provider:
          type: string
          const: cartesia
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Cartesia voice ID (UUID). Use any voice id from your Cartesia
            account — see https://docs.cartesia.ai/api-reference/voices for the
            upstream catalog.
        model:
          anyOf:
            - type: string
              enum:
                - sonic-3
                - sonic-turbo
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        emotion:
          anyOf:
            - type: string
            - type: 'null'
          title: Emotion
        speed:
          anyOf:
            - type: number
              maximum: 1.5
              minimum: 0.6
            - type: 'null'
          title: Speed
        volume:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.5
            - type: 'null'
          title: Volume
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Cartesia
      description: Cartesia TTS variant. Voices fetched dynamically.
    Cerebras:
      properties:
        provider:
          type: string
          const: cerebras
          title: Provider
        model:
          type: string
          enum:
            - llama3.1-8b
            - gpt-oss-120b
            - qwen-3-235b-a22b-instruct-2507
            - zai-glm-4.7
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 1.5
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 40960
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        frequencyPenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Frequencypenalty
        presencePenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Presencepenalty
        reasoningEffort:
          anyOf:
            - type: string
              enum:
                - minimal
                - low
                - medium
                - high
                - none
            - type: 'null'
          title: Reasoningeffort
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: Cerebras
      description: Cerebras LLM (pipeline mode, openai plugin + Cerebras base_url).
    ChannelHealth:
      properties:
        status:
          $ref: '#/components/schemas/ChannelHealthStatus'
          default: operational
        upstreamProviderStatus:
          anyOf:
            - type: string
            - type: 'null'
          title: Upstreamproviderstatus
        whatsappMessagingTier:
          anyOf:
            - type: string
            - type: 'null'
          title: Whatsappmessagingtier
        whatsappQualityRating:
          anyOf:
            - type: string
            - type: 'null'
          title: Whatsappqualityrating
        lastSuccessAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Lastsuccessat
        lastFailureAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Lastfailureat
        failureReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Failurereason
        failureCount24h:
          type: integer
          title: Failurecount24H
          default: 0
      additionalProperties: false
      type: object
      title: ChannelHealth
      description: Health snapshot — updated by send/webhook error handlers.
    ChannelHealthResponse:
      properties:
        channelId:
          type: string
          title: Channelid
        channelType:
          $ref: '#/components/schemas/ChannelType'
        health:
          $ref: '#/components/schemas/ChannelHealth'
      type: object
      required:
        - channelId
        - channelType
        - health
      title: ChannelHealthResponse
      description: |-
        Live health snapshot — possibly augmented from upstream (rate
        limits, quota, quality rating).
    ChannelHealthStatus:
      type: string
      enum:
        - operational
        - degraded
        - suspended
        - credentials_invalid
      title: ChannelHealthStatus
      description: Per-channel operational health (different from lifecycle status).
    ChannelPatchRequest:
      properties:
        displayName:
          anyOf:
            - type: string
              maxLength: 200
              minLength: 1
            - type: 'null'
          title: Displayname
        description:
          anyOf:
            - type: string
              maxLength: 2000
            - type: 'null'
          title: Description
        tags:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Tags
        webhookUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Webhookurl
        defaultReengagementTemplate:
          anyOf:
            - $ref: '#/components/schemas/ReengagementTemplateConfig'
            - type: 'null'
      additionalProperties: false
      type: object
      title: ChannelPatchRequest
    ChannelResponse:
      properties:
        id:
          type: string
          title: Id
          examples:
            - 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
        tenantId:
          type: string
          title: Tenantid
          examples:
            - 9d2f4c81-3e57-4b6a-8f01-c5a7e92d1b43
        organizationId:
          type: string
          title: Organizationid
          examples:
            - 5a8c1f37-9b2d-4e64-a7c9-3f0e81d6b254
        channelType:
          $ref: '#/components/schemas/ChannelType'
        displayName:
          type: string
          title: Displayname
          examples:
            - Support WhatsApp
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
          examples:
            - Main customer support number
        tags:
          items:
            type: string
          type: array
          title: Tags
          examples:
            - - support
              - de
        identifier:
          type: string
          title: Identifier
          examples:
            - '491511234567'
        capabilities:
          items:
            type: string
          type: array
          title: Capabilities
          examples:
            - - send_text
              - send_media
              - send_template
        webhookUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Webhookurl
        signingSecretLastRotatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Signingsecretlastrotatedat
        defaultReengagementTemplate:
          anyOf:
            - $ref: '#/components/schemas/ReengagementTemplateConfig'
            - type: 'null'
        status:
          $ref: '#/components/schemas/ChannelStatus'
        health:
          $ref: '#/components/schemas/ChannelHealth'
        connectedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Connectedat
        disconnectedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Disconnectedat
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - channelType
        - displayName
        - identifier
        - status
        - health
        - createdAt
        - updatedAt
      title: ChannelResponse
    ChannelStatus:
      type: string
      enum:
        - pending
        - active
        - disconnected
        - credential_expired
        - suspended
        - failed
      title: ChannelStatus
      description: Channel lifecycle.
    ChannelType:
      type: string
      enum:
        - whatsapp
        - telegram
        - email
        - sms
      title: ChannelType
      description: Channels we support at v1 launch.
    ChatAnalysisSchema:
      properties:
        summary:
          anyOf:
            - type: string
            - type: 'null'
          title: Summary
        successEvaluation:
          anyOf:
            - type: string
            - type: 'null'
          title: Successevaluation
        structuredData:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Structureddata
      additionalProperties: false
      type: object
      title: ChatAnalysisSchema
      description: Post-session analysis output.
    ChatAnonymizationBlock:
      properties:
        status:
          type: string
          title: Status
          description: '`applied` / `failed` / `skipped`.'
        substitutionsCount:
          type: integer
          title: Substitutionscount
          description: Number of redaction substitutions applied.
      type: object
      required:
        - status
        - substitutionsCount
      title: ChatAnonymizationBlock
    ChatDataRetentionConfigSchema:
      properties:
        conversationDays:
          anyOf:
            - type: integer
              maximum: 365
              minimum: 0
            - type: 'null'
          title: Conversationdays
          description: >-
            Retention for chat conversation data — the ``ChatMessage`` rows that
            make up the transcript. ``0`` deletes the messages synchronously
            after the ``chat-session-end-report`` webhook fires. ``null`` keeps
            them indefinitely.
        analysisDays:
          anyOf:
            - type: integer
              maximum: 365
              minimum: 0
            - type: 'null'
          title: Analysisdays
          description: >-
            Retention for the post-session ``analysis`` sub-doc (summary,
            success evaluation, structured-data extraction). ``0`` clears it
            synchronously after the ``chat-session-end-report`` webhook fires.
            ``null`` keeps it indefinitely.
        chatLogArchiveDays:
          anyOf:
            - type: integer
              maximum: 365
              minimum: 0
            - type: 'null'
          title: Chatlogarchivedays
          description: >-
            Retention for the gzipped per-chat-session log archive in S3 — the
            permanent system-log capture that survives Loki's platform-managed
            90-day window. ``0`` skips the archive entirely (per-session logs
            stay in Loki for the platform retention window but no permanent
            capture is produced).
      type: object
      title: ChatDataRetentionConfigSchema
      description: |-
        Per-assistant CHAT data retention policy.

        Sibling of ``dataRetention`` scoped to the Chat-API channel. Voice
        and chat are independent — voice retention does NOT apply to chat
        sessions and vice versa, so configuring one does not affect the
        other. ``null`` on any field keeps that surface indefinitely;
        a positive day count triggers the async ``chat_retention_worker``
        hourly sweep; ``0`` clears synchronously after the
        ``chat-session-end-report`` webhook fires (receivers see the data
        once and our DB never holds it again).
    ChatEndReportSessionBlock:
      properties:
        id:
          type: string
          title: Id
        tenantId:
          type: string
          title: Tenantid
        organizationId:
          type: string
          title: Organizationid
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
        tier:
          anyOf:
            - type: string
            - type: 'null'
          title: Tier
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
          description: Voice card id — null in tier mode responses.
        startedAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Startedat
        endedAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Endedat
        durationSeconds:
          anyOf:
            - type: integer
            - type: 'null'
          title: Durationseconds
        endedReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Endedreason
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        variableValues:
          additionalProperties: true
          type: object
          title: Variablevalues
        chargedAmountCents:
          anyOf:
            - type: integer
            - type: 'null'
          title: Chargedamountcents
          description: Retail charge — omitted/null on managed tiers.
        tenantWholesaleCents:
          anyOf:
            - type: integer
            - type: 'null'
          title: Tenantwholesalecents
          description: Always null — never exposed downstream.
        error:
          anyOf:
            - type: string
            - type: 'null'
          title: Error
      type: object
      required:
        - id
        - tenantId
        - organizationId
      title: ChatEndReportSessionBlock
      description: The ``session`` block on the end report (worker-built shape).
    ChatInjectRequest:
      properties:
        role:
          type: string
          enum:
            - user
            - assistant
            - system
          title: Role
        content:
          type: string
          maxLength: 32000
          minLength: 1
          title: Content
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: Free-form per-inject metadata. Capped at 8 KB serialized.
      additionalProperties: false
      type: object
      required:
        - role
        - content
      title: ChatInjectRequest
      description: |-
        Body for ``POST /chat/session/{id}/inject`` — operator-only.

        Appends a hidden message to the session's history so the model uses
        it on the next user turn. Unlike voice's monitor add-message there is
        no realtime session to push to mid-flight: chat is request/response,
        so the next ``POST /chat/message`` naturally picks up the injected
        turn from history.
    ChatMessageCreate:
      properties:
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: >-
            ID of a saved assistant to chat with. Required if ``assistant`` is
            not provided.
          examples:
            - 97d6b41e-e7ce-4ab8-b264-077c4731bc06
        assistant:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/PipelineAssistantCreate-Input'
                - $ref: '#/components/schemas/RealtimeAssistantCreate-Input'
              discriminator:
                propertyName: type
                mapping:
                  pipeline: '#/components/schemas/PipelineAssistantCreate-Input'
                  realtime: '#/components/schemas/RealtimeAssistantCreate-Input'
            - type: 'null'
          title: Assistant
          description: >-
            Inline ephemeral assistant config. The exact shape accepted by
            ``POST /assistant``. Required if ``assistantId`` is not provided.
            Realtime-mode assistants (``type="realtime"``) are rejected — use a
            pipeline assistant for chat.
        overrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Overrides
          description: >-
            Per-session overrides applied as a deep-merge patch on top of the
            saved assistant (only honored when ``assistantId`` is set). Supports
            the same shape as ``CallCreate.overrides`` — ``systemPrompt``,
            ``model.*``, ``tools``, ``knowledgeBaseIds``, ``variables``,
            ``analysis``, ``server``, ``webhookEvents``, ``metadata``,
            ``chatMessagesInContext``. Voice-only fields are silently ignored.
        variableValues:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Variablevalues
          description: >-
            Values for ``{{variable}}`` placeholders inside the assistant's
            system prompt / first message. Substituted at session-create and
            frozen on the session snapshot — updates to the saved assistant
            after this point don't reflow into active sessions.
        sessionId:
          anyOf:
            - type: string
            - type: 'null'
          title: Sessionid
          description: Existing session ID. Omit to implicitly create a new session.
          examples:
            - 8a0fdfdd-1926-48c4-98c1-bccd606a0cc7
        content:
          type: string
          maxLength: 32000
          minLength: 1
          title: Content
          description: User message text. Limit 32,000 characters per turn.
          examples:
            - What are your opening hours on weekends?
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: >-
            Per-message metadata stored alongside the user turn. Capped at 8 KB
            serialized. Distinct from session-level ``metadata``.
        messagesInContext:
          anyOf:
            - type: integer
              maximum: 500
              minimum: 5
            - type: 'null'
          title: Messagesincontext
          description: >-
            Sliding-context override for the implicit-create path only. Ignored
            when ``sessionId`` is set (the session's snapshot value wins).
        channelId:
          anyOf:
            - type: string
              maxLength: 128
            - type: 'null'
          title: Channelid
          description: >-
            Messaging bridge — the connected ``Channel`` doc id when this
            message comes from an inbound messaging thread. Combined with
            ``threadKey`` to find-or-create the chat session sharing the 24h
            billing window with ``/messaging/messages``. Only honored on the
            lazy-create path (``sessionId`` absent); when ``sessionId`` is set
            the session's existing binding wins.
        threadKey:
          anyOf:
            - type: string
              maxLength: 256
              minLength: 1
            - type: 'null'
          title: Threadkey
          description: >-
            Messaging bridge — the external party's stable identifier (phone /
            email / handle / widget user id). When set on the lazy-create path,
            the resolver looks up an ACTIVE session matching ``(organizationId,
            channelId or "chat", threadKey)`` and reuses it; otherwise it
            creates a new session bound to that thread. Lowercased server-side.
            Only honored when ``sessionId`` is absent.
      additionalProperties: false
      type: object
      required:
        - content
      title: ChatMessageCreate
      description: |-
        Body for ``POST /chat/message``.

        Two modes:

        - ``sessionId`` is provided → send a turn into that existing session.
          ``assistantId`` / ``assistant`` / ``overrides`` / ``variableValues``
          are **not** accepted on this path (the session is already bound).

        - ``sessionId`` is absent → implicitly create a new session with the
          provided ``assistantId`` (or inline ``assistant``) + optional
          ``overrides`` + ``variableValues``, then post the message into it.
          The response carries ``sessionId`` and ``sessionCreated=true``.
    ChatMessageEvent:
      properties:
        id:
          type: string
          title: Id
          description: Event id (UUID) — dedupe key.
        type:
          type: string
          const: chat-message
          title: Type
          default: chat-message
        timestamp:
          type: string
          title: Timestamp
          description: ISO 8601 delivery timestamp.
        session:
          $ref: '#/components/schemas/ChatSessionBlock'
        message:
          additionalProperties: true
          type: object
          title: Message
          description: >-
            The message in the REST shape: `id`, `sessionId`, `role` (`user` /
            `assistant` / `system`), `content`, `source` (`api` /
            `operator-inject` / `system-auto`), `metadata`, `toolCalls[]`,
            `usage` (null on managed tiers), `error`, `createdAt`,
            `completedAt`.
      type: object
      required:
        - id
        - timestamp
        - session
        - message
      title: ChatMessageEvent
      description: |-
        ``chat-message`` — fired per completed assistant turn and on
        operator injections. ``message`` matches the REST message shape.
    ChatMessageListResponse:
      properties:
        messages:
          items:
            $ref: '#/components/schemas/ChatMessageResponse'
          type: array
          title: Messages
        nextCursor:
          anyOf:
            - type: string
            - type: 'null'
          title: Nextcursor
      type: object
      required:
        - messages
      title: ChatMessageListResponse
      description: >-
        Cursor-paginated list of messages for ``GET
        /chat/session/{id}/message``.
    ChatMessageResponse:
      properties:
        id:
          type: string
          title: Id
        sessionId:
          type: string
          title: Sessionid
        role:
          type: string
          enum:
            - user
            - assistant
            - system
          title: Role
        content:
          type: string
          title: Content
        source:
          type: string
          enum:
            - api
            - operator-inject
            - system-auto
          title: Source
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        toolCalls:
          items:
            $ref: '#/components/schemas/ChatToolCallSchema'
          type: array
          title: Toolcalls
        usage:
          anyOf:
            - $ref: '#/components/schemas/ChatMessageUsageSchema'
            - type: 'null'
        error:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Error
        createdAt:
          type: string
          format: date-time
          title: Createdat
        completedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Completedat
      type: object
      required:
        - id
        - sessionId
        - role
        - content
        - source
        - createdAt
      title: ChatMessageResponse
      description: |-
        One message returned in chat responses.

        The underlying ``ChatMessage`` document persists ``modelUsage`` with
        the LLM vendor + model identity for internal cost reconciliation.
        That field is DELIBERATELY not exposed on this response — the API
        surface is product-neutral, and the vendor identity would be a
        white-label leak to customer workflows / webhook consumers.
        Dashboard views read modelUsage directly from the document.
    ChatMessageSendResponse:
      properties:
        sessionId:
          type: string
          title: Sessionid
        sessionCreated:
          type: boolean
          title: Sessioncreated
          description: >-
            ``true`` when this call lazy-created the session. Clients should
            store the ``sessionId`` and reuse it for subsequent turns.
        message:
          $ref: '#/components/schemas/ChatMessageResponse'
          description: The assistant's reply for this turn.
        usage:
          anyOf:
            - $ref: '#/components/schemas/ChatMessageUsageSchema'
            - type: 'null'
          description: >-
            LLM token counts for this turn only. Cumulative session totals are
            on ``ChatSession.usage``. ``null`` in tier mode — raw token counts
            fingerprint the underlying provider and are reserved for Custom-mode
            callers.
      type: object
      required:
        - sessionId
        - sessionCreated
        - message
      title: ChatMessageSendResponse
      description: Body returned by ``POST /chat/message`` (buffered mode).
    ChatMessageUsageSchema:
      properties:
        inputTokens:
          type: integer
          title: Inputtokens
          default: 0
        outputTokens:
          type: integer
          title: Outputtokens
          default: 0
      additionalProperties: false
      type: object
      title: ChatMessageUsageSchema
      description: Per-turn LLM token counts.
    ChatModelBlock:
      properties:
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
      type: object
      title: ChatModelBlock
      description: Model identity — present for custom-mode assistants only.
    ChatRetentionStatusSchema:
      properties:
        conversation:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
                - skipped
            - type: 'null'
          title: Conversation
        conversationAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Conversationappliedat
        analysis:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
                - skipped
            - type: 'null'
          title: Analysis
        analysisAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Analysisappliedat
        logArchive:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
                - skipped
            - type: 'null'
          title: Logarchive
        logArchiveAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Logarchiveappliedat
      additionalProperties: false
      type: object
      title: ChatRetentionStatusSchema
      description: |-
        Per-surface retention application audit.

        ``<surface>AppliedAt`` is set when the retention worker (or the
        post-chat worker's 0-day fast-path) successfully cleared the
        surface. Surfaces in ``failed`` state have no ``AppliedAt`` until a
        subsequent retry succeeds. ``logArchive`` mirrors voice's
        log-archive surface — populated by the chat-retention worker
        when the gzipped S3 capture is deleted per ``chatLogArchiveDays``.
    ChatSessionBlock:
      properties:
        id:
          type: string
          title: Id
          description: Chat session id.
        tenantId:
          type: string
          title: Tenantid
          description: Owning tenant id.
        organizationId:
          type: string
          title: Organizationid
          description: Owning workspace id.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: Saved assistant id (null for inline assistants).
        assistant:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Assistant
          description: Sanitized assistant snapshot (tier-masked; secrets removed).
        tier:
          anyOf:
            - type: string
            - type: 'null'
          title: Tier
          description: >-
            Managed tier (`standard` / `gold` / `premium`) or null for custom
            mode.
        status:
          type: string
          title: Status
          description: '`active` or `ended`.'
        endedReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Endedreason
          description: >-
            `user-closed` / `api-closed` / `timeout-24h` / `error` /
            `billing-suspended` / `balance-exhausted` / `assistant-deleted`.
        createdAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdat
          description: ISO 8601.
        expiresAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Expiresat
          description: ISO 8601 — createdAt + 24h.
        endedAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Endedat
          description: ISO 8601, set once ended.
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
          description: Session metadata you supplied.
        usage:
          anyOf:
            - $ref: '#/components/schemas/ChatSessionUsageBlock'
            - type: 'null'
          description: Token usage — null on managed tiers.
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - status
      title: ChatSessionBlock
      description: |-
        The ``session`` sub-payload shared by chat lifecycle webhooks.

        Mirrors ``chat_service._webhook_session_block`` — the same
        tier-masked view ``GET /chat/session/{id}`` returns.
    ChatSessionCloseRequest:
      properties:
        reason:
          anyOf:
            - type: string
              enum:
                - user-closed
                - api-closed
            - type: 'null'
          title: Reason
          description: >-
            Why the caller is closing the session. Distinguishes human-initiated
            (``user-closed``) from automated / SDK-driven closes
            (``api-closed``).
          default: api-closed
      additionalProperties: false
      type: object
      title: ChatSessionCloseRequest
      description: >-
        Optional body for ``POST /chat/session/{id}/close``. All fields
        optional.
    ChatSessionCreate:
      properties:
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: >-
            ID of a saved assistant to chat with. Required if ``assistant`` is
            not provided.
          examples:
            - 97d6b41e-e7ce-4ab8-b264-077c4731bc06
        assistant:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/PipelineAssistantCreate-Input'
                - $ref: '#/components/schemas/RealtimeAssistantCreate-Input'
              discriminator:
                propertyName: type
                mapping:
                  pipeline: '#/components/schemas/PipelineAssistantCreate-Input'
                  realtime: '#/components/schemas/RealtimeAssistantCreate-Input'
            - type: 'null'
          title: Assistant
          description: >-
            Inline ephemeral assistant config. The exact shape accepted by
            ``POST /assistant``. Required if ``assistantId`` is not provided.
            Realtime-mode assistants (``type="realtime"``) are rejected — use a
            pipeline assistant for chat.
        overrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Overrides
          description: >-
            Per-session overrides applied as a deep-merge patch on top of the
            saved assistant (only honored when ``assistantId`` is set). Supports
            the same shape as ``CallCreate.overrides`` — ``systemPrompt``,
            ``model.*``, ``tools``, ``knowledgeBaseIds``, ``variables``,
            ``analysis``, ``server``, ``webhookEvents``, ``metadata``,
            ``chatMessagesInContext``. Voice-only fields are silently ignored.
        variableValues:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Variablevalues
          description: >-
            Values for ``{{variable}}`` placeholders inside the assistant's
            system prompt / first message. Substituted at session-create and
            frozen on the session snapshot — updates to the saved assistant
            after this point don't reflow into active sessions.
        messagesInContext:
          anyOf:
            - type: integer
              maximum: 500
              minimum: 5
            - type: 'null'
          title: Messagesincontext
          description: >-
            Per-session override for the sliding-context window size sent to the
            LLM on each turn. Wins over the assistant's
            ``chatMessagesInContext`` default.
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: >-
            Free-form session metadata. Passed through to ``chat-message`` +
            ``chat-session-end-report`` webhooks and the post-session
            anonymization context. Capped at 8 KB serialized.
        channelId:
          anyOf:
            - type: string
              maxLength: 128
            - type: 'null'
          title: Channelid
          description: >-
            Messaging bridge — the connected ``Channel`` doc id (e.g.
            ``ch_abc123``) when this session is the AI side of an inbound
            messaging thread. Take this value from the ``channelId`` field of
            the outbound webhook payload your workflow received. Omit (with
            ``threadKey`` also omitted) for legacy standalone sessions. When
            ``threadKey`` is set and ``channelId`` is omitted, the server stamps
            the synthetic value ``"chat"`` so widget / standalone-API sessions
            still participate in the shared find-or-create lookup.
        threadKey:
          anyOf:
            - type: string
              maxLength: 256
              minLength: 1
            - type: 'null'
          title: Threadkey
          description: >-
            Messaging bridge — the external party's stable identifier (phone,
            email, handle, widget user id). When set, ``POST /chat/message``
            find-or-creates the session against ``(organizationId, channelId,
            threadKey)`` inside the 24h billing window; the same outbound
            message sent via ``POST /messaging/messages`` to the same key reuses
            that window and does not bill twice. Lowercased server-side.
      additionalProperties: false
      type: object
      title: ChatSessionCreate
      description: Body for ``POST /chat/session``.
    ChatSessionEndReportEvent:
      properties:
        id:
          type: string
          title: Id
          description: Event id (UUID) — dedupe key.
        type:
          type: string
          const: chat-session-end-report
          title: Type
          default: chat-session-end-report
        timestamp:
          type: string
          title: Timestamp
          description: ISO 8601 delivery timestamp.
        session:
          $ref: '#/components/schemas/ChatEndReportSessionBlock'
        messages:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Messages
          description: >-
            The full conversation. Each row: `id`, `role`, `content`, `source`,
            `metadata`, `toolCalls[]`, `error`, `createdAt`, `completedAt` —
            anonymized when configured.
        analysis:
          additionalProperties: true
          type: object
          title: Analysis
          description: >-
            `summary` / `structuredData` / `successEvaluation` (empty object
            when no plan).
        usage:
          anyOf:
            - $ref: '#/components/schemas/ChatSessionUsageBlock'
            - type: 'null'
          description: Session token totals — null on managed tiers.
        model:
          anyOf:
            - $ref: '#/components/schemas/ChatModelBlock'
            - type: 'null'
          description: Custom-mode assistants only; null on managed tiers.
        anonymization:
          anyOf:
            - $ref: '#/components/schemas/ChatAnonymizationBlock'
            - type: 'null'
          description: Present only when anonymization is configured.
      type: object
      required:
        - id
        - timestamp
        - session
      title: ChatSessionEndReportEvent
      description: |-
        ``chat-session-end-report`` — the canonical conversation-finished
        record, delivered after analysis and anonymization complete.
    ChatSessionListResponse:
      properties:
        sessions:
          items:
            $ref: '#/components/schemas/ChatSessionResponse'
          type: array
          title: Sessions
        nextCursor:
          anyOf:
            - type: string
            - type: 'null'
          title: Nextcursor
          description: Opaque cursor for the next page; ``null`` when exhausted.
      type: object
      required:
        - sessions
      title: ChatSessionListResponse
      description: Cursor-paginated list of sessions for ``GET /chat/session``.
    ChatSessionResponse:
      properties:
        id:
          type: string
          title: Id
          examples:
            - 8a0fdfdd-1926-48c4-98c1-bccd606a0cc7
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          examples:
            - 97d6b41e-e7ce-4ab8-b264-077c4731bc06
        assistant:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Assistant
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
        tierLanguage:
          anyOf:
            - type: string
            - type: 'null'
          title: Tierlanguage
        status:
          type: string
          enum:
            - active
            - ended
          title: Status
        endedReason:
          anyOf:
            - type: string
              enum:
                - user-closed
                - api-closed
                - timeout-24h
                - error
                - billing-suspended
                - balance-exhausted
                - assistant-deleted
            - type: 'null'
          title: Endedreason
        error:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Error
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
        lastMessageAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Lastmessageat
        expiresAt:
          type: string
          format: date-time
          title: Expiresat
        endedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Endedat
        messagesInContext:
          anyOf:
            - type: integer
            - type: 'null'
          title: Messagesincontext
        variableValues:
          additionalProperties: true
          type: object
          title: Variablevalues
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        channelId:
          anyOf:
            - type: string
            - type: 'null'
          title: Channelid
        threadKey:
          anyOf:
            - type: string
            - type: 'null'
          title: Threadkey
        usage:
          anyOf:
            - $ref: '#/components/schemas/ChatUsageSchema'
            - type: 'null'
        analysis:
          anyOf:
            - $ref: '#/components/schemas/ChatAnalysisSchema'
            - type: 'null'
        anonymizedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Anonymizedat
        anonymizationStatus:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
                - skipped
            - type: 'null'
          title: Anonymizationstatus
        retentionStatus:
          anyOf:
            - $ref: '#/components/schemas/ChatRetentionStatusSchema'
            - type: 'null'
        logArchiveStatus:
          anyOf:
            - type: string
              enum:
                - pending
                - ready
                - failed
                - expired
            - type: 'null'
          title: Logarchivestatus
        chargedAmountCents:
          anyOf:
            - type: integer
            - type: 'null'
          title: Chargedamountcents
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        messages:
          items:
            $ref: '#/components/schemas/ChatMessageResponse'
          type: array
          title: Messages
      type: object
      required:
        - id
        - status
        - createdAt
        - updatedAt
        - expiresAt
      title: ChatSessionResponse
      description: |-
        Body returned by every ``ChatSession`` read endpoint.

        The session id, assistant snapshot, lifecycle timestamps, usage
        counts, analysis output, and billing totals are all here. Internal
        account scoping ids are not exposed — the caller's auth context
        already establishes the scope of every read.
    ChatSessionStatus:
      type: string
      enum:
        - active
        - ended
      title: ChatSessionStatus
      description: Lifecycle state of a chat session.
    ChatSessionStatusUpdateEvent:
      properties:
        id:
          type: string
          title: Id
          description: Event id (UUID) — dedupe key.
        type:
          type: string
          const: chat-session-status-update
          title: Type
          default: chat-session-status-update
        timestamp:
          type: string
          title: Timestamp
          description: ISO 8601 delivery timestamp.
        session:
          $ref: '#/components/schemas/ChatSessionBlock'
        event:
          type: string
          title: Event
          description: '`created`, `ended`, or `updated`.'
      type: object
      required:
        - id
        - timestamp
        - session
        - event
      title: ChatSessionStatusUpdateEvent
      description: '``chat-session-status-update`` — session lifecycle transitions.'
    ChatSessionUsageBlock:
      properties:
        inputTokens:
          type: integer
          title: Inputtokens
          description: Prompt tokens across the session.
        outputTokens:
          type: integer
          title: Outputtokens
          description: Completion tokens across the session.
        totalTokens:
          type: integer
          title: Totaltokens
          description: Sum of input + output.
      type: object
      required:
        - inputTokens
        - outputTokens
        - totalTokens
      title: ChatSessionUsageBlock
      description: Token usage on a chat session. ``null`` on managed tiers.
    ChatToolCallSchema:
      properties:
        id:
          type: string
          title: Id
        name:
          type: string
          title: Name
        arguments:
          additionalProperties: true
          type: object
          title: Arguments
        result:
          anyOf:
            - {}
            - type: 'null'
          title: Result
        error:
          anyOf:
            - type: string
            - type: 'null'
          title: Error
        startedAt:
          type: string
          format: date-time
          title: Startedat
        completedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Completedat
        durationMs:
          anyOf:
            - type: integer
            - type: 'null'
          title: Durationms
      additionalProperties: false
      type: object
      required:
        - id
        - name
        - startedAt
      title: ChatToolCallSchema
      description: Audit record of one tool the LLM invoked inside an assistant turn.
    ChatUsageSchema:
      properties:
        inputTokens:
          type: integer
          title: Inputtokens
          default: 0
        outputTokens:
          type: integer
          title: Outputtokens
          default: 0
        totalTokens:
          type: integer
          title: Totaltokens
          default: 0
      additionalProperties: false
      type: object
      title: ChatUsageSchema
      description: Cumulative session token counts. Masked in tier mode.
    ChunkingConfigInfo:
      properties:
        chunkSize:
          type: integer
          title: Chunksize
          default: 1000
        chunkOverlap:
          type: integer
          title: Chunkoverlap
          default: 200
        separators:
          items:
            type: string
          type: array
          title: Separators
      type: object
      title: ChunkingConfigInfo
      description: Chunking config response shape — tolerates legacy extras.
    ChunkingConfigSchema:
      properties:
        chunkSize:
          type: integer
          maximum: 10000
          minimum: 100
          title: Chunksize
          description: Chunk size in characters.
          default: 1000
        chunkOverlap:
          type: integer
          maximum: 5000
          minimum: 0
          title: Chunkoverlap
          description: >-
            Overlap between chunks in characters. Must be less than
            ``chunkSize``.
          default: 200
        separators:
          items:
            type: string
          type: array
          title: Separators
          description: Text separators tried in order when splitting.
      additionalProperties: false
      type: object
      title: ChunkingConfigSchema
      description: |-
        Chunking configuration (request shape).

        ``chunkOverlap`` must be strictly less than ``chunkSize`` — the
        underlying ``RecursiveCharacterTextSplitter`` rejects overlap ≥ size
        with a clear error, so we surface that as a 422 at the API edge
        instead of letting the worker fail mid-ingestion.
    ConsentMethod:
      type: string
      enum:
        - web_form
        - sms_double_optin
        - inbound_message
        - verbal
        - import
        - api
        - other
      title: ConsentMethod
      description: How consent was captured.
    ConsentRecordResponse:
      properties:
        id:
          type: string
          title: Id
        tenantId:
          type: string
          title: Tenantid
        organizationId:
          type: string
          title: Organizationid
        contactId:
          anyOf:
            - type: string
            - type: 'null'
          title: Contactid
        channelType:
          $ref: '#/components/schemas/ChannelType'
        identifier:
          type: string
          title: Identifier
        method:
          $ref: '#/components/schemas/ConsentMethod'
        scope:
          $ref: '#/components/schemas/ConsentScope'
        source:
          anyOf:
            - type: string
            - type: 'null'
          title: Source
        ipAddress:
          anyOf:
            - type: string
            - type: 'null'
          title: Ipaddress
        proofUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Proofurl
        optedInAt:
          type: string
          format: date-time
          title: Optedinat
        revokedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Revokedat
        revokedByOptOutId:
          anyOf:
            - type: string
            - type: 'null'
          title: Revokedbyoptoutid
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - channelType
        - identifier
        - method
        - scope
        - optedInAt
        - createdAt
        - updatedAt
      title: ConsentRecordResponse
    ConsentScope:
      type: string
      enum:
        - marketing
        - transactional
        - support
        - all
      title: ConsentScope
      description: What was consented to.
    ContactAddress:
      properties:
        street:
          anyOf:
            - type: string
            - type: 'null'
          title: Street
        city:
          anyOf:
            - type: string
            - type: 'null'
          title: City
        state:
          anyOf:
            - type: string
            - type: 'null'
          title: State
        zip:
          anyOf:
            - type: string
            - type: 'null'
          title: Zip
        country:
          anyOf:
            - type: string
            - type: 'null'
          title: Country
        countryCode:
          anyOf:
            - type: string
            - type: 'null'
          title: Countrycode
        type:
          anyOf:
            - type: string
            - type: 'null'
          title: Type
      additionalProperties: false
      type: object
      title: ContactAddress
      description: Postal address (Meta ``addresses[*]``).
    ContactChannel:
      properties:
        channelType:
          $ref: '#/components/schemas/ChannelType'
        identifier:
          type: string
          title: Identifier
        displayName:
          anyOf:
            - type: string
            - type: 'null'
          title: Displayname
        verified:
          type: boolean
          title: Verified
          default: false
      additionalProperties: false
      type: object
      required:
        - channelType
        - identifier
      title: ContactChannel
      description: Per-channel identifier mapping for a Contact.
    ContactCreateRequest:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        profilePictureUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Profilepictureurl
        notes:
          anyOf:
            - type: string
            - type: 'null'
          title: Notes
        tags:
          items:
            type: string
          type: array
          title: Tags
        customFields:
          additionalProperties: true
          type: object
          title: Customfields
        channels:
          items:
            $ref: '#/components/schemas/ContactChannel'
          type: array
          title: Channels
      additionalProperties: false
      type: object
      title: ContactCreateRequest
    ContactEmail:
      properties:
        email:
          type: string
          title: Email
          examples:
            - jane.doe@example.com
        type:
          anyOf:
            - type: string
            - type: 'null'
          title: Type
          examples:
            - WORK
      additionalProperties: false
      type: object
      required:
        - email
      title: ContactEmail
      description: Email entry — ``type`` is HOME/WORK/etc.
    ContactName:
      properties:
        formattedName:
          type: string
          title: Formattedname
          examples:
            - Jane Doe
        firstName:
          anyOf:
            - type: string
            - type: 'null'
          title: Firstname
          examples:
            - Jane
        lastName:
          anyOf:
            - type: string
            - type: 'null'
          title: Lastname
          examples:
            - Doe
        middleName:
          anyOf:
            - type: string
            - type: 'null'
          title: Middlename
        prefix:
          anyOf:
            - type: string
            - type: 'null'
          title: Prefix
        suffix:
          anyOf:
            - type: string
            - type: 'null'
          title: Suffix
      additionalProperties: false
      type: object
      required:
        - formattedName
      title: ContactName
      description: vCard name block. ``formatted_name`` is the only field Meta requires.
    ContactOrg:
      properties:
        company:
          anyOf:
            - type: string
            - type: 'null'
          title: Company
        department:
          anyOf:
            - type: string
            - type: 'null'
          title: Department
        title:
          anyOf:
            - type: string
            - type: 'null'
          title: Title
      additionalProperties: false
      type: object
      title: ContactOrg
      description: Organisation block (vCard ORG).
    ContactPatchRequest:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        profilePictureUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Profilepictureurl
        notes:
          anyOf:
            - type: string
            - type: 'null'
          title: Notes
        tags:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Tags
        customFields:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Customfields
        channels:
          anyOf:
            - items:
                $ref: '#/components/schemas/ContactChannel'
              type: array
            - type: 'null'
          title: Channels
      additionalProperties: false
      type: object
      title: ContactPatchRequest
    ContactPayload:
      properties:
        name:
          $ref: '#/components/schemas/ContactName'
        phones:
          items:
            $ref: '#/components/schemas/ContactPhone'
          type: array
          title: Phones
        emails:
          items:
            $ref: '#/components/schemas/ContactEmail'
          type: array
          title: Emails
        organization:
          anyOf:
            - $ref: '#/components/schemas/ContactOrg'
            - type: 'null'
        addresses:
          items:
            $ref: '#/components/schemas/ContactAddress'
          type: array
          title: Addresses
        urls:
          items:
            $ref: '#/components/schemas/ContactUrl'
          type: array
          title: Urls
        birthday:
          anyOf:
            - type: string
            - type: 'null'
          title: Birthday
      additionalProperties: false
      type: object
      required:
        - name
      title: ContactPayload
      description: >-
        vCard payload — shape matches Meta's WhatsApp Cloud API ``contacts[*]``

        object verbatim so ``model_dump()`` can be passed straight through.


        Required: ``name.formatted_name``. Everything else is optional.
        Telegram's

        ``sendContact`` consumes a flat subset (first phone + first/last name);

        the provider adapter extracts what it needs.
    ContactPhone:
      properties:
        phone:
          type: string
          title: Phone
          examples:
            - '+4915112345678'
        type:
          anyOf:
            - type: string
            - type: 'null'
          title: Type
          examples:
            - CELL
        waId:
          anyOf:
            - type: string
            - type: 'null'
          title: Waid
          examples:
            - '4915112345678'
      additionalProperties: false
      type: object
      required:
        - phone
      title: ContactPhone
      description: >-
        Phone entry — ``type`` is HOME/WORK/CELL/MAIN/etc. ``wa_id`` for
        WhatsApp.
    ContactResponse:
      properties:
        id:
          type: string
          title: Id
        tenantId:
          type: string
          title: Tenantid
        organizationId:
          type: string
          title: Organizationid
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        profilePictureUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Profilepictureurl
        notes:
          anyOf:
            - type: string
            - type: 'null'
          title: Notes
        tags:
          items:
            type: string
          type: array
          title: Tags
        customFields:
          additionalProperties: true
          type: object
          title: Customfields
        channels:
          items:
            $ref: '#/components/schemas/ContactChannel'
          type: array
          title: Channels
        mergedFromContactIds:
          items:
            type: string
          type: array
          title: Mergedfromcontactids
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - createdAt
        - updatedAt
      title: ContactResponse
    ContactUrl:
      properties:
        url:
          type: string
          title: Url
        type:
          anyOf:
            - type: string
            - type: 'null'
          title: Type
      additionalProperties: false
      type: object
      required:
        - url
      title: ContactUrl
      description: URL entry — Meta ``urls[*]``.
    ContextWindowCompression:
      properties:
        enabled:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enabled
        triggerTokens:
          anyOf:
            - type: integer
              maximum: 128000
              minimum: 5000
            - type: 'null'
          title: Triggertokens
        targetTokens:
          anyOf:
            - type: integer
              maximum: 128000
              minimum: 0
            - type: 'null'
          title: Targettokens
      additionalProperties: false
      type: object
      title: ContextWindowCompression
      description: |-
        Gemini Live API context-window compression knobs.

        The Live API replays the full conversation context (system prompt
        + accumulated turns) on every model turn. Compression runs a
        server-side sliding-window summarisation once accumulated tokens
        exceed ``triggerTokens``, leaving roughly ``targetTokens`` of
        summarised context behind.

        Strictly opt-in: ``enabled=True`` is required to apply compression.
        Any other state (``None`` / ``False`` / missing) leaves compression
        OFF and lets Gemini Live use its own default behavior. When the
        user opts in but omits ``triggerTokens`` / ``targetTokens``, the
        backend fills 20_000 / 8_000 as middle-ground defaults — between
        Google's example (100k/4k) and aggressive (10k/4k) options.

        Bounds come from Google's published Live API limits: ``trigger`` ∈
        [5_000, 128_000] and ``target`` ∈ [0, 128_000], with ``target`` <
        ``trigger`` (server requires it for compression to actually shrink
        the window).
    ControlMessage:
      properties:
        type:
          type: string
          enum:
            - say
            - add-message
            - end-call
            - transfer
            - mute
            - agent-leave
          title: Type
          description: Control message kind.
        content:
          anyOf:
            - type: string
            - type: 'null'
          title: Content
          description: Spoken text or message body, depending on ``type``.
        role:
          type: string
          enum:
            - system
            - user
            - assistant
          title: Role
          description: Message role for ``add-message``; ignored for other types.
          default: system
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
          description: Free-form data forwarded to the agent (e.g. transfer target).
      additionalProperties: false
      type: object
      required:
        - type
      title: ControlMessage
      description: >-
        Control message dispatched to the live agent.


        Supported types:


        - ``say`` — agent speaks ``content`` verbatim.

        - ``add-message`` — append a hidden context message (``role`` selects
        sender).

        - ``end-call`` — ends the call immediately; ``content`` is spoken first
        if set.

        - ``transfer`` — transfer the call (target carried in ``metadata``).

        - ``mute`` — mute / unmute the agent.

        - ``agent-leave`` — agent leaves the room while keeping it alive for
        human takeover.
    ControlResponse:
      properties:
        success:
          type: boolean
          title: Success
          description: '``true`` when the message reached the agent''s data channel.'
        message:
          type: string
          title: Message
          description: Human-readable confirmation.
      additionalProperties: false
      type: object
      required:
        - success
        - message
      title: ControlResponse
      description: Acknowledgement returned after a control message is dispatched.
    ConversationMessage-Input:
      properties:
        role:
          type: string
          title: Role
          description: >-
            Entry kind. One of ``user`` / ``assistant`` / ``system`` /
            ``tool_call`` / ``tool_result`` / ``kb_retrieval``.
        content:
          type: string
          title: Content
          description: >-
            Human-readable string body. Always populated so receivers can render
            a flat transcript without inspecting role-specific fields.
        timestamp:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Timestamp
          description: When the entry was added to the thread (UTC ISO-8601).
        interrupted:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Interrupted
          description: >-
            Assistant turns only. True when the assistant's TTS was interrupted
            before the message finished playing.
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Tool name. Present on ``tool_call`` and ``tool_result`` entries.
        toolCallId:
          anyOf:
            - type: string
            - type: 'null'
          title: Toolcallid
          description: >-
            Stable SDK-issued correlation id linking a ``tool_call`` entry to
            its matching ``tool_result`` entry. Receivers can pair them by
            equality. Present on both kinds.
        arguments:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Arguments
          description: >-
            Structured arguments object the LLM produced when invoking the tool.
            Present on ``tool_call`` entries only.
        isError:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Iserror
          description: True when the tool raised. Present on ``tool_result`` entries only.
        query:
          anyOf:
            - type: string
            - type: 'null'
          title: Query
          description: >-
            Retrieval query text the agent searched on. Present on
            ``kb_retrieval`` entries only.
        chunksCount:
          anyOf:
            - type: integer
            - type: 'null'
          title: Chunkscount
          description: >-
            Number of chunks the retrieval returned. Present on ``kb_retrieval``
            entries only.
        topScore:
          anyOf:
            - type: number
            - type: 'null'
          title: Topscore
          description: >-
            Best similarity score across returned chunks (0..1). Present on
            ``kb_retrieval`` entries only.
        knowledgeBaseIds:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Knowledgebaseids
          description: >-
            Distinct KB ids whose chunks contributed to this retrieval. Present
            on ``kb_retrieval`` entries only.
      additionalProperties: true
      type: object
      required:
        - role
        - content
      title: ConversationMessage
      description: |-
        A single entry in the conversation thread.

        Used as the element type in the ``messages`` array delivered with
        each tool function webhook (and embedded as the post-call message
        log on ``end-of-call-report``).

        The ``role`` field discriminates which other fields are populated.
        Every entry carries ``content`` (a human-readable string body) so
        receivers that just want a flat transcript can ignore the rest;
        receivers that want structured prior context read the
        role-specific fields below.

          - ``user`` / ``assistant`` / ``system`` — spoken / typed turn.
            Populates ``content`` (utterance text), optionally
            ``interrupted`` (assistant turns only).
          - ``tool_call`` — the LLM invoked a tool. Populates ``name``
            (tool name), ``arguments`` (structured args object the LLM
            produced), ``toolCallId`` (SDK-issued correlation id), and
            ``content`` (canonical JSON dump of ``arguments`` —
            ``JSON.parse(content) == arguments`` always holds).
          - ``tool_result`` — a previously invoked tool returned its
            result. Populates ``name`` (tool name), ``toolCallId``
            (matches the ``tool_call`` entry that produced it),
            ``content`` (the exact string the tool's webhook returned),
            ``isError`` (True when the tool raised).
          - ``kb_retrieval`` — knowledge-base retrieval (auto-inject
            path or query_knowledge_base tool call). Populates ``query``,
            ``chunksCount``, ``topScore``, ``knowledgeBaseIds``, and
            ``content`` (the query text for transcript compat).

        ``extra="allow"`` is retained so future entry shapes round-trip
        through the array without a schema bump.
    ConversationNote:
      properties:
        id:
          type: string
          title: Id
        authorId:
          type: string
          title: Authorid
        body:
          type: string
          title: Body
        createdAt:
          type: string
          format: date-time
          title: Createdat
      additionalProperties: false
      type: object
      required:
        - authorId
        - body
      title: ConversationNote
      description: >-
        Internal note — visible to org users with messaging.view, NOT to
        contact.
    ConversationPatchRequest:
      properties:
        status:
          anyOf:
            - $ref: '#/components/schemas/ConversationStatus'
            - type: 'null'
        snoozeUntil:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Snoozeuntil
        assignedToUserId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assignedtouserid
        tags:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Tags
        customFields:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Customfields
      additionalProperties: false
      type: object
      title: ConversationPatchRequest
    ConversationResponse:
      properties:
        id:
          type: string
          title: Id
          examples:
            - c7e91b45-2d8f-4a36-b1e0-6f4d29c8a573
        tenantId:
          type: string
          title: Tenantid
          examples:
            - 9d2f4c81-3e57-4b6a-8f01-c5a7e92d1b43
        organizationId:
          type: string
          title: Organizationid
          examples:
            - 5a8c1f37-9b2d-4e64-a7c9-3f0e81d6b254
        channelId:
          type: string
          title: Channelid
          examples:
            - 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
        contactId:
          type: string
          title: Contactid
          examples:
            - 1e5d8a92-4c7b-4f13-9e60-b28f5c3a7d41
        status:
          $ref: '#/components/schemas/ConversationStatus'
        snoozeUntil:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Snoozeuntil
        assignedToUserId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assignedtouserid
        tags:
          items:
            type: string
          type: array
          title: Tags
        customFields:
          additionalProperties: true
          type: object
          title: Customfields
        notes:
          items:
            $ref: '#/components/schemas/ConversationNote'
          type: array
          title: Notes
        lastMessageAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Lastmessageat
        lastMessagePreview:
          anyOf:
            - type: string
            - type: 'null'
          title: Lastmessagepreview
          examples:
            - Thanks, that solved it!
        lastInboundAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Lastinboundat
        lastOutboundAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Lastoutboundat
        unreadCount:
          type: integer
          title: Unreadcount
          default: 0
        messageCount:
          type: integer
          title: Messagecount
          default: 0
        customerCareWindowExpiresAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Customercarewindowexpiresat
        windowState:
          type: string
          enum:
            - open
            - expired
            - not_applicable
          title: Windowstate
          default: not_applicable
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - channelId
        - contactId
        - status
        - createdAt
        - updatedAt
      title: ConversationResponse
    ConversationStatus:
      type: string
      enum:
        - open
        - snoozed
        - closed
      title: ConversationStatus
    CreateConsentRequest:
      properties:
        channelType:
          $ref: '#/components/schemas/ChannelType'
        identifier:
          type: string
          minLength: 1
          title: Identifier
        contactId:
          anyOf:
            - type: string
            - type: 'null'
          title: Contactid
        method:
          $ref: '#/components/schemas/ConsentMethod'
        scope:
          $ref: '#/components/schemas/ConsentScope'
          default: all
        source:
          anyOf:
            - type: string
            - type: 'null'
          title: Source
        ipAddress:
          anyOf:
            - type: string
            - type: 'null'
          title: Ipaddress
        userAgent:
          anyOf:
            - type: string
            - type: 'null'
          title: Useragent
        proofUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Proofurl
      additionalProperties: false
      type: object
      required:
        - channelType
        - identifier
        - method
      title: CreateConsentRequest
    CreateNoteRequest:
      properties:
        body:
          type: string
          maxLength: 4000
          minLength: 1
          title: Body
      additionalProperties: false
      type: object
      required:
        - body
      title: CreateNoteRequest
    CreateOptOutRequest:
      properties:
        channelType:
          $ref: '#/components/schemas/ChannelType'
        identifier:
          type: string
          minLength: 1
          title: Identifier
        contactId:
          anyOf:
            - type: string
            - type: 'null'
          title: Contactid
        source:
          $ref: '#/components/schemas/OptOutSource'
          default: manual
        reason:
          anyOf:
            - type: string
            - type: 'null'
          title: Reason
      additionalProperties: false
      type: object
      required:
        - channelType
        - identifier
      title: CreateOptOutRequest
    CustomOpenAI:
      properties:
        provider:
          type: string
          const: custom-openai
          title: Provider
        model:
          type: string
          title: Model
          description: >-
            Any model id the upstream OpenAI-compatible endpoint accepts. We
            don't ship a catalog — set this to whatever your self-hosted server
            (vLLM, Ollama, LM Studio, llama.cpp, …) responds to at
            ``/v1/chat/completions``. The credential must include a ``base_url``
            pointing at that endpoint.
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 131072
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        frequencyPenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Frequencypenalty
        presencePenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Presencepenalty
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: CustomOpenAI
      description: |-
        Custom OpenAI-compatible LLM (pipeline mode).

        The ``model`` field is open-ended ``str`` because the user picks
        whatever the BYO upstream supports. Credentials must include a
        ``base_url`` (the upstream's OpenAI-compatible endpoint).
    CustomerSchema:
      properties:
        number:
          anyOf:
            - type: string
            - type: 'null'
          title: Number
          description: Phone number in E.164.
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        externalId:
          anyOf:
            - type: string
            - type: 'null'
          title: Externalid
          description: Caller-supplied id for correlating the call with your CRM.
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
          description: Free-form key/value metadata about the customer.
      additionalProperties: false
      type: object
      title: CustomerSchema
      description: The human on the other end of the call.
    DataRetentionConfigSchema:
      properties:
        recordingsDays:
          anyOf:
            - type: integer
              maximum: 365
              minimum: 0
            - type: 'null'
          title: Recordingsdays
          description: >-
            Retention for call audio recordings (stereo / agent / customer
            tracks) in days. ``0`` disables egress entirely for this assistant.
            ``null`` keeps recordings indefinitely.
        conversationDays:
          anyOf:
            - type: integer
              maximum: 365
              minimum: 0
            - type: 'null'
          title: Conversationdays
          description: >-
            Retention for the call's conversation data — transcript, messages,
            timeline (tool calls + knowledge base retrievals), and the
            denormalized tool / KB summaries. ``0`` clears the fields
            synchronously after the post-call webhook fires.
        analysisDays:
          anyOf:
            - type: integer
              maximum: 365
              minimum: 0
            - type: 'null'
          title: Analysisdays
          description: >-
            Retention for the post-call analysis (summary, success evaluation,
            structured data extraction).
        callLogArchiveDays:
          anyOf:
            - type: integer
              maximum: 365
              minimum: 0
            - type: 'null'
          title: Calllogarchivedays
          description: >-
            Retention for the gzipped per-call log archive in S3 — the permanent
            system-log capture that survives Loki's platform-managed 90-day
            window. ``0`` skips the archive entirely.
      type: object
      title: DataRetentionConfigSchema
      description: |-
        Per-assistant data retention policy.

        Each surface is independently configurable. ``None`` means no
        automatic deletion (data lives until the call doc is hard-deleted
        or the assistant's retention is later configured). A non-null
        integer is the retention window in days (0–365 inclusive) where
        ``0`` means "do not persist this surface at all" — the agent /
        post-call worker take the early-exit paths so the data never lands
        on disk in the first place.

        For ``recordingsDays=0`` the agent skips egress entirely, so no S3
        audio object is ever written. For ``callLogArchiveDays=0`` the
        post-call worker skips the Loki→S3 archive — system logs still
        flow through Loki for the platform-managed 90-day retention but
        no per-call permanent archive is produced. For
        ``conversationDays=0`` and ``analysisDays=0`` the post-call worker
        clears the corresponding Mongo fields synchronously after the
        end-of-call webhook fires (the webhook payload itself still
        contains the data — receivers see it once and our DB never does).
    DeepSeek:
      properties:
        provider:
          type: string
          const: deepseek
          title: Provider
        model:
          type: string
          enum:
            - deepseek-chat
            - deepseek-reasoner
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        frequencyPenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Frequencypenalty
        presencePenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Presencepenalty
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: DeepSeek
      description: DeepSeek LLM (pipeline mode, OpenAI-compatible).
    DeepgramSTT:
      properties:
        provider:
          type: string
          const: deepgram
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - flux-general-en
                - flux-general-multi
                - nova-3
                - nova-3-general
                - nova-3-medical
                - nova-2
                - nova-2-general
                - nova-2-meeting
                - nova-2-phonecall
                - nova-2-finance
                - nova-2-conversationalai
                - nova-2-voicemail
                - nova-2-video
                - nova-2-medical
                - nova-2-drivethru
                - nova-2-automotive
                - nova
                - nova-general
                - nova-phonecall
                - nova-medical
                - enhanced
                - enhanced-general
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        keyterms:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Keyterms
        smartFormat:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Smartformat
        endpointing:
          anyOf:
            - type: integer
              maximum: 5000
              minimum: 10
            - type: 'null'
          title: Endpointing
          description: Endpointing silence in ms (Nova only).
        numerals:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Numerals
        punctuate:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Punctuate
        diarization:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Diarization
        eagerEotThreshold:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Eagereotthreshold
        eotThreshold:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Eotthreshold
        eotTimeoutMs:
          anyOf:
            - type: integer
              maximum: 5000
              minimum: 100
            - type: 'null'
          title: Eottimeoutms
      additionalProperties: false
      type: object
      required:
        - provider
      title: DeepgramSTT
      description: Deepgram STT — Flux (STTv2) and Nova (STT) families.
    DeepgramTTS:
      properties:
        provider:
          type: string
          const: deepgram-tts
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Aura-2 voice/model id (e.g. `aura-2-andromeda-en`). See
            https://developers.deepgram.com/docs/tts-models for the upstream
            catalog.
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: DeepgramTTS
      description: Deepgram Aura-2 TTS — voice IS the model id.
    DeepslateRealtime:
      properties:
        provider:
          type: string
          const: deepslate-realtime
          title: Provider
        model:
          anyOf:
            - type: string
              const: deepslate-default
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
            - type: 'null'
          title: Voice
          description: Cloned-voice UUID from Deepslate portal.
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        hostedTtsQuality:
          anyOf:
            - type: string
              enum:
                - high_quality
                - low_latency
            - type: 'null'
          title: Hostedttsquality
        vadConfidenceThreshold:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Vadconfidencethreshold
        vadMinVolume:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Vadminvolume
        vadStartDurationMs:
          anyOf:
            - type: integer
              maximum: 10000
              minimum: 0
            - type: 'null'
          title: Vadstartdurationms
        vadStopDurationMs:
          anyOf:
            - type: integer
              maximum: 10000
              minimum: 0
            - type: 'null'
          title: Vadstopdurationms
        vadBackbufferDurationMs:
          anyOf:
            - type: integer
              maximum: 10000
              minimum: 0
            - type: 'null'
          title: Vadbackbufferdurationms
        generateReplyTimeout:
          anyOf:
            - type: number
              maximum: 30
              minimum: 0.5
            - type: 'null'
          title: Generatereplytimeout
      additionalProperties: false
      type: object
      required:
        - provider
      title: DeepslateRealtime
      description: |-
        Deepslate Realtime — end-to-end audio model.

        Authenticates against Deepslate using the credentials configured in
        your provider keys for ``deepslate`` (API key + vendor and workspace
        identifiers issued by Deepslate via their portal).
    DeliveryListItem:
      properties:
        id:
          type: string
          title: Id
        eventType:
          type: string
          title: Eventtype
        webhookUrl:
          type: string
          title: Webhookurl
        status:
          type: string
          title: Status
        attemptsCount:
          type: integer
          title: Attemptscount
        deliveredAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Deliveredat
        createdAt:
          type: string
          format: date-time
          title: Createdat
      additionalProperties: false
      type: object
      required:
        - id
        - eventType
        - webhookUrl
        - status
        - attemptsCount
        - createdAt
      title: DeliveryListItem
    DeliveryStatus:
      type: string
      enum:
        - pending
        - in_flight
        - delivered
        - retrying
        - permanently_failed
        - disabled
      title: DeliveryStatus
    ElevenLabsSTT:
      properties:
        provider:
          type: string
          const: elevenlabs-stt
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - scribe_v2_realtime
                - scribe_v1
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        keyterms:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Keyterms
        serverVad:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Servervad
      additionalProperties: false
      type: object
      required:
        - provider
      title: ElevenLabsSTT
      description: ElevenLabs Scribe STT variant.
    ElevenLabs:
      properties:
        provider:
          type: string
          const: elevenlabs
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            ElevenLabs voice ID (UUID-like). Use any voice id from your
            ElevenLabs account — see
            https://elevenlabs.io/docs/api-reference/voices for the upstream
            catalog.
        model:
          anyOf:
            - type: string
              enum:
                - eleven_flash_v2_5
                - eleven_turbo_v2_5
                - eleven_multilingual_v2
                - eleven_v3
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        stability:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Stability
        similarityBoost:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Similarityboost
        style:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Style
        useSpeakerBoost:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Usespeakerboost
        speed:
          anyOf:
            - type: number
              maximum: 1.2
              minimum: 0.7
            - type: 'null'
          title: Speed
        applyTextNormalization:
          anyOf:
            - type: string
              enum:
                - auto
                - 'off'
                - 'on'
            - type: 'null'
          title: Applytextnormalization
          description: >-
            Text normalization: ``auto`` (default — engine decides), ``on``
            (always normalise numbers/dates/URLs), or ``off`` (skip
            normalization for lower latency, may mispronounce edge cases).
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: ElevenLabs
      description: ElevenLabs TTS variant. Voices are dynamic (fetched from API).
    EmbeddingConfigInfo:
      properties:
        model:
          type: string
          title: Model
          default: text-embedding-3-small
        dimensions:
          type: integer
          title: Dimensions
          default: 1536
      type: object
      title: EmbeddingConfigInfo
      description: Embedding config response shape — tolerates legacy extras.
    EndCallTool-Input:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: endCall
          title: Type
          default: endCall
        endCallPhrases:
          items:
            type: string
          type: array
          title: Endcallphrases
          description: >-
            Exact phrases that trigger an instant end call. Empty ⇒ LLM-only
            trigger.
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Input'
            - type: 'null'
          description: Optional custom description exposed to the LLM.
      additionalProperties: false
      type: object
      title: EndCallTool
      description: Ends the call — via phrase match or LLM-triggered tool call.
    EndCallTool-Output:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: endCall
          title: Type
          default: endCall
        endCallPhrases:
          items:
            type: string
          type: array
          title: Endcallphrases
          description: >-
            Exact phrases that trigger an instant end call. Empty ⇒ LLM-only
            trigger.
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Output'
            - type: 'null'
          description: Optional custom description exposed to the LLM.
      additionalProperties: false
      type: object
      title: EndCallTool
      description: Ends the call — via phrase match or LLM-triggered tool call.
    EndCallToolResponse:
      properties:
        id:
          type: string
          title: Id
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: Folder this tool belongs to, or ``null`` when unfiled.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        type:
          type: string
          const: endCall
          title: Type
          default: endCall
        endCallPhrases:
          items:
            type: string
          type: array
          title: Endcallphrases
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Output'
            - type: 'null'
      type: object
      required:
        - id
      title: EndCallToolResponse
    EndCallToolUpdate:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Move this tool to a folder. Pass an explicit ``null`` to unassign
            and return the tool to the list root. A PATCH that only changes
            ``folderId`` is treated as an organisational move — ``updatedAt`` is
            preserved.
        messages:
          anyOf:
            - items:
                $ref: '#/components/schemas/ToolMessage'
              type: array
            - type: 'null'
          title: Messages
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
        type:
          type: string
          const: endCall
          title: Type
          default: endCall
        endCallPhrases:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Endcallphrases
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Input'
            - type: 'null'
      additionalProperties: false
      type: object
      title: EndCallToolUpdate
    EndOfCallAnalysis:
      properties:
        summary:
          anyOf:
            - type: string
            - type: 'null'
          title: Summary
          description: Free-text recap produced by the configured summary prompt.
        structuredData:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Structureddata
          description: JSON object extracted per the assistant's structured-data schema.
        successEvaluation:
          anyOf:
            - type: string
            - type: 'null'
          title: Successevaluation
          description: Grade produced by the configured success-evaluation rubric.
      additionalProperties: true
      type: object
      title: EndOfCallAnalysis
      description: |-
        Post-call analysis output. Populated only when the assistant has
        an analysis plan configured; otherwise an empty object.
        Extra keys round-trip so future analysis fields surface without a
        schema bump.
    EndOfCallMessage:
      properties:
        role:
          type: string
          title: Role
          description: >-
            Entry kind. One of ``user`` / ``assistant`` / ``system`` /
            ``tool_call`` / ``tool_result`` / ``kb_retrieval``.
        message:
          type: string
          title: Message
          description: >-
            Human-readable string body — utterance text, JSON-stringified tool
            arguments, raw tool output, or KB query depending on ``role``.
            Always populated.
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Tool name. Present on ``tool_call`` and ``tool_result`` entries.
        toolCallId:
          anyOf:
            - type: string
            - type: 'null'
          title: Toolcallid
          description: >-
            SDK-issued correlation id linking a ``tool_call`` entry to its
            matching ``tool_result`` entry.
        arguments:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Arguments
          description: Structured arguments object. Present on ``tool_call`` only.
        isError:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Iserror
          description: True when the tool raised. Present on ``tool_result`` only.
        query:
          anyOf:
            - type: string
            - type: 'null'
          title: Query
          description: Retrieval query text. Present on ``kb_retrieval`` only.
        chunksCount:
          anyOf:
            - type: integer
            - type: 'null'
          title: Chunkscount
          description: Chunks returned by the retrieval. Present on ``kb_retrieval`` only.
        topScore:
          anyOf:
            - type: number
            - type: 'null'
          title: Topscore
          description: >-
            Best similarity score across returned chunks (0..1).
            ``kb_retrieval`` only.
        knowledgeBaseIds:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Knowledgebaseids
          description: Distinct KB ids whose chunks contributed. ``kb_retrieval`` only.
      additionalProperties: true
      type: object
      required:
        - role
        - message
      title: EndOfCallMessage
      description: |-
        One entry in the post-call conversation log.

        Carries spoken / typed turns (``user`` / ``assistant`` / ``system``)
        interleaved chronologically with the structured tool I/O and
        knowledge-base retrievals that fired during the call. Mirrors
        :class:`ConversationMessage` from the live ``tool-calls`` webhook
        so post-call ingestion pipelines can reuse the same parser:

          - ``user`` / ``assistant`` / ``system`` — populates ``message``
            with the utterance text.
          - ``tool_call`` — populates ``name`` (tool), ``arguments``
            (structured object the LLM produced), ``toolCallId`` (SDK
            correlation id), and ``message`` (canonical JSON dump of
            ``arguments``).
          - ``tool_result`` — populates ``name``, ``toolCallId`` (matches
            the ``tool_call`` entry that produced it), ``message`` (the
            exact string the tool returned), and ``isError``.
          - ``kb_retrieval`` — populates ``query``, ``chunksCount``,
            ``topScore``, ``knowledgeBaseIds``, and ``message`` (the
            retrieval query for transcript compat).

        ``extra="allow"`` is retained so future entry shapes round-trip
        without a schema bump.
    EndOfCallReportEvent:
      properties:
        id:
          type: string
          title: Id
        type:
          type: string
          const: end-of-call-report
          title: Type
          default: end-of-call-report
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        call:
          $ref: '#/components/schemas/AssistantCallObject'
        transcript:
          type: string
          title: Transcript
          description: >-
            Role-prefixed conversation transcript. Format: ``USER:
            ...\nASSISTANT: ...`` (newline separated, role names in uppercase).
        messages:
          items:
            $ref: '#/components/schemas/EndOfCallMessage'
          type: array
          title: Messages
          description: Full message history.
        analysis:
          $ref: '#/components/schemas/EndOfCallAnalysis'
          description: Post-call analysis output.
        overrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Overrides
          description: Per-call overrides applied at the start of the call (if any).
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: Free-form metadata supplied at call creation.
        timeline:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Timeline
          description: >-
            Chronological session timeline — message arrivals, tool invocations,
            KB retrievals, errors. Each item carries a ``type`` discriminator
            and type-specific payload.
        toolCallSummary:
          items:
            $ref: '#/components/schemas/ToolCallSummaryItem'
          type: array
          title: Toolcallsummary
          description: Denormalized list of every tool invoked during the call.
        kbRetrievalSummary:
          items:
            $ref: '#/components/schemas/KbRetrievalSummaryItem'
          type: array
          title: Kbretrievalsummary
          description: Denormalized list of every knowledge-base retrieval.
        recording:
          anyOf:
            - $ref: '#/components/schemas/CallRecording-Input'
            - type: 'null'
          description: >-
            Recording artifacts. ``null`` if recording is disabled or upload
            failed.
        modelUsage:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Modelusage
          description: >-
            Per-model usage breakdown (LLM / TTS / STT / interruption). Each
            item contains the model identifier, provider, and tokens /
            characters / seconds counters as applicable.
        sessionConfig:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Sessionconfig
          description: >-
            Snapshot of the active STT / LLM / TTS configuration at call start.
            Useful for comparing assistant config across calls.
        phaseTimestamps:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Phasetimestamps
          description: >-
            ISO-8601 timestamps for the agent / worker lifecycle phases
            (agentStartedAt, agentConnectedAt, configLoadedAt, sessionReadyAt,
            firstAssistantResponseAt, firstUserInputAt, firstToolAt,
            cleanupStartedAt, agentExitedAt, workerDequeuedAt,
            workerFinishedAt). Useful for latency analysis.
        anonymization:
          anyOf:
            - $ref: '#/components/schemas/CallAnonymizationMeta'
            - type: 'null'
          description: >-
            Anonymization audit metadata. Present on every event when the
            assistant has the feature configured (regardless of outcome); absent
            when the assistant has no anonymization config at all.
            ``status="failed"`` indicates the transcript / messages / summary in
            this payload are RAW because the redaction pass exhausted its
            retries.
        variables:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Variables
          description: >-
            Call-time variables supplied at call creation (``POST /call``
            ``overrides.variables``) plus any auto-merged ``sip_*`` headers —
            the same bag carried on the ``tool-calls`` webhook. Surfaced here so
            post-call automations can correlate the call (CRM ids, campaign
            codes, caller context) without a second lookup. Omitted from the
            payload entirely when the call carried no variables.
        assistant:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Assistant
          description: >-
            Snapshot of the assistant configuration that handled the call —
            ``name`` / ``systemPrompt`` / ``firstMessage`` / ``tools`` /
            ``knowledgeBaseIds`` / ``analysis`` / ``metadata`` / behavioural
            configs / etc. — so receivers can persist the exact config that ran
            without calling back into the API. The same sanitized snapshot the
            ``tool-calls`` webhook carries: model / voice / transcriber details
            are included only when the assistant is configured with them
            directly (Custom mode); tier assistants omit the underlying vendor
            stack to keep the white-label contract clean (the customer's
            ``systemPrompt`` is always preserved). Webhook signing secrets are
            never included. Omitted when the configuration is unavailable (e.g.
            archive-only post-call jobs).
      type: object
      required:
        - id
        - timestamp
        - call
        - transcript
        - messages
        - analysis
      title: EndOfCallReportEvent
      description: |-
        Post-call snapshot — fires once per call after analysis +
        recording split finish.

        This is the most important event for downstream systems: persist
        transcripts, archive recordings, push CRM updates, run
        analytics. Subscribe via ``webhookEvents`` containing
        ``"end-of-call-report"``.

        Tier-mode calls have ``modelUsage`` and ``sessionConfig`` masked
        to honour the white-label contract — see
        ``services/call_payload_masking`` for the rules.
    EndTakeoverRequest:
      properties:
        reason:
          type: string
          enum:
            - human-ended-call
            - customer-ended-call
          title: Reason
          description: >-
            Why the takeover ended. ``human-ended-call`` — supervisor ended the
            session; ``customer-ended-call`` — customer hung up and the room is
            being cleaned up.
      additionalProperties: false
      type: object
      required:
        - reason
      title: EndTakeoverRequest
      description: Body for ``POST /call/{callId}/monitor/end-takeover``.
    EndTakeoverResponse:
      properties:
        success:
          type: boolean
          title: Success
          description: '``true`` when cleanup completed without errors.'
        message:
          type: string
          title: Message
          description: Human-readable confirmation.
        callId:
          type: string
          title: Callid
        roomDeleted:
          type: boolean
          title: Roomdeleted
          description: Whether the call's media room was actually deleted.
          default: false
        participantsRemoved:
          type: integer
          title: Participantsremoved
          description: Number of participants disconnected during cleanup.
          default: 0
      additionalProperties: false
      type: object
      required:
        - success
        - message
        - callId
      title: EndTakeoverResponse
      description: Result of an end-takeover call.
    EndpointCreateRequest:
      properties:
        url:
          type: string
          maxLength: 2083
          minLength: 1
          format: uri
          title: Url
        subscribedEvents:
          items:
            type: string
          type: array
          minItems: 1
          title: Subscribedevents
          description: >-
            Subset of the audit event catalog (e.g.
            'conversation.user.message').
        enabled:
          type: boolean
          title: Enabled
          default: true
      additionalProperties: false
      type: object
      required:
        - url
        - subscribedEvents
      title: EndpointCreateRequest
    EndpointCreateResponse:
      properties:
        id:
          type: string
          title: Id
        url:
          type: string
          title: Url
        subscribedEvents:
          items:
            type: string
          type: array
          title: Subscribedevents
        enabled:
          type: boolean
          title: Enabled
        createdAt:
          type: string
          format: date-time
          title: Createdat
        hasRotationInProgress:
          type: boolean
          title: Hasrotationinprogress
          default: false
        signingSecret:
          type: string
          title: Signingsecret
      additionalProperties: false
      type: object
      required:
        - id
        - url
        - subscribedEvents
        - enabled
        - createdAt
        - signingSecret
      title: EndpointCreateResponse
      description: Create response — embeds the one-time-reveal signing secret.
    EndpointResponse:
      properties:
        id:
          type: string
          title: Id
        url:
          type: string
          title: Url
        subscribedEvents:
          items:
            type: string
          type: array
          title: Subscribedevents
        enabled:
          type: boolean
          title: Enabled
        createdAt:
          type: string
          format: date-time
          title: Createdat
        hasRotationInProgress:
          type: boolean
          title: Hasrotationinprogress
          default: false
      additionalProperties: false
      type: object
      required:
        - id
        - url
        - subscribedEvents
        - enabled
        - createdAt
      title: EndpointResponse
      description: Standard read shape — masks the signing secret.
    EndpointUpdateRequest:
      properties:
        url:
          anyOf:
            - type: string
              maxLength: 2083
              minLength: 1
              format: uri
            - type: 'null'
          title: Url
        subscribedEvents:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Subscribedevents
        enabled:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enabled
      additionalProperties: false
      type: object
      title: EndpointUpdateRequest
    EndpointingConfigSchema:
      properties:
        smartEnabled:
          type: boolean
          title: Smartenabled
          default: false
        waitSeconds:
          type: number
          maximum: 5
          minimum: 0
          title: Waitseconds
          default: 0.4
        maxDelay:
          type: number
          maximum: 10
          minimum: 0.5
          title: Maxdelay
          default: 3
      type: object
      title: EndpointingConfigSchema
    ErrorEnvelope:
      properties:
        statusCode:
          type: integer
          title: Statuscode
          description: HTTP status code.
        data:
          anyOf:
            - {}
            - type: 'null'
          title: Data
          description: >-
            Optional structured context for the error. Most errors leave this
            ``null``; some surface contextual fields (e.g. ``callId`` and
            ``endedReason`` on outbound-call init failures) so clients can
            navigate to the failed resource without an extra round-trip.
        message:
          type: string
          title: Message
          description: Human-readable error message.
        success:
          type: boolean
          title: Success
          description: Always ``false`` for errors.
          default: false
        errors:
          anyOf:
            - items:
                additionalProperties: true
                type: object
              type: array
            - type: 'null'
          title: Errors
          description: Optional per-field validation errors.
      type: object
      required:
        - statusCode
        - message
      title: ErrorEnvelope
      description: Standard error envelope for every 4xx/5xx response body.
    EvaluationConfigSchema:
      properties:
        enabled:
          type: boolean
          title: Enabled
          default: false
        prompt:
          type: string
          title: Prompt
          default: >-
            You are an expert conversation evaluator. You will be given a
            transcript of a conversation (voice call or chat session). Evaluate
            if the conversation achieved its objectives.
        rubric:
          type: string
          enum:
            - NumericScale
            - DescriptiveScale
            - Checklist
            - Matrix
            - PercentageScale
            - LikertScale
            - AutomaticRubric
            - PassFail
          title: Rubric
          default: NumericScale
        timeoutSeconds:
          type: integer
          maximum: 60
          minimum: 1
          title: Timeoutseconds
          default: 10
      type: object
      title: EvaluationConfigSchema
      description: |-
        Success evaluation — applies to BOTH voice calls and chat sessions.

        Pick a rubric and a prompt; the same evaluation runs on every
        conversation that ends, regardless of channel.
    FallbackNumberDestination:
      properties:
        type:
          type: string
          const: number
          title: Type
          default: number
        number:
          type: string
          title: Number
          description: Destination phone number in E.164 (e.g. ``+14155551234``).
        extension:
          anyOf:
            - type: string
            - type: 'null'
          title: Extension
          description: DTMF extension dialled after the call connects.
        callerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Callerid
          description: Caller ID presented to the destination.
      additionalProperties: false
      type: object
      required:
        - number
      title: FallbackNumberDestination
      description: Fallback destination — a phone number.
    FallbackSipDestination:
      properties:
        type:
          type: string
          const: sip
          title: Type
          default: sip
        sipUri:
          type: string
          title: Sipuri
          description: Full SIP URI (e.g. ``sip:desk@pbx.example.com``).
        sipHeaders:
          additionalProperties: true
          type: object
          title: Sipheaders
          description: Custom SIP headers to include on the INVITE / REFER.
      additionalProperties: false
      type: object
      required:
        - sipUri
      title: FallbackSipDestination
      description: Fallback destination — a SIP URI.
    FileResponse:
      properties:
        id:
          type: string
          title: Id
          description: Unique file identifier (nanoid).
        name:
          type: string
          title: Name
          description: Current display name (may have been renamed since upload).
        mimeType:
          type: string
          title: Mimetype
          description: MIME type declared at upload time.
        size:
          type: integer
          title: Size
          description: File size in bytes.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: Folder this file belongs to, or ``null`` when unfiled.
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
          description: >-
            Time-limited presigned URL for downloading the file. Regenerated on
            every ``GET /file/{id}`` call.
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
          description: Upload timestamp.
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
          description: Last update timestamp.
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
          description: User ID that uploaded the file.
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
          description: User ID that last updated the file.
      additionalProperties: false
      type: object
      required:
        - id
        - name
        - mimeType
        - size
      title: FileResponse
      description: Uploaded file record returned by the Files API.
    FolderCreate:
      properties:
        resourceType:
          $ref: '#/components/schemas/FolderResourceType'
          description: >-
            The family of resources this folder will contain. Immutable after
            creation — children of one resource family can never be moved into a
            folder of another.
        name:
          type: string
          title: Name
          description: Human-readable folder label (1-100 characters, trimmed).
      type: object
      required:
        - resourceType
        - name
      title: FolderCreate
      description: Create-folder request body.
    FolderDeleteResponse:
      properties:
        folder:
          $ref: '#/components/schemas/FolderResponse'
          description: The soft-deleted folder.
        orphanedCount:
          type: integer
          title: Orphanedcount
          description: Child items detached from the folder (``cascade=false``).
          default: 0
        deletedCount:
          type: integer
          title: Deletedcount
          description: Child items soft-deleted along with the folder (``cascade=true``).
          default: 0
      type: object
      required:
        - folder
      title: FolderDeleteResponse
      description: |-
        Result of a folder delete, reporting how children were handled.

        The counts let the frontend toast accurately ("3 assistants moved to
        root" vs "3 assistants deleted") without a second round-trip.
    FolderListResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
          description: Total folders returned in this response.
          default: 0
        folders:
          items:
            $ref: '#/components/schemas/FolderResponse'
          type: array
          title: Folders
          description: Folders for the requested resource type, newest first.
      type: object
      title: FolderListResponse
      description: |-
        List-folders response body.

        No pagination: a workspace realistically holds tens of folders, not
        thousands, so returning the full list keeps the UI simple. If that
        assumption ever breaks the list endpoint can add skip/limit without
        changing the envelope.
    FolderResourceType:
      type: string
      enum:
        - assistant
        - phone_number
        - tool
        - file
      title: FolderResourceType
      description: |-
        Resource families that can be organised into folders.

        Closed enum: adding a new value is the single entry point that
        opens the folders feature to a new resource. Router logic
        dispatches on the value (cascade delete, item-count aggregation),
        so any new type must also be wired into the router's resource
        model + permission maps.
    FolderResponse:
      properties:
        id:
          type: string
          title: Id
          description: Stable folder identifier.
        resourceType:
          $ref: '#/components/schemas/FolderResourceType'
          description: Resource family this folder groups.
        name:
          type: string
          title: Name
        itemCount:
          type: integer
          title: Itemcount
          description: >-
            Number of non-deleted items of ``resourceType`` currently assigned
            to this folder. Computed at request time — always reflects the live
            collection state.
          default: 0
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
      type: object
      required:
        - id
        - resourceType
        - name
      title: FolderResponse
      description: Folder response — includes the live item count for the UI badge.
    FolderUpdate:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: New label (1-100 characters, trimmed).
      type: object
      title: FolderUpdate
      description: Update-folder request body — every field optional (PATCH semantics).
    FooterComponent:
      properties:
        type:
          type: string
          const: FOOTER
          title: Type
        text:
          type: string
          maxLength: 60
          minLength: 1
          title: Text
      additionalProperties: false
      type: object
      required:
        - type
        - text
      title: FooterComponent
    Fruitly:
      properties:
        provider:
          type: string
          const: fruitly
          title: Provider
        voiceId:
          type: string
          enum:
            - '0'
            - '1'
            - '2'
            - '3'
            - '4'
            - '5'
            - '6'
          title: Voiceid
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        numSteps:
          anyOf:
            - type: integer
              maximum: 50
              minimum: 3
            - type: 'null'
          title: Numsteps
          description: Diffusion steps (3-50).
        embeddingScale:
          anyOf:
            - type: number
              maximum: 5
              minimum: 0
            - type: 'null'
          title: Embeddingscale
          description: Guidance scale.
        directMix:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Directmix
          description: Voice clone strength.
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Fruitly
      description: Fruitly TTS — German diffusion TTS. ``voiceId`` is a 0-6 index string.
    FunctionCallDescriptor:
      properties:
        name:
          type: string
          title: Name
          description: Function name (matches the tool's registered name).
        arguments:
          additionalProperties: true
          type: object
          title: Arguments
          description: Arguments object the LLM produced.
      type: object
      required:
        - name
        - arguments
      title: FunctionCallDescriptor
      description: Function name + arguments — OpenAI-compatible nested shape.
    FunctionDefinition-Input:
      properties:
        name:
          type: string
          title: Name
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
        parameters:
          $ref: '#/components/schemas/JsonSchema-Input'
          description: >-
            JSON Schema describing the function's arguments. Always an object at
            the root with a ``properties`` bag — each entry supports ``string``
            / ``number`` / ``integer`` / ``boolean`` / ``object`` / ``array``
            types and is recursive for nested objects and array items.
        strict:
          type: boolean
          title: Strict
          description: Enable OpenAI Structured Outputs (strict schema adherence).
          default: false
      additionalProperties: false
      type: object
      required:
        - name
      title: FunctionDefinition
      description: OpenAI-compatible function definition exposed to the LLM.
    FunctionDefinition-Output:
      properties:
        name:
          type: string
          title: Name
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
        parameters:
          $ref: '#/components/schemas/JsonSchema-Output'
          description: >-
            JSON Schema describing the function's arguments. Always an object at
            the root with a ``properties`` bag — each entry supports ``string``
            / ``number`` / ``integer`` / ``boolean`` / ``object`` / ``array``
            types and is recursive for nested objects and array items.
        strict:
          type: boolean
          title: Strict
          description: Enable OpenAI Structured Outputs (strict schema adherence).
          default: false
      additionalProperties: false
      type: object
      required:
        - name
      title: FunctionDefinition
      description: OpenAI-compatible function definition exposed to the LLM.
    FunctionTool-Input:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: function
          title: Type
          default: function
        function:
          $ref: '#/components/schemas/FunctionDefinition-Input'
        server:
          $ref: '#/components/schemas/ServerConfig'
        asyncMode:
          type: boolean
          title: Asyncmode
          description: >-
            Fire-and-forget mode. When ``true`` the webhook runs detached and
            the LLM gets an immediate ack.
          default: false
      additionalProperties: false
      type: object
      required:
        - function
        - server
      title: FunctionTool
      description: HTTP webhook tool — executes a POST to ``server.url`` when called.
    FunctionTool-Output:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: function
          title: Type
          default: function
        function:
          $ref: '#/components/schemas/FunctionDefinition-Output'
        server:
          $ref: '#/components/schemas/ServerConfig'
        asyncMode:
          type: boolean
          title: Asyncmode
          description: >-
            Fire-and-forget mode. When ``true`` the webhook runs detached and
            the LLM gets an immediate ack.
          default: false
      additionalProperties: false
      type: object
      required:
        - function
        - server
      title: FunctionTool
      description: HTTP webhook tool — executes a POST to ``server.url`` when called.
    FunctionToolCallItem:
      properties:
        id:
          type: string
          title: Id
          description: >-
            Stable invocation ID. When the agent runtime exposes the underlying
            SDK ``call_id`` it is forwarded verbatim so the value matches the
            ``toolCallId`` on the corresponding ``messages[]`` entries;
            otherwise a platform-generated id is used.
        type:
          type: string
          const: function
          title: Type
          default: function
        function:
          $ref: '#/components/schemas/FunctionCallDescriptor'
      type: object
      required:
        - id
        - function
      title: FunctionToolCallItem
      description: |-
        One invocation in a tool function webhook.

        Always uses ``type: "function"`` (the only tool type that emits a
        webhook). The ``function.{name, arguments}`` nesting matches the
        OpenAI tool-call shape.
    FunctionToolResponse:
      properties:
        id:
          type: string
          title: Id
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: Folder this tool belongs to, or ``null`` when unfiled.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        type:
          type: string
          const: function
          title: Type
          default: function
        function:
          $ref: '#/components/schemas/FunctionDefinition-Output'
        server:
          $ref: '#/components/schemas/ServerConfigInfo'
        asyncMode:
          type: boolean
          title: Asyncmode
          default: false
      type: object
      required:
        - id
        - function
        - server
      title: FunctionToolResponse
    FunctionToolUpdate:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Move this tool to a folder. Pass an explicit ``null`` to unassign
            and return the tool to the list root. A PATCH that only changes
            ``folderId`` is treated as an organisational move — ``updatedAt`` is
            preserved.
        messages:
          anyOf:
            - items:
                $ref: '#/components/schemas/ToolMessage'
              type: array
            - type: 'null'
          title: Messages
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
        type:
          type: string
          const: function
          title: Type
          default: function
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Input'
            - type: 'null'
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfig'
            - type: 'null'
        asyncMode:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Asyncmode
      additionalProperties: false
      type: object
      title: FunctionToolUpdate
    GdprRequestResponse:
      properties:
        id:
          type: string
          title: Id
        requestType:
          type: string
          title: Requesttype
        status:
          type: string
          title: Status
        createdAt:
          type: string
          format: date-time
          title: Createdat
        completedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Completedat
        exportSignedUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Exportsignedurl
        exportSignedUrlExpiresAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Exportsignedurlexpiresat
      additionalProperties: false
      type: object
      required:
        - id
        - requestType
        - status
        - createdAt
      title: GdprRequestResponse
    GeminiAutomaticActivityDetection:
      properties:
        startOfSpeechSensitivity:
          anyOf:
            - type: string
              enum:
                - HIGH
                - LOW
            - type: 'null'
          title: Startofspeechsensitivity
          description: >-
            How aggressively Gemini commits to 'user is speaking'. HIGH =
            trigger on small voice signals (more interrupt-eager — single words
            / short backchannels cut the agent). LOW = require stronger evidence
            before interrupting (fewer false interrupts, may miss very short
            utterances). When unset, Google's default applies.
        endOfSpeechSensitivity:
          anyOf:
            - type: string
              enum:
                - HIGH
                - LOW
            - type: 'null'
          title: Endofspeechsensitivity
          description: >-
            How aggressively Gemini commits to 'user has stopped'. HIGH = end
            the turn quickly on first silence (snappier). LOW = wait longer
            (tolerates user pauses mid-sentence). When unset, Google's default
            applies.
        prefixPaddingMs:
          anyOf:
            - type: integer
              maximum: 2000
              minimum: 1
            - type: 'null'
          title: Prefixpaddingms
          description: >-
            Milliseconds of detected voice required before start-of-speech is
            committed. Google's docs note that 0 may clip word starts, so the
            minimum is 1. Higher values reject very brief noise and breath. When
            unset, Google's default applies.
        silenceDurationMs:
          anyOf:
            - type: integer
              maximum: 2000
              minimum: 100
            - type: 'null'
          title: Silencedurationms
          description: >-
            Milliseconds of silence required before end-of-speech is committed.
            Google recommends 500-800 ms; server default is approximately 800
            ms. Below 100 ms causes fragmented audio; above 2000 ms is
            noticeably laggy. When unset, Google's default applies.
      additionalProperties: false
      type: object
      title: GeminiAutomaticActivityDetection
      description: Tune Gemini Live's server-side voice-activity detection.
    GeminiRealtimeInputConfig:
      properties:
        automaticActivityDetection:
          anyOf:
            - $ref: '#/components/schemas/GeminiAutomaticActivityDetection'
            - type: 'null'
      additionalProperties: false
      type: object
      title: GeminiRealtimeInputConfig
      description: Server-side audio-input behaviour for Gemini Live.
    GeminiTTS:
      properties:
        provider:
          type: string
          const: gemini-tts
          title: Provider
        voiceId:
          type: string
          enum:
            - Puck
            - Charon
            - Kore
            - Fenrir
            - Leda
            - Orus
            - Aoede
            - Zephyr
            - Callirrhoe
            - Autonoe
            - Enceladus
            - Iapetus
            - Umbriel
            - Algieba
            - Despina
            - Erinome
            - Algenib
            - Rasalgethi
            - Laomedeia
            - Achernar
            - Alnilam
            - Schedar
            - Gacrux
            - Pulcherrima
            - Achird
            - Zubenelgenubi
            - Vindemiatrix
            - Sadachbia
            - Sadaltager
            - Sulafat
          title: Voiceid
        model:
          anyOf:
            - type: string
              enum:
                - gemini-2.5-flash-preview-tts
                - gemini-2.5-pro-preview-tts
                - gemini-3.1-flash-tts-preview
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        stylePrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Styleprompt
          description: Natural-language style control.
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: GeminiTTS
      description: Gemini API TTS — 30 static voices, plain api-key auth.
    GeminiThinkingConfig:
      properties:
        includeThoughts:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Includethoughts
        thinkingBudget:
          anyOf:
            - type: integer
              maximum: 8192
              minimum: 0
            - type: 'null'
          title: Thinkingbudget
        thinkingLevel:
          anyOf:
            - type: string
              enum:
                - minimal
                - low
                - medium
                - high
            - type: 'null'
          title: Thinkinglevel
          description: >-
            Gemini 3.x native reasoning depth (minimal/low/medium/high). Gemini
            3.5 Flash defaults to medium. Mutually exclusive with
            thinkingBudget; preferred for gemini-3.x models.
      additionalProperties: false
      type: object
      title: GeminiThinkingConfig
      description: >-
        Gemini thinking/reasoning config — used by both pipeline and realtime
        Gemini.


        Two mutually-exclusive knobs (the API returns 400 if BOTH reach it):


        - ``thinkingLevel`` (``minimal`` / ``low`` / ``medium`` / ``high``) —
          the native Gemini 3.x control. Gemini 3.5 Flash defaults to ``medium``.
        - ``thinkingBudget`` (int tokens) — the legacy Gemini 2.5 control,
          deprecated on Gemini 3.x.

        The LLM factory sends exactly one: it prefers ``thinkingLevel`` on

        gemini-3.x models, uses ``thinkingBudget`` on gemini-2.5, and (for

        back-compat) translates a legacy ``thinkingBudget`` into a level when a

        gemini-3.x config carries no ``thinkingLevel``.
    Google:
      properties:
        provider:
          type: string
          const: google
          title: Provider
        model:
          type: string
          enum:
            - gemini-3.5-flash
            - gemini-3.5-flash-lite
            - gemini-3-flash-preview
            - gemini-3.1-flash-lite-preview
            - gemini-2.5-flash
            - gemini-2.5-pro
            - gemini-2.5-flash-lite
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        topK:
          anyOf:
            - type: integer
              maximum: 100
              minimum: 1
            - type: 'null'
          title: Topk
          description: >-
            Gemini-specific top-k sampling. Range 0–100 per Google's docs
            (default 40).
        safetyLevel:
          anyOf:
            - type: string
              enum:
                - default
                - permissive
                - strict
                - block_none
            - type: 'null'
          title: Safetylevel
        thinkingConfig:
          anyOf:
            - $ref: '#/components/schemas/GeminiThinkingConfig'
            - type: 'null'
        googleLocation:
          anyOf:
            - type: string
            - type: 'null'
          title: Googlelocation
          description: GCP region (Vertex AI mode only). Default us-central1.
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: Google
      description: |-
        Google Gemini LLM (pipeline mode).

        No `frequencyPenalty` / `presencePenalty` — Gemini API doesn't accept
        those. Per-Gemini extras: ``topK``, ``safetyLevel``, ``thinkingConfig``,
        ``googleLocation``.
    GoogleRealtimeConfig-Input:
      properties:
        provider:
          type: string
          const: google-realtime
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - gemini-2.5-flash-native-audio-preview-12-2025
                - gemini-3.1-flash-live-preview
                - gemini-live-2.5-flash-native-audio
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
              enum:
                - Puck
                - Charon
                - Kore
                - Fenrir
                - Leda
                - Orus
                - Aoede
                - Zephyr
                - Callirrhoe
                - Autonoe
                - Enceladus
                - Iapetus
                - Umbriel
                - Algieba
                - Despina
                - Erinome
                - Algenib
                - Rasalgethi
                - Laomedeia
                - Achernar
                - Alnilam
                - Schedar
                - Gacrux
                - Pulcherrima
                - Achird
                - Zubenelgenubi
                - Vindemiatrix
                - Sadachbia
                - Sadaltager
                - Sulafat
            - type: 'null'
          title: Voice
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        affectiveDialog:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Affectivedialog
          description: Adapt tone to user emotion (native-audio only).
        proactivity:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Proactivity
          description: Skip irrelevant inputs (native-audio only).
        thinkingConfig:
          anyOf:
            - $ref: '#/components/schemas/GeminiThinkingConfig'
            - type: 'null'
        contextWindowCompression:
          anyOf:
            - $ref: '#/components/schemas/ContextWindowCompression'
            - type: 'null'
        googleLocation:
          anyOf:
            - type: string
            - type: 'null'
          title: Googlelocation
          description: GCP region for Vertex mode.
        realtimeInputConfig:
          anyOf:
            - $ref: '#/components/schemas/GeminiRealtimeInputConfig'
            - type: 'null'
          description: >-
            Tune Gemini Live's server-side voice-activity detection (VAD). When
            unset, Google's internal defaults apply (no behaviour change vs.
            existing assistants). Useful for tuning interrupt sensitivity on
            telephony calls where the default may interrupt the agent on short
            backchannel sounds like 'Ja' or 'Mhm'.
        enableSessionResumption:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enablesessionresumption
          description: >-
            Enables Gemini Live session resumption: server emits a resumption
            token periodically; on disconnect the client can reconnect with the
            token within 2h to preserve conversation context. See
            https://ai.google.dev/gemini-api/docs/live-session
        toolResponseScheduling:
          anyOf:
            - type: string
              enum:
                - INTERRUPT
                - WHEN_IDLE
                - SILENT
            - type: 'null'
          title: Toolresponsescheduling
          description: >-
            How tool responses are surfaced: ``INTERRUPT`` cuts the current
            generation, ``WHEN_IDLE`` waits for natural pause, ``SILENT``
            updates context without speaking. Defaults to the plugin's behavior
            when unset (typically ``WHEN_IDLE``).
      additionalProperties: false
      type: object
      required:
        - provider
      title: GoogleRealtimeConfig
      description: Google Gemini Realtime (Live API) — end-to-end audio.
    GoogleRealtimeConfig-Output:
      properties:
        provider:
          type: string
          const: google-realtime
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - gemini-2.5-flash-native-audio-preview-12-2025
                - gemini-3.1-flash-live-preview
                - gemini-live-2.5-flash-native-audio
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
              enum:
                - Puck
                - Charon
                - Kore
                - Fenrir
                - Leda
                - Orus
                - Aoede
                - Zephyr
                - Callirrhoe
                - Autonoe
                - Enceladus
                - Iapetus
                - Umbriel
                - Algieba
                - Despina
                - Erinome
                - Algenib
                - Rasalgethi
                - Laomedeia
                - Achernar
                - Alnilam
                - Schedar
                - Gacrux
                - Pulcherrima
                - Achird
                - Zubenelgenubi
                - Vindemiatrix
                - Sadachbia
                - Sadaltager
                - Sulafat
            - type: 'null'
          title: Voice
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        affectiveDialog:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Affectivedialog
          description: Adapt tone to user emotion (native-audio only).
        proactivity:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Proactivity
          description: Skip irrelevant inputs (native-audio only).
        thinkingConfig:
          anyOf:
            - $ref: '#/components/schemas/GeminiThinkingConfig'
            - type: 'null'
        contextWindowCompression:
          anyOf:
            - $ref: '#/components/schemas/ContextWindowCompression'
            - type: 'null'
        googleLocation:
          anyOf:
            - type: string
            - type: 'null'
          title: Googlelocation
          description: GCP region for Vertex mode.
        realtimeInputConfig:
          anyOf:
            - $ref: '#/components/schemas/GeminiRealtimeInputConfig'
            - type: 'null'
          description: >-
            Tune Gemini Live's server-side voice-activity detection (VAD). When
            unset, Google's internal defaults apply (no behaviour change vs.
            existing assistants). Useful for tuning interrupt sensitivity on
            telephony calls where the default may interrupt the agent on short
            backchannel sounds like 'Ja' or 'Mhm'.
        enableSessionResumption:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enablesessionresumption
          description: >-
            Enables Gemini Live session resumption: server emits a resumption
            token periodically; on disconnect the client can reconnect with the
            token within 2h to preserve conversation context. See
            https://ai.google.dev/gemini-api/docs/live-session
        toolResponseScheduling:
          anyOf:
            - type: string
              enum:
                - INTERRUPT
                - WHEN_IDLE
                - SILENT
            - type: 'null'
          title: Toolresponsescheduling
          description: >-
            How tool responses are surfaced: ``INTERRUPT`` cuts the current
            generation, ``WHEN_IDLE`` waits for natural pause, ``SILENT``
            updates context without speaking. Defaults to the plugin's behavior
            when unset (typically ``WHEN_IDLE``).
      additionalProperties: false
      type: object
      required:
        - provider
      title: GoogleRealtimeConfig
      description: Google Gemini Realtime (Live API) — end-to-end audio.
    GoogleSTT:
      properties:
        provider:
          type: string
          const: google-stt
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - chirp_3
                - chirp_2
                - latest_long
                - latest_short
                - telephony
                - medical_conversation
                - medical_dictation
                - chirp
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        multipleLanguages:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Multiplelanguages
        detectLanguage:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Detectlanguage
        punctuate:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Punctuate
        enableSpokenPunctuation:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enablespokenpunctuation
        profanityFilter:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Profanityfilter
        enableWordConfidence:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enablewordconfidence
        minConfidenceThreshold:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Minconfidencethreshold
        keywords:
          anyOf:
            - items: {}
              type: array
            - type: 'null'
          title: Keywords
        endpointingSensitivity:
          anyOf:
            - type: string
              enum:
                - ENDPOINTING_SENSITIVITY_STANDARD
                - ENDPOINTING_SENSITIVITY_SHORT
                - ENDPOINTING_SENSITIVITY_SUPERSHORT
            - type: 'null'
          title: Endpointingsensitivity
        speechStartTimeout:
          anyOf:
            - type: number
              maximum: 300
              minimum: 0
            - type: 'null'
          title: Speechstarttimeout
        speechEndTimeout:
          anyOf:
            - type: number
              maximum: 300
              minimum: 0
            - type: 'null'
          title: Speechendtimeout
        googleLocation:
          anyOf:
            - type: string
            - type: 'null'
          title: Googlelocation
        sampleRate:
          anyOf:
            - type: integer
              maximum: 48000
              minimum: 8000
            - type: 'null'
          title: Samplerate
      additionalProperties: false
      type: object
      required:
        - provider
      title: GoogleSTT
      description: Google Cloud STT — service-account auth, multilingual.
    GoogleTTS:
      properties:
        provider:
          type: string
          const: google-tts
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Google Cloud TTS voice name (e.g. `en-US-Chirp3-HD-Aoede`). See
            https://cloud.google.com/text-to-speech/docs/list-voices-and-types
            for the upstream catalog.
        model:
          anyOf:
            - type: string
              enum:
                - chirp_3
                - gemini-3.1-flash-tts-preview
                - gemini-2.5-flash-tts
                - gemini-2.5-pro-tts
                - gemini-2.5-flash-lite-preview-tts
                - chirp_2
                - Studio
                - Neural2
                - WaveNet
                - Polyglot
                - Standard
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        speakingRate:
          anyOf:
            - type: number
              maximum: 4
              minimum: 0.25
            - type: 'null'
          title: Speakingrate
        pitch:
          anyOf:
            - type: number
              maximum: 20
              minimum: -20
            - type: 'null'
          title: Pitch
        volume:
          anyOf:
            - type: number
              maximum: 16
              minimum: -96
            - type: 'null'
          title: Volume
        sampleRate:
          anyOf:
            - type: integer
              maximum: 48000
              minimum: 8000
            - type: 'null'
          title: Samplerate
        audioEncoding:
          anyOf:
            - type: string
              enum:
                - PCM
                - LINEAR16
                - OGG_OPUS
                - MP3
            - type: 'null'
          title: Audioencoding
        effectsProfileId:
          anyOf:
            - type: string
            - type: 'null'
          title: Effectsprofileid
        stylePrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Styleprompt
        voiceCloneKey:
          anyOf:
            - type: string
            - type: 'null'
          title: Voiceclonekey
        googleLocation:
          anyOf:
            - type: string
            - type: 'null'
          title: Googlelocation
        enableSsml:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enablessml
        useMarkup:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Usemarkup
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: GoogleTTS
      description: Google Cloud TTS — service-account auth, dynamic voice catalog.
    GradiumSTT:
      properties:
        provider:
          type: string
          const: gradium-stt
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
      additionalProperties: false
      type: object
      required:
        - provider
      title: GradiumSTT
      description: Gradium STT variant — EU-based, 5 languages.
    GradiumTTS:
      properties:
        provider:
          type: string
          const: gradium-tts
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Gradium voice id. See https://docs.gradium.ai for the upstream voice
            catalog.
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: GradiumTTS
      description: Gradium TTS — EU-based streaming TTS with voice cloning.
    Groq:
      properties:
        provider:
          type: string
          const: groq
          title: Provider
        model:
          type: string
          enum:
            - openai/gpt-oss-120b
            - openai/gpt-oss-20b
            - llama-3.3-70b-versatile
            - llama-3.1-8b-instant
            - meta-llama/llama-4-scout-17b-16e-instruct
            - qwen/qwen3-32b
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 131072
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: Groq
      description: |-
        Groq LPU-accelerated open-weight LLM (pipeline mode).

        ``frequency_penalty`` / ``presence_penalty`` are intentionally absent
        — the Groq plugin's ``LLM`` constructor rejects them and won't
        forward them via ``extra_body``.
    GroqSTT:
      properties:
        provider:
          type: string
          const: groq-stt
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - whisper-large-v3-turbo
                - whisper-large-v3
                - distil-whisper-large-v3-en
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        prompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Prompt
        detectLanguage:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Detectlanguage
      additionalProperties: false
      type: object
      required:
        - provider
      title: GroqSTT
      description: Groq STT (LPU-accelerated Whisper) variant.
    GroqTTS:
      properties:
        provider:
          type: string
          const: groq-tts
          title: Provider
        voiceId:
          type: string
          enum:
            - autumn
            - diana
            - hannah
            - austin
            - daniel
            - troy
            - fahad
            - sultan
            - lulwa
            - noura
          title: Voiceid
        model:
          anyOf:
            - type: string
              enum:
                - canopylabs/orpheus-v1-english
                - canopylabs/orpheus-arabic-saudi
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: GroqTTS
      description: >-
        Groq Orpheus TTS — voice locks to one of two models. Factory
        auto-derives model.
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    HeaderComponent:
      properties:
        type:
          type: string
          const: HEADER
          title: Type
        format:
          type: string
          const: TEXT
          title: Format
          default: TEXT
        text:
          type: string
          maxLength: 60
          minLength: 1
          title: Text
        example:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Example
      additionalProperties: false
      type: object
      required:
        - type
        - text
      title: HeaderComponent
    HomeCard:
      properties:
        id:
          type: string
          title: Id
        type:
          type: string
          enum:
            - whatsapp
            - telegram
            - email
            - phone
            - sms
            - custom
          title: Type
        label:
          type: string
          maxLength: 64
          minLength: 1
          title: Label
        description:
          anyOf:
            - type: string
              maxLength: 128
            - type: 'null'
          title: Description
        iconUrl:
          anyOf:
            - type: string
              maxLength: 2048
            - type: 'null'
          title: Iconurl
        url:
          type: string
          maxLength: 2048
          minLength: 1
          title: Url
        enabled:
          type: boolean
          title: Enabled
          default: true
      additionalProperties: false
      type: object
      required:
        - type
        - label
        - url
      title: HomeCard
      description: |-
        One tappable channel card on the widget's home screen.

        Renders as a row card (``.agf-row``) inside the "Contact us" section
        on the Home space. Deep-links out to an external channel — WhatsApp
        Web, Telegram, mailto, tel, sms, or an admin-defined custom URL.

        Pre-configured types (``whatsapp``/``telegram``/``email``/``phone``/
        ``sms``) render with a built-in SVG icon from the widget bundle —
        ``iconUrl`` is ignored on those. ``custom`` cards REQUIRE a caller-
        supplied HTTPS ``iconUrl`` because the bundle has no icon for them.
    Hume:
      properties:
        provider:
          type: string
          const: hume
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Hume voice name. Use any voice from your Hume voice library — see
            https://dev.hume.ai/docs/text-to-speech-tts/overview for the
            upstream catalog.
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        instructions:
          anyOf:
            - type: string
            - type: 'null'
          title: Instructions
          description: Acting instructions / tone description.
        speed:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.5
            - type: 'null'
          title: Speed
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Hume
      description: >-
        Hume AI TTS variant. Voices fetched dynamically. ``instructions`` is the
        acting prompt.
    IdleConfigSchema:
      properties:
        enabled:
          type: boolean
          title: Enabled
          default: false
        timeoutSeconds:
          type: number
          maximum: 120
          minimum: 1
          title: Timeoutseconds
          default: 10
        maxPromptCount:
          type: integer
          maximum: 10
          minimum: 1
          title: Maxpromptcount
          default: 3
        messages:
          items:
            type: string
          type: array
          title: Messages
      type: object
      title: IdleConfigSchema
    InboundServerConfig:
      properties:
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
          description: Webhook URL.
        secret:
          anyOf:
            - type: string
            - type: 'null'
          title: Secret
          description: >-
            HMAC-SHA256 signing secret used to compute the
            ``X-Webhook-Signature`` header.
        timeoutSeconds:
          type: integer
          maximum: 7
          minimum: 1
          title: Timeoutseconds
          description: Webhook timeout. Bounded by the 7.5s telephony response budget.
          default: 7
      additionalProperties: false
      type: object
      title: InboundServerConfig
      description: >-
        Inbound ``assistant-request`` webhook.


        Called BEFORE the assistant loads for an inbound call, so the caller can

        pick / override the assistant dynamically. Must respond within

        ``timeoutSeconds`` (hard ceiling of 7.5s dictated by the telephony
        stack).


        Request body::

            {
              "message": {
                "type": "assistant-request",
                "call": { ... },
                "phoneNumber": { "id": "...", "number": "...", "name": "..." },
                "customer": { "number": "..." }
              }
            }

        Expected response (one of):


        - ``{ "assistantId": "..." }``                              use existing
        assistant

        - ``{ "assistant": { ... } }``                              inline
        ephemeral assistant

        - ``{ "assistantId": "...", "overrides": { ... } }``        assistant +
        per-call overrides

        - ``{ "destination": { "type": "number", "number": "..." } }``  transfer
        immediately

        - ``{ "error": "message" }``                                speak error
        and end call
    InboundServerConfigInfo:
      properties:
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
        secret:
          anyOf:
            - type: string
            - type: 'null'
          title: Secret
        timeoutSeconds:
          type: integer
          title: Timeoutseconds
          default: 7
      additionalProperties: false
      type: object
      title: InboundServerConfigInfo
      description: >-
        Inbound webhook config — response shape.


        Deliberately NOT validated: see
        ``InboundServerConfig._no_internal_target``.
    InteractivePayload:
      properties:
        type:
          type: string
          title: Type
          examples:
            - button
        header:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Header
        body:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Body
          examples:
            - text: Confirm your appointment?
        footer:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Footer
        action:
          additionalProperties: true
          type: object
          title: Action
          examples:
            - buttons:
                - reply:
                    id: 'yes'
                    title: 'Yes'
                  type: reply
                - reply:
                    id: 'no'
                    title: 'No'
                  type: reply
      additionalProperties: true
      type: object
      required:
        - type
        - action
      title: InteractivePayload
      description: |-
        Interactive message — WhatsApp buttons/list, Telegram inline keyboard.

        Schema mirrors WhatsApp Cloud API verbatim; provider adapters translate.
    InterruptionConfigSchema:
      properties:
        numWords:
          type: integer
          maximum: 10
          minimum: 0
          title: Numwords
          default: 5
        voiceSeconds:
          type: number
          maximum: 2
          minimum: 0
          title: Voiceseconds
          default: 0.5
        backoffSeconds:
          type: number
          maximum: 5
          minimum: 0
          title: Backoffseconds
          default: 1
      type: object
      title: InterruptionConfigSchema
    InterruptionUsageRecord:
      properties:
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        type:
          type: string
          const: interruption_usage
          title: Type
          default: interruption_usage
        count:
          anyOf:
            - type: integer
            - type: 'null'
          title: Count
      additionalProperties: true
      type: object
      title: InterruptionUsageRecord
      description: >-
        ``interruption_usage`` entries emitted by the agent — counted
        separately.
    InvoiceJsonBilledTo:
      properties:
        snapshot:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Snapshot
          description: >-
            Name / address / tax ids as they were WHEN THE INVOICE WAS ISSUED.
            Null on invoices that pre-date snapshotting.
        ownerType:
          type: string
          enum:
            - tenant
            - organization
          title: Ownertype
        ownerId:
          type: string
          title: Ownerid
      type: object
      required:
        - ownerType
        - ownerId
      title: InvoiceJsonBilledTo
    InvoiceJsonDates:
      properties:
        issuedAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Issuedat
        dueAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Dueat
        paidAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Paidat
        voidedAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Voidedat
        refundedAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Refundedat
        periodStart:
          anyOf:
            - type: string
            - type: 'null'
          title: Periodstart
        periodEnd:
          anyOf:
            - type: string
            - type: 'null'
          title: Periodend
      type: object
      title: InvoiceJsonDates
      description: ISO-8601, or null. Never an empty string.
    InvoiceJsonIssuedBy:
      properties:
        snapshot:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Snapshot
          description: >-
            Name / address / tax ids as they were WHEN THE INVOICE WAS ISSUED.
            Null on invoices that pre-date snapshotting.
        issuerType:
          anyOf:
            - type: string
              enum:
                - platform
                - tenant
            - type: 'null'
          title: Issuertype
        issuerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Issuerid
        platformId:
          type: string
          title: Platformid
      type: object
      required:
        - platformId
      title: InvoiceJsonIssuedBy
    InvoiceJsonLineItem:
      properties:
        description:
          type: string
          title: Description
        quantity:
          type: integer
          title: Quantity
        unitAmount:
          $ref: '#/components/schemas/InvoiceMoney'
        amount:
          $ref: '#/components/schemas/InvoiceMoney'
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
      type: object
      required:
        - description
        - quantity
        - unitAmount
        - amount
      title: InvoiceJsonLineItem
    InvoiceJsonPackage:
      properties:
        schemaVersion:
          type: string
          title: Schemaversion
          description: Bumped only on a BREAKING change.
        id:
          type: string
          title: Id
        invoiceNumber:
          type: string
          title: Invoicenumber
        status:
          type: string
          enum:
            - draft
            - open
            - paid
            - uncollectible
            - void
            - refunded
          title: Status
        mode:
          type: string
          enum:
            - stripe
            - manual_contract
          title: Mode
        billedTo:
          $ref: '#/components/schemas/InvoiceJsonBilledTo'
        issuedBy:
          $ref: '#/components/schemas/InvoiceJsonIssuedBy'
        totals:
          $ref: '#/components/schemas/InvoiceJsonTotals'
        lineItems:
          items:
            $ref: '#/components/schemas/InvoiceJsonLineItem'
          type: array
          title: Lineitems
        dates:
          $ref: '#/components/schemas/InvoiceJsonDates'
        period:
          $ref: '#/components/schemas/InvoiceJsonPeriod'
        references:
          $ref: '#/components/schemas/InvoiceJsonReferences'
        refundReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Refundreason
      type: object
      required:
        - schemaVersion
        - id
        - invoiceNumber
        - status
        - mode
        - billedTo
        - issuedBy
        - totals
        - dates
        - period
        - references
      title: InvoiceJsonPackage
      description: |-
        Self-contained, machine-readable representation of one invoice.

        Everything the PDF renders, including the frozen party snapshots, so
        an invoice pulled years later reconciles against the parties as they
        were at issue time. Built purely from the stored document — no Stripe
        round-trip, no live re-resolution — so re-downloading never changes
        the result unless the invoice itself changed.

        Additive-only: key off ``schemaVersion`` and ignore unknown fields.
    InvoiceJsonPeriod:
      properties:
        months:
          type: integer
          title: Months
        commitmentTermNumber:
          type: integer
          title: Commitmenttermnumber
      type: object
      required:
        - months
        - commitmentTermNumber
      title: InvoiceJsonPeriod
    InvoiceJsonReferences:
      properties:
        poNumber:
          anyOf:
            - type: string
            - type: 'null'
          title: Ponumber
        stripeInvoiceId:
          anyOf:
            - type: string
            - type: 'null'
          title: Stripeinvoiceid
        stripeHostedInvoiceUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Stripehostedinvoiceurl
        stripeInvoicePdfUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Stripeinvoicepdfurl
      type: object
      title: InvoiceJsonReferences
    InvoiceJsonTotals:
      properties:
        subtotalExclTax:
          $ref: '#/components/schemas/InvoiceMoney'
        tax:
          $ref: '#/components/schemas/InvoiceMoney'
        total:
          $ref: '#/components/schemas/InvoiceMoney'
        paid:
          $ref: '#/components/schemas/InvoiceMoney'
        refunded:
          $ref: '#/components/schemas/InvoiceMoney'
        due:
          $ref: '#/components/schemas/InvoiceMoney'
          description: >-
            Still collectible (total − paid, floored at zero). Stated explicitly
            so two integrations cannot disagree about it.
        reverseChargeApplied:
          type: boolean
          title: Reversechargeapplied
      type: object
      required:
        - subtotalExclTax
        - tax
        - total
        - paid
        - refunded
        - due
        - reverseChargeApplied
      title: InvoiceJsonTotals
    InvoiceLineItemResponse:
      properties:
        description:
          type: string
          title: Description
        quantity:
          type: integer
          title: Quantity
        unitAmountCents:
          type: integer
          title: Unitamountcents
        amountCents:
          type: integer
          title: Amountcents
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
      type: object
      required:
        - description
        - quantity
        - unitAmountCents
        - amountCents
      title: InvoiceLineItemResponse
      description: One line on an invoice.
    InvoiceMoney:
      properties:
        amountCents:
          type: integer
          title: Amountcents
        amount:
          type: string
          title: Amount
          description: Decimal string, e.g. "50.00".
        currency:
          type: string
          title: Currency
      type: object
      required:
        - amountCents
        - amount
        - currency
      title: InvoiceMoney
      description: |-
        An amount, in both machine and human form.

        ``amountCents`` is authoritative. ``amount`` is a convenience decimal
        string — provided so consumers don't divide by 100 in floating point
        and introduce a rounding error into their ledger.
    Inworld:
      properties:
        provider:
          type: string
          const: inworld
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Inworld voice name (e.g. ``Ashley``, ``Dennis``, ``Alex``,
            ``Clive``). See
            https://docs.inworld.ai/api-reference/ttsAPI/texttospeech/list-voices
            for the full multilingual voice catalog. Custom-cloned voice ids
            from the Inworld portal also work.
        model:
          anyOf:
            - type: string
              enum:
                - inworld-tts-2
                - inworld-tts-1.5-max
                - inworld-tts-1.5-mini
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        speakingRate:
          anyOf:
            - type: number
              maximum: 1.5
              minimum: 0.5
            - type: 'null'
          title: Speakingrate
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
          description: >-
            Sampling temperature for v1.5 models (0.6-1.1 recommended).
            **Ignored by ``inworld-tts-2``** — that model uses inline steering
            (``[whisper]``/``[laugh]``) instead. Documented at
            https://docs.inworld.ai/tts/tts-models.
        textNormalization:
          anyOf:
            - type: string
              enum:
                - 'ON'
                - 'OFF'
            - type: 'null'
          title: Textnormalization
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Inworld
      description: |-
        Inworld TTS variant. Voices fetched dynamically.

        ``inworld-tts-2`` accepts inline steering tokens directly in the
        text the agent speaks — e.g. ``"[whisper warmly] glad you called"``
        or ``"[laugh] that's amazing"``. No schema field is needed for
        those: they flow through the existing ``systemPrompt`` /
        ``firstMessage`` text. Supported tokens include ``[laugh]``,
        ``[breathe]``, ``[clear_throat]``, ``[sigh]``, ``[cough]``,
        ``[yawn]``, plus free-form style descriptions like
        ``[say excitedly]`` / ``[whisper in a hushed style]``.

        The ``deliveryMode`` knob (STABLE / BALANCED / CREATIVE) on
        ``inworld-tts-2`` is intentionally NOT exposed here — the upstream
        LiveKit plugin doesn't yet plumb ``delivery_mode`` into the
        request body and Inworld's default ``BALANCED`` is the recommended
        setting for conversational use. When upstream adds the kwarg
        (tracked in livekit/agents repo) this variant gains the field
        in one diff.
    JsonSchema-Input:
      properties:
        type:
          anyOf:
            - type: string
              const: object
            - type: 'null'
          title: Type
          description: Always ``object`` at the top level.
          default: object
        properties:
          anyOf:
            - additionalProperties:
                $ref: '#/components/schemas/JsonSchemaProperty-Input'
              type: object
            - type: 'null'
          title: Properties
          description: Map of field name → property descriptor.
        required:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Required
          description: Top-level property names the model must produce.
      additionalProperties: true
      type: object
      title: JsonSchema
      description: |-
        Top-level JSON Schema — always an object with a ``properties`` bag.

        Used wherever an LLM-facing surface accepts a JSON Schema spec
        (function-tool parameters, structured-data extraction, etc.).
        Each entry under ``properties`` is a ``JsonSchemaProperty``
        (recursive). ``extra="allow"`` keeps the door open for arbitrary
        JSON Schema metadata callers may include.
    JsonSchema-Output:
      properties:
        type:
          anyOf:
            - type: string
              const: object
            - type: 'null'
          title: Type
          description: Always ``object`` at the top level.
          default: object
        properties:
          anyOf:
            - additionalProperties:
                $ref: '#/components/schemas/JsonSchemaProperty-Output'
              type: object
            - type: 'null'
          title: Properties
          description: Map of field name → property descriptor.
        required:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Required
          description: Top-level property names the model must produce.
      additionalProperties: true
      type: object
      title: JsonSchema
      description: |-
        Top-level JSON Schema — always an object with a ``properties`` bag.

        Used wherever an LLM-facing surface accepts a JSON Schema spec
        (function-tool parameters, structured-data extraction, etc.).
        Each entry under ``properties`` is a ``JsonSchemaProperty``
        (recursive). ``extra="allow"`` keeps the door open for arbitrary
        JSON Schema metadata callers may include.
    JsonSchemaProperty-Input:
      properties:
        type:
          type: string
          enum:
            - string
            - number
            - integer
            - boolean
            - object
            - array
          title: Type
          default: string
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
        enum:
          anyOf:
            - items: {}
              type: array
            - type: 'null'
          title: Enum
          description: >-
            Fixed set of allowed values. Use with ``type=string`` for
            categorical fields.
        items:
          anyOf:
            - $ref: '#/components/schemas/JsonSchemaProperty-Input'
            - type: 'null'
          description: >-
            For ``type=array`` — describes what each item in the array looks
            like.
        properties:
          anyOf:
            - additionalProperties:
                $ref: '#/components/schemas/JsonSchemaProperty-Input'
              type: object
            - type: 'null'
          title: Properties
          description: For ``type=object`` — nested fields keyed by property name.
        required:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Required
          description: For ``type=object`` — names of properties the model must produce.
      additionalProperties: true
      type: object
      title: JsonSchemaProperty
      description: |-
        A single field in a JSON Schema spec.

        Subset of the JSON Schema spec our LLM-facing surfaces handle:

          - ``type`` — one of ``string`` / ``number`` / ``integer`` /
            ``boolean`` / ``object`` / ``array``
          - ``description`` — human-readable hint shown in the prompt
          - ``enum`` — fixed set of allowed values (typically used with
            strings to constrain to a categorical set)
          - ``items`` — for ``type=array``, describes what each entry looks
            like (recursive — can be another object / array / scalar)
          - ``properties`` + ``required`` — for ``type=object``, describes
            nested fields (recursive — full JSON Schema tree)

        ``extra="allow"`` so additional JSON Schema keywords (``$ref``,
        ``format``, ``pattern``, ``allOf`` etc.) round-trip cleanly even
        though our engines may ignore them.
    JsonSchemaProperty-Output:
      properties:
        type:
          type: string
          enum:
            - string
            - number
            - integer
            - boolean
            - object
            - array
          title: Type
          default: string
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
        enum:
          anyOf:
            - items: {}
              type: array
            - type: 'null'
          title: Enum
          description: >-
            Fixed set of allowed values. Use with ``type=string`` for
            categorical fields.
        items:
          anyOf:
            - $ref: '#/components/schemas/JsonSchemaProperty-Output'
            - type: 'null'
          description: >-
            For ``type=array`` — describes what each item in the array looks
            like.
        properties:
          anyOf:
            - additionalProperties:
                $ref: '#/components/schemas/JsonSchemaProperty-Output'
              type: object
            - type: 'null'
          title: Properties
          description: For ``type=object`` — nested fields keyed by property name.
        required:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Required
          description: For ``type=object`` — names of properties the model must produce.
      additionalProperties: true
      type: object
      title: JsonSchemaProperty
      description: |-
        A single field in a JSON Schema spec.

        Subset of the JSON Schema spec our LLM-facing surfaces handle:

          - ``type`` — one of ``string`` / ``number`` / ``integer`` /
            ``boolean`` / ``object`` / ``array``
          - ``description`` — human-readable hint shown in the prompt
          - ``enum`` — fixed set of allowed values (typically used with
            strings to constrain to a categorical set)
          - ``items`` — for ``type=array``, describes what each entry looks
            like (recursive — can be another object / array / scalar)
          - ``properties`` + ``required`` — for ``type=object``, describes
            nested fields (recursive — full JSON Schema tree)

        ``extra="allow"`` so additional JSON Schema keywords (``$ref``,
        ``format``, ``pattern``, ``allOf`` etc.) round-trip cleanly even
        though our engines may ignore them.
    KBStatsResponse:
      properties:
        totalSources:
          type: integer
          title: Totalsources
          default: 0
        readySources:
          type: integer
          title: Readysources
          default: 0
        failedSources:
          type: integer
          title: Failedsources
          default: 0
        totalChunks:
          type: integer
          title: Totalchunks
          default: 0
        totalTokens:
          type: integer
          title: Totaltokens
          default: 0
      type: object
      title: KBStatsResponse
      description: Knowledge base statistics.
    KbRetrievalSummaryItem:
      properties:
        query:
          type: string
          title: Query
          description: Query text the agent searched on.
        knowledgeBaseIds:
          items:
            type: string
          type: array
          title: Knowledgebaseids
          description: >-
            Distinct KB ids whose chunks contributed to this retrieval. Empty
            when the retrieval found no matches.
        sourceCount:
          type: integer
          title: Sourcecount
          description: Number of sources returned.
          default: 0
        topScore:
          anyOf:
            - type: number
            - type: 'null'
          title: Topscore
          description: Best similarity score across returned sources.
        at:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: At
          description: Retrieval time (UTC).
      additionalProperties: true
      type: object
      required:
        - query
      title: KbRetrievalSummaryItem
      description: |-
        Denormalized knowledge-base retrieval record.

        A single retrieval fans out across every linked knowledge base in
        parallel, so ``knowledgeBaseIds`` is plural — it lists the distinct
        KB ids that contributed at least one chunk to the result.
    KbRetrievalSummaryItemSchema:
      properties:
        query:
          type: string
          title: Query
        knowledgeBaseIds:
          items:
            type: string
          type: array
          title: Knowledgebaseids
        sourceCount:
          type: integer
          title: Sourcecount
          default: 0
        topScore:
          anyOf:
            - type: number
            - type: 'null'
          title: Topscore
        at:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: At
      type: object
      required:
        - query
      title: KbRetrievalSummaryItemSchema
      description: |-
        One knowledge-base retrieval, denormalized from the timeline.

        ``knowledgeBaseIds`` is plural because a single retrieval fans out
        across every linked KB in parallel; the list holds the distinct
        KB ids that contributed at least one chunk to the result.
    KnowledgeBaseCreate:
      properties:
        name:
          type: string
          maxLength: 200
          minLength: 1
          title: Name
          description: Display name for the knowledge base.
        description:
          anyOf:
            - type: string
              maxLength: 2000
            - type: 'null'
          title: Description
          description: Free-form description.
        chunkingConfig:
          anyOf:
            - $ref: '#/components/schemas/ChunkingConfigSchema'
            - type: 'null'
        searchConfig:
          anyOf:
            - $ref: '#/components/schemas/SearchConfigSchema'
            - type: 'null'
      additionalProperties: false
      type: object
      required:
        - name
      title: KnowledgeBaseCreate
      description: Create knowledge base request.
    KnowledgeBaseResponse:
      properties:
        id:
          type: string
          title: Id
        name:
          type: string
          title: Name
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
        sources:
          items:
            $ref: '#/components/schemas/SourceResponse'
          type: array
          title: Sources
        chunkingConfig:
          anyOf:
            - $ref: '#/components/schemas/ChunkingConfigInfo'
            - type: 'null'
        embeddingConfig:
          anyOf:
            - $ref: '#/components/schemas/EmbeddingConfigInfo'
            - type: 'null'
        searchConfig:
          anyOf:
            - $ref: '#/components/schemas/SearchConfigInfo'
            - type: 'null'
        stats:
          anyOf:
            - $ref: '#/components/schemas/KBStatsResponse'
            - type: 'null'
        status:
          type: string
          title: Status
          default: ready
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
      additionalProperties: false
      type: object
      required:
        - id
        - name
      title: KnowledgeBaseResponse
      description: Knowledge base response.
    KnowledgeBaseUpdate:
      properties:
        name:
          anyOf:
            - type: string
              maxLength: 200
              minLength: 1
            - type: 'null'
          title: Name
        description:
          anyOf:
            - type: string
              maxLength: 2000
            - type: 'null'
          title: Description
        chunkingConfig:
          anyOf:
            - $ref: '#/components/schemas/ChunkingConfigSchema'
            - type: 'null'
        searchConfig:
          anyOf:
            - $ref: '#/components/schemas/SearchConfigSchema'
            - type: 'null'
      additionalProperties: false
      type: object
      title: KnowledgeBaseUpdate
      description: Update knowledge base request — every field optional.
    ListenTokenResponse:
      properties:
        token:
          type: string
          title: Token
          description: WebRTC access token. Subscribe-only — cannot publish or send data.
        url:
          type: string
          title: Url
          description: WebSocket URL to connect to.
        roomName:
          type: string
          title: Roomname
          description: Live-call room name to join.
        identity:
          type: string
          title: Identity
          description: Participant identity baked into the token.
        expiresInSeconds:
          type: integer
          title: Expiresinseconds
          description: Token TTL in seconds.
      additionalProperties: false
      type: object
      required:
        - token
        - url
        - roomName
        - identity
        - expiresInSeconds
      title: ListenTokenResponse
      description: Listen-only access token for live-call monitoring.
    LiveAgentUpdateRequest:
      properties:
        instructions:
          anyOf:
            - type: string
              maxLength: 100000
            - type: 'null'
          title: Instructions
          description: >-
            New system instructions. With ``mode=replace`` (default) these
            become the assistant's instructions for the rest of the call; with
            ``mode=append`` they are layered on top of the configured prompt,
            which stays in force.
        mode:
          type: string
          enum:
            - replace
            - append
          title: Mode
          description: How ``instructions`` combine with the assistant's configured prompt.
          default: replace
        tools:
          anyOf:
            - items: {}
              type: array
              maxItems: 100
            - type: 'null'
          title: Tools
          description: >-
            Replaces the active tool set wholesale. Entries are either saved
            tool IDs or inline tool definitions — the same mixed shape an
            assistant's ``tools`` array accepts. Omit the field to leave tools
            untouched; send ``[]`` to run with none. Knowledge-base search tools
            are always retained, since they follow the assistant's linked
            knowledge bases rather than this list.
      additionalProperties: false
      type: object
      title: LiveAgentUpdateRequest
      description: |-
        Replace the running agent's instructions and/or tools mid-call.

        Applies to the call already in progress — same caller leg, same
        conversation, no transfer and no handoff. The change takes effect from
        the assistant's next turn.

        At least one of ``instructions`` / ``tools`` must be supplied.
    LiveAgentUpdateResponse:
      properties:
        applied:
          type: boolean
          title: Applied
          description: >-
            ``true`` when the running agent confirmed the change. ``false`` with
            HTTP 202 means it was dispatched but not confirmed in time.
        reconnected:
          type: boolean
          title: Reconnected
          description: >-
            Whether applying the change required re-establishing the upstream
            model connection (~1s). The caller's audio leg is never affected.
        mechanism:
          anyOf:
            - type: string
            - type: 'null'
          title: Mechanism
          description: 'How it was applied: ``pipeline``, ``in_place`` or ``reconnect``.'
        activeToolNames:
          items:
            type: string
          type: array
          title: Activetoolnames
          description: Tools available to the assistant after the update.
        requestId:
          type: string
          title: Requestid
          description: Correlation id, also recorded on the call timeline.
      additionalProperties: false
      type: object
      required:
        - applied
        - reconnected
        - requestId
      title: LiveAgentUpdateResponse
      description: Outcome of a mid-call agent update.
    LiveEscalateConfig:
      properties:
        briefingPrompt:
          type: string
          title: Briefingprompt
          default: >-
            You are briefing a supervisor about an ongoing live call. Provide a
            concise summary of the conversation so far and the reason for
            escalation. Ask if the supervisor is ready to take over the call.
        agentBehavior:
          type: string
          enum:
            - continue-naturally
            - steer-conversation
          title: Agentbehavior
          default: continue-naturally
        agentInstructions:
          type: string
          title: Agentinstructions
          description: >-
            Extra instructions injected into the main agent's context while
            escalation runs. Only used when
            ``agentBehavior='steer-conversation'``.
          default: ''
        transitionMessage:
          type: string
          title: Transitionmessage
          description: >-
            Message the agent says to the customer before handing off. Empty ⇒
            LLM generates one.
          default: ''
        speakFirstSummary:
          type: boolean
          title: Speakfirstsummary
          default: true
        dialTimeoutSeconds:
          type: integer
          title: Dialtimeoutseconds
          default: 60
        confirmTimeoutSeconds:
          type: integer
          title: Confirmtimeoutseconds
          default: 120
        onDecline:
          type: string
          enum:
            - continue
            - end-call
            - retry
          title: Ondecline
          default: continue
        declineMessage:
          type: string
          title: Declinemessage
          default: ''
        extension:
          type: string
          title: Extension
          description: DTMF extension to dial after connection.
          default: ''
        customHeaders:
          additionalProperties: true
          type: object
          title: Customheaders
          description: Custom SIP headers for the consultation leg.
        dtmfConfirmation:
          type: boolean
          title: Dtmfconfirmation
          description: >-
            Require the supervisor to confirm/decline by pressing a phone key
            (DTMF) instead of speaking. Off (default): the AI interprets the
            supervisor's spoken response.
          default: false
        dtmfAcceptKey:
          type: string
          title: Dtmfacceptkey
          description: >-
            Key the supervisor presses to accept and take over the call (0-9, *
            or #).
          default: '1'
        dtmfDeclineKey:
          type: string
          title: Dtmfdeclinekey
          description: >-
            Key the supervisor presses to decline the takeover (0-9, * or #).
            Empty string disables the decline key — the supervisor declines by
            simply hanging up.
          default: '2'
        dtmfRepeatKey:
          type: string
          title: Dtmfrepeatkey
          description: >-
            Key the supervisor presses to hear the briefing again (0-9, * or #).
            Empty string disables the repeat option.
          default: '0'
        connectionTone:
          type: boolean
          title: Connectiontone
          description: >-
            Play a short chime into the call the instant the supervisor is
            bridged to the customer, so both parties hear that the connection is
            live (avoids the silent-handoff gap). Set false to disable.
          default: true
      additionalProperties: false
      type: object
      title: LiveEscalateConfig
      description: |-
        Background escalation behavior.

        The AI keeps talking to the customer (no hold) while a supervisor is
        dialed and briefed on a consultation leg. On confirm, the supervisor
        replaces the agent in the customer's room.
    LiveEscalateTool-Input:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: liveEscalate
          title: Type
          default: liveEscalate
        destinations:
          items:
            oneOf:
              - $ref: '#/components/schemas/NumberDestination'
              - $ref: '#/components/schemas/SipDestination'
              - $ref: '#/components/schemas/AssistantDestination'
            discriminator:
              propertyName: type
              mapping:
                assistant: '#/components/schemas/AssistantDestination'
                number: '#/components/schemas/NumberDestination'
                sip: '#/components/schemas/SipDestination'
          type: array
          minItems: 1
          title: Destinations
          description: Specialists the LLM can escalate to. At least one is required.
        liveEscalateConfig:
          $ref: '#/components/schemas/LiveEscalateConfig'
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Input'
            - type: 'null'
          description: Optional custom description exposed to the LLM.
        aiHeaders:
          items:
            $ref: '#/components/schemas/AiHeader'
          type: array
          title: Aiheaders
          description: >-
            Headers whose VALUES the AI fills at call time from the
            conversation, then sends on the outbound escalation dial to the
            specialist — alongside ``liveEscalateConfig.customHeaders``.
      additionalProperties: false
      type: object
      required:
        - destinations
        - liveEscalateConfig
      title: LiveEscalateTool
      description: Seamless background escalation to a human specialist.
    LiveEscalateTool-Output:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: liveEscalate
          title: Type
          default: liveEscalate
        destinations:
          items:
            oneOf:
              - $ref: '#/components/schemas/NumberDestination'
              - $ref: '#/components/schemas/SipDestination'
              - $ref: '#/components/schemas/AssistantDestination'
            discriminator:
              propertyName: type
              mapping:
                assistant: '#/components/schemas/AssistantDestination'
                number: '#/components/schemas/NumberDestination'
                sip: '#/components/schemas/SipDestination'
          type: array
          minItems: 1
          title: Destinations
          description: Specialists the LLM can escalate to. At least one is required.
        liveEscalateConfig:
          $ref: '#/components/schemas/LiveEscalateConfig'
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Output'
            - type: 'null'
          description: Optional custom description exposed to the LLM.
        aiHeaders:
          items:
            $ref: '#/components/schemas/AiHeader'
          type: array
          title: Aiheaders
          description: >-
            Headers whose VALUES the AI fills at call time from the
            conversation, then sends on the outbound escalation dial to the
            specialist — alongside ``liveEscalateConfig.customHeaders``.
      additionalProperties: false
      type: object
      required:
        - destinations
        - liveEscalateConfig
      title: LiveEscalateTool
      description: Seamless background escalation to a human specialist.
    LiveEscalateToolResponse:
      properties:
        id:
          type: string
          title: Id
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: Folder this tool belongs to, or ``null`` when unfiled.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        type:
          type: string
          const: liveEscalate
          title: Type
          default: liveEscalate
        destinations:
          items:
            oneOf:
              - $ref: '#/components/schemas/NumberDestination'
              - $ref: '#/components/schemas/SipDestination'
              - $ref: '#/components/schemas/AssistantDestination'
            discriminator:
              propertyName: type
              mapping:
                assistant: '#/components/schemas/AssistantDestination'
                number: '#/components/schemas/NumberDestination'
                sip: '#/components/schemas/SipDestination'
          type: array
          title: Destinations
        liveEscalateConfig:
          $ref: '#/components/schemas/LiveEscalateConfig'
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Output'
            - type: 'null'
        aiHeaders:
          items:
            $ref: '#/components/schemas/AiHeader'
          type: array
          title: Aiheaders
      type: object
      required:
        - id
        - liveEscalateConfig
      title: LiveEscalateToolResponse
    LiveEscalateToolUpdate:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Move this tool to a folder. Pass an explicit ``null`` to unassign
            and return the tool to the list root. A PATCH that only changes
            ``folderId`` is treated as an organisational move — ``updatedAt`` is
            preserved.
        messages:
          anyOf:
            - items:
                $ref: '#/components/schemas/ToolMessage'
              type: array
            - type: 'null'
          title: Messages
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
        type:
          type: string
          const: liveEscalate
          title: Type
          default: liveEscalate
        destinations:
          anyOf:
            - items:
                oneOf:
                  - $ref: '#/components/schemas/NumberDestination'
                  - $ref: '#/components/schemas/SipDestination'
                  - $ref: '#/components/schemas/AssistantDestination'
                discriminator:
                  propertyName: type
                  mapping:
                    assistant: '#/components/schemas/AssistantDestination'
                    number: '#/components/schemas/NumberDestination'
                    sip: '#/components/schemas/SipDestination'
              type: array
            - type: 'null'
          title: Destinations
        liveEscalateConfig:
          anyOf:
            - $ref: '#/components/schemas/LiveEscalateConfig'
            - type: 'null'
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Input'
            - type: 'null'
        aiHeaders:
          anyOf:
            - items:
                $ref: '#/components/schemas/AiHeader'
              type: array
            - type: 'null'
          title: Aiheaders
      additionalProperties: false
      type: object
      title: LiveEscalateToolUpdate
    LlmUsageRecord:
      properties:
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        type:
          type: string
          const: llm_usage
          title: Type
          default: llm_usage
        promptTokens:
          anyOf:
            - type: integer
            - type: 'null'
          title: Prompttokens
        completionTokens:
          anyOf:
            - type: integer
            - type: 'null'
          title: Completiontokens
        totalTokens:
          anyOf:
            - type: integer
            - type: 'null'
          title: Totaltokens
      additionalProperties: true
      type: object
      title: LlmUsageRecord
    Lmnt:
      properties:
        provider:
          type: string
          const: lmnt
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            LMNT voice id (default ``leah``). See
            https://docs.lmnt.com/api-reference/voice/list-voices for the
            upstream catalog.
        model:
          anyOf:
            - type: string
              enum:
                - blizzard
                - aurora
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        temperature:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Temperature
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Lmnt
      description: |-
        LMNT TTS variant — voice cloning, 20 languages.

        The plugin's ``TTS`` constructor takes ``temperature`` and ``top_p``
        but no speaking-rate knob — fields removed accordingly.
    LocationPayload:
      properties:
        latitude:
          type: number
          title: Latitude
          examples:
            - 52.520008
        longitude:
          type: number
          title: Longitude
          examples:
            - 13.404954
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          examples:
            - Berlin Office
        address:
          anyOf:
            - type: string
            - type: 'null'
          title: Address
          examples:
            - Unter den Linden 1, 10117 Berlin
      additionalProperties: false
      type: object
      required:
        - latitude
        - longitude
      title: LocationPayload
    MarkInvoicePaidRequest:
      properties:
        paidAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Paidat
          description: Defaults to now if omitted.
        note:
          anyOf:
            - type: string
              maxLength: 500
            - type: 'null'
          title: Note
      additionalProperties: false
      type: object
      title: MarkInvoicePaidRequest
      description: |-
        ``POST /billing/invoices/{id}/mark-paid`` — admin-only manual mark.

        Triggers the same ledger credit as a Stripe ``invoice.paid`` webhook
        would. Logs an ActivityLog row for audit.
    MediaAttachment:
      properties:
        mediaId:
          anyOf:
            - type: string
            - type: 'null'
          title: Mediaid
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
        mimeType:
          anyOf:
            - type: string
            - type: 'null'
          title: Mimetype
        sizeBytes:
          anyOf:
            - type: integer
            - type: 'null'
          title: Sizebytes
        filename:
          anyOf:
            - type: string
            - type: 'null'
          title: Filename
        caption:
          anyOf:
            - type: string
            - type: 'null'
          title: Caption
        sha256:
          anyOf:
            - type: string
            - type: 'null'
          title: Sha256
      additionalProperties: false
      type: object
      title: MediaAttachment
      description: Media payload — either a stored object or a provider link.
    MediaPayload:
      properties:
        mediaId:
          anyOf:
            - type: string
            - type: 'null'
          title: Mediaid
          examples:
            - 7a1e29c4-88d2-4f3a-9b16-5f0a3c9e21d7
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
          examples:
            - https://files.example.com/invoice-042.pdf
        caption:
          anyOf:
            - type: string
            - type: 'null'
          title: Caption
          examples:
            - Your invoice
        filename:
          anyOf:
            - type: string
            - type: 'null'
          title: Filename
          examples:
            - invoice-042.pdf
      additionalProperties: false
      type: object
      title: MediaPayload
      description: Used for type=image|video|audio|document|sticker.
    MediaUploadResponse:
      properties:
        id:
          type: string
          title: Id
        url:
          type: string
          title: Url
          description: Presigned URL for download (1h TTL).
        sha256:
          type: string
          title: Sha256
        mimeType:
          type: string
          title: Mimetype
        sizeBytes:
          type: integer
          title: Sizebytes
      type: object
      required:
        - id
        - url
        - sha256
        - mimeType
        - sizeBytes
      title: MediaUploadResponse
    MessageDirection:
      type: string
      enum:
        - inbound
        - outbound
      title: MessageDirection
    MessageReaction:
      properties:
        actor:
          type: string
          title: Actor
        actorType:
          type: string
          title: Actortype
        emoji:
          type: string
          title: Emoji
        reactedAt:
          type: string
          format: date-time
          title: Reactedat
      additionalProperties: false
      type: object
      required:
        - actor
        - actorType
        - emoji
      title: MessageReaction
      description: A single emoji reaction on this message.
    MessageResponse:
      properties:
        id:
          type: string
          title: Id
          examples:
            - b4c8e2a1-6f3d-4e79-8c25-91a0d7f3e6b2
        tenantId:
          type: string
          title: Tenantid
          examples:
            - 9d2f4c81-3e57-4b6a-8f01-c5a7e92d1b43
        organizationId:
          type: string
          title: Organizationid
          examples:
            - 5a8c1f37-9b2d-4e64-a7c9-3f0e81d6b254
        channelId:
          type: string
          title: Channelid
          examples:
            - 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
        conversationId:
          type: string
          title: Conversationid
          examples:
            - c7e91b45-2d8f-4a36-b1e0-6f4d29c8a573
        contactId:
          type: string
          title: Contactid
          examples:
            - 1e5d8a92-4c7b-4f13-9e60-b28f5c3a7d41
        direction:
          $ref: '#/components/schemas/MessageDirection'
        type:
          $ref: '#/components/schemas/MessageType'
        status:
          $ref: '#/components/schemas/MessageStatus'
        statusHistory:
          items:
            $ref: '#/components/schemas/MessageStatusEvent'
          type: array
          title: Statushistory
        failureReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Failurereason
        providerMessageId:
          anyOf:
            - type: string
            - type: 'null'
          title: Providermessageid
          examples:
            - wamid.HBgNNDkxNTExMjM0NTY3OBUCABEYEjA5
        replyToMessageId:
          anyOf:
            - type: string
            - type: 'null'
          title: Replytomessageid
        text:
          anyOf:
            - type: string
            - type: 'null'
          title: Text
          examples:
            - Your order ORD-42 is ready for pickup 🎉
        subject:
          anyOf:
            - type: string
            - type: 'null'
          title: Subject
        htmlBody:
          anyOf:
            - type: string
            - type: 'null'
          title: Htmlbody
        media:
          anyOf:
            - $ref: '#/components/schemas/MediaAttachment'
            - type: 'null'
        locationLat:
          anyOf:
            - type: number
            - type: 'null'
          title: Locationlat
        locationLng:
          anyOf:
            - type: number
            - type: 'null'
          title: Locationlng
        locationName:
          anyOf:
            - type: string
            - type: 'null'
          title: Locationname
        locationAddress:
          anyOf:
            - type: string
            - type: 'null'
          title: Locationaddress
        contactVcard:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Contactvcard
        templateName:
          anyOf:
            - type: string
            - type: 'null'
          title: Templatename
        templateLanguage:
          anyOf:
            - type: string
            - type: 'null'
          title: Templatelanguage
        templateComponents:
          anyOf:
            - items:
                additionalProperties: true
                type: object
              type: array
            - type: 'null'
          title: Templatecomponents
        interactivePayload:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Interactivepayload
        reactionTargetMessageId:
          anyOf:
            - type: string
            - type: 'null'
          title: Reactiontargetmessageid
        reactionEmoji:
          anyOf:
            - type: string
            - type: 'null'
          title: Reactionemoji
        reactions:
          items:
            $ref: '#/components/schemas/MessageReaction'
          type: array
          title: Reactions
        whatsappCategory:
          anyOf:
            - $ref: '#/components/schemas/WhatsAppCategory'
            - type: 'null'
        destinationCountry:
          anyOf:
            - type: string
            - type: 'null'
          title: Destinationcountry
        segmentCount:
          anyOf:
            - type: integer
            - type: 'null'
          title: Segmentcount
        isMms:
          type: boolean
          title: Ismms
          default: false
        billedCents:
          type: integer
          title: Billedcents
          default: 0
        scheduledFor:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Scheduledfor
        fallbackUsed:
          type: boolean
          title: Fallbackused
          default: false
        originalType:
          anyOf:
            - $ref: '#/components/schemas/MessageType'
            - type: 'null'
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - channelId
        - conversationId
        - contactId
        - direction
        - type
        - status
        - createdAt
        - updatedAt
      title: MessageResponse
      description: Single message resource.
    MessageStatus:
      type: string
      enum:
        - draft
        - scheduled
        - queued
        - sent
        - delivered
        - read
        - failed
        - retracted
      title: MessageStatus
    MessageStatusEvent:
      properties:
        status:
          $ref: '#/components/schemas/MessageStatus'
        at:
          type: string
          format: date-time
          title: At
        detail:
          anyOf:
            - type: string
            - type: 'null'
          title: Detail
      additionalProperties: false
      type: object
      required:
        - status
      title: MessageStatusEvent
      description: One entry in the message's status timeline.
    MessageStatusHistoryResponse:
      properties:
        messageId:
          type: string
          title: Messageid
        events:
          items:
            $ref: '#/components/schemas/MessageStatusEvent'
          type: array
          title: Events
      type: object
      required:
        - messageId
        - events
      title: MessageStatusHistoryResponse
    MessageType:
      type: string
      enum:
        - text
        - image
        - video
        - audio
        - document
        - sticker
        - location
        - contact
        - template
        - interactive
        - reaction
        - system
      title: MessageType
    MessagingLocationBlock:
      properties:
        latitude:
          anyOf:
            - type: number
            - type: 'null'
          title: Latitude
        longitude:
          anyOf:
            - type: number
            - type: 'null'
          title: Longitude
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        address:
          anyOf:
            - type: string
            - type: 'null'
          title: Address
      type: object
      title: MessagingLocationBlock
    MessagingMediaBlock:
      properties:
        mediaId:
          anyOf:
            - type: string
            - type: 'null'
          title: Mediaid
          description: Provider-native media id — reusable in sends.
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
        mimeType:
          anyOf:
            - type: string
            - type: 'null'
          title: Mimetype
        sizeBytes:
          anyOf:
            - type: integer
            - type: 'null'
          title: Sizebytes
        filename:
          anyOf:
            - type: string
            - type: 'null'
          title: Filename
        caption:
          anyOf:
            - type: string
            - type: 'null'
          title: Caption
        sha256:
          anyOf:
            - type: string
            - type: 'null'
          title: Sha256
        downloadUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Downloadurl
          description: >-
            Signed download URL (24h) — stamped at delivery; null when the
            download failed.
        downloadUrlExpiresAt:
          anyOf:
            - type: string
            - type: 'null'
          title: Downloadurlexpiresat
        downloadFailedReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Downloadfailedreason
          description: >-
            Why no downloadUrl: `media_storage_disabled`, `missing_media_id`,
            `missing_sha256`, `missing_access_token`, `meta_url_failed:<code>`,
            `download_failed:<code>`, `s3_put_failed:<code>`, `presign_failed`.
      type: object
      title: MessagingMediaBlock
      description: Inbound media descriptor on messaging events.
    MessagingWebhookEvent:
      properties:
        eventId:
          type: string
          title: Eventid
          description: Delivery id — dedupe key (also in the `X-Webhook-Event-Id` header).
        eventType:
          type: string
          title: Eventtype
          description: >-
            `message.received` / `message.status_changed` / `message.failed` /
            `message.reaction_added` / `message.reaction_removed` /
            `contact.opted_out` / `contact.opted_in`.
        channelId:
          type: string
          title: Channelid
        channelType:
          type: string
          title: Channeltype
          description: '`whatsapp` / `telegram` / `sms` / `email`.'
        organizationId:
          type: string
          title: Organizationid
        occurredAt:
          type: string
          title: Occurredat
          description: ISO 8601 — order by this; delivery order is not guaranteed.
        senderIdentifier:
          anyOf:
            - type: string
            - type: 'null'
          title: Senderidentifier
          description: Who sent the inbound message (phone / bot user / email).
        providerMessageId:
          anyOf:
            - type: string
            - type: 'null'
          title: Providermessageid
        messageId:
          anyOf:
            - type: string
            - type: 'null'
          title: Messageid
          description: >-
            Our message id — fetch the full record via GET
            /messaging/messages/{id}.
        conversationId:
          anyOf:
            - type: string
            - type: 'null'
          title: Conversationid
        contactId:
          anyOf:
            - type: string
            - type: 'null'
          title: Contactid
        messageType:
          anyOf:
            - type: string
            - type: 'null'
          title: Messagetype
          description: >-
            `text` / `image` / `audio` / `video` / `document` / `sticker` /
            `location` / `contacts` / `interactive` / `reaction` / `unknown`.
        text:
          anyOf:
            - type: string
            - type: 'null'
          title: Text
        subject:
          anyOf:
            - type: string
            - type: 'null'
          title: Subject
          description: Inbound email subject.
        media:
          anyOf:
            - $ref: '#/components/schemas/MessagingMediaBlock'
            - type: 'null'
        location:
          anyOf:
            - $ref: '#/components/schemas/MessagingLocationBlock'
            - type: 'null'
        contactVcard:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Contactvcard
        interactive:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Interactive
          description: >-
            `type` is `button_reply` / `list_reply` / `nfm_reply`; the tapped
            title also lands in `text`.
        newStatus:
          anyOf:
            - type: string
            - type: 'null'
          title: Newstatus
          description: '`sent` / `delivered` / `read` — status_changed events only.'
        failureReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Failurereason
          description: Provider's verbatim reason — message.failed only.
        reactionEmoji:
          anyOf:
            - type: string
            - type: 'null'
          title: Reactionemoji
        reactionTargetMessageId:
          anyOf:
            - type: string
            - type: 'null'
          title: Reactiontargetmessageid
          description: Provider id of the message that was reacted to.
        windowOpenUntil:
          anyOf:
            - type: string
            - type: 'null'
          title: Windowopenuntil
          description: WhatsApp 24h customer-care window expiry (ISO 8601).
        windowState:
          anyOf:
            - type: string
            - type: 'null'
          title: Windowstate
          description: '`open` / `expired` / `not_applicable`.'
      type: object
      required:
        - eventId
        - eventType
        - channelId
        - channelType
        - organizationId
        - occurredAt
      title: MessagingWebhookEvent
      description: |-
        Envelope shared by every messaging channel event. All keys are
        always present — inapplicable fields are ``null``, not omitted.
    MinimaxTTS:
      properties:
        provider:
          type: string
          const: minimax-tts
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            MiniMax voice id. See
            https://www.minimax.io/platform/document/T2A%20V2 for the upstream
            voice catalog.
        model:
          anyOf:
            - type: string
              enum:
                - speech-02-hd
                - speech-02-turbo
                - speech-01-hd
                - speech-01-turbo
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        emotion:
          anyOf:
            - type: string
              enum:
                - happy
                - sad
                - angry
                - fearful
                - disgusted
                - surprised
                - neutral
            - type: 'null'
          title: Emotion
        speed:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.5
            - type: 'null'
          title: Speed
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: MinimaxTTS
      description: MiniMax TTS variant with emotion knob.
    MonitorInfo:
      properties:
        callId:
          type: string
          title: Callid
        roomName:
          type: string
          title: Roomname
        listenEnabled:
          type: boolean
          title: Listenenabled
          description: Whether the assistant permits live listening.
          default: true
        controlEnabled:
          type: boolean
          title: Controlenabled
          description: Whether the assistant permits supervisor control messages.
          default: true
        takeoverEnabled:
          type: boolean
          title: Takeoverenabled
          description: Whether the assistant permits human takeover.
          default: true
        monitor:
          $ref: '#/components/schemas/MonitorUrls'
      additionalProperties: false
      type: object
      required:
        - callId
        - roomName
        - monitor
      title: MonitorInfo
      description: Live-call monitoring info — capability flags + pre-built URLs.
    MonitorUrls:
      properties:
        listenUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Listenurl
          description: >-
            Pre-signed listen-only WebRTC URL (token embedded in the query
            string).
        controlUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Controlurl
          description: REST endpoint for ``POST``-ing control messages to the live call.
      additionalProperties: false
      type: object
      title: MonitorUrls
      description: Pre-built URLs for live-call audio + control.
    MonitoringConfigSchema:
      properties:
        listenEnabled:
          type: boolean
          title: Listenenabled
          default: true
        controlEnabled:
          type: boolean
          title: Controlenabled
          default: true
      type: object
      title: MonitoringConfigSchema
    Moonshot:
      properties:
        provider:
          type: string
          const: moonshot
          title: Provider
        model:
          type: string
          enum:
            - kimi-k2.5
            - kimi-k2-0905-preview
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        frequencyPenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Frequencypenalty
        presencePenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Presencepenalty
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: Moonshot
      description: Moonshot Kimi LLM (pipeline mode, OpenAI-compatible).
    Munsit:
      properties:
        provider:
          type: string
          const: munsit
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Munsit voice id from your voice library (e.g. ``ar-uae-male-1``).
            Copy it from https://app.munsit.com/en/voices.
        model:
          anyOf:
            - type: string
              enum:
                - faseeh-v1-preview
                - faseeh-mini-v1-preview
            - type: 'null'
          title: Model
          description: >-
            Defaults to ``faseeh-v1-preview`` (highest quality). Use
            ``faseeh-mini-v1-preview`` for lower latency in real-time agents.
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        stability:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Stability
          description: >-
            Voice consistency. 0.0–0.4 = more expressive (can drift); 0.5–0.7 =
            balanced (recommended); 0.8–1.0 = very consistent. Default 0.5.
        speed:
          anyOf:
            - type: number
              maximum: 1.2
              minimum: 0.7
            - type: 'null'
          title: Speed
          description: >-
            Speech rate. 0.7–0.9 = slower / clearer, 1.0 = normal, 1.1–1.2 =
            faster. Default 1.0.
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Munsit
      description: |-
        Munsit (Faseeh) TTS — Arabic (MSA + Gulf dialects) speech synthesis.

        Voices are managed in the Munsit voice library and identified by a
        free-text ``voiceId`` (copied from the dashboard, e.g.
        ``ar-uae-male-1``); the dashboard picker is populated live from the
        upstream ``GET /voices`` endpoint.
    Neuphonic:
      properties:
        provider:
          type: string
          const: neuphonic
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Neuphonic voice id (UUID). See https://docs.neuphonic.com for the
            upstream voice catalog.
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        speed:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.7
            - type: 'null'
          title: Speed
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Neuphonic
      description: Neuphonic TTS variant — low-latency streaming, voice cloning.
    NewsAdminResponse:
      properties:
        id:
          type: string
          title: Id
        widgetId:
          type: string
          title: Widgetid
        title:
          additionalProperties:
            type: string
          type: object
          title: Title
        body:
          additionalProperties:
            type: string
          type: object
          title: Body
        coverImageUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Coverimageurl
        ctaText:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Ctatext
        ctaUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Ctaurl
        status:
          type: string
          title: Status
        publishedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Publishedat
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - widgetId
        - title
        - body
        - status
        - createdAt
        - updatedAt
      title: NewsAdminResponse
    NewsCreateRequest:
      properties:
        title:
          additionalProperties:
            type: string
          type: object
          title: Title
        body:
          additionalProperties:
            type: string
          type: object
          title: Body
        coverImageUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Coverimageurl
        ctaText:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Ctatext
        ctaUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Ctaurl
      additionalProperties: false
      type: object
      required:
        - title
        - body
      title: NewsCreateRequest
    NewsUpdateRequest:
      properties:
        title:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Title
        body:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Body
        coverImageUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Coverimageurl
        ctaText:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Ctatext
        ctaUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Ctaurl
        status:
          anyOf:
            - type: string
              pattern: ^(draft|published)$
            - type: 'null'
          title: Status
      additionalProperties: false
      type: object
      title: NewsUpdateRequest
    NoteResponse:
      properties:
        id:
          type: string
          title: Id
        authorId:
          type: string
          title: Authorid
        body:
          type: string
          title: Body
        createdAt:
          type: string
          format: date-time
          title: Createdat
      type: object
      required:
        - id
        - authorId
        - body
        - createdAt
      title: NoteResponse
    NumberDestination:
      properties:
        type:
          type: string
          const: number
          title: Type
          default: number
        number:
          type: string
          title: Number
          description: Destination phone number.
        extension:
          anyOf:
            - type: string
            - type: 'null'
          title: Extension
          description: DTMF extension dialed after connect.
        callerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Callerid
          description: >-
            Calling number presented to the destination. **Warm mode only** — a
            warm transfer places a NEW outbound call, so this becomes its
            ``From`` user. Blind modes send a SIP REFER, which creates no new
            leg and carries the original caller's ``From`` unchanged, so this
            field is ignored there. Supports ``{{variable}}`` substitution (e.g.
            ``{{customer.number}}`` to present the original caller's number).
        numberE164CheckEnabled:
          type: boolean
          title: Numbere164Checkenabled
          description: Validate that ``number`` is E.164 before placing the call.
          default: true
        sipHeaders:
          additionalProperties: true
          type: object
          title: Sipheaders
          description: >-
            Custom SIP headers sent with the transfer — on the ``REFER`` in
            blind modes, on the outbound ``INVITE`` in warm mode. Values support
            ``{{variable}}`` substitution resolved at transfer time (e.g.
            ``{{customer.number}}``).
        message:
          anyOf:
            - type: string
            - type: 'null'
          title: Message
          description: Spoken to the caller before the transfer.
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
          description: Hint the LLM uses to pick this destination.
        transferPlan:
          $ref: '#/components/schemas/TransferPlan'
      additionalProperties: false
      type: object
      required:
        - number
      title: NumberDestination
      description: Transfer destination — a phone number (SIP REFER to ``tel:+E164``).
    NvidiaSTT:
      properties:
        provider:
          type: string
          const: nvidia-stt
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        diarization:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Diarization
      additionalProperties: false
      type: object
      required:
        - provider
      title: NvidiaSTT
      description: NVIDIA Riva STT variant — gRPC streaming.
    NvidiaTTS:
      properties:
        provider:
          type: string
          const: nvidia-tts
          title: Provider
        voiceId:
          type: string
          title: Voiceid
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: NvidiaTTS
      description: NVIDIA Riva TTS — gRPC streaming.
    OpenAI:
      properties:
        provider:
          type: string
          const: openai
          title: Provider
        model:
          type: string
          enum:
            - gpt-5.6-terra
            - gpt-5.6-luna
            - gpt-5.6-sol
            - gpt-5.4-mini
            - gpt-5.4-nano
            - gpt-5.4
            - gpt-5.2
            - gpt-5.1
            - gpt-5-mini
            - gpt-5-nano
            - gpt-5
            - gpt-4.1-mini
            - gpt-4.1-nano
            - gpt-4.1
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        frequencyPenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Frequencypenalty
        presencePenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Presencepenalty
        reasoningEffort:
          anyOf:
            - type: string
              enum:
                - minimal
                - low
                - medium
                - high
                - none
                - xhigh
            - type: 'null'
          title: Reasoningeffort
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: OpenAI
      description: |-
        OpenAI LLM configuration (pipeline mode).

        ``provider`` is fixed to ``"openai"`` — Pydantic uses it as the
        discriminator value when this variant lands inside the
        ``ModelConfigSchema`` union.

        ``model`` is constrained to the exact list of supported model IDs
        in ``MODELS``. Mintlify renders it as a searchable enum chip set.
    OpenAIRealtime:
      properties:
        provider:
          type: string
          const: openai-realtime
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - gpt-realtime-2.1
                - gpt-realtime-2.1-mini
                - gpt-realtime-2
                - gpt-realtime-1.5
                - gpt-realtime
                - gpt-realtime-mini
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
              enum:
                - alloy
                - ash
                - ballad
                - cedar
                - coral
                - echo
                - marin
                - sage
                - shimmer
                - verse
            - type: 'null'
          title: Voice
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        inputAudioTranscription:
          anyOf:
            - $ref: '#/components/schemas/RealtimeInputAudioTranscription'
            - type: 'null'
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        turnDetection:
          anyOf:
            - {}
            - type: 'null'
          title: Turndetection
          description: '''server_vad'' | ''semantic_vad'' | full dict.'
        inputAudioNoiseReduction:
          anyOf:
            - type: string
              enum:
                - near_field
                - far_field
            - type: 'null'
          title: Inputaudionoisereduction
        speed:
          anyOf:
            - type: number
              maximum: 4
              minimum: 0.25
            - type: 'null'
          title: Speed
        reasoningEffort:
          anyOf:
            - type: string
              enum:
                - minimal
                - low
                - medium
                - high
                - xhigh
            - type: 'null'
          title: Reasoningeffort
          description: >-
            Reasoning effort for the ``gpt-realtime-2`` generation (2.1,
            2.1-mini, 2 — the only models that honour this field). ``minimal``
            for single-turn yes/no, ``low`` (default) for everyday support,
            ``medium`` for tool dispatch / disambiguation, ``high`` for
            multi-step reasoning, ``xhigh`` for hard analytical questions.
            Higher levels trade latency for depth — start with ``low`` and tune
            up if the agent needs more deliberation. Silently ignored on
            non-reasoning models (gpt-realtime / 1.5 / mini).
      additionalProperties: false
      type: object
      required:
        - provider
      title: OpenAIRealtime
      description: OpenAI Realtime — end-to-end speech-to-speech.
    OpenAISTT:
      properties:
        provider:
          type: string
          const: openai-stt
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - gpt-4o-mini-transcribe
                - gpt-4o-transcribe
                - whisper-1
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        prompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Prompt
      additionalProperties: false
      type: object
      required:
        - provider
      title: OpenAISTT
      description: OpenAI STT variant.
    OpenAITTS:
      properties:
        provider:
          type: string
          const: openai-tts
          title: Provider
        voiceId:
          anyOf:
            - type: string
              enum:
                - alloy
                - ash
                - ballad
                - cedar
                - coral
                - echo
                - fable
                - marin
                - nova
                - onyx
                - sage
                - shimmer
                - verse
            - type: 'null'
          title: Voiceid
        model:
          anyOf:
            - type: string
              enum:
                - gpt-4o-mini-tts
                - tts-1
                - tts-1-hd
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        speed:
          anyOf:
            - type: number
              maximum: 4
              minimum: 0.25
            - type: 'null'
          title: Speed
        instructions:
          anyOf:
            - type: string
            - type: 'null'
          title: Instructions
          description: Style prompt — gpt-4o-mini-tts only.
      additionalProperties: false
      type: object
      required:
        - provider
      title: OpenAITTS
      description: |-
        OpenAI TTS variant — 13 static voices.

        ``voiceId`` is optional on the wire (defaults to ``ash`` at runtime,
        matching the legacy behavior) so that older saved assistants without
        a voice still load. New assistants are expected to pick one.
    OpenRouter:
      properties:
        provider:
          type: string
          const: openrouter
          title: Provider
        model:
          type: string
          title: Model
          description: >-
            Any OpenRouter model id (e.g. ``openai/gpt-4o``,
            ``anthropic/claude-sonnet-4-6``). See https://openrouter.ai/models
            for the upstream catalog of 350+ models across every major provider.
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 131072
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        frequencyPenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Frequencypenalty
        presencePenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Presencepenalty
        reasoningEffort:
          anyOf:
            - type: string
              enum:
                - minimal
                - low
                - medium
                - high
                - none
            - type: 'null'
          title: Reasoningeffort
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: OpenRouter
      description: |-
        OpenRouter LLM (pipeline mode, gateway to 350+ models).

        ``model`` is open-ended ``str`` because the upstream catalog moves
        fast. See https://openrouter.ai/models for the live list — we don't
        embed it in the schema because new models land weekly.
    OptOutCheckResponse:
      properties:
        blocked:
          type: boolean
          title: Blocked
        optOut:
          anyOf:
            - $ref: '#/components/schemas/OptOutResponse'
            - type: 'null'
      type: object
      required:
        - blocked
      title: OptOutCheckResponse
    OptOutResponse:
      properties:
        id:
          type: string
          title: Id
        tenantId:
          type: string
          title: Tenantid
        organizationId:
          type: string
          title: Organizationid
        channelType:
          $ref: '#/components/schemas/ChannelType'
        identifier:
          type: string
          title: Identifier
        contactId:
          anyOf:
            - type: string
            - type: 'null'
          title: Contactid
        source:
          $ref: '#/components/schemas/OptOutSource'
        reason:
          anyOf:
            - type: string
            - type: 'null'
          title: Reason
        sourceMessageId:
          anyOf:
            - type: string
            - type: 'null'
          title: Sourcemessageid
        optedOutAt:
          type: string
          format: date-time
          title: Optedoutat
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - channelType
        - identifier
        - source
        - optedOutAt
        - createdAt
        - updatedAt
      title: OptOutResponse
    OptOutSource:
      type: string
      enum:
        - keyword
        - manual
        - unsubscribe_link
        - provider_signal
        - complaint
      title: OptOutSource
      description: How the opt-out was captured.
    PaginatedAssistantResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
        skip:
          type: integer
          title: Skip
        limit:
          type: integer
          title: Limit
        assistants:
          items:
            oneOf:
              - $ref: '#/components/schemas/PipelineAssistantResponse'
              - $ref: '#/components/schemas/RealtimeAssistantResponse'
            discriminator:
              propertyName: type
              mapping:
                pipeline: '#/components/schemas/PipelineAssistantResponse'
                realtime: '#/components/schemas/RealtimeAssistantResponse'
          type: array
          title: Assistants
          description: Assistants in this page, newest first.
      type: object
      required:
        - totalCount
        - skip
        - limit
      title: PaginatedAssistantResponse
      description: Paginated list of assistants — returned by ``GET /assistant``.
    PaginatedBillingInvoiceResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
        skip:
          type: integer
          title: Skip
        limit:
          type: integer
          title: Limit
        items:
          items:
            $ref: '#/components/schemas/BillingInvoiceResponse'
          type: array
          title: Items
      type: object
      required:
        - totalCount
        - skip
        - limit
        - items
      title: PaginatedBillingInvoiceResponse
    PaginatedCallResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
        skip:
          type: integer
          title: Skip
        limit:
          type: integer
          title: Limit
        calls:
          items:
            $ref: '#/components/schemas/CallResponse'
          type: array
          title: Calls
      type: object
      required:
        - totalCount
        - skip
        - limit
      title: PaginatedCallResponse
      description: Paginated call response.
    PaginatedFileResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
        skip:
          type: integer
          title: Skip
        limit:
          type: integer
          title: Limit
        files:
          items:
            $ref: '#/components/schemas/FileResponse'
          type: array
          title: Files
          description: Files in this page, in the requested sort order.
      type: object
      required:
        - totalCount
        - skip
        - limit
      title: PaginatedFileResponse
      description: Paginated list of files — returned by ``GET /file``.
    PaginatedKnowledgeBaseResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
        skip:
          type: integer
          title: Skip
        limit:
          type: integer
          title: Limit
        knowledgeBases:
          items:
            $ref: '#/components/schemas/KnowledgeBaseResponse'
          type: array
          title: Knowledgebases
          description: Knowledge bases in this page, newest first.
      type: object
      required:
        - totalCount
        - skip
        - limit
      title: PaginatedKnowledgeBaseResponse
      description: Paginated list of knowledge bases — returned by ``GET /knowledge-base``.
    PaginatedPhoneNumberResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
        skip:
          type: integer
          title: Skip
        limit:
          type: integer
          title: Limit
        phoneNumbers:
          items: {}
          type: array
          title: Phonenumbers
          description: >-
            Phone numbers in this page, newest first. Each item is one of
            ``TwilioPhoneNumberResponse`` or ``ByoSipPhoneNumberResponse``
            (discriminated by ``provider``).
      type: object
      required:
        - totalCount
        - skip
        - limit
      title: PaginatedPhoneNumberResponse
      description: Paginated list of phone numbers — returned by ``GET /phone-number``.
    PaginatedResourceVersionsResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
        skip:
          type: integer
          title: Skip
        limit:
          type: integer
          title: Limit
        versions:
          items:
            $ref: '#/components/schemas/ResourceVersionSummary'
          type: array
          title: Versions
          description: Versions in this page, newest first.
      type: object
      required:
        - totalCount
        - skip
        - limit
      title: PaginatedResourceVersionsResponse
      description: '``GET /{resource}/{id}/versions`` envelope.'
    PaginatedResponse_BatchResponse_:
      properties:
        items:
          items:
            $ref: '#/components/schemas/BatchResponse'
          type: array
          title: Items
        nextCursor:
          anyOf:
            - type: string
            - type: 'null'
          title: Nextcursor
          description: Pass as ``after`` to fetch the next page; null when no more pages.
        count:
          type: integer
          minimum: 0
          title: Count
      type: object
      required:
        - items
        - count
      title: PaginatedResponse[BatchResponse]
    PaginatedResponse_ChannelResponse_:
      properties:
        items:
          items:
            $ref: '#/components/schemas/ChannelResponse'
          type: array
          title: Items
        nextCursor:
          anyOf:
            - type: string
            - type: 'null'
          title: Nextcursor
          description: Pass as ``after`` to fetch the next page; null when no more pages.
        count:
          type: integer
          minimum: 0
          title: Count
      type: object
      required:
        - items
        - count
      title: PaginatedResponse[ChannelResponse]
    PaginatedResponse_ContactResponse_:
      properties:
        items:
          items:
            $ref: '#/components/schemas/ContactResponse'
          type: array
          title: Items
        nextCursor:
          anyOf:
            - type: string
            - type: 'null'
          title: Nextcursor
          description: Pass as ``after`` to fetch the next page; null when no more pages.
        count:
          type: integer
          minimum: 0
          title: Count
      type: object
      required:
        - items
        - count
      title: PaginatedResponse[ContactResponse]
    PaginatedResponse_ConversationResponse_:
      properties:
        items:
          items:
            $ref: '#/components/schemas/ConversationResponse'
          type: array
          title: Items
        nextCursor:
          anyOf:
            - type: string
            - type: 'null'
          title: Nextcursor
          description: Pass as ``after`` to fetch the next page; null when no more pages.
        count:
          type: integer
          minimum: 0
          title: Count
      type: object
      required:
        - items
        - count
      title: PaginatedResponse[ConversationResponse]
    PaginatedResponse_MessageResponse_:
      properties:
        items:
          items:
            $ref: '#/components/schemas/MessageResponse'
          type: array
          title: Items
        nextCursor:
          anyOf:
            - type: string
            - type: 'null'
          title: Nextcursor
          description: Pass as ``after`` to fetch the next page; null when no more pages.
        count:
          type: integer
          minimum: 0
          title: Count
      type: object
      required:
        - items
        - count
      title: PaginatedResponse[MessageResponse]
    PaginatedSipTrunkResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
        skip:
          type: integer
          title: Skip
        limit:
          type: integer
          title: Limit
        sipTrunks:
          items:
            $ref: '#/components/schemas/SipTrunkResponse'
          type: array
          title: Siptrunks
          description: SIP trunks in this page, newest first.
      type: object
      required:
        - totalCount
        - skip
        - limit
      title: PaginatedSipTrunkResponse
      description: Paginated list of SIP trunks — returned by ``GET /sip-trunk``.
    PaginatedToolResponse:
      properties:
        totalCount:
          type: integer
          title: Totalcount
        skip:
          type: integer
          title: Skip
        limit:
          type: integer
          title: Limit
        tools:
          items: {}
          type: array
          title: Tools
          description: >-
            Tools in this page, newest first. Each item is one of the
            ``<Type>ToolResponse`` shapes.
      type: object
      required:
        - totalCount
        - skip
        - limit
      title: PaginatedToolResponse
      description: Paginated list of tools — returned by ``GET /tool``.
    PerformanceSchema:
      properties:
        minConsecutiveSpeechDelay:
          type: number
          maximum: 2
          minimum: 0
          title: Minconsecutivespeechdelay
          default: 0
        discardAudioIfUninterruptible:
          type: boolean
          title: Discardaudioifuninterruptible
          default: true
      type: object
      title: PerformanceSchema
    Perplexity:
      properties:
        provider:
          type: string
          const: perplexity
          title: Provider
        model:
          type: string
          enum:
            - sonar
            - sonar-pro
            - sonar-reasoning-pro
            - sonar-deep-research
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        reasoningEffort:
          anyOf:
            - type: string
              enum:
                - minimal
                - low
                - medium
                - high
                - none
            - type: 'null'
          title: Reasoningeffort
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: Perplexity
      description: |-
        Perplexity Sonar — search-augmented LLM (pipeline mode).

        No `frequencyPenalty` / `presencePenalty` — Perplexity API rejects them.
    PhaseTimestampsSchema:
      properties:
        agentStartedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Agentstartedat
        agentConnectedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Agentconnectedat
        configLoadedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Configloadedat
        sessionReadyAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Sessionreadyat
        firstAssistantResponseAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Firstassistantresponseat
        firstUserInputAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Firstuserinputat
        firstToolAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Firsttoolat
        cleanupStartedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Cleanupstartedat
        agentExitedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Agentexitedat
        workerDequeuedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Workerdequeuedat
        workerFinishedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Workerfinishedat
      additionalProperties: false
      type: object
      title: PhaseTimestampsSchema
      description: |-
        ISO-8601 timestamps for the key phases of a call's lifecycle.

        Every field is optional — different call paths hit different phases, and
        the doc is written incrementally as the call progresses.
    PhoneButton:
      properties:
        type:
          type: string
          const: PHONE_NUMBER
          title: Type
        text:
          type: string
          maxLength: 25
          minLength: 1
          title: Text
        phone_number:
          type: string
          maxLength: 20
          minLength: 1
          title: Phone Number
      additionalProperties: false
      type: object
      required:
        - type
        - text
        - phone_number
      title: PhoneButton
    PhoneNumberUpdate:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Move this number to a folder. Pass an explicit ``null`` to unassign
            and return the number to the list root.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: >-
            Default assistant that handles inbound calls. Pass ``null`` to
            unassign.
        server:
          anyOf:
            - $ref: '#/components/schemas/InboundServerConfig'
            - type: 'null'
          description: >-
            Dynamic-assistant selection webhook. Takes precedence over
            ``assistantId`` when both are set.
        fallbackDestination:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/FallbackNumberDestination'
                - $ref: '#/components/schemas/FallbackSipDestination'
              discriminator:
                propertyName: type
                mapping:
                  number: '#/components/schemas/FallbackNumberDestination'
                  sip: '#/components/schemas/FallbackSipDestination'
            - type: 'null'
          title: Fallbackdestination
          description: >-
            Inbound safety net — caller transferred here if the assistant can't
            be set up. Pass ``null`` to clear an existing fallback.
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: Free-form key/value metadata.
        twilioAccountSid:
          anyOf:
            - type: string
            - type: 'null'
          title: Twilioaccountsid
          description: Rotate the Twilio account SID. Twilio-provider numbers only.
        twilioAuthToken:
          anyOf:
            - type: string
            - type: 'null'
          title: Twilioauthtoken
          description: Rotate the Twilio auth token. Twilio-provider numbers only.
        outboundCallerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundcallerid
          description: >-
            Number shown as the caller ID on outbound calls from this phone
            number. Twilio: must be a Twilio-provisioned number on the account
            or a Verified Caller ID — pre-validated against Twilio at
            create/PATCH time. BYO SIP: validated by the carrier at call time.
            Leave null to use this number's own E.164. Pass null to clear an
            existing override and fall back to this number's own E.164.
      additionalProperties: false
      type: object
      title: PhoneNumberUpdate
      description: |-
        Update a phone number — every field optional.

        ``provider`` is immutable; ``sipTrunkId`` is immutable (delete and
        recreate to switch trunks). Twilio credential fields apply only to
        Twilio-provider numbers; sending them against a BYO number is a
        no-op rather than an error.
    PhonicRealtime:
      properties:
        provider:
          type: string
          const: phonic-realtime
          title: Provider
        model:
          anyOf:
            - type: string
              const: phonic-live
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
            - type: 'null'
          title: Voice
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        welcomeMessage:
          anyOf:
            - type: string
            - type: 'null'
          title: Welcomemessage
        generateWelcomeMessage:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Generatewelcomemessage
        audioSpeed:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.5
            - type: 'null'
          title: Audiospeed
      additionalProperties: false
      type: object
      required:
        - provider
      title: PhonicRealtime
      description: |-
        Phonic Live Conversation — proprietary speech-to-speech.

        The Phonic ``RealtimeModel`` constructor doesn't accept
        ``temperature`` — Phonic's persona is controlled via ``phonic_agent``
        (a server-side agent definition) and welcome messaging. The field
        is intentionally absent. ``systemPrompt`` is still accepted because
        the agent layer (``DynamicAssistant``) wires it as the agent's
        instructions independently of the plugin.
    PipelineAssistantCreate-Input:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
        firstMessage:
          anyOf:
            - type: string
            - type: 'null'
          title: Firstmessage
        firstMessageMode:
          type: string
          enum:
            - assistant-speaks-first
            - assistant-speaks-first-with-model-generated-message
            - assistant-waits-for-user
          title: Firstmessagemode
          default: assistant-speaks-first
        firstMessageFallbackSeconds:
          anyOf:
            - type: number
            - type: 'null'
          title: Firstmessagefallbackseconds
          description: >-
            First-response fallback: if the assistant has not spoken within this
            many seconds of the call being **answered**, it opens the
            conversation itself — ``firstMessage`` verbatim when set, otherwise
            a model-generated opener. Fires at most once per call and only when
            the assistant has produced no speech yet, so in
            ``assistant-speaks-first`` modes it acts purely as a safety net for
            a failed greeting. Primarily for ``assistant-waits-for-user``
            assistants, where a silent callee (or a short utterance the model
            declines to answer) would otherwise leave dead air. ``null``
            (default) or ``0`` disables the fallback; otherwise 1-120 seconds.
        tools:
          items:
            anyOf:
              - type: string
              - oneOf:
                  - $ref: '#/components/schemas/FunctionTool-Input'
                  - $ref: '#/components/schemas/EndCallTool-Input'
                  - $ref: '#/components/schemas/TransferCallTool-Input'
                  - $ref: '#/components/schemas/VoicemailTool-Input'
                  - $ref: '#/components/schemas/LiveEscalateTool-Input'
                discriminator:
                  propertyName: type
                  mapping:
                    endCall: '#/components/schemas/EndCallTool-Input'
                    function: '#/components/schemas/FunctionTool-Input'
                    liveEscalate: '#/components/schemas/LiveEscalateTool-Input'
                    transferCall: '#/components/schemas/TransferCallTool-Input'
                    voicemail: '#/components/schemas/VoicemailTool-Input'
          type: array
          title: Tools
          description: >-
            Tools the assistant is allowed to use during a call. Each entry is
            one of two shapes:


            - **String** — id of an existing tool (created via ``POST /tool``).
            Reusable across assistants, scoped to your workspace.

            - **Inline tool object** — a one-off tool definition with the same
            shape ``POST /tool`` accepts (``function``, ``endCall``,
            ``transferCall``, ``voicemail``, or ``liveEscalate``). Useful for
            tools tied to a single assistant — never written to the tools
            collection.
        knowledgeBaseIds:
          items:
            type: string
          type: array
          title: Knowledgebaseids
        kbRetrievalMode:
          anyOf:
            - type: string
              enum:
                - auto
                - tool
            - type: 'null'
          title: Kbretrievalmode
          description: >-
            How knowledge bases are surfaced to the LLM during a call.
            **``auto``** — per-turn pre-prompt injection. Every user turn embeds
            the utterance, searches every linked KB in parallel, and prepends
            the top chunks as a system message before the LLM call.
            Pipeline-mode only. Best for FAQ-style assistants where most user
            turns should hit the KB. **``tool``** — the LLM gets one function
            tool per linked KB and decides when to call it. Works in pipeline
            AND realtime mode. Best for multi-KB assistants and any realtime
            assistant. **``null``** (default) — derive at runtime: pipeline →
            ``auto``, realtime → ``tool``. Tier mode forces the value (Premium →
            ``tool``, Standard / Gold → ``auto``).
        silenceTimeoutSeconds:
          type: number
          maximum: 600
          minimum: 10
          title: Silencetimeoutseconds
          default: 30
        maxDurationSeconds:
          type: integer
          maximum: 43200
          minimum: 5
          title: Maxdurationseconds
          default: 3600
        allowInterruptions:
          type: boolean
          title: Allowinterruptions
          default: true
        preemptiveGeneration:
          type: boolean
          title: Preemptivegeneration
          default: false
        maxToolSteps:
          type: integer
          maximum: 10
          minimum: 1
          title: Maxtoolsteps
          default: 3
        chatMessagesInContext:
          anyOf:
            - type: integer
              maximum: 500
              minimum: 5
            - type: 'null'
          title: Chatmessagesincontext
          description: >-
            Sliding-window size for the chat-session context fed to the LLM on
            each new turn. Counts user + assistant messages. Leave ``null`` to
            use the platform default. Ignored on voice (``Call``) paths.
        backgroundSound:
          anyOf:
            - $ref: '#/components/schemas/BackgroundSoundSchema'
            - type: 'null'
        webhookEvents:
          items:
            type: string
            enum:
              - status-update
              - transcript
              - end-of-call-report
              - chat-session-status-update
              - chat-message
              - chat-session-end-report
          type: array
          title: Webhookevents
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfigSchema'
            - type: 'null'
        analysis:
          anyOf:
            - $ref: '#/components/schemas/AnalysisConfigSchema-Input'
            - type: 'null'
        dataRetention:
          anyOf:
            - $ref: '#/components/schemas/DataRetentionConfigSchema'
            - type: 'null'
        chatDataRetention:
          anyOf:
            - $ref: '#/components/schemas/ChatDataRetentionConfigSchema'
            - type: 'null'
        endpointing:
          anyOf:
            - $ref: '#/components/schemas/EndpointingConfigSchema'
            - type: 'null'
        interruption:
          anyOf:
            - $ref: '#/components/schemas/InterruptionConfigSchema'
            - type: 'null'
        idle:
          anyOf:
            - $ref: '#/components/schemas/IdleConfigSchema'
            - type: 'null'
        recording:
          anyOf:
            - $ref: '#/components/schemas/RecordingConfigSchema'
            - type: 'null'
        monitoring:
          anyOf:
            - $ref: '#/components/schemas/MonitoringConfigSchema'
            - type: 'null'
        textProcessing:
          anyOf:
            - $ref: '#/components/schemas/TextProcessingSchema'
            - type: 'null'
        performance:
          anyOf:
            - $ref: '#/components/schemas/PerformanceSchema'
            - type: 'null'
        telephony:
          anyOf:
            - $ref: '#/components/schemas/TelephonySchema'
            - type: 'null'
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
          description: >-
            Productized tier: ``standard`` (curated pipeline voices),
            ``premium`` (realtime end-to-end audio), or ``gold`` (pipeline with
            the platform's premium voice library). When set, the server resolves
            the underlying provider stack from the tier registry — the client
            must supply ``voiceCardId`` and must NOT supply ``model`` /
            ``voice`` / ``transcriber``. Leave ``null`` to use the per-provider
            Custom flow.
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
          description: >-
            Tier voice-card identifier (e.g. ``"eva"``, ``"holly"``). Required
            when ``tier`` is set; must be ``null`` otherwise. Look up valid ids
            via ``GET /providers/tiers/{tier}/voices``.
        tierLanguage:
          anyOf:
            - type: string
            - type: 'null'
          title: Tierlanguage
          description: >-
            BCP-47 language code for the resolved transcriber. Required when
            ``tier='gold'`` (Gold voices are language-agnostic, so the user
            picks the call language). Ignored for Standard (language is derived
            from the voice card) and Premium (the realtime model handles
            language internally).
        tierSttOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiersttoverrides
          description: >-
            Optional transcription overrides for ``tier='gold'``. Merged on top
            of the tier's default transcriber config. ``provider`` and ``model``
            are tier-locked and silently dropped if supplied. Ignored for
            Standard / Premium.
        tierVoiceOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiervoiceoverrides
          description: >-
            Optional voice tuning overrides for ``tier='gold'``. Merged on top
            of the tier's default voice config so the user can tune stability /
            similarity / style / speed / etc. for the picked voice card.
            ``provider`` / ``model`` / ``voiceId`` are tier-locked and silently
            dropped if supplied. Ignored for Standard / Premium.
        tierRealtimeOverrides:
          anyOf:
            - $ref: '#/components/schemas/TierRealtimeOverrides'
            - type: 'null'
          description: >-
            Optional listening-behaviour tuning for ``tier='premium'``. Adjusts
            how quickly the assistant decides the caller is speaking, how long
            it waits for silence before ending the turn, and the noise-rejection
            padding. Leave any field unset to use the tier's default. Rejected
            on Standard / Gold / Custom.
        tierVersion:
          anyOf:
            - type: integer
            - type: 'null'
          title: Tierversion
          description: >-
            Server-managed tier definition version stamp. Read-only — ignored on
            input. Bumped whenever the tier's underlying provider mapping
            changes; lets a future migration script re-resolve assistants stuck
            on an older version.
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
          description: >-
            System prompt / persona instructions. **Use this field in tier
            mode** — the resolver injects it into the underlying LLM/realtime
            model config server-side. In Custom mode supply the prompt via
            ``model.systemPrompt`` instead (setting both is rejected to keep the
            path unambiguous).
        type:
          type: string
          const: pipeline
          title: Type
        model:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/Anthropic'
                - $ref: '#/components/schemas/AwsBedrock'
                - $ref: '#/components/schemas/AzureOpenai'
                - $ref: '#/components/schemas/Baseten'
                - $ref: '#/components/schemas/Cerebras'
                - $ref: '#/components/schemas/CustomOpenAI'
                - $ref: '#/components/schemas/DeepSeek'
                - $ref: '#/components/schemas/Google'
                - $ref: '#/components/schemas/Groq'
                - $ref: '#/components/schemas/Moonshot'
                - $ref: '#/components/schemas/OpenAI'
                - $ref: '#/components/schemas/OpenRouter'
                - $ref: '#/components/schemas/Perplexity'
                - $ref: '#/components/schemas/Xai'
              discriminator:
                propertyName: provider
                mapping:
                  anthropic: '#/components/schemas/Anthropic'
                  aws-bedrock: '#/components/schemas/AwsBedrock'
                  azure-openai: '#/components/schemas/AzureOpenai'
                  baseten: '#/components/schemas/Baseten'
                  cerebras: '#/components/schemas/Cerebras'
                  custom-openai: '#/components/schemas/CustomOpenAI'
                  deepseek: '#/components/schemas/DeepSeek'
                  google: '#/components/schemas/Google'
                  groq: '#/components/schemas/Groq'
                  moonshot: '#/components/schemas/Moonshot'
                  openai: '#/components/schemas/OpenAI'
                  openrouter: '#/components/schemas/OpenRouter'
                  perplexity: '#/components/schemas/Perplexity'
                  xai: '#/components/schemas/Xai'
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AsyncAI'
                - $ref: '#/components/schemas/AwsPolly'
                - $ref: '#/components/schemas/AzureTTS'
                - $ref: '#/components/schemas/AzureOpenAITTS'
                - $ref: '#/components/schemas/Cartesia'
                - $ref: '#/components/schemas/DeepgramTTS'
                - $ref: '#/components/schemas/ElevenLabs'
                - $ref: '#/components/schemas/Fruitly'
                - $ref: '#/components/schemas/GeminiTTS'
                - $ref: '#/components/schemas/GoogleTTS'
                - $ref: '#/components/schemas/GradiumTTS'
                - $ref: '#/components/schemas/GroqTTS'
                - $ref: '#/components/schemas/Hume'
                - $ref: '#/components/schemas/Inworld'
                - $ref: '#/components/schemas/Lmnt'
                - $ref: '#/components/schemas/MinimaxTTS'
                - $ref: '#/components/schemas/Munsit'
                - $ref: '#/components/schemas/Neuphonic'
                - $ref: '#/components/schemas/NvidiaTTS'
                - $ref: '#/components/schemas/OpenAITTS'
                - $ref: '#/components/schemas/Rime'
                - $ref: '#/components/schemas/SarvamTTS'
                - $ref: '#/components/schemas/Silma'
                - $ref: '#/components/schemas/SmallestAI'
                - $ref: '#/components/schemas/Speechify'
                - $ref: '#/components/schemas/SpeechmaticsTTS'
                - $ref: '#/components/schemas/XaiTTS'
              discriminator:
                propertyName: provider
                mapping:
                  asyncai: '#/components/schemas/AsyncAI'
                  aws-polly: '#/components/schemas/AwsPolly'
                  azure-openai-tts: '#/components/schemas/AzureOpenAITTS'
                  azure-tts: '#/components/schemas/AzureTTS'
                  cartesia: '#/components/schemas/Cartesia'
                  deepgram-tts: '#/components/schemas/DeepgramTTS'
                  elevenlabs: '#/components/schemas/ElevenLabs'
                  fruitly: '#/components/schemas/Fruitly'
                  gemini-tts: '#/components/schemas/GeminiTTS'
                  google-tts: '#/components/schemas/GoogleTTS'
                  gradium-tts: '#/components/schemas/GradiumTTS'
                  groq-tts: '#/components/schemas/GroqTTS'
                  hume: '#/components/schemas/Hume'
                  inworld: '#/components/schemas/Inworld'
                  lmnt: '#/components/schemas/Lmnt'
                  minimax-tts: '#/components/schemas/MinimaxTTS'
                  munsit: '#/components/schemas/Munsit'
                  neuphonic: '#/components/schemas/Neuphonic'
                  nvidia-tts: '#/components/schemas/NvidiaTTS'
                  openai-tts: '#/components/schemas/OpenAITTS'
                  rime: '#/components/schemas/Rime'
                  sarvam-tts: '#/components/schemas/SarvamTTS'
                  silma: '#/components/schemas/Silma'
                  smallestai: '#/components/schemas/SmallestAI'
                  speechify: '#/components/schemas/Speechify'
                  speechmatics-tts: '#/components/schemas/SpeechmaticsTTS'
                  xai-tts: '#/components/schemas/XaiTTS'
            - type: 'null'
          title: Voice
        transcriber:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AssemblyAI'
                - $ref: '#/components/schemas/AwsTranscribe'
                - $ref: '#/components/schemas/AzureSTT'
                - $ref: '#/components/schemas/AzureOpenAISTT'
                - $ref: '#/components/schemas/CartesiaSTT'
                - $ref: '#/components/schemas/DeepgramSTT'
                - $ref: '#/components/schemas/ElevenLabsSTT'
                - $ref: '#/components/schemas/GoogleSTT'
                - $ref: '#/components/schemas/GradiumSTT'
                - $ref: '#/components/schemas/GroqSTT'
                - $ref: '#/components/schemas/NvidiaSTT'
                - $ref: '#/components/schemas/OpenAISTT'
                - $ref: '#/components/schemas/SarvamSTT'
                - $ref: '#/components/schemas/SonioxSTT'
                - $ref: '#/components/schemas/SpeechmaticsSTT'
              discriminator:
                propertyName: provider
                mapping:
                  assemblyai: '#/components/schemas/AssemblyAI'
                  aws-transcribe: '#/components/schemas/AwsTranscribe'
                  azure-openai-stt: '#/components/schemas/AzureOpenAISTT'
                  azure-stt: '#/components/schemas/AzureSTT'
                  cartesia-stt: '#/components/schemas/CartesiaSTT'
                  deepgram: '#/components/schemas/DeepgramSTT'
                  elevenlabs-stt: '#/components/schemas/ElevenLabsSTT'
                  google-stt: '#/components/schemas/GoogleSTT'
                  gradium-stt: '#/components/schemas/GradiumSTT'
                  groq-stt: '#/components/schemas/GroqSTT'
                  nvidia-stt: '#/components/schemas/NvidiaSTT'
                  openai-stt: '#/components/schemas/OpenAISTT'
                  sarvam-stt: '#/components/schemas/SarvamSTT'
                  soniox: '#/components/schemas/SonioxSTT'
                  speechmatics-stt: '#/components/schemas/SpeechmaticsSTT'
            - type: 'null'
          title: Transcriber
        modelFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/Anthropic'
                - $ref: '#/components/schemas/AwsBedrock'
                - $ref: '#/components/schemas/AzureOpenai'
                - $ref: '#/components/schemas/Baseten'
                - $ref: '#/components/schemas/Cerebras'
                - $ref: '#/components/schemas/CustomOpenAI'
                - $ref: '#/components/schemas/DeepSeek'
                - $ref: '#/components/schemas/Google'
                - $ref: '#/components/schemas/Groq'
                - $ref: '#/components/schemas/Moonshot'
                - $ref: '#/components/schemas/OpenAI'
                - $ref: '#/components/schemas/OpenRouter'
                - $ref: '#/components/schemas/Perplexity'
                - $ref: '#/components/schemas/Xai'
              discriminator:
                propertyName: provider
                mapping:
                  anthropic: '#/components/schemas/Anthropic'
                  aws-bedrock: '#/components/schemas/AwsBedrock'
                  azure-openai: '#/components/schemas/AzureOpenai'
                  baseten: '#/components/schemas/Baseten'
                  cerebras: '#/components/schemas/Cerebras'
                  custom-openai: '#/components/schemas/CustomOpenAI'
                  deepseek: '#/components/schemas/DeepSeek'
                  google: '#/components/schemas/Google'
                  groq: '#/components/schemas/Groq'
                  moonshot: '#/components/schemas/Moonshot'
                  openai: '#/components/schemas/OpenAI'
                  openrouter: '#/components/schemas/OpenRouter'
                  perplexity: '#/components/schemas/Perplexity'
                  xai: '#/components/schemas/Xai'
            - type: 'null'
          title: Modelfallback
          description: >-
            Optional fallback LLM. When the primary LLM provider fails mid-call
            (network error, rate limit, API outage), the agent transparently
            swaps to this provider for the rest of the session. **Custom-mode
            only** (``tier`` must be null) and **pipeline-only** (realtime
            providers are rejected at the schema layer). Credentials are
            resolved from the same org BYO key pool as the primary; no
            platform-credential subsidy. The swap is sticky for the call's
            lifetime — voice agents don't flicker between models per turn.
        voiceFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AsyncAI'
                - $ref: '#/components/schemas/AwsPolly'
                - $ref: '#/components/schemas/AzureTTS'
                - $ref: '#/components/schemas/AzureOpenAITTS'
                - $ref: '#/components/schemas/Cartesia'
                - $ref: '#/components/schemas/DeepgramTTS'
                - $ref: '#/components/schemas/ElevenLabs'
                - $ref: '#/components/schemas/Fruitly'
                - $ref: '#/components/schemas/GeminiTTS'
                - $ref: '#/components/schemas/GoogleTTS'
                - $ref: '#/components/schemas/GradiumTTS'
                - $ref: '#/components/schemas/GroqTTS'
                - $ref: '#/components/schemas/Hume'
                - $ref: '#/components/schemas/Inworld'
                - $ref: '#/components/schemas/Lmnt'
                - $ref: '#/components/schemas/MinimaxTTS'
                - $ref: '#/components/schemas/Munsit'
                - $ref: '#/components/schemas/Neuphonic'
                - $ref: '#/components/schemas/NvidiaTTS'
                - $ref: '#/components/schemas/OpenAITTS'
                - $ref: '#/components/schemas/Rime'
                - $ref: '#/components/schemas/SarvamTTS'
                - $ref: '#/components/schemas/Silma'
                - $ref: '#/components/schemas/SmallestAI'
                - $ref: '#/components/schemas/Speechify'
                - $ref: '#/components/schemas/SpeechmaticsTTS'
                - $ref: '#/components/schemas/XaiTTS'
              discriminator:
                propertyName: provider
                mapping:
                  asyncai: '#/components/schemas/AsyncAI'
                  aws-polly: '#/components/schemas/AwsPolly'
                  azure-openai-tts: '#/components/schemas/AzureOpenAITTS'
                  azure-tts: '#/components/schemas/AzureTTS'
                  cartesia: '#/components/schemas/Cartesia'
                  deepgram-tts: '#/components/schemas/DeepgramTTS'
                  elevenlabs: '#/components/schemas/ElevenLabs'
                  fruitly: '#/components/schemas/Fruitly'
                  gemini-tts: '#/components/schemas/GeminiTTS'
                  google-tts: '#/components/schemas/GoogleTTS'
                  gradium-tts: '#/components/schemas/GradiumTTS'
                  groq-tts: '#/components/schemas/GroqTTS'
                  hume: '#/components/schemas/Hume'
                  inworld: '#/components/schemas/Inworld'
                  lmnt: '#/components/schemas/Lmnt'
                  minimax-tts: '#/components/schemas/MinimaxTTS'
                  munsit: '#/components/schemas/Munsit'
                  neuphonic: '#/components/schemas/Neuphonic'
                  nvidia-tts: '#/components/schemas/NvidiaTTS'
                  openai-tts: '#/components/schemas/OpenAITTS'
                  rime: '#/components/schemas/Rime'
                  sarvam-tts: '#/components/schemas/SarvamTTS'
                  silma: '#/components/schemas/Silma'
                  smallestai: '#/components/schemas/SmallestAI'
                  speechify: '#/components/schemas/Speechify'
                  speechmatics-tts: '#/components/schemas/SpeechmaticsTTS'
                  xai-tts: '#/components/schemas/XaiTTS'
            - type: 'null'
          title: Voicefallback
          description: >-
            Optional fallback TTS voice. When the primary TTS provider fails
            mid-call, the agent synthesises subsequent audio with this voice for
            the rest of the session. **Custom-mode only** and **pipeline-only**.
            Cross-vendor fallback is supported (e.g. ElevenLabs → Cartesia);
            sample-rate mismatch is handled transparently via in-flight
            resampling. Credentials resolve from the same org BYO key pool as
            the primary.
        transcriberFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AssemblyAI'
                - $ref: '#/components/schemas/AwsTranscribe'
                - $ref: '#/components/schemas/AzureSTT'
                - $ref: '#/components/schemas/AzureOpenAISTT'
                - $ref: '#/components/schemas/CartesiaSTT'
                - $ref: '#/components/schemas/DeepgramSTT'
                - $ref: '#/components/schemas/ElevenLabsSTT'
                - $ref: '#/components/schemas/GoogleSTT'
                - $ref: '#/components/schemas/GradiumSTT'
                - $ref: '#/components/schemas/GroqSTT'
                - $ref: '#/components/schemas/NvidiaSTT'
                - $ref: '#/components/schemas/OpenAISTT'
                - $ref: '#/components/schemas/SarvamSTT'
                - $ref: '#/components/schemas/SonioxSTT'
                - $ref: '#/components/schemas/SpeechmaticsSTT'
              discriminator:
                propertyName: provider
                mapping:
                  assemblyai: '#/components/schemas/AssemblyAI'
                  aws-transcribe: '#/components/schemas/AwsTranscribe'
                  azure-openai-stt: '#/components/schemas/AzureOpenAISTT'
                  azure-stt: '#/components/schemas/AzureSTT'
                  cartesia-stt: '#/components/schemas/CartesiaSTT'
                  deepgram: '#/components/schemas/DeepgramSTT'
                  elevenlabs-stt: '#/components/schemas/ElevenLabsSTT'
                  google-stt: '#/components/schemas/GoogleSTT'
                  gradium-stt: '#/components/schemas/GradiumSTT'
                  groq-stt: '#/components/schemas/GroqSTT'
                  nvidia-stt: '#/components/schemas/NvidiaSTT'
                  openai-stt: '#/components/schemas/OpenAISTT'
                  sarvam-stt: '#/components/schemas/SarvamSTT'
                  soniox: '#/components/schemas/SonioxSTT'
                  speechmatics-stt: '#/components/schemas/SpeechmaticsSTT'
            - type: 'null'
          title: Transcriberfallback
          description: >-
            Optional fallback STT transcriber. When the primary STT provider
            fails mid-call, the agent transcribes subsequent audio with this
            provider for the rest of the session. **Custom-mode only** and
            **pipeline-only**. Credentials resolve from the same org BYO key
            pool as the primary.
      additionalProperties: false
      type: object
      required:
        - type
      title: PipelineAssistantCreate
      description: |-
        Create a **pipeline** assistant: separate STT → LLM → TTS components.

        ``model`` accepts only pipeline-LLM providers; ``voice`` and
        ``transcriber`` are required when ``tier`` is null. When ``tier``
        is set (Standard / Gold), all three blocks are server-resolved
        from the tier registry — the client must omit them and supply
        ``voiceCardId`` (plus ``tierLanguage`` for Gold) instead.
    PipelineAssistantCreate-Output:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
        firstMessage:
          anyOf:
            - type: string
            - type: 'null'
          title: Firstmessage
        firstMessageMode:
          type: string
          enum:
            - assistant-speaks-first
            - assistant-speaks-first-with-model-generated-message
            - assistant-waits-for-user
          title: Firstmessagemode
          default: assistant-speaks-first
        firstMessageFallbackSeconds:
          anyOf:
            - type: number
            - type: 'null'
          title: Firstmessagefallbackseconds
          description: >-
            First-response fallback: if the assistant has not spoken within this
            many seconds of the call being **answered**, it opens the
            conversation itself — ``firstMessage`` verbatim when set, otherwise
            a model-generated opener. Fires at most once per call and only when
            the assistant has produced no speech yet, so in
            ``assistant-speaks-first`` modes it acts purely as a safety net for
            a failed greeting. Primarily for ``assistant-waits-for-user``
            assistants, where a silent callee (or a short utterance the model
            declines to answer) would otherwise leave dead air. ``null``
            (default) or ``0`` disables the fallback; otherwise 1-120 seconds.
        tools:
          items:
            anyOf:
              - type: string
              - oneOf:
                  - $ref: '#/components/schemas/FunctionTool-Output'
                  - $ref: '#/components/schemas/EndCallTool-Output'
                  - $ref: '#/components/schemas/TransferCallTool-Output'
                  - $ref: '#/components/schemas/VoicemailTool-Output'
                  - $ref: '#/components/schemas/LiveEscalateTool-Output'
                discriminator:
                  propertyName: type
                  mapping:
                    endCall: '#/components/schemas/EndCallTool-Output'
                    function: '#/components/schemas/FunctionTool-Output'
                    liveEscalate: '#/components/schemas/LiveEscalateTool-Output'
                    transferCall: '#/components/schemas/TransferCallTool-Output'
                    voicemail: '#/components/schemas/VoicemailTool-Output'
          type: array
          title: Tools
          description: >-
            Tools the assistant is allowed to use during a call. Each entry is
            one of two shapes:


            - **String** — id of an existing tool (created via ``POST /tool``).
            Reusable across assistants, scoped to your workspace.

            - **Inline tool object** — a one-off tool definition with the same
            shape ``POST /tool`` accepts (``function``, ``endCall``,
            ``transferCall``, ``voicemail``, or ``liveEscalate``). Useful for
            tools tied to a single assistant — never written to the tools
            collection.
        knowledgeBaseIds:
          items:
            type: string
          type: array
          title: Knowledgebaseids
        kbRetrievalMode:
          anyOf:
            - type: string
              enum:
                - auto
                - tool
            - type: 'null'
          title: Kbretrievalmode
          description: >-
            How knowledge bases are surfaced to the LLM during a call.
            **``auto``** — per-turn pre-prompt injection. Every user turn embeds
            the utterance, searches every linked KB in parallel, and prepends
            the top chunks as a system message before the LLM call.
            Pipeline-mode only. Best for FAQ-style assistants where most user
            turns should hit the KB. **``tool``** — the LLM gets one function
            tool per linked KB and decides when to call it. Works in pipeline
            AND realtime mode. Best for multi-KB assistants and any realtime
            assistant. **``null``** (default) — derive at runtime: pipeline →
            ``auto``, realtime → ``tool``. Tier mode forces the value (Premium →
            ``tool``, Standard / Gold → ``auto``).
        silenceTimeoutSeconds:
          type: number
          maximum: 600
          minimum: 10
          title: Silencetimeoutseconds
          default: 30
        maxDurationSeconds:
          type: integer
          maximum: 43200
          minimum: 5
          title: Maxdurationseconds
          default: 3600
        allowInterruptions:
          type: boolean
          title: Allowinterruptions
          default: true
        preemptiveGeneration:
          type: boolean
          title: Preemptivegeneration
          default: false
        maxToolSteps:
          type: integer
          maximum: 10
          minimum: 1
          title: Maxtoolsteps
          default: 3
        chatMessagesInContext:
          anyOf:
            - type: integer
              maximum: 500
              minimum: 5
            - type: 'null'
          title: Chatmessagesincontext
          description: >-
            Sliding-window size for the chat-session context fed to the LLM on
            each new turn. Counts user + assistant messages. Leave ``null`` to
            use the platform default. Ignored on voice (``Call``) paths.
        backgroundSound:
          anyOf:
            - $ref: '#/components/schemas/BackgroundSoundSchema'
            - type: 'null'
        webhookEvents:
          items:
            type: string
            enum:
              - status-update
              - transcript
              - end-of-call-report
              - chat-session-status-update
              - chat-message
              - chat-session-end-report
          type: array
          title: Webhookevents
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfigSchema'
            - type: 'null'
        analysis:
          anyOf:
            - $ref: '#/components/schemas/AnalysisConfigSchema-Output'
            - type: 'null'
        dataRetention:
          anyOf:
            - $ref: '#/components/schemas/DataRetentionConfigSchema'
            - type: 'null'
        chatDataRetention:
          anyOf:
            - $ref: '#/components/schemas/ChatDataRetentionConfigSchema'
            - type: 'null'
        endpointing:
          anyOf:
            - $ref: '#/components/schemas/EndpointingConfigSchema'
            - type: 'null'
        interruption:
          anyOf:
            - $ref: '#/components/schemas/InterruptionConfigSchema'
            - type: 'null'
        idle:
          anyOf:
            - $ref: '#/components/schemas/IdleConfigSchema'
            - type: 'null'
        recording:
          anyOf:
            - $ref: '#/components/schemas/RecordingConfigSchema'
            - type: 'null'
        monitoring:
          anyOf:
            - $ref: '#/components/schemas/MonitoringConfigSchema'
            - type: 'null'
        textProcessing:
          anyOf:
            - $ref: '#/components/schemas/TextProcessingSchema'
            - type: 'null'
        performance:
          anyOf:
            - $ref: '#/components/schemas/PerformanceSchema'
            - type: 'null'
        telephony:
          anyOf:
            - $ref: '#/components/schemas/TelephonySchema'
            - type: 'null'
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
          description: >-
            Productized tier: ``standard`` (curated pipeline voices),
            ``premium`` (realtime end-to-end audio), or ``gold`` (pipeline with
            the platform's premium voice library). When set, the server resolves
            the underlying provider stack from the tier registry — the client
            must supply ``voiceCardId`` and must NOT supply ``model`` /
            ``voice`` / ``transcriber``. Leave ``null`` to use the per-provider
            Custom flow.
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
          description: >-
            Tier voice-card identifier (e.g. ``"eva"``, ``"holly"``). Required
            when ``tier`` is set; must be ``null`` otherwise. Look up valid ids
            via ``GET /providers/tiers/{tier}/voices``.
        tierLanguage:
          anyOf:
            - type: string
            - type: 'null'
          title: Tierlanguage
          description: >-
            BCP-47 language code for the resolved transcriber. Required when
            ``tier='gold'`` (Gold voices are language-agnostic, so the user
            picks the call language). Ignored for Standard (language is derived
            from the voice card) and Premium (the realtime model handles
            language internally).
        tierSttOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiersttoverrides
          description: >-
            Optional transcription overrides for ``tier='gold'``. Merged on top
            of the tier's default transcriber config. ``provider`` and ``model``
            are tier-locked and silently dropped if supplied. Ignored for
            Standard / Premium.
        tierVoiceOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiervoiceoverrides
          description: >-
            Optional voice tuning overrides for ``tier='gold'``. Merged on top
            of the tier's default voice config so the user can tune stability /
            similarity / style / speed / etc. for the picked voice card.
            ``provider`` / ``model`` / ``voiceId`` are tier-locked and silently
            dropped if supplied. Ignored for Standard / Premium.
        tierRealtimeOverrides:
          anyOf:
            - $ref: '#/components/schemas/TierRealtimeOverrides'
            - type: 'null'
          description: >-
            Optional listening-behaviour tuning for ``tier='premium'``. Adjusts
            how quickly the assistant decides the caller is speaking, how long
            it waits for silence before ending the turn, and the noise-rejection
            padding. Leave any field unset to use the tier's default. Rejected
            on Standard / Gold / Custom.
        tierVersion:
          anyOf:
            - type: integer
            - type: 'null'
          title: Tierversion
          description: >-
            Server-managed tier definition version stamp. Read-only — ignored on
            input. Bumped whenever the tier's underlying provider mapping
            changes; lets a future migration script re-resolve assistants stuck
            on an older version.
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
          description: >-
            System prompt / persona instructions. **Use this field in tier
            mode** — the resolver injects it into the underlying LLM/realtime
            model config server-side. In Custom mode supply the prompt via
            ``model.systemPrompt`` instead (setting both is rejected to keep the
            path unambiguous).
        type:
          type: string
          const: pipeline
          title: Type
        model:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/Anthropic'
                - $ref: '#/components/schemas/AwsBedrock'
                - $ref: '#/components/schemas/AzureOpenai'
                - $ref: '#/components/schemas/Baseten'
                - $ref: '#/components/schemas/Cerebras'
                - $ref: '#/components/schemas/CustomOpenAI'
                - $ref: '#/components/schemas/DeepSeek'
                - $ref: '#/components/schemas/Google'
                - $ref: '#/components/schemas/Groq'
                - $ref: '#/components/schemas/Moonshot'
                - $ref: '#/components/schemas/OpenAI'
                - $ref: '#/components/schemas/OpenRouter'
                - $ref: '#/components/schemas/Perplexity'
                - $ref: '#/components/schemas/Xai'
              discriminator:
                propertyName: provider
                mapping:
                  anthropic: '#/components/schemas/Anthropic'
                  aws-bedrock: '#/components/schemas/AwsBedrock'
                  azure-openai: '#/components/schemas/AzureOpenai'
                  baseten: '#/components/schemas/Baseten'
                  cerebras: '#/components/schemas/Cerebras'
                  custom-openai: '#/components/schemas/CustomOpenAI'
                  deepseek: '#/components/schemas/DeepSeek'
                  google: '#/components/schemas/Google'
                  groq: '#/components/schemas/Groq'
                  moonshot: '#/components/schemas/Moonshot'
                  openai: '#/components/schemas/OpenAI'
                  openrouter: '#/components/schemas/OpenRouter'
                  perplexity: '#/components/schemas/Perplexity'
                  xai: '#/components/schemas/Xai'
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AsyncAI'
                - $ref: '#/components/schemas/AwsPolly'
                - $ref: '#/components/schemas/AzureTTS'
                - $ref: '#/components/schemas/AzureOpenAITTS'
                - $ref: '#/components/schemas/Cartesia'
                - $ref: '#/components/schemas/DeepgramTTS'
                - $ref: '#/components/schemas/ElevenLabs'
                - $ref: '#/components/schemas/Fruitly'
                - $ref: '#/components/schemas/GeminiTTS'
                - $ref: '#/components/schemas/GoogleTTS'
                - $ref: '#/components/schemas/GradiumTTS'
                - $ref: '#/components/schemas/GroqTTS'
                - $ref: '#/components/schemas/Hume'
                - $ref: '#/components/schemas/Inworld'
                - $ref: '#/components/schemas/Lmnt'
                - $ref: '#/components/schemas/MinimaxTTS'
                - $ref: '#/components/schemas/Munsit'
                - $ref: '#/components/schemas/Neuphonic'
                - $ref: '#/components/schemas/NvidiaTTS'
                - $ref: '#/components/schemas/OpenAITTS'
                - $ref: '#/components/schemas/Rime'
                - $ref: '#/components/schemas/SarvamTTS'
                - $ref: '#/components/schemas/Silma'
                - $ref: '#/components/schemas/SmallestAI'
                - $ref: '#/components/schemas/Speechify'
                - $ref: '#/components/schemas/SpeechmaticsTTS'
                - $ref: '#/components/schemas/XaiTTS'
              discriminator:
                propertyName: provider
                mapping:
                  asyncai: '#/components/schemas/AsyncAI'
                  aws-polly: '#/components/schemas/AwsPolly'
                  azure-openai-tts: '#/components/schemas/AzureOpenAITTS'
                  azure-tts: '#/components/schemas/AzureTTS'
                  cartesia: '#/components/schemas/Cartesia'
                  deepgram-tts: '#/components/schemas/DeepgramTTS'
                  elevenlabs: '#/components/schemas/ElevenLabs'
                  fruitly: '#/components/schemas/Fruitly'
                  gemini-tts: '#/components/schemas/GeminiTTS'
                  google-tts: '#/components/schemas/GoogleTTS'
                  gradium-tts: '#/components/schemas/GradiumTTS'
                  groq-tts: '#/components/schemas/GroqTTS'
                  hume: '#/components/schemas/Hume'
                  inworld: '#/components/schemas/Inworld'
                  lmnt: '#/components/schemas/Lmnt'
                  minimax-tts: '#/components/schemas/MinimaxTTS'
                  munsit: '#/components/schemas/Munsit'
                  neuphonic: '#/components/schemas/Neuphonic'
                  nvidia-tts: '#/components/schemas/NvidiaTTS'
                  openai-tts: '#/components/schemas/OpenAITTS'
                  rime: '#/components/schemas/Rime'
                  sarvam-tts: '#/components/schemas/SarvamTTS'
                  silma: '#/components/schemas/Silma'
                  smallestai: '#/components/schemas/SmallestAI'
                  speechify: '#/components/schemas/Speechify'
                  speechmatics-tts: '#/components/schemas/SpeechmaticsTTS'
                  xai-tts: '#/components/schemas/XaiTTS'
            - type: 'null'
          title: Voice
        transcriber:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AssemblyAI'
                - $ref: '#/components/schemas/AwsTranscribe'
                - $ref: '#/components/schemas/AzureSTT'
                - $ref: '#/components/schemas/AzureOpenAISTT'
                - $ref: '#/components/schemas/CartesiaSTT'
                - $ref: '#/components/schemas/DeepgramSTT'
                - $ref: '#/components/schemas/ElevenLabsSTT'
                - $ref: '#/components/schemas/GoogleSTT'
                - $ref: '#/components/schemas/GradiumSTT'
                - $ref: '#/components/schemas/GroqSTT'
                - $ref: '#/components/schemas/NvidiaSTT'
                - $ref: '#/components/schemas/OpenAISTT'
                - $ref: '#/components/schemas/SarvamSTT'
                - $ref: '#/components/schemas/SonioxSTT'
                - $ref: '#/components/schemas/SpeechmaticsSTT'
              discriminator:
                propertyName: provider
                mapping:
                  assemblyai: '#/components/schemas/AssemblyAI'
                  aws-transcribe: '#/components/schemas/AwsTranscribe'
                  azure-openai-stt: '#/components/schemas/AzureOpenAISTT'
                  azure-stt: '#/components/schemas/AzureSTT'
                  cartesia-stt: '#/components/schemas/CartesiaSTT'
                  deepgram: '#/components/schemas/DeepgramSTT'
                  elevenlabs-stt: '#/components/schemas/ElevenLabsSTT'
                  google-stt: '#/components/schemas/GoogleSTT'
                  gradium-stt: '#/components/schemas/GradiumSTT'
                  groq-stt: '#/components/schemas/GroqSTT'
                  nvidia-stt: '#/components/schemas/NvidiaSTT'
                  openai-stt: '#/components/schemas/OpenAISTT'
                  sarvam-stt: '#/components/schemas/SarvamSTT'
                  soniox: '#/components/schemas/SonioxSTT'
                  speechmatics-stt: '#/components/schemas/SpeechmaticsSTT'
            - type: 'null'
          title: Transcriber
        modelFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/Anthropic'
                - $ref: '#/components/schemas/AwsBedrock'
                - $ref: '#/components/schemas/AzureOpenai'
                - $ref: '#/components/schemas/Baseten'
                - $ref: '#/components/schemas/Cerebras'
                - $ref: '#/components/schemas/CustomOpenAI'
                - $ref: '#/components/schemas/DeepSeek'
                - $ref: '#/components/schemas/Google'
                - $ref: '#/components/schemas/Groq'
                - $ref: '#/components/schemas/Moonshot'
                - $ref: '#/components/schemas/OpenAI'
                - $ref: '#/components/schemas/OpenRouter'
                - $ref: '#/components/schemas/Perplexity'
                - $ref: '#/components/schemas/Xai'
              discriminator:
                propertyName: provider
                mapping:
                  anthropic: '#/components/schemas/Anthropic'
                  aws-bedrock: '#/components/schemas/AwsBedrock'
                  azure-openai: '#/components/schemas/AzureOpenai'
                  baseten: '#/components/schemas/Baseten'
                  cerebras: '#/components/schemas/Cerebras'
                  custom-openai: '#/components/schemas/CustomOpenAI'
                  deepseek: '#/components/schemas/DeepSeek'
                  google: '#/components/schemas/Google'
                  groq: '#/components/schemas/Groq'
                  moonshot: '#/components/schemas/Moonshot'
                  openai: '#/components/schemas/OpenAI'
                  openrouter: '#/components/schemas/OpenRouter'
                  perplexity: '#/components/schemas/Perplexity'
                  xai: '#/components/schemas/Xai'
            - type: 'null'
          title: Modelfallback
          description: >-
            Optional fallback LLM. When the primary LLM provider fails mid-call
            (network error, rate limit, API outage), the agent transparently
            swaps to this provider for the rest of the session. **Custom-mode
            only** (``tier`` must be null) and **pipeline-only** (realtime
            providers are rejected at the schema layer). Credentials are
            resolved from the same org BYO key pool as the primary; no
            platform-credential subsidy. The swap is sticky for the call's
            lifetime — voice agents don't flicker between models per turn.
        voiceFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AsyncAI'
                - $ref: '#/components/schemas/AwsPolly'
                - $ref: '#/components/schemas/AzureTTS'
                - $ref: '#/components/schemas/AzureOpenAITTS'
                - $ref: '#/components/schemas/Cartesia'
                - $ref: '#/components/schemas/DeepgramTTS'
                - $ref: '#/components/schemas/ElevenLabs'
                - $ref: '#/components/schemas/Fruitly'
                - $ref: '#/components/schemas/GeminiTTS'
                - $ref: '#/components/schemas/GoogleTTS'
                - $ref: '#/components/schemas/GradiumTTS'
                - $ref: '#/components/schemas/GroqTTS'
                - $ref: '#/components/schemas/Hume'
                - $ref: '#/components/schemas/Inworld'
                - $ref: '#/components/schemas/Lmnt'
                - $ref: '#/components/schemas/MinimaxTTS'
                - $ref: '#/components/schemas/Munsit'
                - $ref: '#/components/schemas/Neuphonic'
                - $ref: '#/components/schemas/NvidiaTTS'
                - $ref: '#/components/schemas/OpenAITTS'
                - $ref: '#/components/schemas/Rime'
                - $ref: '#/components/schemas/SarvamTTS'
                - $ref: '#/components/schemas/Silma'
                - $ref: '#/components/schemas/SmallestAI'
                - $ref: '#/components/schemas/Speechify'
                - $ref: '#/components/schemas/SpeechmaticsTTS'
                - $ref: '#/components/schemas/XaiTTS'
              discriminator:
                propertyName: provider
                mapping:
                  asyncai: '#/components/schemas/AsyncAI'
                  aws-polly: '#/components/schemas/AwsPolly'
                  azure-openai-tts: '#/components/schemas/AzureOpenAITTS'
                  azure-tts: '#/components/schemas/AzureTTS'
                  cartesia: '#/components/schemas/Cartesia'
                  deepgram-tts: '#/components/schemas/DeepgramTTS'
                  elevenlabs: '#/components/schemas/ElevenLabs'
                  fruitly: '#/components/schemas/Fruitly'
                  gemini-tts: '#/components/schemas/GeminiTTS'
                  google-tts: '#/components/schemas/GoogleTTS'
                  gradium-tts: '#/components/schemas/GradiumTTS'
                  groq-tts: '#/components/schemas/GroqTTS'
                  hume: '#/components/schemas/Hume'
                  inworld: '#/components/schemas/Inworld'
                  lmnt: '#/components/schemas/Lmnt'
                  minimax-tts: '#/components/schemas/MinimaxTTS'
                  munsit: '#/components/schemas/Munsit'
                  neuphonic: '#/components/schemas/Neuphonic'
                  nvidia-tts: '#/components/schemas/NvidiaTTS'
                  openai-tts: '#/components/schemas/OpenAITTS'
                  rime: '#/components/schemas/Rime'
                  sarvam-tts: '#/components/schemas/SarvamTTS'
                  silma: '#/components/schemas/Silma'
                  smallestai: '#/components/schemas/SmallestAI'
                  speechify: '#/components/schemas/Speechify'
                  speechmatics-tts: '#/components/schemas/SpeechmaticsTTS'
                  xai-tts: '#/components/schemas/XaiTTS'
            - type: 'null'
          title: Voicefallback
          description: >-
            Optional fallback TTS voice. When the primary TTS provider fails
            mid-call, the agent synthesises subsequent audio with this voice for
            the rest of the session. **Custom-mode only** and **pipeline-only**.
            Cross-vendor fallback is supported (e.g. ElevenLabs → Cartesia);
            sample-rate mismatch is handled transparently via in-flight
            resampling. Credentials resolve from the same org BYO key pool as
            the primary.
        transcriberFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AssemblyAI'
                - $ref: '#/components/schemas/AwsTranscribe'
                - $ref: '#/components/schemas/AzureSTT'
                - $ref: '#/components/schemas/AzureOpenAISTT'
                - $ref: '#/components/schemas/CartesiaSTT'
                - $ref: '#/components/schemas/DeepgramSTT'
                - $ref: '#/components/schemas/ElevenLabsSTT'
                - $ref: '#/components/schemas/GoogleSTT'
                - $ref: '#/components/schemas/GradiumSTT'
                - $ref: '#/components/schemas/GroqSTT'
                - $ref: '#/components/schemas/NvidiaSTT'
                - $ref: '#/components/schemas/OpenAISTT'
                - $ref: '#/components/schemas/SarvamSTT'
                - $ref: '#/components/schemas/SonioxSTT'
                - $ref: '#/components/schemas/SpeechmaticsSTT'
              discriminator:
                propertyName: provider
                mapping:
                  assemblyai: '#/components/schemas/AssemblyAI'
                  aws-transcribe: '#/components/schemas/AwsTranscribe'
                  azure-openai-stt: '#/components/schemas/AzureOpenAISTT'
                  azure-stt: '#/components/schemas/AzureSTT'
                  cartesia-stt: '#/components/schemas/CartesiaSTT'
                  deepgram: '#/components/schemas/DeepgramSTT'
                  elevenlabs-stt: '#/components/schemas/ElevenLabsSTT'
                  google-stt: '#/components/schemas/GoogleSTT'
                  gradium-stt: '#/components/schemas/GradiumSTT'
                  groq-stt: '#/components/schemas/GroqSTT'
                  nvidia-stt: '#/components/schemas/NvidiaSTT'
                  openai-stt: '#/components/schemas/OpenAISTT'
                  sarvam-stt: '#/components/schemas/SarvamSTT'
                  soniox: '#/components/schemas/SonioxSTT'
                  speechmatics-stt: '#/components/schemas/SpeechmaticsSTT'
            - type: 'null'
          title: Transcriberfallback
          description: >-
            Optional fallback STT transcriber. When the primary STT provider
            fails mid-call, the agent transcribes subsequent audio with this
            provider for the rest of the session. **Custom-mode only** and
            **pipeline-only**. Credentials resolve from the same org BYO key
            pool as the primary.
      additionalProperties: false
      type: object
      required:
        - type
      title: PipelineAssistantCreate
      description: |-
        Create a **pipeline** assistant: separate STT → LLM → TTS components.

        ``model`` accepts only pipeline-LLM providers; ``voice`` and
        ``transcriber`` are required when ``tier`` is null. When ``tier``
        is set (Standard / Gold), all three blocks are server-resolved
        from the tier registry — the client must omit them and supply
        ``voiceCardId`` (plus ``tierLanguage`` for Gold) instead.
    PipelineAssistantResponse:
      properties:
        id:
          type: string
          title: Id
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
        firstMessage:
          anyOf:
            - type: string
            - type: 'null'
          title: Firstmessage
        firstMessageMode:
          type: string
          enum:
            - assistant-speaks-first
            - assistant-speaks-first-with-model-generated-message
            - assistant-waits-for-user
          title: Firstmessagemode
          default: assistant-speaks-first
        firstMessageFallbackSeconds:
          anyOf:
            - type: number
            - type: 'null'
          title: Firstmessagefallbackseconds
        tools:
          items:
            anyOf:
              - type: string
              - oneOf:
                  - $ref: '#/components/schemas/FunctionTool-Output'
                  - $ref: '#/components/schemas/EndCallTool-Output'
                  - $ref: '#/components/schemas/TransferCallTool-Output'
                  - $ref: '#/components/schemas/VoicemailTool-Output'
                  - $ref: '#/components/schemas/LiveEscalateTool-Output'
                discriminator:
                  propertyName: type
                  mapping:
                    endCall: '#/components/schemas/EndCallTool-Output'
                    function: '#/components/schemas/FunctionTool-Output'
                    liveEscalate: '#/components/schemas/LiveEscalateTool-Output'
                    transferCall: '#/components/schemas/TransferCallTool-Output'
                    voicemail: '#/components/schemas/VoicemailTool-Output'
          type: array
          title: Tools
          description: >-
            Tools the assistant is allowed to use during a call. Each entry is
            one of two shapes:


            - **String** — id of an existing tool (created via ``POST /tool``).
            Reusable across assistants, scoped to your workspace.

            - **Inline tool object** — a one-off tool definition with the same
            shape ``POST /tool`` accepts (``function``, ``endCall``,
            ``transferCall``, ``voicemail``, or ``liveEscalate``). Useful for
            tools tied to a single assistant — never written to the tools
            collection.
        knowledgeBaseIds:
          items:
            type: string
          type: array
          title: Knowledgebaseids
        kbRetrievalMode:
          anyOf:
            - type: string
              enum:
                - auto
                - tool
            - type: 'null'
          title: Kbretrievalmode
          description: >-
            Effective KB retrieval mode for this assistant. ``null`` for legacy
            assistants — the agent derives the mode from ``type`` at runtime.
            Tier-mode assistants always carry an explicit value.
        silenceTimeoutSeconds:
          type: number
          title: Silencetimeoutseconds
          default: 30
        maxDurationSeconds:
          type: integer
          title: Maxdurationseconds
          default: 3600
        allowInterruptions:
          type: boolean
          title: Allowinterruptions
          default: true
        preemptiveGeneration:
          type: boolean
          title: Preemptivegeneration
          default: false
        maxToolSteps:
          type: integer
          title: Maxtoolsteps
          default: 3
        chatMessagesInContext:
          anyOf:
            - type: integer
            - type: 'null'
          title: Chatmessagesincontext
        backgroundSound:
          anyOf:
            - $ref: '#/components/schemas/BackgroundSoundSchema'
            - type: 'null'
        webhookEvents:
          items:
            type: string
            enum:
              - status-update
              - transcript
              - end-of-call-report
              - chat-session-status-update
              - chat-message
              - chat-session-end-report
          type: array
          title: Webhookevents
        server:
          anyOf:
            - $ref: >-
                #/components/schemas/src__api__schemas__assistant__ServerConfigInfo
            - type: 'null'
        analysis:
          anyOf:
            - $ref: '#/components/schemas/AnalysisConfigSchema-Output'
            - type: 'null'
        dataRetention:
          anyOf:
            - $ref: '#/components/schemas/DataRetentionConfigSchema'
            - type: 'null'
        chatDataRetention:
          anyOf:
            - $ref: '#/components/schemas/ChatDataRetentionConfigSchema'
            - type: 'null'
        endpointing:
          anyOf:
            - $ref: '#/components/schemas/EndpointingConfigSchema'
            - type: 'null'
        interruption:
          anyOf:
            - $ref: '#/components/schemas/InterruptionConfigSchema'
            - type: 'null'
        idle:
          anyOf:
            - $ref: '#/components/schemas/IdleConfigSchema'
            - type: 'null'
        recording:
          anyOf:
            - $ref: '#/components/schemas/RecordingConfigSchema'
            - type: 'null'
        monitoring:
          anyOf:
            - $ref: '#/components/schemas/MonitoringConfigSchema'
            - type: 'null'
        textProcessing:
          anyOf:
            - $ref: '#/components/schemas/TextProcessingSchema'
            - type: 'null'
        performance:
          anyOf:
            - $ref: '#/components/schemas/PerformanceSchema'
            - type: 'null'
        telephony:
          anyOf:
            - $ref: '#/components/schemas/TelephonySchema'
            - type: 'null'
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
        tierLanguage:
          anyOf:
            - type: string
            - type: 'null'
          title: Tierlanguage
        tierSttOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiersttoverrides
        tierVoiceOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiervoiceoverrides
        tierRealtimeOverrides:
          anyOf:
            - $ref: '#/components/schemas/TierRealtimeOverrides'
            - type: 'null'
        tierVersion:
          anyOf:
            - type: integer
            - type: 'null'
          title: Tierversion
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        type:
          type: string
          const: pipeline
          title: Type
        model:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/Anthropic'
                - $ref: '#/components/schemas/AwsBedrock'
                - $ref: '#/components/schemas/AzureOpenai'
                - $ref: '#/components/schemas/Baseten'
                - $ref: '#/components/schemas/Cerebras'
                - $ref: '#/components/schemas/CustomOpenAI'
                - $ref: '#/components/schemas/DeepSeek'
                - $ref: '#/components/schemas/Google'
                - $ref: '#/components/schemas/Groq'
                - $ref: '#/components/schemas/Moonshot'
                - $ref: '#/components/schemas/OpenAI'
                - $ref: '#/components/schemas/OpenRouter'
                - $ref: '#/components/schemas/Perplexity'
                - $ref: '#/components/schemas/Xai'
              discriminator:
                propertyName: provider
                mapping:
                  anthropic: '#/components/schemas/Anthropic'
                  aws-bedrock: '#/components/schemas/AwsBedrock'
                  azure-openai: '#/components/schemas/AzureOpenai'
                  baseten: '#/components/schemas/Baseten'
                  cerebras: '#/components/schemas/Cerebras'
                  custom-openai: '#/components/schemas/CustomOpenAI'
                  deepseek: '#/components/schemas/DeepSeek'
                  google: '#/components/schemas/Google'
                  groq: '#/components/schemas/Groq'
                  moonshot: '#/components/schemas/Moonshot'
                  openai: '#/components/schemas/OpenAI'
                  openrouter: '#/components/schemas/OpenRouter'
                  perplexity: '#/components/schemas/Perplexity'
                  xai: '#/components/schemas/Xai'
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AsyncAI'
                - $ref: '#/components/schemas/AwsPolly'
                - $ref: '#/components/schemas/AzureTTS'
                - $ref: '#/components/schemas/AzureOpenAITTS'
                - $ref: '#/components/schemas/Cartesia'
                - $ref: '#/components/schemas/DeepgramTTS'
                - $ref: '#/components/schemas/ElevenLabs'
                - $ref: '#/components/schemas/Fruitly'
                - $ref: '#/components/schemas/GeminiTTS'
                - $ref: '#/components/schemas/GoogleTTS'
                - $ref: '#/components/schemas/GradiumTTS'
                - $ref: '#/components/schemas/GroqTTS'
                - $ref: '#/components/schemas/Hume'
                - $ref: '#/components/schemas/Inworld'
                - $ref: '#/components/schemas/Lmnt'
                - $ref: '#/components/schemas/MinimaxTTS'
                - $ref: '#/components/schemas/Munsit'
                - $ref: '#/components/schemas/Neuphonic'
                - $ref: '#/components/schemas/NvidiaTTS'
                - $ref: '#/components/schemas/OpenAITTS'
                - $ref: '#/components/schemas/Rime'
                - $ref: '#/components/schemas/SarvamTTS'
                - $ref: '#/components/schemas/Silma'
                - $ref: '#/components/schemas/SmallestAI'
                - $ref: '#/components/schemas/Speechify'
                - $ref: '#/components/schemas/SpeechmaticsTTS'
                - $ref: '#/components/schemas/XaiTTS'
              discriminator:
                propertyName: provider
                mapping:
                  asyncai: '#/components/schemas/AsyncAI'
                  aws-polly: '#/components/schemas/AwsPolly'
                  azure-openai-tts: '#/components/schemas/AzureOpenAITTS'
                  azure-tts: '#/components/schemas/AzureTTS'
                  cartesia: '#/components/schemas/Cartesia'
                  deepgram-tts: '#/components/schemas/DeepgramTTS'
                  elevenlabs: '#/components/schemas/ElevenLabs'
                  fruitly: '#/components/schemas/Fruitly'
                  gemini-tts: '#/components/schemas/GeminiTTS'
                  google-tts: '#/components/schemas/GoogleTTS'
                  gradium-tts: '#/components/schemas/GradiumTTS'
                  groq-tts: '#/components/schemas/GroqTTS'
                  hume: '#/components/schemas/Hume'
                  inworld: '#/components/schemas/Inworld'
                  lmnt: '#/components/schemas/Lmnt'
                  minimax-tts: '#/components/schemas/MinimaxTTS'
                  munsit: '#/components/schemas/Munsit'
                  neuphonic: '#/components/schemas/Neuphonic'
                  nvidia-tts: '#/components/schemas/NvidiaTTS'
                  openai-tts: '#/components/schemas/OpenAITTS'
                  rime: '#/components/schemas/Rime'
                  sarvam-tts: '#/components/schemas/SarvamTTS'
                  silma: '#/components/schemas/Silma'
                  smallestai: '#/components/schemas/SmallestAI'
                  speechify: '#/components/schemas/Speechify'
                  speechmatics-tts: '#/components/schemas/SpeechmaticsTTS'
                  xai-tts: '#/components/schemas/XaiTTS'
            - type: 'null'
          title: Voice
        transcriber:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AssemblyAI'
                - $ref: '#/components/schemas/AwsTranscribe'
                - $ref: '#/components/schemas/AzureSTT'
                - $ref: '#/components/schemas/AzureOpenAISTT'
                - $ref: '#/components/schemas/CartesiaSTT'
                - $ref: '#/components/schemas/DeepgramSTT'
                - $ref: '#/components/schemas/ElevenLabsSTT'
                - $ref: '#/components/schemas/GoogleSTT'
                - $ref: '#/components/schemas/GradiumSTT'
                - $ref: '#/components/schemas/GroqSTT'
                - $ref: '#/components/schemas/NvidiaSTT'
                - $ref: '#/components/schemas/OpenAISTT'
                - $ref: '#/components/schemas/SarvamSTT'
                - $ref: '#/components/schemas/SonioxSTT'
                - $ref: '#/components/schemas/SpeechmaticsSTT'
              discriminator:
                propertyName: provider
                mapping:
                  assemblyai: '#/components/schemas/AssemblyAI'
                  aws-transcribe: '#/components/schemas/AwsTranscribe'
                  azure-openai-stt: '#/components/schemas/AzureOpenAISTT'
                  azure-stt: '#/components/schemas/AzureSTT'
                  cartesia-stt: '#/components/schemas/CartesiaSTT'
                  deepgram: '#/components/schemas/DeepgramSTT'
                  elevenlabs-stt: '#/components/schemas/ElevenLabsSTT'
                  google-stt: '#/components/schemas/GoogleSTT'
                  gradium-stt: '#/components/schemas/GradiumSTT'
                  groq-stt: '#/components/schemas/GroqSTT'
                  nvidia-stt: '#/components/schemas/NvidiaSTT'
                  openai-stt: '#/components/schemas/OpenAISTT'
                  sarvam-stt: '#/components/schemas/SarvamSTT'
                  soniox: '#/components/schemas/SonioxSTT'
                  speechmatics-stt: '#/components/schemas/SpeechmaticsSTT'
            - type: 'null'
          title: Transcriber
        modelFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/Anthropic'
                - $ref: '#/components/schemas/AwsBedrock'
                - $ref: '#/components/schemas/AzureOpenai'
                - $ref: '#/components/schemas/Baseten'
                - $ref: '#/components/schemas/Cerebras'
                - $ref: '#/components/schemas/CustomOpenAI'
                - $ref: '#/components/schemas/DeepSeek'
                - $ref: '#/components/schemas/Google'
                - $ref: '#/components/schemas/Groq'
                - $ref: '#/components/schemas/Moonshot'
                - $ref: '#/components/schemas/OpenAI'
                - $ref: '#/components/schemas/OpenRouter'
                - $ref: '#/components/schemas/Perplexity'
                - $ref: '#/components/schemas/Xai'
              discriminator:
                propertyName: provider
                mapping:
                  anthropic: '#/components/schemas/Anthropic'
                  aws-bedrock: '#/components/schemas/AwsBedrock'
                  azure-openai: '#/components/schemas/AzureOpenai'
                  baseten: '#/components/schemas/Baseten'
                  cerebras: '#/components/schemas/Cerebras'
                  custom-openai: '#/components/schemas/CustomOpenAI'
                  deepseek: '#/components/schemas/DeepSeek'
                  google: '#/components/schemas/Google'
                  groq: '#/components/schemas/Groq'
                  moonshot: '#/components/schemas/Moonshot'
                  openai: '#/components/schemas/OpenAI'
                  openrouter: '#/components/schemas/OpenRouter'
                  perplexity: '#/components/schemas/Perplexity'
                  xai: '#/components/schemas/Xai'
            - type: 'null'
          title: Modelfallback
          description: >-
            Configured fallback LLM. ``None`` means the assistant runs without
            LLM failover (or is a tier-mode / realtime assistant — those
            server-derive or omit fallback entirely).
        voiceFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AsyncAI'
                - $ref: '#/components/schemas/AwsPolly'
                - $ref: '#/components/schemas/AzureTTS'
                - $ref: '#/components/schemas/AzureOpenAITTS'
                - $ref: '#/components/schemas/Cartesia'
                - $ref: '#/components/schemas/DeepgramTTS'
                - $ref: '#/components/schemas/ElevenLabs'
                - $ref: '#/components/schemas/Fruitly'
                - $ref: '#/components/schemas/GeminiTTS'
                - $ref: '#/components/schemas/GoogleTTS'
                - $ref: '#/components/schemas/GradiumTTS'
                - $ref: '#/components/schemas/GroqTTS'
                - $ref: '#/components/schemas/Hume'
                - $ref: '#/components/schemas/Inworld'
                - $ref: '#/components/schemas/Lmnt'
                - $ref: '#/components/schemas/MinimaxTTS'
                - $ref: '#/components/schemas/Munsit'
                - $ref: '#/components/schemas/Neuphonic'
                - $ref: '#/components/schemas/NvidiaTTS'
                - $ref: '#/components/schemas/OpenAITTS'
                - $ref: '#/components/schemas/Rime'
                - $ref: '#/components/schemas/SarvamTTS'
                - $ref: '#/components/schemas/Silma'
                - $ref: '#/components/schemas/SmallestAI'
                - $ref: '#/components/schemas/Speechify'
                - $ref: '#/components/schemas/SpeechmaticsTTS'
                - $ref: '#/components/schemas/XaiTTS'
              discriminator:
                propertyName: provider
                mapping:
                  asyncai: '#/components/schemas/AsyncAI'
                  aws-polly: '#/components/schemas/AwsPolly'
                  azure-openai-tts: '#/components/schemas/AzureOpenAITTS'
                  azure-tts: '#/components/schemas/AzureTTS'
                  cartesia: '#/components/schemas/Cartesia'
                  deepgram-tts: '#/components/schemas/DeepgramTTS'
                  elevenlabs: '#/components/schemas/ElevenLabs'
                  fruitly: '#/components/schemas/Fruitly'
                  gemini-tts: '#/components/schemas/GeminiTTS'
                  google-tts: '#/components/schemas/GoogleTTS'
                  gradium-tts: '#/components/schemas/GradiumTTS'
                  groq-tts: '#/components/schemas/GroqTTS'
                  hume: '#/components/schemas/Hume'
                  inworld: '#/components/schemas/Inworld'
                  lmnt: '#/components/schemas/Lmnt'
                  minimax-tts: '#/components/schemas/MinimaxTTS'
                  munsit: '#/components/schemas/Munsit'
                  neuphonic: '#/components/schemas/Neuphonic'
                  nvidia-tts: '#/components/schemas/NvidiaTTS'
                  openai-tts: '#/components/schemas/OpenAITTS'
                  rime: '#/components/schemas/Rime'
                  sarvam-tts: '#/components/schemas/SarvamTTS'
                  silma: '#/components/schemas/Silma'
                  smallestai: '#/components/schemas/SmallestAI'
                  speechify: '#/components/schemas/Speechify'
                  speechmatics-tts: '#/components/schemas/SpeechmaticsTTS'
                  xai-tts: '#/components/schemas/XaiTTS'
            - type: 'null'
          title: Voicefallback
          description: >-
            Configured fallback TTS voice. ``None`` means no TTS failover
            configured.
        transcriberFallback:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AssemblyAI'
                - $ref: '#/components/schemas/AwsTranscribe'
                - $ref: '#/components/schemas/AzureSTT'
                - $ref: '#/components/schemas/AzureOpenAISTT'
                - $ref: '#/components/schemas/CartesiaSTT'
                - $ref: '#/components/schemas/DeepgramSTT'
                - $ref: '#/components/schemas/ElevenLabsSTT'
                - $ref: '#/components/schemas/GoogleSTT'
                - $ref: '#/components/schemas/GradiumSTT'
                - $ref: '#/components/schemas/GroqSTT'
                - $ref: '#/components/schemas/NvidiaSTT'
                - $ref: '#/components/schemas/OpenAISTT'
                - $ref: '#/components/schemas/SarvamSTT'
                - $ref: '#/components/schemas/SonioxSTT'
                - $ref: '#/components/schemas/SpeechmaticsSTT'
              discriminator:
                propertyName: provider
                mapping:
                  assemblyai: '#/components/schemas/AssemblyAI'
                  aws-transcribe: '#/components/schemas/AwsTranscribe'
                  azure-openai-stt: '#/components/schemas/AzureOpenAISTT'
                  azure-stt: '#/components/schemas/AzureSTT'
                  cartesia-stt: '#/components/schemas/CartesiaSTT'
                  deepgram: '#/components/schemas/DeepgramSTT'
                  elevenlabs-stt: '#/components/schemas/ElevenLabsSTT'
                  google-stt: '#/components/schemas/GoogleSTT'
                  gradium-stt: '#/components/schemas/GradiumSTT'
                  groq-stt: '#/components/schemas/GroqSTT'
                  nvidia-stt: '#/components/schemas/NvidiaSTT'
                  openai-stt: '#/components/schemas/OpenAISTT'
                  sarvam-stt: '#/components/schemas/SarvamSTT'
                  soniox: '#/components/schemas/SonioxSTT'
                  speechmatics-stt: '#/components/schemas/SpeechmaticsSTT'
            - type: 'null'
          title: Transcriberfallback
          description: >-
            Configured fallback STT transcriber. ``None`` means no STT failover
            configured.
      type: object
      required:
        - id
        - type
      title: PipelineAssistantResponse
      description: |-
        A pipeline assistant returned by GET / list endpoints.

        For Custom-mode assistants ``model`` / ``voice`` / ``transcriber``
        (and the optional fallback variants) are always populated. For
        tier-mode assistants these are intentionally **omitted from the
        response** so the customer doesn't see the underlying provider
        stack — the resolved configs are still on the persisted document
        (the agent reads them directly), just not on the wire to the
        customer. Fallback configs follow the same masking rule:
        tier-mode never carries them (validated at write time) so they're
        always ``None`` in tier responses anyway, but documenting the
        contract makes the masking explicit.
    PreviousToolCallItem:
      properties:
        name:
          type: string
          title: Name
          description: Tool name as registered on the assistant.
        toolCallId:
          type: string
          title: Toolcallid
          description: >-
            SDK-issued correlation id. Matches the ``toolCallId`` on the paired
            ``messages[]`` entries.
        arguments:
          additionalProperties: true
          type: object
          title: Arguments
          description: Structured arguments the LLM produced for this invocation.
        result:
          anyOf:
            - type: string
            - type: 'null'
          title: Result
          description: >-
            Exact string the tool's webhook returned (for tool-fired function
            tools, this is the JSON-stringified response body). ``null`` if the
            call returned an empty body.
        isError:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Iserror
          description: True when the prior tool raised.
        calledAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Calledat
          description: When the LLM invoked the tool (UTC ISO-8601).
        completedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Completedat
          description: When the tool's result was added to the thread (UTC ISO-8601).
      type: object
      required:
        - name
        - toolCallId
        - arguments
      title: PreviousToolCallItem
      description: |-
        Denormalized prior tool invocation paired with its result.

        The same data is also available interleaved with conversation
        turns inside ``messages[]`` (as ``role:"tool_calls"`` + matching
        ``role:"tool"`` entries linked by ``toolCallId``).
        ``previousToolCalls`` is a convenience view for receivers that
        just want "what tools have already run on this call, in order"
        without reconstructing the chronology themselves.

        Only **completed** tool calls are included — entries currently
        in flight (or that never returned) are omitted, since the result
        is not yet available. The CURRENT invocation (the one this
        webhook is firing for) is also omitted; it lives on
        ``toolCalls[]``.
    QuickReplyButton:
      properties:
        type:
          type: string
          const: QUICK_REPLY
          title: Type
        text:
          type: string
          maxLength: 25
          minLength: 1
          title: Text
      additionalProperties: false
      type: object
      required:
        - type
        - text
      title: QuickReplyButton
    QuickReplyCreateRequest:
      properties:
        name:
          type: string
          maxLength: 100
          minLength: 1
          title: Name
        body:
          type: string
          maxLength: 4000
          minLength: 1
          title: Body
        tags:
          items:
            type: string
          type: array
          title: Tags
        category:
          anyOf:
            - type: string
            - type: 'null'
          title: Category
        sharedAcrossOrg:
          type: boolean
          title: Sharedacrossorg
          default: true
      additionalProperties: false
      type: object
      required:
        - name
        - body
      title: QuickReplyCreateRequest
    QuickReplyPatchRequest:
      properties:
        name:
          anyOf:
            - type: string
              maxLength: 100
              minLength: 1
            - type: 'null'
          title: Name
        body:
          anyOf:
            - type: string
              maxLength: 4000
              minLength: 1
            - type: 'null'
          title: Body
        tags:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Tags
        category:
          anyOf:
            - type: string
            - type: 'null'
          title: Category
        sharedAcrossOrg:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Sharedacrossorg
      additionalProperties: false
      type: object
      title: QuickReplyPatchRequest
    QuickReplyResponse:
      properties:
        id:
          type: string
          title: Id
        tenantId:
          type: string
          title: Tenantid
        organizationId:
          type: string
          title: Organizationid
        name:
          type: string
          title: Name
        body:
          type: string
          title: Body
        tags:
          items:
            type: string
          type: array
          title: Tags
        category:
          anyOf:
            - type: string
            - type: 'null'
          title: Category
        sharedAcrossOrg:
          type: boolean
          title: Sharedacrossorg
          default: true
        usageCount:
          type: integer
          title: Usagecount
          default: 0
        lastUsedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Lastusedat
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - name
        - body
        - createdAt
        - updatedAt
      title: QuickReplyResponse
    ReactionPayload:
      properties:
        messageId:
          type: string
          title: Messageid
          examples:
            - b4c8e2a1-6f3d-4e79-8c25-91a0d7f3e6b2
        emoji:
          type: string
          title: Emoji
          examples:
            - 👍
      additionalProperties: false
      type: object
      required:
        - messageId
        - emoji
      title: ReactionPayload
    ReactionRequest:
      properties:
        emoji:
          type: string
          maxLength: 8
          minLength: 1
          title: Emoji
      additionalProperties: false
      type: object
      required:
        - emoji
      title: ReactionRequest
    RealtimeAssistantCreate-Input:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
        firstMessage:
          anyOf:
            - type: string
            - type: 'null'
          title: Firstmessage
        firstMessageMode:
          type: string
          enum:
            - assistant-speaks-first
            - assistant-speaks-first-with-model-generated-message
            - assistant-waits-for-user
          title: Firstmessagemode
          default: assistant-speaks-first
        firstMessageFallbackSeconds:
          anyOf:
            - type: number
            - type: 'null'
          title: Firstmessagefallbackseconds
          description: >-
            First-response fallback: if the assistant has not spoken within this
            many seconds of the call being **answered**, it opens the
            conversation itself — ``firstMessage`` verbatim when set, otherwise
            a model-generated opener. Fires at most once per call and only when
            the assistant has produced no speech yet, so in
            ``assistant-speaks-first`` modes it acts purely as a safety net for
            a failed greeting. Primarily for ``assistant-waits-for-user``
            assistants, where a silent callee (or a short utterance the model
            declines to answer) would otherwise leave dead air. ``null``
            (default) or ``0`` disables the fallback; otherwise 1-120 seconds.
        tools:
          items:
            anyOf:
              - type: string
              - oneOf:
                  - $ref: '#/components/schemas/FunctionTool-Input'
                  - $ref: '#/components/schemas/EndCallTool-Input'
                  - $ref: '#/components/schemas/TransferCallTool-Input'
                  - $ref: '#/components/schemas/VoicemailTool-Input'
                  - $ref: '#/components/schemas/LiveEscalateTool-Input'
                discriminator:
                  propertyName: type
                  mapping:
                    endCall: '#/components/schemas/EndCallTool-Input'
                    function: '#/components/schemas/FunctionTool-Input'
                    liveEscalate: '#/components/schemas/LiveEscalateTool-Input'
                    transferCall: '#/components/schemas/TransferCallTool-Input'
                    voicemail: '#/components/schemas/VoicemailTool-Input'
          type: array
          title: Tools
          description: >-
            Tools the assistant is allowed to use during a call. Each entry is
            one of two shapes:


            - **String** — id of an existing tool (created via ``POST /tool``).
            Reusable across assistants, scoped to your workspace.

            - **Inline tool object** — a one-off tool definition with the same
            shape ``POST /tool`` accepts (``function``, ``endCall``,
            ``transferCall``, ``voicemail``, or ``liveEscalate``). Useful for
            tools tied to a single assistant — never written to the tools
            collection.
        knowledgeBaseIds:
          items:
            type: string
          type: array
          title: Knowledgebaseids
        kbRetrievalMode:
          anyOf:
            - type: string
              enum:
                - auto
                - tool
            - type: 'null'
          title: Kbretrievalmode
          description: >-
            How knowledge bases are surfaced to the LLM during a call.
            **``auto``** — per-turn pre-prompt injection. Every user turn embeds
            the utterance, searches every linked KB in parallel, and prepends
            the top chunks as a system message before the LLM call.
            Pipeline-mode only. Best for FAQ-style assistants where most user
            turns should hit the KB. **``tool``** — the LLM gets one function
            tool per linked KB and decides when to call it. Works in pipeline
            AND realtime mode. Best for multi-KB assistants and any realtime
            assistant. **``null``** (default) — derive at runtime: pipeline →
            ``auto``, realtime → ``tool``. Tier mode forces the value (Premium →
            ``tool``, Standard / Gold → ``auto``).
        silenceTimeoutSeconds:
          type: number
          maximum: 600
          minimum: 10
          title: Silencetimeoutseconds
          default: 30
        maxDurationSeconds:
          type: integer
          maximum: 43200
          minimum: 5
          title: Maxdurationseconds
          default: 3600
        allowInterruptions:
          type: boolean
          title: Allowinterruptions
          default: true
        preemptiveGeneration:
          type: boolean
          title: Preemptivegeneration
          default: false
        maxToolSteps:
          type: integer
          maximum: 10
          minimum: 1
          title: Maxtoolsteps
          default: 3
        chatMessagesInContext:
          anyOf:
            - type: integer
              maximum: 500
              minimum: 5
            - type: 'null'
          title: Chatmessagesincontext
          description: >-
            Sliding-window size for the chat-session context fed to the LLM on
            each new turn. Counts user + assistant messages. Leave ``null`` to
            use the platform default. Ignored on voice (``Call``) paths.
        backgroundSound:
          anyOf:
            - $ref: '#/components/schemas/BackgroundSoundSchema'
            - type: 'null'
        webhookEvents:
          items:
            type: string
            enum:
              - status-update
              - transcript
              - end-of-call-report
              - chat-session-status-update
              - chat-message
              - chat-session-end-report
          type: array
          title: Webhookevents
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfigSchema'
            - type: 'null'
        analysis:
          anyOf:
            - $ref: '#/components/schemas/AnalysisConfigSchema-Input'
            - type: 'null'
        dataRetention:
          anyOf:
            - $ref: '#/components/schemas/DataRetentionConfigSchema'
            - type: 'null'
        chatDataRetention:
          anyOf:
            - $ref: '#/components/schemas/ChatDataRetentionConfigSchema'
            - type: 'null'
        endpointing:
          anyOf:
            - $ref: '#/components/schemas/EndpointingConfigSchema'
            - type: 'null'
        interruption:
          anyOf:
            - $ref: '#/components/schemas/InterruptionConfigSchema'
            - type: 'null'
        idle:
          anyOf:
            - $ref: '#/components/schemas/IdleConfigSchema'
            - type: 'null'
        recording:
          anyOf:
            - $ref: '#/components/schemas/RecordingConfigSchema'
            - type: 'null'
        monitoring:
          anyOf:
            - $ref: '#/components/schemas/MonitoringConfigSchema'
            - type: 'null'
        textProcessing:
          anyOf:
            - $ref: '#/components/schemas/TextProcessingSchema'
            - type: 'null'
        performance:
          anyOf:
            - $ref: '#/components/schemas/PerformanceSchema'
            - type: 'null'
        telephony:
          anyOf:
            - $ref: '#/components/schemas/TelephonySchema'
            - type: 'null'
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
          description: >-
            Productized tier: ``standard`` (curated pipeline voices),
            ``premium`` (realtime end-to-end audio), or ``gold`` (pipeline with
            the platform's premium voice library). When set, the server resolves
            the underlying provider stack from the tier registry — the client
            must supply ``voiceCardId`` and must NOT supply ``model`` /
            ``voice`` / ``transcriber``. Leave ``null`` to use the per-provider
            Custom flow.
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
          description: >-
            Tier voice-card identifier (e.g. ``"eva"``, ``"holly"``). Required
            when ``tier`` is set; must be ``null`` otherwise. Look up valid ids
            via ``GET /providers/tiers/{tier}/voices``.
        tierLanguage:
          anyOf:
            - type: string
            - type: 'null'
          title: Tierlanguage
          description: >-
            BCP-47 language code for the resolved transcriber. Required when
            ``tier='gold'`` (Gold voices are language-agnostic, so the user
            picks the call language). Ignored for Standard (language is derived
            from the voice card) and Premium (the realtime model handles
            language internally).
        tierSttOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiersttoverrides
          description: >-
            Optional transcription overrides for ``tier='gold'``. Merged on top
            of the tier's default transcriber config. ``provider`` and ``model``
            are tier-locked and silently dropped if supplied. Ignored for
            Standard / Premium.
        tierVoiceOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiervoiceoverrides
          description: >-
            Optional voice tuning overrides for ``tier='gold'``. Merged on top
            of the tier's default voice config so the user can tune stability /
            similarity / style / speed / etc. for the picked voice card.
            ``provider`` / ``model`` / ``voiceId`` are tier-locked and silently
            dropped if supplied. Ignored for Standard / Premium.
        tierRealtimeOverrides:
          anyOf:
            - $ref: '#/components/schemas/TierRealtimeOverrides'
            - type: 'null'
          description: >-
            Optional listening-behaviour tuning for ``tier='premium'``. Adjusts
            how quickly the assistant decides the caller is speaking, how long
            it waits for silence before ending the turn, and the noise-rejection
            padding. Leave any field unset to use the tier's default. Rejected
            on Standard / Gold / Custom.
        tierVersion:
          anyOf:
            - type: integer
            - type: 'null'
          title: Tierversion
          description: >-
            Server-managed tier definition version stamp. Read-only — ignored on
            input. Bumped whenever the tier's underlying provider mapping
            changes; lets a future migration script re-resolve assistants stuck
            on an older version.
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
          description: >-
            System prompt / persona instructions. **Use this field in tier
            mode** — the resolver injects it into the underlying LLM/realtime
            model config server-side. In Custom mode supply the prompt via
            ``model.systemPrompt`` instead (setting both is rejected to keep the
            path unambiguous).
        type:
          type: string
          const: realtime
          title: Type
        model:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AwsNovaSonic'
                - $ref: '#/components/schemas/OpenAIRealtime'
                - $ref: '#/components/schemas/AzureOpenAIRealtime'
                - $ref: '#/components/schemas/DeepslateRealtime'
                - $ref: '#/components/schemas/GoogleRealtimeConfig-Input'
                - $ref: '#/components/schemas/PhonicRealtime'
                - $ref: '#/components/schemas/UltravoxRealtime'
                - $ref: '#/components/schemas/XaiRealtime'
              discriminator:
                propertyName: provider
                mapping:
                  aws-nova-sonic: '#/components/schemas/AwsNovaSonic'
                  azure-openai-realtime: '#/components/schemas/AzureOpenAIRealtime'
                  deepslate-realtime: '#/components/schemas/DeepslateRealtime'
                  google-realtime: '#/components/schemas/GoogleRealtimeConfig-Input'
                  openai-realtime: '#/components/schemas/OpenAIRealtime'
                  phonic-realtime: '#/components/schemas/PhonicRealtime'
                  ultravox-realtime: '#/components/schemas/UltravoxRealtime'
                  xai-realtime: '#/components/schemas/XaiRealtime'
            - type: 'null'
          title: Model
      additionalProperties: false
      type: object
      required:
        - type
      title: RealtimeAssistantCreate
      description: |-
        Create a **realtime** assistant: end-to-end audio model.

        ``model`` accepts only realtime providers; ``voice`` and
        ``transcriber`` are not allowed (the realtime model handles audio
        I/O directly). When ``tier='premium'`` is set, ``model`` is
        server-resolved from the tier registry instead — the client
        supplies ``voiceCardId`` and must omit ``model``.
    RealtimeAssistantCreate-Output:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
        firstMessage:
          anyOf:
            - type: string
            - type: 'null'
          title: Firstmessage
        firstMessageMode:
          type: string
          enum:
            - assistant-speaks-first
            - assistant-speaks-first-with-model-generated-message
            - assistant-waits-for-user
          title: Firstmessagemode
          default: assistant-speaks-first
        firstMessageFallbackSeconds:
          anyOf:
            - type: number
            - type: 'null'
          title: Firstmessagefallbackseconds
          description: >-
            First-response fallback: if the assistant has not spoken within this
            many seconds of the call being **answered**, it opens the
            conversation itself — ``firstMessage`` verbatim when set, otherwise
            a model-generated opener. Fires at most once per call and only when
            the assistant has produced no speech yet, so in
            ``assistant-speaks-first`` modes it acts purely as a safety net for
            a failed greeting. Primarily for ``assistant-waits-for-user``
            assistants, where a silent callee (or a short utterance the model
            declines to answer) would otherwise leave dead air. ``null``
            (default) or ``0`` disables the fallback; otherwise 1-120 seconds.
        tools:
          items:
            anyOf:
              - type: string
              - oneOf:
                  - $ref: '#/components/schemas/FunctionTool-Output'
                  - $ref: '#/components/schemas/EndCallTool-Output'
                  - $ref: '#/components/schemas/TransferCallTool-Output'
                  - $ref: '#/components/schemas/VoicemailTool-Output'
                  - $ref: '#/components/schemas/LiveEscalateTool-Output'
                discriminator:
                  propertyName: type
                  mapping:
                    endCall: '#/components/schemas/EndCallTool-Output'
                    function: '#/components/schemas/FunctionTool-Output'
                    liveEscalate: '#/components/schemas/LiveEscalateTool-Output'
                    transferCall: '#/components/schemas/TransferCallTool-Output'
                    voicemail: '#/components/schemas/VoicemailTool-Output'
          type: array
          title: Tools
          description: >-
            Tools the assistant is allowed to use during a call. Each entry is
            one of two shapes:


            - **String** — id of an existing tool (created via ``POST /tool``).
            Reusable across assistants, scoped to your workspace.

            - **Inline tool object** — a one-off tool definition with the same
            shape ``POST /tool`` accepts (``function``, ``endCall``,
            ``transferCall``, ``voicemail``, or ``liveEscalate``). Useful for
            tools tied to a single assistant — never written to the tools
            collection.
        knowledgeBaseIds:
          items:
            type: string
          type: array
          title: Knowledgebaseids
        kbRetrievalMode:
          anyOf:
            - type: string
              enum:
                - auto
                - tool
            - type: 'null'
          title: Kbretrievalmode
          description: >-
            How knowledge bases are surfaced to the LLM during a call.
            **``auto``** — per-turn pre-prompt injection. Every user turn embeds
            the utterance, searches every linked KB in parallel, and prepends
            the top chunks as a system message before the LLM call.
            Pipeline-mode only. Best for FAQ-style assistants where most user
            turns should hit the KB. **``tool``** — the LLM gets one function
            tool per linked KB and decides when to call it. Works in pipeline
            AND realtime mode. Best for multi-KB assistants and any realtime
            assistant. **``null``** (default) — derive at runtime: pipeline →
            ``auto``, realtime → ``tool``. Tier mode forces the value (Premium →
            ``tool``, Standard / Gold → ``auto``).
        silenceTimeoutSeconds:
          type: number
          maximum: 600
          minimum: 10
          title: Silencetimeoutseconds
          default: 30
        maxDurationSeconds:
          type: integer
          maximum: 43200
          minimum: 5
          title: Maxdurationseconds
          default: 3600
        allowInterruptions:
          type: boolean
          title: Allowinterruptions
          default: true
        preemptiveGeneration:
          type: boolean
          title: Preemptivegeneration
          default: false
        maxToolSteps:
          type: integer
          maximum: 10
          minimum: 1
          title: Maxtoolsteps
          default: 3
        chatMessagesInContext:
          anyOf:
            - type: integer
              maximum: 500
              minimum: 5
            - type: 'null'
          title: Chatmessagesincontext
          description: >-
            Sliding-window size for the chat-session context fed to the LLM on
            each new turn. Counts user + assistant messages. Leave ``null`` to
            use the platform default. Ignored on voice (``Call``) paths.
        backgroundSound:
          anyOf:
            - $ref: '#/components/schemas/BackgroundSoundSchema'
            - type: 'null'
        webhookEvents:
          items:
            type: string
            enum:
              - status-update
              - transcript
              - end-of-call-report
              - chat-session-status-update
              - chat-message
              - chat-session-end-report
          type: array
          title: Webhookevents
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfigSchema'
            - type: 'null'
        analysis:
          anyOf:
            - $ref: '#/components/schemas/AnalysisConfigSchema-Output'
            - type: 'null'
        dataRetention:
          anyOf:
            - $ref: '#/components/schemas/DataRetentionConfigSchema'
            - type: 'null'
        chatDataRetention:
          anyOf:
            - $ref: '#/components/schemas/ChatDataRetentionConfigSchema'
            - type: 'null'
        endpointing:
          anyOf:
            - $ref: '#/components/schemas/EndpointingConfigSchema'
            - type: 'null'
        interruption:
          anyOf:
            - $ref: '#/components/schemas/InterruptionConfigSchema'
            - type: 'null'
        idle:
          anyOf:
            - $ref: '#/components/schemas/IdleConfigSchema'
            - type: 'null'
        recording:
          anyOf:
            - $ref: '#/components/schemas/RecordingConfigSchema'
            - type: 'null'
        monitoring:
          anyOf:
            - $ref: '#/components/schemas/MonitoringConfigSchema'
            - type: 'null'
        textProcessing:
          anyOf:
            - $ref: '#/components/schemas/TextProcessingSchema'
            - type: 'null'
        performance:
          anyOf:
            - $ref: '#/components/schemas/PerformanceSchema'
            - type: 'null'
        telephony:
          anyOf:
            - $ref: '#/components/schemas/TelephonySchema'
            - type: 'null'
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
          description: >-
            Productized tier: ``standard`` (curated pipeline voices),
            ``premium`` (realtime end-to-end audio), or ``gold`` (pipeline with
            the platform's premium voice library). When set, the server resolves
            the underlying provider stack from the tier registry — the client
            must supply ``voiceCardId`` and must NOT supply ``model`` /
            ``voice`` / ``transcriber``. Leave ``null`` to use the per-provider
            Custom flow.
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
          description: >-
            Tier voice-card identifier (e.g. ``"eva"``, ``"holly"``). Required
            when ``tier`` is set; must be ``null`` otherwise. Look up valid ids
            via ``GET /providers/tiers/{tier}/voices``.
        tierLanguage:
          anyOf:
            - type: string
            - type: 'null'
          title: Tierlanguage
          description: >-
            BCP-47 language code for the resolved transcriber. Required when
            ``tier='gold'`` (Gold voices are language-agnostic, so the user
            picks the call language). Ignored for Standard (language is derived
            from the voice card) and Premium (the realtime model handles
            language internally).
        tierSttOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiersttoverrides
          description: >-
            Optional transcription overrides for ``tier='gold'``. Merged on top
            of the tier's default transcriber config. ``provider`` and ``model``
            are tier-locked and silently dropped if supplied. Ignored for
            Standard / Premium.
        tierVoiceOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiervoiceoverrides
          description: >-
            Optional voice tuning overrides for ``tier='gold'``. Merged on top
            of the tier's default voice config so the user can tune stability /
            similarity / style / speed / etc. for the picked voice card.
            ``provider`` / ``model`` / ``voiceId`` are tier-locked and silently
            dropped if supplied. Ignored for Standard / Premium.
        tierRealtimeOverrides:
          anyOf:
            - $ref: '#/components/schemas/TierRealtimeOverrides'
            - type: 'null'
          description: >-
            Optional listening-behaviour tuning for ``tier='premium'``. Adjusts
            how quickly the assistant decides the caller is speaking, how long
            it waits for silence before ending the turn, and the noise-rejection
            padding. Leave any field unset to use the tier's default. Rejected
            on Standard / Gold / Custom.
        tierVersion:
          anyOf:
            - type: integer
            - type: 'null'
          title: Tierversion
          description: >-
            Server-managed tier definition version stamp. Read-only — ignored on
            input. Bumped whenever the tier's underlying provider mapping
            changes; lets a future migration script re-resolve assistants stuck
            on an older version.
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
          description: >-
            System prompt / persona instructions. **Use this field in tier
            mode** — the resolver injects it into the underlying LLM/realtime
            model config server-side. In Custom mode supply the prompt via
            ``model.systemPrompt`` instead (setting both is rejected to keep the
            path unambiguous).
        type:
          type: string
          const: realtime
          title: Type
        model:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AwsNovaSonic'
                - $ref: '#/components/schemas/OpenAIRealtime'
                - $ref: '#/components/schemas/AzureOpenAIRealtime'
                - $ref: '#/components/schemas/DeepslateRealtime'
                - $ref: '#/components/schemas/GoogleRealtimeConfig-Output'
                - $ref: '#/components/schemas/PhonicRealtime'
                - $ref: '#/components/schemas/UltravoxRealtime'
                - $ref: '#/components/schemas/XaiRealtime'
              discriminator:
                propertyName: provider
                mapping:
                  aws-nova-sonic: '#/components/schemas/AwsNovaSonic'
                  azure-openai-realtime: '#/components/schemas/AzureOpenAIRealtime'
                  deepslate-realtime: '#/components/schemas/DeepslateRealtime'
                  google-realtime: '#/components/schemas/GoogleRealtimeConfig-Output'
                  openai-realtime: '#/components/schemas/OpenAIRealtime'
                  phonic-realtime: '#/components/schemas/PhonicRealtime'
                  ultravox-realtime: '#/components/schemas/UltravoxRealtime'
                  xai-realtime: '#/components/schemas/XaiRealtime'
            - type: 'null'
          title: Model
      additionalProperties: false
      type: object
      required:
        - type
      title: RealtimeAssistantCreate
      description: |-
        Create a **realtime** assistant: end-to-end audio model.

        ``model`` accepts only realtime providers; ``voice`` and
        ``transcriber`` are not allowed (the realtime model handles audio
        I/O directly). When ``tier='premium'`` is set, ``model`` is
        server-resolved from the tier registry instead — the client
        supplies ``voiceCardId`` and must omit ``model``.
    RealtimeAssistantResponse:
      properties:
        id:
          type: string
          title: Id
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
        firstMessage:
          anyOf:
            - type: string
            - type: 'null'
          title: Firstmessage
        firstMessageMode:
          type: string
          enum:
            - assistant-speaks-first
            - assistant-speaks-first-with-model-generated-message
            - assistant-waits-for-user
          title: Firstmessagemode
          default: assistant-speaks-first
        firstMessageFallbackSeconds:
          anyOf:
            - type: number
            - type: 'null'
          title: Firstmessagefallbackseconds
        tools:
          items:
            anyOf:
              - type: string
              - oneOf:
                  - $ref: '#/components/schemas/FunctionTool-Output'
                  - $ref: '#/components/schemas/EndCallTool-Output'
                  - $ref: '#/components/schemas/TransferCallTool-Output'
                  - $ref: '#/components/schemas/VoicemailTool-Output'
                  - $ref: '#/components/schemas/LiveEscalateTool-Output'
                discriminator:
                  propertyName: type
                  mapping:
                    endCall: '#/components/schemas/EndCallTool-Output'
                    function: '#/components/schemas/FunctionTool-Output'
                    liveEscalate: '#/components/schemas/LiveEscalateTool-Output'
                    transferCall: '#/components/schemas/TransferCallTool-Output'
                    voicemail: '#/components/schemas/VoicemailTool-Output'
          type: array
          title: Tools
          description: >-
            Tools the assistant is allowed to use during a call. Each entry is
            one of two shapes:


            - **String** — id of an existing tool (created via ``POST /tool``).
            Reusable across assistants, scoped to your workspace.

            - **Inline tool object** — a one-off tool definition with the same
            shape ``POST /tool`` accepts (``function``, ``endCall``,
            ``transferCall``, ``voicemail``, or ``liveEscalate``). Useful for
            tools tied to a single assistant — never written to the tools
            collection.
        knowledgeBaseIds:
          items:
            type: string
          type: array
          title: Knowledgebaseids
        kbRetrievalMode:
          anyOf:
            - type: string
              enum:
                - auto
                - tool
            - type: 'null'
          title: Kbretrievalmode
          description: >-
            Effective KB retrieval mode for this assistant. ``null`` for legacy
            assistants — the agent derives the mode from ``type`` at runtime.
            Tier-mode assistants always carry an explicit value.
        silenceTimeoutSeconds:
          type: number
          title: Silencetimeoutseconds
          default: 30
        maxDurationSeconds:
          type: integer
          title: Maxdurationseconds
          default: 3600
        allowInterruptions:
          type: boolean
          title: Allowinterruptions
          default: true
        preemptiveGeneration:
          type: boolean
          title: Preemptivegeneration
          default: false
        maxToolSteps:
          type: integer
          title: Maxtoolsteps
          default: 3
        chatMessagesInContext:
          anyOf:
            - type: integer
            - type: 'null'
          title: Chatmessagesincontext
        backgroundSound:
          anyOf:
            - $ref: '#/components/schemas/BackgroundSoundSchema'
            - type: 'null'
        webhookEvents:
          items:
            type: string
            enum:
              - status-update
              - transcript
              - end-of-call-report
              - chat-session-status-update
              - chat-message
              - chat-session-end-report
          type: array
          title: Webhookevents
        server:
          anyOf:
            - $ref: >-
                #/components/schemas/src__api__schemas__assistant__ServerConfigInfo
            - type: 'null'
        analysis:
          anyOf:
            - $ref: '#/components/schemas/AnalysisConfigSchema-Output'
            - type: 'null'
        dataRetention:
          anyOf:
            - $ref: '#/components/schemas/DataRetentionConfigSchema'
            - type: 'null'
        chatDataRetention:
          anyOf:
            - $ref: '#/components/schemas/ChatDataRetentionConfigSchema'
            - type: 'null'
        endpointing:
          anyOf:
            - $ref: '#/components/schemas/EndpointingConfigSchema'
            - type: 'null'
        interruption:
          anyOf:
            - $ref: '#/components/schemas/InterruptionConfigSchema'
            - type: 'null'
        idle:
          anyOf:
            - $ref: '#/components/schemas/IdleConfigSchema'
            - type: 'null'
        recording:
          anyOf:
            - $ref: '#/components/schemas/RecordingConfigSchema'
            - type: 'null'
        monitoring:
          anyOf:
            - $ref: '#/components/schemas/MonitoringConfigSchema'
            - type: 'null'
        textProcessing:
          anyOf:
            - $ref: '#/components/schemas/TextProcessingSchema'
            - type: 'null'
        performance:
          anyOf:
            - $ref: '#/components/schemas/PerformanceSchema'
            - type: 'null'
        telephony:
          anyOf:
            - $ref: '#/components/schemas/TelephonySchema'
            - type: 'null'
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
        tierLanguage:
          anyOf:
            - type: string
            - type: 'null'
          title: Tierlanguage
        tierSttOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiersttoverrides
        tierVoiceOverrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Tiervoiceoverrides
        tierRealtimeOverrides:
          anyOf:
            - $ref: '#/components/schemas/TierRealtimeOverrides'
            - type: 'null'
        tierVersion:
          anyOf:
            - type: integer
            - type: 'null'
          title: Tierversion
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        type:
          type: string
          const: realtime
          title: Type
        model:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/AwsNovaSonic'
                - $ref: '#/components/schemas/OpenAIRealtime'
                - $ref: '#/components/schemas/AzureOpenAIRealtime'
                - $ref: '#/components/schemas/DeepslateRealtime'
                - $ref: '#/components/schemas/GoogleRealtimeConfig-Output'
                - $ref: '#/components/schemas/PhonicRealtime'
                - $ref: '#/components/schemas/UltravoxRealtime'
                - $ref: '#/components/schemas/XaiRealtime'
              discriminator:
                propertyName: provider
                mapping:
                  aws-nova-sonic: '#/components/schemas/AwsNovaSonic'
                  azure-openai-realtime: '#/components/schemas/AzureOpenAIRealtime'
                  deepslate-realtime: '#/components/schemas/DeepslateRealtime'
                  google-realtime: '#/components/schemas/GoogleRealtimeConfig-Output'
                  openai-realtime: '#/components/schemas/OpenAIRealtime'
                  phonic-realtime: '#/components/schemas/PhonicRealtime'
                  ultravox-realtime: '#/components/schemas/UltravoxRealtime'
                  xai-realtime: '#/components/schemas/XaiRealtime'
            - type: 'null'
          title: Model
      type: object
      required:
        - id
        - type
      title: RealtimeAssistantResponse
      description: |-
        A realtime assistant returned by GET / list endpoints.

        For Custom-mode (Premium=null) assistants ``model`` is always
        populated. For Premium-tier assistants it is intentionally
        omitted from the response — the customer sees only ``tier`` +
        ``voiceCardId`` and never the underlying realtime model id.
    RealtimeInputAudioTranscription:
      properties:
        model:
          type: string
          title: Model
          description: >-
            Transcription model id (OpenAI) or deployment name (Azure). For
            Azure this must be a deployment that exists in your resource.
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
          description: >-
            ISO-639-1 language hint (e.g. 'de'). Falls back to the assistant's
            language when omitted.
        prompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Prompt
          description: >-
            Optional transcription prompt / biasing hint — free text for the
            gpt-4o-transcribe family (e.g. domain vocabulary and proper nouns),
            a keyword list for whisper-1. NOT supported by
            ``gpt-realtime-whisper``.
        delay:
          anyOf:
            - type: string
              enum:
                - minimal
                - low
                - medium
                - high
                - xhigh
            - type: 'null'
          title: Delay
          description: >-
            Latency/accuracy tradeoff for ``gpt-realtime-whisper`` only. Higher
            levels give the model more audio context before emitting the
            transcript, which can lower word error rate; it delays the
            TRANSCRIPT only, never the conversation (the realtime model hears
            the caller directly). Leave unset for the provider default.
      additionalProperties: false
      type: object
      required:
        - model
      title: RealtimeInputAudioTranscription
      description: >-
        Override for the input-audio-transcription side-car — the model that

        transcribes the CALLER's audio in a realtime session (the model handles

        its own audio in/out; the caller-side transcript comes from this
        separate

        transcription model).


        For **Azure**, ``model`` is the **deployment name in your own resource**
        —

        the transcription model must be deployed there or the caller side is

        silently missing from the transcript. For **direct OpenAI**, it's a
        model

        id (see :data:`TRANSCRIPTION_MODELS`).

        Leave unset to use the default (``gpt-4o-mini-transcribe``).
    RealtimeUsageRecord:
      properties:
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        type:
          type: string
          const: realtime_usage
          title: Type
          default: realtime_usage
        inputTokens:
          anyOf:
            - type: integer
            - type: 'null'
          title: Inputtokens
        outputTokens:
          anyOf:
            - type: integer
            - type: 'null'
          title: Outputtokens
        audioDuration:
          anyOf:
            - type: number
            - type: 'null'
          title: Audioduration
      additionalProperties: true
      type: object
      title: RealtimeUsageRecord
    RecordingConfigSchema:
      properties:
        stereoEnabled:
          type: boolean
          title: Stereoenabled
          default: true
        agentEnabled:
          type: boolean
          title: Agentenabled
          default: false
        customerEnabled:
          type: boolean
          title: Customerenabled
          default: false
      type: object
      title: RecordingConfigSchema
    ReengagementTemplateConfig:
      properties:
        name:
          type: string
          maxLength: 512
          minLength: 1
          title: Name
          description: Template name as registered + APPROVED on the channel.
        language:
          type: string
          maxLength: 16
          minLength: 2
          title: Language
          description: BCP-47 language tag (e.g. ``en`` / ``en_US`` / ``de``).
        components:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Components
          description: >-
            Template component overrides with placeholder tokens. Shape mirrors
            Meta's WhatsApp template components array. Empty list = send
            template with no parameter substitutions (only valid when the
            underlying Meta template has no variables).
      additionalProperties: false
      type: object
      required:
        - name
        - language
      title: ReengagementTemplateConfig
      description: |-
        Config for the channel's default WhatsApp re-engagement template.

        Used by the auto-fallback path: when a caller posts free-form text to
        WhatsApp outside the 24h customer-care window and no per-request
        ``fallbackTemplate`` was supplied, the platform swaps the payload to
        a ``type: "template"`` send using this config.

        Variable substitution: ``components`` may carry placeholder tokens
        like ``{{contact.firstName}}`` / ``{{contact.fullName}}`` /
        ``{{org.name}}`` / ``{{customAttr.<key>}}``. The template resolver
        (Phase 5 of the WhatsApp rollout) substitutes from the resolved
        Contact + Organization at send time. Missing tokens render as empty
        strings so the send never aborts on a missing variable.

        Only meaningful on WhatsApp channels — the channel-level PATCH
        validator rejects this field on non-WhatsApp channels.
    ResourceVersionResponse:
      properties:
        id:
          type: string
          title: Id
          description: Stable version identifier.
        versionNumber:
          type: integer
          title: Versionnumber
          description: >-
            Monotonic version number within this resource's history. Starts at
            1.
        label:
          anyOf:
            - type: string
            - type: 'null'
          title: Label
          description: >-
            Optional human-readable label set via PATCH after the fact. Omitted
            on versions the user hasn't named.
        note:
          anyOf:
            - type: string
            - type: 'null'
          title: Note
          description: Optional free-text note attached to this version.
        pinned:
          type: boolean
          title: Pinned
          description: Whether the user has starred this version for quick discovery.
          default: false
        createdAt:
          type: string
          format: date-time
          title: Createdat
          description: When the parent resource save that produced this version landed.
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
          description: >-
            When the editable metadata (label / note / pinned) was last
            modified. Equal to ``createdAt`` for versions whose metadata has
            never been patched — the snapshot itself is immutable, so this clock
            only moves on a metadata edit.
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
          description: User who triggered the save that produced this version.
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
          description: >-
            User who last patched the metadata. ``null`` for versions whose
            metadata has never been touched.
        resourceType:
          $ref: '#/components/schemas/ResourceVersionType'
          description: Resource family this version belongs to.
        resourceId:
          type: string
          title: Resourceid
          description: ID of the assistant or tool this snapshot was taken from.
        snapshot:
          additionalProperties: true
          type: object
          title: Snapshot
          description: >-
            Full resource document as it existed at the time the version was
            saved. The shape matches whatever ``GET /{resource}/{id}`` returned
            for that resource on that date. The UI loads this back into the
            editor form when the user picks a version; the standard PATCH path
            records the new state as the next version on save.
      type: object
      required:
        - id
        - versionNumber
        - createdAt
        - updatedAt
        - resourceType
        - resourceId
        - snapshot
      title: ResourceVersionResponse
      description: |-
        Full version shape returned by the get endpoint.

        Adds the ``resourceType`` + ``resourceId`` pointer and the full
        ``snapshot`` payload.
    ResourceVersionSummary:
      properties:
        id:
          type: string
          title: Id
          description: Stable version identifier.
        versionNumber:
          type: integer
          title: Versionnumber
          description: >-
            Monotonic version number within this resource's history. Starts at
            1.
        label:
          anyOf:
            - type: string
            - type: 'null'
          title: Label
          description: >-
            Optional human-readable label set via PATCH after the fact. Omitted
            on versions the user hasn't named.
        note:
          anyOf:
            - type: string
            - type: 'null'
          title: Note
          description: Optional free-text note attached to this version.
        pinned:
          type: boolean
          title: Pinned
          description: Whether the user has starred this version for quick discovery.
          default: false
        createdAt:
          type: string
          format: date-time
          title: Createdat
          description: When the parent resource save that produced this version landed.
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
          description: >-
            When the editable metadata (label / note / pinned) was last
            modified. Equal to ``createdAt`` for versions whose metadata has
            never been patched — the snapshot itself is immutable, so this clock
            only moves on a metadata edit.
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
          description: User who triggered the save that produced this version.
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
          description: >-
            User who last patched the metadata. ``null`` for versions whose
            metadata has never been touched.
      type: object
      required:
        - id
        - versionNumber
        - createdAt
        - updatedAt
      title: ResourceVersionSummary
      description: |-
        Per-row shape returned by the list endpoint.

        Omits the heavy ``snapshot`` field — the list is purely an index,
        callers load the full snapshot via
        ``GET /{resource}/{id}/versions/{vId}`` when they need it.
    ResourceVersionType:
      type: string
      enum:
        - assistant
        - tool
      title: ResourceVersionType
      description: Resource families that carry version history.
    RetentionStatusSchema:
      properties:
        recordings:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
            - type: 'null'
          title: Recordings
          description: >-
            Status of the recording-deletion pass. ``applied`` means audio files
            have been removed and ``recording.*Url`` is null.
        recordingsAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Recordingsappliedat
        conversation:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
            - type: 'null'
          title: Conversation
          description: >-
            Status of the conversation-data deletion pass — clears transcript /
            messages / timeline / tool-call summaries / knowledge-base
            summaries.
        conversationAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Conversationappliedat
        analysis:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
            - type: 'null'
          title: Analysis
          description: >-
            Status of the analysis-deletion pass — clears summary, evaluation
            and structured data.
        analysisAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Analysisappliedat
        logArchive:
          anyOf:
            - type: string
              enum:
                - applied
                - failed
            - type: 'null'
          title: Logarchive
          description: >-
            Status of the call log archive deletion pass — removes the gzipped
            log archive from S3.
        logArchiveAppliedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Logarchiveappliedat
      type: object
      title: RetentionStatusSchema
      description: |-
        Per-surface retention application status.

        Surfaced on the Call response so customers can see which surfaces
        have been redacted by their assistant's retention policy. Each
        surface tracks independently — a partial failure on recordings
        doesn't block the conversation / analysis surfaces from applying.
    Rime:
      properties:
        provider:
          type: string
          const: rime
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Rime speaker name (default `astra`). See https://docs.rime.ai for
            the upstream catalog.
        model:
          anyOf:
            - type: string
              enum:
                - arcana
                - mistv3
                - mistv2
                - mist
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        speed:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.7
            - type: 'null'
          title: Speed
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Rime
      description: Rime TTS — uses ``speaker``+``lang`` (mapped from voiceId+language).
    SarvamSTT:
      properties:
        provider:
          type: string
          const: sarvam-stt
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - saaras:v3
                - saarika:v2.5
                - saaras:v2.5
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        mode:
          anyOf:
            - type: string
              enum:
                - transcribe
                - translate
                - verbatim
                - translit
                - codemix
            - type: 'null'
          title: Mode
        prompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Prompt
      additionalProperties: false
      type: object
      required:
        - provider
      title: SarvamSTT
      description: Sarvam AI STT variant — Indian-language specialist.
    SarvamTTS:
      properties:
        provider:
          type: string
          const: sarvam-tts
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Sarvam speaker name. Must be one the selected model supports — the
            plugin rejects a mismatch at agent init.
        model:
          anyOf:
            - type: string
              enum:
                - bulbul:v3
                - bulbul:v2
                - bulbul:v3-beta
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
              enum:
                - bn-IN
                - en-IN
                - gu-IN
                - hi-IN
                - kn-IN
                - ml-IN
                - mr-IN
                - od-IN
                - pa-IN
                - ta-IN
                - te-IN
            - type: 'null'
          title: Language
        pace:
          anyOf:
            - type: number
              maximum: 3
              minimum: 0.3
            - type: 'null'
          title: Pace
          description: Speech rate multiplier. Default 1.0.
        speechSampleRate:
          anyOf:
            - type: integer
              enum:
                - 8000
                - 16000
                - 22050
                - 24000
                - 32000
                - 44100
                - 48000
            - type: 'null'
          title: Speechsamplerate
        pitch:
          anyOf:
            - type: number
              maximum: 0.75
              minimum: -0.75
            - type: 'null'
          title: Pitch
          description: Voice pitch adjustment. Bulbul v2 only.
        loudness:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.5
            - type: 'null'
          title: Loudness
          description: Volume multiplier. Bulbul v2 only.
        enablePreprocessing:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enablepreprocessing
          description: Normalise mixed-language / numeric text. Bulbul v2 only.
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.01
            - type: 'null'
          title: Temperature
          description: Sampling temperature. Bulbul v3 only.
        dictId:
          anyOf:
            - type: string
            - type: 'null'
          title: Dictid
          description: Custom pronunciation dictionary id. Bulbul v3 only (not beta).
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: SarvamTTS
      description: Sarvam AI TTS — Indian-language specialist.
    ScheduleMessageRequest:
      properties:
        channelId:
          type: string
          title: Channelid
        to:
          type: string
          minLength: 1
          title: To
        type:
          $ref: '#/components/schemas/MessageType'
        sendAt:
          type: string
          format: date-time
          title: Sendat
          description: When to dispatch. Must be in the future (UTC).
        text:
          anyOf:
            - $ref: '#/components/schemas/TextPayload'
            - type: 'null'
        image:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        video:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        audio:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        document:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        sticker:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        location:
          anyOf:
            - $ref: '#/components/schemas/LocationPayload'
            - type: 'null'
        contact:
          anyOf:
            - $ref: '#/components/schemas/ContactPayload'
            - type: 'null'
        template:
          anyOf:
            - $ref: '#/components/schemas/TemplatePayload'
            - type: 'null'
        interactive:
          anyOf:
            - $ref: '#/components/schemas/InteractivePayload'
            - type: 'null'
        reaction:
          anyOf:
            - $ref: '#/components/schemas/ReactionPayload'
            - type: 'null'
        replyToMessageId:
          anyOf:
            - type: string
            - type: 'null'
          title: Replytomessageid
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        templateVariables:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Templatevariables
          description: >-
            Jinja2 variables for ``{{token}}`` placeholders in the template
            components. Substituted at schedule-create time (NOT at dispatch
            time) so the persisted Message carries the rendered values. Missing
            keys render as empty strings; bad template syntax falls back to
            passing the raw string through with a WARNING log.
      additionalProperties: false
      type: object
      required:
        - channelId
        - to
        - type
        - sendAt
      title: ScheduleMessageRequest
      description: >-
        Polymorphic single-recipient schedule. Same shape as
        ``SendMessageRequest``

        plus a required ``sendAt``.
    SearchConfigInfo:
      properties:
        scoreThreshold:
          type: number
          title: Scorethreshold
          default: 0.3
        topK:
          type: integer
          title: Topk
          default: 5
      type: object
      title: SearchConfigInfo
      description: Search config response shape — tolerates legacy extras.
    SearchConfigSchema:
      properties:
        scoreThreshold:
          type: number
          maximum: 1
          minimum: 0
          title: Scorethreshold
          description: Minimum similarity score (0-1).
          default: 0.3
        topK:
          type: integer
          maximum: 50
          minimum: 1
          title: Topk
          description: Max chunks to retrieve per query.
          default: 5
      additionalProperties: false
      type: object
      title: SearchConfigSchema
      description: Search / retrieval configuration (request shape).
    SendMessageRequest:
      properties:
        channelId:
          type: string
          title: Channelid
          examples:
            - 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
        to:
          type: string
          minLength: 1
          title: To
          examples:
            - '+4915112345678'
        type:
          $ref: '#/components/schemas/MessageType'
        text:
          anyOf:
            - $ref: '#/components/schemas/TextPayload'
            - type: 'null'
        image:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        video:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        audio:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        document:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        sticker:
          anyOf:
            - $ref: '#/components/schemas/MediaPayload'
            - type: 'null'
        location:
          anyOf:
            - $ref: '#/components/schemas/LocationPayload'
            - type: 'null'
        contact:
          anyOf:
            - $ref: '#/components/schemas/ContactPayload'
            - type: 'null'
        template:
          anyOf:
            - $ref: '#/components/schemas/TemplatePayload'
            - type: 'null'
        interactive:
          anyOf:
            - $ref: '#/components/schemas/InteractivePayload'
            - type: 'null'
        reaction:
          anyOf:
            - $ref: '#/components/schemas/ReactionPayload'
            - type: 'null'
        replyToMessageId:
          anyOf:
            - type: string
            - type: 'null'
          title: Replytomessageid
          description: Our internal message id to reply to (we resolve to the provider id).
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
          examples:
            - campaign: summer-24
              crmId: C-1042
        fallbackTemplate:
          anyOf:
            - $ref: '#/components/schemas/ReengagementTemplateConfig'
            - type: 'null'
          description: >-
            Optional WhatsApp template to use instead of free-form when the 24h
            customer-care window is closed for the recipient. Overrides the
            channel's ``defaultReengagementTemplate`` for this single send.
            Ignored on non-WhatsApp channels.
        templateVariables:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Templatevariables
          description: >-
            Flat dict of placeholder name -> value used to fill ``{{token}}``
            markers in any template components on this send (direct
            ``type=template`` or auto-fallback). Jinja2 syntax supported:
            filters (``| upper``), defaults (``| default("N/A")``), conditionals
            (``{% if vip %}...{% endif %}``). Missing keys render empty. Max 8
            KB serialized; keys must match ``^[a-zA-Z_][a-zA-Z0-9_]*$``.
          examples:
            - customerName: Jane Doe
              orderId: ORD-42
              pickupTime: 3:00 PM
      additionalProperties: true
      type: object
      required:
        - channelId
        - to
        - type
      title: SendMessageRequest
      description: >-
        Polymorphic outbound send body.


        Mirrors WhatsApp Cloud API's send-message shape — works for all
        providers.

        Exactly one of the type-specific payload slots is populated per
        ``type``.
    SendMessageResponse:
      properties:
        message:
          $ref: '#/components/schemas/MessageResponse'
        billedConversation:
          type: boolean
          title: Billedconversation
          description: >-
            True when this send opened a NEW 24h conversation window (and was
            billed).
          default: false
        billedAmountCents:
          type: integer
          title: Billedamountcents
          default: 0
      type: object
      required:
        - message
      title: SendMessageResponse
      description: Echo of the created message plus billing decision summary.
    ServerConfig:
      properties:
        url:
          type: string
          title: Url
        secret:
          anyOf:
            - type: string
            - type: 'null'
          title: Secret
          description: >-
            HMAC-SHA256 signing secret used to compute the
            ``X-Webhook-Signature`` header.
        timeoutSeconds:
          type: integer
          title: Timeoutseconds
          default: 20
        headers:
          additionalProperties: true
          type: object
          title: Headers
          description: Extra HTTP headers to send with each call.
      additionalProperties: false
      type: object
      required:
        - url
      title: ServerConfig
      description: |-
        Webhook server configuration for function / transfer tools.

        Outbound delivery is HMAC-SHA256 signed; the ``secret`` is the
        signing key used to compute the ``X-Webhook-Signature`` header.
    ServerConfigInfo:
      properties:
        url:
          type: string
          title: Url
        secret:
          anyOf:
            - type: string
            - type: 'null'
          title: Secret
        timeoutSeconds:
          type: integer
          title: Timeoutseconds
          default: 20
        headers:
          additionalProperties: true
          type: object
          title: Headers
      additionalProperties: false
      type: object
      required:
        - url
      title: ServerConfigInfo
      description: |-
        Tool webhook config — response shape.

        Deliberately NOT validated: see ``ServerConfig._no_internal_target``.
    ServerConfigSchema:
      properties:
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
          description: Webhook endpoint URL.
        secret:
          anyOf:
            - type: string
            - type: 'null'
          title: Secret
          description: >-
            HMAC-SHA256 signing secret used to compute the
            ``X-Webhook-Signature`` header.
        timeoutSeconds:
          type: integer
          maximum: 120
          minimum: 1
          title: Timeoutseconds
          description: Per-request timeout in seconds.
          default: 20
      type: object
      title: ServerConfigSchema
      description: |-
        Webhook server config — request shape.

        Outbound delivery is HMAC-SHA256 signed; the ``secret`` is the
        signing key used to compute the ``X-Webhook-Signature: sha256=<hex>``
        header.
    SessionConfigComponentSchema:
      properties:
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
            - type: 'null'
          title: Voice
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        temperature:
          anyOf:
            - type: number
            - type: 'null'
          title: Temperature
        max_tokens:
          anyOf:
            - type: integer
            - type: 'null'
          title: Max Tokens
        extra:
          additionalProperties: true
          type: object
          title: Extra
      additionalProperties: false
      type: object
      title: SessionConfigComponentSchema
      description: >-
        One component of the STT / LLM / TTS pipeline as it actually ran for
        this call.
    SessionConfigSnapshotSchema:
      properties:
        mode:
          anyOf:
            - type: string
              enum:
                - pipeline
                - realtime
            - type: 'null'
          title: Mode
        stt:
          anyOf:
            - $ref: '#/components/schemas/SessionConfigComponentSchema'
            - type: 'null'
        llm:
          anyOf:
            - $ref: '#/components/schemas/SessionConfigComponentSchema'
            - type: 'null'
        tts:
          anyOf:
            - $ref: '#/components/schemas/SessionConfigComponentSchema'
            - type: 'null'
        vad_model:
          anyOf:
            - type: string
            - type: 'null'
          title: Vad Model
        turn_detector:
          anyOf:
            - type: string
            - type: 'null'
          title: Turn Detector
        allow_interruptions:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Allow Interruptions
        preemptive_generation:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Preemptive Generation
        max_tool_steps:
          anyOf:
            - type: integer
            - type: 'null'
          title: Max Tool Steps
      additionalProperties: false
      type: object
      title: SessionConfigSnapshotSchema
      description: >-
        Frozen-at-session-start snapshot of the pipeline components.


        Authoritative for explaining the call's behavior later, even after the

        referenced assistant has been edited. ``mode`` is ``"pipeline"`` for the

        classic three-stage pipeline or ``"realtime"`` for end-to-end audio
        models.
    Silma:
      properties:
        provider:
          type: string
          const: silma
          title: Provider
        voiceId:
          type: string
          enum:
            - sarah
            - salma
            - salwa
            - saja
            - sultan
            - salman
            - sulaiman
            - salim
          title: Voiceid
          description: >-
            Voice id. Female: sarah, salma, salwa, saja. Male: sultan, salman,
            sulaiman, salim.
        model:
          anyOf:
            - type: string
              enum:
                - silma-tts-v2-msa
                - silma-tts-v2-ksa
            - type: 'null'
          title: Model
          description: >-
            Defaults to ``silma-tts-v2-msa`` (Modern Standard Arabic). Use
            ``silma-tts-v2-ksa`` for Saudi / Khaleeji dialect.
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        creativity:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Creativity
          description: Variance in speech prosody. Default 0.2.
        speed:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Speed
          description: Speed of generated speech. Default 0.2.
        userId:
          anyOf:
            - type: string
            - type: 'null'
          title: Userid
          description: >-
            SILMA account user id — required only when using ``customAudioId``
            or server-side pronunciation overrides. Find it under
            https://app.silma.ai/api-keys.
        customAudioId:
          anyOf:
            - type: string
            - type: 'null'
          title: Customaudioid
          description: >-
            Cloned-voice id from your SILMA account (e.g.
            ``voice_1769817467123``). Requires ``userId`` to be set. Manage
            clones at https://app.silma.ai/voices.
        enableServerPronunciationOverrides:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enableserverpronunciationoverrides
          description: >-
            Apply per-account pronunciation overrides configured at
            https://app.silma.ai/control. Default false.
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Silma
      description: |-
        SILMA AI TTS — Arabic speech synthesis with MSA + KSA dialects.

        Per-call text is hard-capped at 250 characters by the upstream API;
        long inputs are pre-chunked on word boundaries by the plugin.
    SipDestination:
      properties:
        type:
          type: string
          const: sip
          title: Type
          default: sip
        sipUri:
          type: string
          title: Sipuri
          description: Full SIP URI (e.g. ``sip:agent@pbx.example.com``).
        sipHeaders:
          additionalProperties: true
          type: object
          title: Sipheaders
          description: >-
            Custom SIP headers sent with the transfer — on the ``REFER`` in
            blind modes. Values support ``{{variable}}`` substitution resolved
            at transfer time (e.g. ``{{customer.number}}``).
        message:
          anyOf:
            - type: string
            - type: 'null'
          title: Message
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
        transferPlan:
          $ref: '#/components/schemas/TransferPlan'
      additionalProperties: false
      type: object
      required:
        - sipUri
      title: SipDestination
      description: Transfer destination — a raw SIP URI.
    SipGatewayInfo:
      properties:
        ip:
          type: string
          title: Ip
        port:
          type: integer
          title: Port
          default: 5060
        netmask:
          type: integer
          title: Netmask
          default: 32
        outboundProtocol:
          type: string
          title: Outboundprotocol
          default: udp
        inboundProtocol:
          type: string
          title: Inboundprotocol
          default: udp
        inboundEnabled:
          type: boolean
          title: Inboundenabled
          default: true
        outboundEnabled:
          type: boolean
          title: Outboundenabled
          default: true
      type: object
      required:
        - ip
      title: SipGatewayInfo
      description: |-
        Response-only SIP gateway schema — no input validators.

        Used by ``SipTrunkResponse`` so legacy documents stored before the
        wildcard-IP / protocol-enum validators shipped still render correctly
        in the dashboard. Without this split, a historical trunk with
        ``ip='0.0.0.0'`` would 500 the ``GET /sip-trunk`` endpoint because
        response construction would re-run input validators on data that came
        straight off disk. Input paths use the strict ``SipGatewaySchema`` so
        new records can't store wildcards or unknown protocols.
    SipGatewaySchema:
      properties:
        ip:
          type: string
          title: Ip
          description: SIP gateway IP address, CIDR or hostname.
        port:
          type: integer
          maximum: 65535
          minimum: 1
          title: Port
          description: SIP signaling port.
          default: 5060
        netmask:
          type: integer
          maximum: 32
          minimum: 8
          title: Netmask
          description: >-
            Subnet mask for inbound IP allowlisting (8-32). Combined with ``ip``
            to compute the allowed CIDR.
          default: 32
        outboundProtocol:
          type: string
          title: Outboundprotocol
          description: 'Transport used for OUTBOUND INVITE: udp, tcp, tls, tls/srtp.'
          default: udp
        inboundProtocol:
          type: string
          title: Inboundprotocol
          description: >-
            Transport the provider uses for INBOUND INVITE: udp, tcp, tls. The
            platform's inbound listener accepts UDP and TCP unconditionally; TLS
            requires platform-level certificate provisioning.
          default: udp
        inboundEnabled:
          type: boolean
          title: Inboundenabled
          description: Accept inbound calls from this gateway.
          default: true
        outboundEnabled:
          type: boolean
          title: Outboundenabled
          description: Route outbound calls through this gateway.
          default: true
      additionalProperties: false
      type: object
      required:
        - ip
      title: SipGatewaySchema
      description: SIP gateway configuration (request shape — strict).
    SipTrunkCreate:
      properties:
        name:
          type: string
          minLength: 1
          title: Name
          description: Display name for this SIP trunk.
        gateways:
          items:
            $ref: '#/components/schemas/SipGatewaySchema'
          type: array
          minItems: 1
          title: Gateways
          description: >-
            At least one gateway. All inbound-enabled gateways are used for
            inbound IP allowlisting.
        authUsername:
          anyOf:
            - type: string
            - type: 'null'
          title: Authusername
          description: SIP authentication username.
        authPassword:
          anyOf:
            - type: string
            - type: 'null'
          title: Authpassword
          description: SIP authentication password (stored encrypted).
        authId:
          anyOf:
            - type: string
            - type: 'null'
          title: Authid
          description: >-
            Digest-authentication username, only if your provider issues an
            Authorization/Auth ID distinct from the SIP username (e.g.
            RingCentral). The username stays the SIP identity in
            From/To/Contact; this ID is used only to answer auth challenges.
            Leave unset to authenticate with the username.
        headersToAttributes:
          additionalProperties: true
          type: object
          title: Headerstoattributes
          description: >-
            Map SIP headers → participant attributes (e.g. {"X-Customer-ID":
            "customerId"}).
        attributesToHeaders:
          additionalProperties: true
          type: object
          title: Attributestoheaders
          description: >-
            Map participant attributes → outgoing SIP headers (reverse of
            headersToAttributes).
        includeHeaders:
          type: string
          title: Includeheaders
          description: 'Which SIP headers to capture: none, x-headers, all.'
          default: x-headers
        registrationEnabled:
          type: boolean
          title: Registrationenabled
          description: Enable SIP REGISTER for this trunk.
          default: false
        registrationServer:
          anyOf:
            - type: string
            - type: 'null'
          title: Registrationserver
          description: Registrar address (host[:port]). Defaults to first gateway if unset.
        outboundProxy:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundproxy
          description: >-
            Outbound proxy (host[:port]) where REGISTER and outbound calls are
            SENT, when your provider separates it from the registrar/domain
            (e.g. RingCentral: registrar sip.ringcentral.com, proxy
            sip32.ringcentral.com:5090). Leave unset to send to the registrar
            directly.
        registrationTransport:
          type: string
          title: Registrationtransport
          description: >-
            Transport for REGISTER: udp, tcp, tls. Independent of call
            transport.
          default: udp
        registrationExpirySeconds:
          anyOf:
            - type: integer
              maximum: 3600
              minimum: 30
            - type: 'null'
          title: Registrationexpiryseconds
          description: >-
            REGISTER session length in seconds we request (30-3600). Leave unset
            to use the platform default (300). Lower it only if your provider
            expires registrations sooner than that — e.g. Starface/teamfon
            expire at 180s. The sidecar refreshes at ~75% of the granted value,
            so a lower number renews more often.
        scope:
          type: string
          title: Scope
          description: >-
            Connectivity mode: ``public`` (routed via the public media endpoint)
            or ``vpn`` (routed via your dedicated IPSec tunnel to the media
            node's private interface). ``vpn`` requires your workspace to be
            entitled AND provisioned for private VPN; enforced by the create
            endpoint. Provisioning ``vpn`` trunks is a managed operation.
          default: public
      additionalProperties: false
      type: object
      required:
        - name
        - gateways
      title: SipTrunkCreate
      description: Create SIP trunk request.
    SipTrunkResponse:
      properties:
        id:
          type: string
          title: Id
        name:
          type: string
          title: Name
        gateways:
          items:
            $ref: '#/components/schemas/SipGatewayInfo'
          type: array
          title: Gateways
        authUsername:
          anyOf:
            - type: string
            - type: 'null'
          title: Authusername
        authId:
          anyOf:
            - type: string
            - type: 'null'
          title: Authid
        headersToAttributes:
          additionalProperties: true
          type: object
          title: Headerstoattributes
        attributesToHeaders:
          additionalProperties: true
          type: object
          title: Attributestoheaders
        includeHeaders:
          type: string
          title: Includeheaders
          default: x-headers
        registrationEnabled:
          type: boolean
          title: Registrationenabled
          default: false
        registrationServer:
          anyOf:
            - type: string
            - type: 'null'
          title: Registrationserver
        outboundProxy:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundproxy
        registrationTransport:
          type: string
          title: Registrationtransport
          default: udp
        registrationExpirySeconds:
          anyOf:
            - type: integer
            - type: 'null'
          title: Registrationexpiryseconds
        registrationStatus:
          anyOf:
            - type: string
            - type: 'null'
          title: Registrationstatus
        registrationError:
          anyOf:
            - type: string
            - type: 'null'
          title: Registrationerror
        registrationLastSuccess:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Registrationlastsuccess
        scope:
          type: string
          title: Scope
          default: public
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
      additionalProperties: false
      type: object
      required:
        - id
        - name
      title: SipTrunkResponse
      description: |-
        SIP trunk response.

        ``authPassword`` is intentionally absent — the password is
        encrypted at rest and never returned to API clients. To wipe the
        credentials, call ``DELETE /sip-trunk/{id}/credentials``; to
        rotate, ``PATCH`` a new ``authPassword``.
    SipTrunkSyncFailure:
      properties:
        phoneNumberId:
          type: string
          title: Phonenumberid
          description: Our internal phone number id.
        phoneNumber:
          type: string
          title: Phonenumber
          description: E.164 number, for display.
        reason:
          type: string
          title: Reason
          description: Short description of the failure.
      additionalProperties: false
      type: object
      required:
        - phoneNumberId
        - phoneNumber
        - reason
      title: SipTrunkSyncFailure
      description: One phone number whose routing state couldn't be re-synced.
    SipTrunkSyncResult:
      properties:
        totalPhoneNumbers:
          type: integer
          title: Totalphonenumbers
          description: Total linked phone numbers.
        updated:
          type: integer
          title: Updated
          description: Phone numbers whose routing was successfully re-synced.
        failed:
          type: integer
          title: Failed
          description: Phone numbers whose routing update raised an error.
        skipped:
          type: integer
          title: Skipped
          description: >-
            Phone numbers that were never fully provisioned, or whose status is
            FAILED. Those require re-provisioning (delete + recreate) rather
            than re-sync.
        changedFields:
          items:
            type: string
          type: array
          title: Changedfields
          description: Which trunk fields changed and triggered the propagation.
        failures:
          items:
            $ref: '#/components/schemas/SipTrunkSyncFailure'
          type: array
          title: Failures
          description: Per-phone-number details for the ``failed`` count.
      additionalProperties: false
      type: object
      required:
        - totalPhoneNumbers
        - updated
        - failed
        - skipped
      title: SipTrunkSyncResult
      description: |-
        Summary of propagating a SIP trunk edit to every linked phone number.

        Returned on PATCH when the edit touched any field that affects
        routing (``gateways``, auth, header policy, REGISTER mode, …).
        ``null`` on the parent response when the edit was cosmetic (e.g.
        name only).
    SipTrunkUpdate:
      properties:
        name:
          anyOf:
            - type: string
              minLength: 1
            - type: 'null'
          title: Name
        gateways:
          anyOf:
            - items:
                $ref: '#/components/schemas/SipGatewaySchema'
              type: array
              minItems: 1
            - type: 'null'
          title: Gateways
        authUsername:
          anyOf:
            - type: string
            - type: 'null'
          title: Authusername
        authPassword:
          anyOf:
            - type: string
            - type: 'null'
          title: Authpassword
        authId:
          anyOf:
            - type: string
            - type: 'null'
          title: Authid
        headersToAttributes:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Headerstoattributes
        attributesToHeaders:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Attributestoheaders
        includeHeaders:
          anyOf:
            - type: string
            - type: 'null'
          title: Includeheaders
        registrationEnabled:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Registrationenabled
        registrationServer:
          anyOf:
            - type: string
            - type: 'null'
          title: Registrationserver
        outboundProxy:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundproxy
        registrationTransport:
          anyOf:
            - type: string
            - type: 'null'
          title: Registrationtransport
        registrationExpirySeconds:
          anyOf:
            - type: integer
              maximum: 3600
              minimum: 30
            - type: 'null'
          title: Registrationexpiryseconds
          description: >-
            REGISTER session length in seconds we request (30-3600). Leave unset
            to use the platform default (300). Lower it only if your provider
            expires registrations sooner than that — e.g. Starface/teamfon
            expire at 180s. The sidecar refreshes at ~75% of the granted value,
            so a lower number renews more often.
        scope:
          anyOf:
            - type: string
            - type: 'null'
          title: Scope
      additionalProperties: false
      type: object
      title: SipTrunkUpdate
      description: >-
        Update SIP trunk request — every field optional, omitted fields
        untouched.
    SipTrunkUpdateResponse:
      properties:
        trunk:
          $ref: '#/components/schemas/SipTrunkResponse'
          description: The updated SIP trunk.
        sync:
          anyOf:
            - $ref: '#/components/schemas/SipTrunkSyncResult'
            - type: 'null'
          description: >-
            Per-phone-number propagation outcome. ``null`` when the edit did not
            touch any routing-affecting fields.
      additionalProperties: false
      type: object
      required:
        - trunk
      title: SipTrunkUpdateResponse
      description: |-
        Response envelope for ``PATCH /sip-trunk/{id}``.

        Distinct from ``SipTrunkResponse`` because the PATCH endpoint needs
        to surface per-phone-number propagation details in the same round
        trip — a caller rotating an ``authPassword`` must see immediately
        which phone numbers got the new credentials and which didn't, not
        wait for a background job.
    SmallestAI:
      properties:
        provider:
          type: string
          const: smallestai
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Smallest AI voice id (default `irisha`). See
            https://waves-docs.smallest.ai for the upstream catalog.
        model:
          anyOf:
            - type: string
              enum:
                - lightning-large
                - lightning
                - lightning-v2
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        speed:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0.7
            - type: 'null'
          title: Speed
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: SmallestAI
      description: Smallest AI Waves TTS variant. Voices fetched dynamically.
    SonioxSTT:
      properties:
        provider:
          type: string
          const: soniox
          title: Provider
        model:
          anyOf:
            - type: string
              const: stt-rt-v3
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        context:
          anyOf:
            - type: string
            - type: 'null'
          title: Context
        diarization:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Diarization
      additionalProperties: false
      type: object
      required:
        - provider
      title: SonioxSTT
      description: Soniox STT variant. Uses STTOptions pattern internally.
    SortOrder:
      type: string
      enum:
        - asc
        - desc
      title: SortOrder
    SourceResponse:
      properties:
        id:
          type: string
          title: Id
        type:
          type: string
          title: Type
        fileId:
          anyOf:
            - type: string
            - type: 'null'
          title: Fileid
        fileName:
          anyOf:
            - type: string
            - type: 'null'
          title: Filename
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
        status:
          type: string
          title: Status
          default: pending
        chunkCount:
          type: integer
          title: Chunkcount
          default: 0
        tokenCount:
          type: integer
          title: Tokencount
          default: 0
        error:
          anyOf:
            - type: string
            - type: 'null'
          title: Error
        addedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Addedat
        processedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Processedat
      type: object
      required:
        - id
        - type
      title: SourceResponse
      description: A source within a knowledge base (response shape).
    Speechify:
      properties:
        provider:
          type: string
          const: speechify
          title: Provider
        voiceId:
          type: string
          title: Voiceid
          description: >-
            Speechify voice id (default `jack`). See
            https://docs.sws.speechify.com for the upstream catalog.
        model:
          anyOf:
            - type: string
              enum:
                - simba-english
                - simba-multilingual
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: Speechify
      description: Speechify TTS variant. Voices fetched dynamically.
    SpeechmaticsSTT:
      properties:
        provider:
          type: string
          const: speechmatics-stt
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - enhanced
                - standard
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        diarization:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Diarization
      additionalProperties: false
      type: object
      required:
        - provider
      title: SpeechmaticsSTT
      description: Speechmatics STT — ``model`` maps to operating_point.
    SpeechmaticsTTS:
      properties:
        provider:
          type: string
          const: speechmatics-tts
          title: Provider
        voiceId:
          type: string
          enum:
            - sarah
            - theo
            - megan
          title: Voiceid
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: SpeechmaticsTTS
      description: Speechmatics TTS — 3 static voices, English only.
    StatusUpdateEvent:
      properties:
        id:
          type: string
          title: Id
        type:
          type: string
          const: status-update
          title: Type
          default: status-update
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        call:
          $ref: '#/components/schemas/AssistantCallObject'
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: >-
            Per-call metadata supplied at call creation (``POST /call`` body) or
            via the ``assistant-request`` webhook return on inbound calls. Same
            value on every Family-B event for a given call. Absent when not set.
        overrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Overrides
          description: >-
            Per-call assistant overrides applied at the start of the call
            (``POST /call.overrides`` or the ``assistant-request`` webhook
            return). Vendor-identifying sub-keys
            (``model``/``voice``/``transcriber``) are stripped in tier mode.
            Absent when not set.
      type: object
      required:
        - id
        - timestamp
        - call
      title: StatusUpdateEvent
      description: |-
        Call lifecycle transition (queued → ringing → in-progress → ended).

        The ``status`` value lives inside ``call.status``; there is **no**
        top-level ``status`` field on the envelope. Live-monitoring URLs
        are attached to ``call.monitor`` only while the call is
        in-progress.

        ``metadata`` + ``overrides`` carry per-call data exactly as on
        every other Family-B event — receivers can read the same fields
        uniformly across ``status-update``, ``transcript``, and
        ``end-of-call-report``. Absent on the wire when the caller never
        set them.
    StructuredDataConfigSchema-Input:
      properties:
        enabled:
          type: boolean
          title: Enabled
          default: false
        prompt:
          type: string
          title: Prompt
          default: >-
            You are an expert data extractor. Extract structured data from the
            transcript based on the provided schema.
        timeoutSeconds:
          type: integer
          maximum: 60
          minimum: 1
          title: Timeoutseconds
          default: 10
        jsonSchema:
          anyOf:
            - $ref: '#/components/schemas/JsonSchema-Input'
            - type: 'null'
      type: object
      title: StructuredDataConfigSchema
      description: >-
        Structured data extraction — applies to BOTH voice calls and chat
        sessions.


        Define ``jsonSchema`` once; the extractor pulls matching fields out

        of the transcript regardless of whether the conversation was a

        phone call or a chat session.
    StructuredDataConfigSchema-Output:
      properties:
        enabled:
          type: boolean
          title: Enabled
          default: false
        prompt:
          type: string
          title: Prompt
          default: >-
            You are an expert data extractor. Extract structured data from the
            transcript based on the provided schema.
        timeoutSeconds:
          type: integer
          maximum: 60
          minimum: 1
          title: Timeoutseconds
          default: 10
        jsonSchema:
          anyOf:
            - $ref: '#/components/schemas/JsonSchema-Output'
            - type: 'null'
      type: object
      title: StructuredDataConfigSchema
      description: >-
        Structured data extraction — applies to BOTH voice calls and chat
        sessions.


        Define ``jsonSchema`` once; the extractor pulls matching fields out

        of the transcript regardless of whether the conversation was a

        phone call or a chat session.
    SttUsageRecord:
      properties:
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        type:
          type: string
          const: stt_usage
          title: Type
          default: stt_usage
        audioDuration:
          anyOf:
            - type: number
            - type: 'null'
          title: Audioduration
      additionalProperties: true
      type: object
      title: SttUsageRecord
    SummaryConfigSchema:
      properties:
        enabled:
          type: boolean
          title: Enabled
          default: true
        prompt:
          type: string
          title: Prompt
          default: >-
            You are an expert note-taker. You will be given a transcript of a
            conversation (voice call or chat session). Summarize the
            conversation in 2-3 sentences, if applicable.
        timeoutSeconds:
          type: integer
          maximum: 60
          minimum: 1
          title: Timeoutseconds
          default: 10
      type: object
      title: SummaryConfigSchema
      description: |-
        Summary generation — applies to BOTH voice calls and chat sessions.

        The same prompt + timeout is used regardless of channel. Customize
        the prompt to match your domain; the engine accepts any transcript-
        shaped input (voice STT transcript or chat message log).
    SurveyAggregateResponse:
      properties:
        totalResponses:
          type: integer
          title: Totalresponses
        averageScore:
          anyOf:
            - type: number
            - type: 'null'
          title: Averagescore
        distribution:
          additionalProperties:
            type: integer
          type: object
          title: Distribution
      additionalProperties: false
      type: object
      required:
        - totalResponses
      title: SurveyAggregateResponse
      description: Admin's view of survey responses with light aggregation.
    SurveyCreateRequest:
      properties:
        name:
          type: string
          maxLength: 128
          minLength: 1
          title: Name
        type:
          type: string
          const: post_chat_csat
          title: Type
          default: post_chat_csat
        scaleType:
          type: string
          enum:
            - stars_5
            - smileys_3
            - thumbs
          title: Scaletype
          default: stars_5
        promptText:
          additionalProperties:
            type: string
          type: object
          title: Prompttext
          description: 'Multi-locale prompt text, e.g. {''en'': ''How was your chat?''}'
        commentLabel:
          additionalProperties:
            type: string
          type: object
          title: Commentlabel
        thankYouText:
          additionalProperties:
            type: string
          type: object
          title: Thankyoutext
        enabled:
          type: boolean
          title: Enabled
          default: true
      additionalProperties: false
      type: object
      required:
        - name
        - promptText
      title: SurveyCreateRequest
    SurveyResponseSchema:
      properties:
        id:
          type: string
          title: Id
        widgetId:
          type: string
          title: Widgetid
        name:
          type: string
          title: Name
        type:
          type: string
          title: Type
        scaleType:
          type: string
          title: Scaletype
        promptText:
          additionalProperties:
            type: string
          type: object
          title: Prompttext
        commentLabel:
          additionalProperties:
            type: string
          type: object
          title: Commentlabel
        thankYouText:
          additionalProperties:
            type: string
          type: object
          title: Thankyoutext
        enabled:
          type: boolean
          title: Enabled
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - widgetId
        - name
        - type
        - scaleType
        - promptText
        - commentLabel
        - thankYouText
        - enabled
        - createdAt
        - updatedAt
      title: SurveyResponseSchema
    SurveyUpdateRequest:
      properties:
        name:
          anyOf:
            - type: string
              maxLength: 128
              minLength: 1
            - type: 'null'
          title: Name
        scaleType:
          anyOf:
            - type: string
              enum:
                - stars_5
                - smileys_3
                - thumbs
            - type: 'null'
          title: Scaletype
        promptText:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Prompttext
        commentLabel:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Commentlabel
        thankYouText:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Thankyoutext
        enabled:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Enabled
      additionalProperties: false
      type: object
      title: SurveyUpdateRequest
    TakeoverTokenResponse:
      properties:
        token:
          type: string
          title: Token
          description: >-
            WebRTC access token with full publish + subscribe + data
            permissions. The human supervisor uses this to join the call as a
            visible participant.
        url:
          type: string
          title: Url
          description: WebSocket URL to connect to.
        roomName:
          type: string
          title: Roomname
          description: Live-call room name to join.
        identity:
          type: string
          title: Identity
          description: Participant identity baked into the token.
        expiresInSeconds:
          type: integer
          title: Expiresinseconds
          description: Token TTL in seconds.
      additionalProperties: false
      type: object
      required:
        - token
        - url
        - roomName
        - identity
        - expiresInSeconds
      title: TakeoverTokenResponse
      description: Full-participant access token for human takeover of a live call.
    TelephonySchema:
      properties:
        ivrDetection:
          type: boolean
          title: Ivrdetection
          default: false
      type: object
      title: TelephonySchema
    TemplateComponent:
      properties:
        type:
          type: string
          title: Type
        format:
          anyOf:
            - type: string
            - type: 'null'
          title: Format
        text:
          anyOf:
            - type: string
            - type: 'null'
          title: Text
        example:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Example
        buttons:
          anyOf:
            - items:
                additionalProperties: true
                type: object
              type: array
            - type: 'null'
          title: Buttons
      additionalProperties: true
      type: object
      required:
        - type
      title: TemplateComponent
      description: A single component (header / body / footer / buttons).
    TemplateCreateRequest:
      properties:
        name:
          type: string
          maxLength: 512
          minLength: 1
          pattern: ^[a-z0-9_]+$
          title: Name
        category:
          type: string
          enum:
            - MARKETING
            - UTILITY
            - AUTHENTICATION
          title: Category
        language:
          type: string
          pattern: ^[a-z]{2}(_[A-Z]{2})?$
          title: Language
        components:
          items:
            oneOf:
              - $ref: '#/components/schemas/HeaderComponent'
              - $ref: '#/components/schemas/BodyComponent'
              - $ref: '#/components/schemas/FooterComponent'
              - $ref: '#/components/schemas/ButtonsComponent'
            discriminator:
              propertyName: type
              mapping:
                BODY: '#/components/schemas/BodyComponent'
                BUTTONS: '#/components/schemas/ButtonsComponent'
                FOOTER: '#/components/schemas/FooterComponent'
                HEADER: '#/components/schemas/HeaderComponent'
          type: array
          minItems: 1
          title: Components
      additionalProperties: false
      type: object
      required:
        - name
        - category
        - language
        - components
      title: TemplateCreateRequest
      description: Create a new WhatsApp message template (submitted to Meta for review).
    TemplatePayload:
      properties:
        name:
          type: string
          title: Name
          examples:
            - order_confirmation
        language:
          type: string
          title: Language
          examples:
            - de
        components:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Components
          examples:
            - - parameters:
                  - text: ORD-42
                    type: text
                type: body
      additionalProperties: false
      type: object
      required:
        - name
        - language
      title: TemplatePayload
      description: WhatsApp template send.
    TemplatePreviewRequest:
      properties:
        variables:
          additionalProperties:
            type: string
          type: object
          title: Variables
      additionalProperties: false
      type: object
      title: TemplatePreviewRequest
      description: Render a template with sample variable values — no send.
    TemplatePreviewResponse:
      properties:
        rendered:
          type: string
          title: Rendered
        components:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Components
        unresolvedVariables:
          items:
            type: string
          type: array
          title: Unresolvedvariables
      type: object
      required:
        - rendered
        - components
      title: TemplatePreviewResponse
    TemplateResponse:
      properties:
        id:
          type: string
          title: Id
          examples:
            - a2d94f17-6b8e-4c53-9f20-e17c4b8d3a65
        tenantId:
          type: string
          title: Tenantid
          examples:
            - 9d2f4c81-3e57-4b6a-8f01-c5a7e92d1b43
        organizationId:
          type: string
          title: Organizationid
          examples:
            - 5a8c1f37-9b2d-4e64-a7c9-3f0e81d6b254
        channelId:
          type: string
          title: Channelid
          examples:
            - 3f6b2a91-7c4e-4d28-b5a0-8e19c7d4f632
        channelType:
          $ref: '#/components/schemas/ChannelType'
        providerTemplateId:
          anyOf:
            - type: string
            - type: 'null'
          title: Providertemplateid
          examples:
            - '1629351018350177'
        name:
          type: string
          title: Name
          examples:
            - order_confirmation
        language:
          type: string
          title: Language
          examples:
            - de
        category:
          anyOf:
            - $ref: '#/components/schemas/WhatsAppCategory'
            - type: 'null'
        components:
          items:
            $ref: '#/components/schemas/TemplateComponent'
          type: array
          title: Components
        variableNames:
          items:
            type: string
          type: array
          title: Variablenames
          examples:
            - - orderId
              - customerName
        status:
          $ref: '#/components/schemas/TemplateStatus'
        rejectionReason:
          anyOf:
            - type: string
            - type: 'null'
          title: Rejectionreason
        lastSyncedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Lastsyncedat
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - tenantId
        - organizationId
        - channelId
        - channelType
        - name
        - language
        - status
        - createdAt
        - updatedAt
      title: TemplateResponse
    TemplateStatus:
      type: string
      enum:
        - pending
        - approved
        - rejected
        - paused
        - disabled
      title: TemplateStatus
      description: Template review status — mirrors Meta's enum for WhatsApp.
    TemplateSyncResponse:
      properties:
        synced:
          type: integer
          title: Synced
        inserted:
          type: integer
          title: Inserted
        updated:
          type: integer
          title: Updated
      type: object
      required:
        - synced
        - inserted
        - updated
      title: TemplateSyncResponse
    TextPayload:
      properties:
        body:
          type: string
          maxLength: 4096
          title: Body
          examples:
            - Your order ORD-42 is ready for pickup 🎉
        previewUrl:
          type: boolean
          title: Previewurl
          default: false
      additionalProperties: false
      type: object
      required:
        - body
      title: TextPayload
    TextProcessingSchema:
      properties:
        filterMarkdown:
          type: boolean
          title: Filtermarkdown
          default: true
        filterEmoji:
          type: boolean
          title: Filteremoji
          default: true
        useTtsAlignedTranscript:
          type: boolean
          title: Usettsalignedtranscript
          default: false
      type: object
      title: TextProcessingSchema
    TierRealtimeOverrides:
      properties:
        startOfSpeechSensitivity:
          anyOf:
            - type: string
              enum:
                - HIGH
                - LOW
            - type: 'null'
          title: Startofspeechsensitivity
          description: >-
            How eagerly the assistant commits to 'caller is speaking'. HIGH =
            react quickly to short utterances (more interrupt-eager). LOW =
            require stronger evidence before interrupting (fewer false
            interrupts, may miss very short utterances). Leave unset to use the
            platform default.
        endOfSpeechSensitivity:
          anyOf:
            - type: string
              enum:
                - HIGH
                - LOW
            - type: 'null'
          title: Endofspeechsensitivity
          description: >-
            How eagerly the assistant commits to 'caller has stopped'. HIGH =
            end the turn quickly at first silence (snappier). LOW = wait longer
            (tolerates user pauses mid-sentence). Leave unset to use the
            platform default.
        prefixPaddingMs:
          anyOf:
            - type: integer
              maximum: 2000
              minimum: 1
            - type: 'null'
          title: Prefixpaddingms
          description: >-
            Milliseconds of sustained voice required before the assistant treats
            audio as speech. Higher values reject brief noise; the minimum (1)
            avoids clipping word starts.
        silenceDurationMs:
          anyOf:
            - type: integer
              maximum: 2000
              minimum: 100
            - type: 'null'
          title: Silencedurationms
          description: >-
            Milliseconds of silence required before the assistant treats the
            caller as finished speaking. 500-800 ms feels natural; <100 ms
            causes fragmented audio; >2000 ms is noticeably laggy.
      additionalProperties: false
      type: object
      title: TierRealtimeOverrides
      description: |-
        Premium-tier optional listening-behaviour tuning.

        Lets a Premium-tier assistant tune the underlying realtime model's
        voice-activity-detection knobs without exposing the provider stack.
        All four fields are optional — leaving any unset preserves the
        tier's default behaviour. Tier=Premium only; rejected on Standard /
        Gold / Custom at the cross-field validator.

        The resolver merges these values into the resolved realtime model
        config under ``realtimeInputConfig.automaticActivityDetection`` at
        write time; the agent's runtime then forwards them to the realtime
        plugin's VAD config. Bounds match the upstream API's documented
        ranges (see ``backend/src/providers/realtime/google.py``).
    TimelineErrorEvent:
      properties:
        timestamp:
          anyOf:
            - type: number
            - type: 'null'
          title: Timestamp
          description: Unix epoch seconds.
        type:
          type: string
          const: error
          title: Type
          default: error
        errorType:
          anyOf:
            - type: string
            - type: 'null'
          title: Errortype
        message:
          anyOf:
            - type: string
            - type: 'null'
          title: Message
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
        code:
          anyOf:
            - type: integer
            - type: 'null'
          title: Code
      additionalProperties: true
      type: object
      title: TimelineErrorEvent
    TimelineHandoffEvent:
      properties:
        timestamp:
          anyOf:
            - type: number
            - type: 'null'
          title: Timestamp
          description: Unix epoch seconds.
        type:
          type: string
          enum:
            - handoff
            - agent_handoff
          title: Type
          default: handoff
        reason:
          anyOf:
            - type: string
            - type: 'null'
          title: Reason
        kind:
          type: string
          enum:
            - assistant
            - phone
            - sip
            - live_escalate
          title: Kind
          default: assistant
        fromAssistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Fromassistantid
        fromAssistantName:
          anyOf:
            - type: string
            - type: 'null'
          title: Fromassistantname
        toAssistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Toassistantid
        toAssistantName:
          anyOf:
            - type: string
            - type: 'null'
          title: Toassistantname
        destination:
          anyOf:
            - type: string
            - type: 'null'
          title: Destination
          description: >-
            Phone number / SIP URI / human-readable escalation target. Populated
            for non-assistant transfers.
        transferMode:
          anyOf:
            - type: string
            - type: 'null'
          title: Transfermode
      additionalProperties: true
      type: object
      title: TimelineHandoffEvent
      description: |-
        A transfer / handoff that occurred mid-call.

        Emitted by the agent at the moment a transfer tool fires. One
        record covers all four transfer surfaces (assistant-to-assistant,
        phone, SIP, live escalate) — frontend renders kind-specific
        affordances based on ``kind``.
    TimelineKbRetrievalEvent:
      properties:
        timestamp:
          anyOf:
            - type: number
            - type: 'null'
          title: Timestamp
          description: Unix epoch seconds.
        type:
          type: string
          const: kb_retrieval
          title: Type
          default: kb_retrieval
        query:
          type: string
          title: Query
        chunksCount:
          type: integer
          title: Chunkscount
          description: Number of chunks returned across all linked KBs.
          default: 0
        sources:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Sources
          description: >-
            Per-chunk entries with ``name``, ``score``, ``knowledgeBaseId`` and
            a 500-char content preview.
      additionalProperties: true
      type: object
      required:
        - query
      title: TimelineKbRetrievalEvent
      description: |-
        Knowledge-base retrieval that ran between two user turns.

        A retrieval fans out across every linked KB in parallel — each
        entry in ``sources`` carries its own ``knowledgeBaseId`` so the
        frontend can show which KB each chunk came from. The roll-up
        (top-score, distinct kb ids) lives on ``Call.kbRetrievalSummary``,
        not here.
    TimelineMessageEvent:
      properties:
        timestamp:
          anyOf:
            - type: number
            - type: 'null'
          title: Timestamp
          description: Unix epoch seconds.
        type:
          type: string
          const: message
          title: Type
          default: message
        role:
          type: string
          enum:
            - user
            - assistant
          title: Role
        content:
          anyOf:
            - type: string
            - type: 'null'
          title: Content
      additionalProperties: true
      type: object
      required:
        - role
      title: TimelineMessageEvent
    TimelineToolCallEvent:
      properties:
        timestamp:
          anyOf:
            - type: number
            - type: 'null'
          title: Timestamp
          description: Unix epoch seconds.
        type:
          type: string
          const: tool_call
          title: Type
          default: tool_call
        name:
          type: string
          title: Name
        arguments:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Arguments
      additionalProperties: true
      type: object
      required:
        - name
      title: TimelineToolCallEvent
    TimelineToolResultEvent:
      properties:
        timestamp:
          anyOf:
            - type: number
            - type: 'null'
          title: Timestamp
          description: Unix epoch seconds.
        type:
          type: string
          const: tool_result
          title: Type
          default: tool_result
        name:
          type: string
          title: Name
        result:
          title: Result
        durationMs:
          anyOf:
            - type: number
            - type: 'null'
          title: Durationms
        success:
          type: boolean
          title: Success
          default: true
      additionalProperties: true
      type: object
      required:
        - name
      title: TimelineToolResultEvent
    ToolCallEvent:
      properties:
        id:
          type: string
          title: Id
          description: Unique event ID.
        type:
          type: string
          const: tool-calls
          title: Type
          default: tool-calls
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        call:
          $ref: '#/components/schemas/ToolCallEventCallContext'
        customer:
          anyOf:
            - $ref: '#/components/schemas/CallCustomer'
            - type: 'null'
          description: >-
            Caller / customer record, surfaced at the **top level** for parity
            with the ``assistant-request`` webhook (which carries ``customer``
            at the top level because no full call object exists pre-call). The
            identical record is always available under ``call.customer``; this
            top-level copy lets a receiver read ``customer.number`` from the
            same path across both webhooks. Only ``number`` is guaranteed — for
            inbound calls it is the caller's number. Omitted when the call
            carries no customer record.
        toolCalls:
          items:
            $ref: '#/components/schemas/FunctionToolCallItem'
          type: array
          title: Toolcalls
        messages:
          anyOf:
            - items:
                $ref: '#/components/schemas/ConversationMessage-Input'
              type: array
            - type: 'null'
          title: Messages
          description: >-
            Conversation history up to the moment the LLM invoked the tool. Each
            item carries ``role`` / ``content`` plus role-specific structured
            fields (``arguments`` / ``toolCallId`` / ``isError`` / KB-retrieval
            metadata) so downstream tools can reason on prior tool I/O. Absent
            for invocations that fire before any messages exist.
        previousToolCalls:
          anyOf:
            - items:
                $ref: '#/components/schemas/PreviousToolCallItem'
              type: array
            - type: 'null'
          title: Previoustoolcalls
          description: >-
            Completed prior tool invocations paired with their results, in
            chronological order. The CURRENT invocation (see ``toolCalls[]``)
            and any in-flight calls are excluded. Field is omitted when no tool
            has completed yet.
        kbRetrievalSummary:
          anyOf:
            - items:
                $ref: '#/components/schemas/KbRetrievalSummaryItem'
              type: array
            - type: 'null'
          title: Kbretrievalsummary
          description: >-
            Knowledge-base retrievals fired so far on this call, in
            chronological order. Same shape as the field on
            ``end-of-call-report``. Field is omitted when no retrieval has
            happened yet.
        variables:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Variables
          description: >-
            Call-time variables supplied at call creation (and auto-merged
            ``sip_*`` headers). Tools commonly key into these for per-customer
            lookups.
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: Call-time metadata supplied at call creation.
        overrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Overrides
          description: >-
            Per-call assistant overrides applied via ``assistant-request`` or
            ``POST /call`` when set.
        assistant:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Assistant
          description: >-
            Snapshot of the assistant configuration that handled the call —
            ``name`` / ``systemPrompt`` / ``firstMessage`` / ``tools`` /
            ``knowledgeBaseIds`` / ``metadata`` / etc. — so a tool handler can
            act on the invocation without calling back into the API. Model /
            voice / transcriber details are included only when the assistant is
            configured with them directly. Webhook signing secrets are never
            included. Omitted when the configuration is unavailable.
      type: object
      required:
        - id
        - timestamp
        - call
        - toolCalls
      title: ToolCallEvent
      description: |-
        Tool function invocation webhook.

        Sent to **tool.server.url** (the tool-scoped URL).

        Sync mode (``async = false``) — the LLM blocks on your response
        and forwards your reply as the tool result. Async mode
        (``async = true``) — the LLM sees ``{"async": true, "message":
        "Request sent"}`` immediately; your reply (if any) is discarded.

        The event carries enough context to make a tool-side decision
        without round-tripping the platform API:

          - ``call`` — full call context (id, type, assistantId,
            startedAt, phoneNumber, customer, sipHeaders).
          - ``customer`` — the caller / customer record surfaced at the
            top level (same value as ``call.customer``) so receivers can
            read ``customer.number`` from the same path as the
            ``assistant-request`` webhook.
          - ``messages`` — the conversation history up to the moment the
            LLM invoked the tool, including prior ``tool_call`` /
            ``tool_result`` / ``kb_retrieval`` entries. Lets a downstream
            tool reason on what earlier tools returned without
            round-tripping the platform.
          - ``previousToolCalls`` — denormalized list of completed prior
            tool invocations paired with their results. Same data as the
            ``tool_call`` + ``tool_result`` entries in ``messages[]``, just
            flattened for receivers that want "what ran before me".
          - ``kbRetrievalSummary`` — denormalized list of every KB
            retrieval that fired so far. Same data the post-call
            ``end-of-call-report`` carries; surfaced live so tools can
            reference what knowledge has already been pulled.
          - ``variables`` — call-time variables supplied at call creation
            (or merged from SIP headers via the ``sip_*`` template
            substitution path). Use these for per-customer lookups.
          - ``metadata`` — call-time metadata supplied at call creation
            (e.g. CRM lead ids).
          - ``overrides`` — per-call overrides applied via
            ``assistant-request`` or ``POST /call``, when present.
    ToolCallEventCallContext:
      properties:
        id:
          anyOf:
            - type: string
            - type: 'null'
          title: Id
          description: Call ID.
        type:
          anyOf:
            - type: string
              enum:
                - inboundPhoneCall
                - outboundPhoneCall
                - webCall
            - type: 'null'
          title: Type
          description: Call type — inbound phone, outbound phone, or web call.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: Assistant handling the call.
        startedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Startedat
          description: Call start (UTC ISO-8601).
        roomName:
          anyOf:
            - type: string
            - type: 'null'
          title: Roomname
          description: Realtime room identifier.
        phoneNumber:
          anyOf:
            - $ref: '#/components/schemas/CallPhoneNumber'
            - type: 'null'
          description: >-
            Phone number record the call is bound to (record id, E.164 number,
            configured name). Absent for web calls.
        customer:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Customer
          description: Customer record — same shape as :class:`CallCustomer`.
        sipHeaders:
          anyOf:
            - additionalProperties:
                type: string
              type: object
            - type: 'null'
          title: Sipheaders
          description: >-
            SIP participant attribute headers captured from the inbound leg.
            Present for SIP calls; absent for other transports.
        callerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Callerid
          description: >-
            Caller ID actually used on the SIP INVITE for this outbound call.
            Reflects the resolved per-call override / per-number default / SIP
            registration username / phone number's E.164. Absent for inbound and
            web calls.
        tier:
          anyOf:
            - type: string
              enum:
                - standard
                - premium
                - gold
            - type: 'null'
          title: Tier
          description: >-
            Productized tier the assistant was bound to at call start. Absent on
            Custom-mode assistants. When set, the receiver can use ``call.tier``
            + ``call.voiceCardId`` for routing decisions without inspecting the
            assistant config directly.
        voiceCardId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voicecardid
          description: Tier voice-card id (only when ``tier`` is set).
      type: object
      title: ToolCallEventCallContext
      description: |-
        Call context echoed on every tool function webhook.

        Mirrors :class:`AssistantCallObject` field-for-field where it
        makes sense — ``id``, ``type``, ``assistantId``, ``startedAt``,
        ``phoneNumber``, ``customer``, ``sipHeaders`` — plus ``roomName``
        so telephony-side receivers can correlate with their own
        realtime-room records. Field-presence is dispatcher-controlled
        (``exclude_unset=True``); only fields the agent had in scope at
        fire time appear on the wire.
    ToolCallResponse:
      properties:
        result:
          anyOf:
            - {}
            - type: 'null'
          title: Result
          description: The tool's return value — forwarded verbatim to the LLM.
      type: object
      title: ToolCallResponse
      description: |-
        Recommended response shape for a tool function webhook.

        The platform parses your reply leniently:

          - **JSON object** — forwarded to the LLM as the tool result.
          - **Non-JSON / plain text** — wrapped as ``{"result": "<text>"}``.
          - **Empty body** — wrapped as ``{"result": ""}``.

        The recommended shape is ``{"result": ...}`` — the LLM works best
        when given a single ``result`` key it can reason about. You may
        include any additional fields; the LLM sees the full object.
    ToolCallSummaryItem:
      properties:
        name:
          type: string
          title: Name
          description: Tool name as registered on the assistant.
        calledAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Calledat
          description: Invocation start (UTC).
        durationMs:
          anyOf:
            - type: number
            - type: 'null'
          title: Durationms
          description: Wall-clock duration.
        success:
          type: boolean
          title: Success
          description: True if the tool returned without error.
        error:
          anyOf:
            - type: string
            - type: 'null'
          title: Error
          description: Error message when ``success`` is false.
      additionalProperties: true
      type: object
      required:
        - name
        - success
      title: ToolCallSummaryItem
      description: Denormalized tool invocation record.
    ToolCallSummaryItemSchema:
      properties:
        name:
          type: string
          title: Name
        calledAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Calledat
        durationMs:
          anyOf:
            - type: number
            - type: 'null'
          title: Durationms
        success:
          type: boolean
          title: Success
          default: true
        error:
          anyOf:
            - type: string
            - type: 'null'
          title: Error
      additionalProperties: false
      type: object
      required:
        - name
      title: ToolCallSummaryItemSchema
      description: One tool invocation, denormalized from the timeline.
    ToolMessage:
      properties:
        type:
          type: string
          enum:
            - request-start
            - request-complete
            - request-failed
            - request-response-delayed
          title: Type
        content:
          anyOf:
            - type: string
            - type: 'null'
          title: Content
        role:
          type: string
          enum:
            - speak
            - hint
          title: Role
          default: speak
        blocking:
          type: boolean
          title: Blocking
          default: false
        endCallAfterSpoken:
          type: boolean
          title: Endcallafterspoken
          default: false
        timingMilliseconds:
          anyOf:
            - type: integer
            - type: 'null'
          title: Timingmilliseconds
          description: >-
            For ``request-response-delayed`` only — how many ms to wait before
            playing.
      additionalProperties: false
      type: object
      required:
        - type
      title: ToolMessage
      description: |-
        Message played during a tool execution phase.

        ``role="speak"`` → content is spoken verbatim via TTS (no LLM step).
        ``role="hint"``  → content is passed to the LLM as a system instruction;
        the LLM generates the adaptive reply. ``blocking=True`` waits for speech
        to finish before continuing.
    TranscriptEvent:
      properties:
        id:
          type: string
          title: Id
        type:
          type: string
          const: transcript
          title: Type
          default: transcript
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        call:
          $ref: '#/components/schemas/AssistantCallObject'
        role:
          type: string
          enum:
            - user
            - assistant
          title: Role
          description: Speaker.
        isFinal:
          type: boolean
          title: Isfinal
          description: True for stable transcripts; false for streaming partials.
        transcript:
          type: string
          title: Transcript
          description: Transcript text.
        language:
          type: string
          title: Language
          description: BCP-47 language code (e.g. ``en``).
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
          description: >-
            Per-call metadata supplied at call creation (``POST /call`` body) or
            via the ``assistant-request`` webhook return on inbound calls. Same
            value on every Family-B event for a given call. Absent when not set.
        overrides:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Overrides
          description: >-
            Per-call assistant overrides applied at the start of the call.
            Vendor-identifying sub-keys (``model``/``voice``/``transcriber``)
            are stripped in tier mode. Absent when not set.
      type: object
      required:
        - id
        - timestamp
        - call
        - role
        - isFinal
        - transcript
        - language
      title: TranscriptEvent
      description: |-
        Final / partial transcript chunk for a single utterance.

        Emitted for both user and assistant turns. Use ``isFinal`` to
        distinguish stable transcripts from streaming partials — partials
        arrive frequently while the user is speaking and may be revised by
        subsequent events.

        ``metadata`` + ``overrides`` mirror the fields on every other
        Family-B event so receivers can read the same per-call context
        uniformly across event types.
    TransferCallTool-Input:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: transferCall
          title: Type
          default: transferCall
        destinations:
          items:
            oneOf:
              - $ref: '#/components/schemas/NumberDestination'
              - $ref: '#/components/schemas/SipDestination'
              - $ref: '#/components/schemas/AssistantDestination'
            discriminator:
              propertyName: type
              mapping:
                assistant: '#/components/schemas/AssistantDestination'
                number: '#/components/schemas/NumberDestination'
                sip: '#/components/schemas/SipDestination'
          type: array
          minItems: 1
          title: Destinations
          description: Available transfer destinations. At least one is required.
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfig'
            - type: 'null'
          description: >-
            Optional webhook used to resolve destinations dynamically when the
            LLM names one not in the static list.
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Input'
            - type: 'null'
          description: >-
            Optional custom prose explaining WHEN the LLM should invoke the
            tool. Augments — does not replace — the auto-generated destination
            catalogue; the list of valid destination labels is always sent to
            the LLM (and enforced via JSON-Schema enum when no dynamic
            ``server`` is configured).
        aiHeaders:
          items:
            $ref: '#/components/schemas/AiHeader'
          type: array
          title: Aiheaders
          description: >-
            Headers whose VALUES the AI fills at call time from the conversation
            (guided by each header's description + optional allowed values),
            then sends on the outbound SIP transfer — alongside any static
            per-destination ``sipHeaders``.
      additionalProperties: false
      type: object
      required:
        - destinations
      title: TransferCallTool
      description: |-
        Transfers the active call to one of the configured destinations.

        When ``server`` is set, the LLM-supplied destination string is first
        looked up in the static ``destinations`` list; on a miss the agent
        POSTs to ``server.url`` with the requested destination + reason and
        expects a ``{ "destination": <TransferDestination> }`` response.
    TransferCallTool-Output:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: transferCall
          title: Type
          default: transferCall
        destinations:
          items:
            oneOf:
              - $ref: '#/components/schemas/NumberDestination'
              - $ref: '#/components/schemas/SipDestination'
              - $ref: '#/components/schemas/AssistantDestination'
            discriminator:
              propertyName: type
              mapping:
                assistant: '#/components/schemas/AssistantDestination'
                number: '#/components/schemas/NumberDestination'
                sip: '#/components/schemas/SipDestination'
          type: array
          minItems: 1
          title: Destinations
          description: Available transfer destinations. At least one is required.
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfig'
            - type: 'null'
          description: >-
            Optional webhook used to resolve destinations dynamically when the
            LLM names one not in the static list.
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Output'
            - type: 'null'
          description: >-
            Optional custom prose explaining WHEN the LLM should invoke the
            tool. Augments — does not replace — the auto-generated destination
            catalogue; the list of valid destination labels is always sent to
            the LLM (and enforced via JSON-Schema enum when no dynamic
            ``server`` is configured).
        aiHeaders:
          items:
            $ref: '#/components/schemas/AiHeader'
          type: array
          title: Aiheaders
          description: >-
            Headers whose VALUES the AI fills at call time from the conversation
            (guided by each header's description + optional allowed values),
            then sends on the outbound SIP transfer — alongside any static
            per-destination ``sipHeaders``.
      additionalProperties: false
      type: object
      required:
        - destinations
      title: TransferCallTool
      description: |-
        Transfers the active call to one of the configured destinations.

        When ``server`` is set, the LLM-supplied destination string is first
        looked up in the static ``destinations`` list; on a miss the agent
        POSTs to ``server.url`` with the requested destination + reason and
        expects a ``{ "destination": <TransferDestination> }`` response.
    TransferCallToolResponse:
      properties:
        id:
          type: string
          title: Id
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: Folder this tool belongs to, or ``null`` when unfiled.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        type:
          type: string
          const: transferCall
          title: Type
          default: transferCall
        destinations:
          items:
            oneOf:
              - $ref: '#/components/schemas/NumberDestination'
              - $ref: '#/components/schemas/SipDestination'
              - $ref: '#/components/schemas/AssistantDestination'
            discriminator:
              propertyName: type
              mapping:
                assistant: '#/components/schemas/AssistantDestination'
                number: '#/components/schemas/NumberDestination'
                sip: '#/components/schemas/SipDestination'
          type: array
          title: Destinations
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfigInfo'
            - type: 'null'
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Output'
            - type: 'null'
        aiHeaders:
          items:
            $ref: '#/components/schemas/AiHeader'
          type: array
          title: Aiheaders
      type: object
      required:
        - id
      title: TransferCallToolResponse
    TransferCallToolUpdate:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Move this tool to a folder. Pass an explicit ``null`` to unassign
            and return the tool to the list root. A PATCH that only changes
            ``folderId`` is treated as an organisational move — ``updatedAt`` is
            preserved.
        messages:
          anyOf:
            - items:
                $ref: '#/components/schemas/ToolMessage'
              type: array
            - type: 'null'
          title: Messages
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
        type:
          type: string
          const: transferCall
          title: Type
          default: transferCall
        destinations:
          anyOf:
            - items:
                oneOf:
                  - $ref: '#/components/schemas/NumberDestination'
                  - $ref: '#/components/schemas/SipDestination'
                  - $ref: '#/components/schemas/AssistantDestination'
                discriminator:
                  propertyName: type
                  mapping:
                    assistant: '#/components/schemas/AssistantDestination'
                    number: '#/components/schemas/NumberDestination'
                    sip: '#/components/schemas/SipDestination'
              type: array
            - type: 'null'
          title: Destinations
        server:
          anyOf:
            - $ref: '#/components/schemas/ServerConfig'
            - type: 'null'
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Input'
            - type: 'null'
        aiHeaders:
          anyOf:
            - items:
                $ref: '#/components/schemas/AiHeader'
              type: array
            - type: 'null'
          title: Aiheaders
      additionalProperties: false
      type: object
      title: TransferCallToolUpdate
    TransferPlan:
      properties:
        mode:
          type: string
          enum:
            - blind
            - blind-with-summary
            - warm
          title: Mode
          description: >-
            Execution mode:

            - ``blind``: immediate SIP REFER, no consultation.

            - ``blind-with-summary``: blind transfer that attaches a call
            summary to an ``X-Transfer-Summary`` SIP header.

            - ``warm``: agent-assisted — the AI briefs the destination on a
            consultation leg before the caller is connected.
          default: blind
        message:
          anyOf:
            - type: string
            - type: 'null'
          title: Message
          description: Message to play for the destination party.
        timeout:
          type: integer
          title: Timeout
          description: Warm-transfer consultation timeout (seconds).
          default: 60
        dialTimeout:
          type: integer
          title: Dialtimeout
          description: Seconds to ring the destination before giving up.
          default: 60
        playDialtone:
          type: boolean
          title: Playdialtone
          description: Play dialtone to the caller while connecting.
          default: false
        holdAudioUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Holdaudiourl
          description: Audio URL played to the caller on hold.
        briefingPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Briefingprompt
          description: >-
            Warm-mode only. Extra system-prompt guidance appended to the
            briefing agent's instructions when this destination is invoked. Use
            this to enforce language, tone, summary length, or any other style
            preference. Behavioural rules (don't repeat briefing, react to
            affirmative responses by connecting the caller) are always included
            by the runtime — this field augments, not replaces. When empty, an
            automatic 'match the conversation language' hint is used instead.
            Supports ``{{variable}}`` substitution.
      additionalProperties: false
      type: object
      title: TransferPlan
      description: How a transfer is carried out on the wire.
    TtsUsageRecord:
      properties:
        provider:
          anyOf:
            - type: string
            - type: 'null'
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        type:
          type: string
          const: tts_usage
          title: Type
          default: tts_usage
        characters:
          anyOf:
            - type: integer
            - type: 'null'
          title: Characters
      additionalProperties: true
      type: object
      title: TtsUsageRecord
    TwilioPhoneNumber:
      properties:
        number:
          type: string
          title: Number
          description: Phone number in E.164 format (e.g. ``+14155551234``).
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this number belongs to. Must reference a live folder with
            ``resourceType='phone_number'`` in the caller's workspace. Null
            places the number at the list root.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
          description: >-
            Default assistant that handles inbound calls on this number. Used
            only when ``server`` is not set.
        server:
          anyOf:
            - $ref: '#/components/schemas/InboundServerConfig'
            - type: 'null'
          description: >-
            Dynamic-assistant selection webhook. Takes precedence over
            ``assistantId`` when both are set.
        fallbackDestination:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/FallbackNumberDestination'
                - $ref: '#/components/schemas/FallbackSipDestination'
              discriminator:
                propertyName: type
                mapping:
                  number: '#/components/schemas/FallbackNumberDestination'
                  sip: '#/components/schemas/FallbackSipDestination'
            - type: 'null'
          title: Fallbackdestination
          description: >-
            Inbound safety net. If the assistant can't be set up for a call (any
            init failure — webhook error, config load, session start), the
            caller is transferred to this ``number`` / ``sip`` destination
            instead of being dropped. Leave null to drop on failure (default).
        outboundCallerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundcallerid
          description: >-
            Number shown as the caller ID on outbound calls from this phone
            number. Twilio: must be a Twilio-provisioned number on the account
            or a Verified Caller ID — pre-validated against Twilio at
            create/PATCH time. BYO SIP: validated by the carrier at call time.
            Leave null to use this number's own E.164.
        provider:
          type: string
          const: twilio
          title: Provider
          default: twilio
        twilioAccountSid:
          type: string
          title: Twilioaccountsid
          description: >-
            Twilio account SID (``AC…``). Stored for trunk management and
            outbound calls.
        twilioAuthToken:
          type: string
          title: Twilioauthtoken
          description: Twilio auth token. Encrypted at rest; masked in every response.
        twilioRegion:
          anyOf:
            - type: string
            - type: 'null'
          enum:
            - au1
            - ie1
            - us1
          title: Twilioregion
          description: >-
            Twilio region the Elastic SIP Trunk is provisioned in — one of
            ['au1', 'ie1', 'us1']. Controls data residency + call processing.
            Omit (or ``us1``) for the United States default. Choose a region
            that MATCHES this number's regional-routing setting in Twilio (e.g.
            ``ie1`` for a European number configured for EU routing) — a
            mismatch means inbound calls won't connect. Immutable after
            creation; delete and re-import to change it.
      additionalProperties: false
      type: object
      required:
        - number
        - twilioAccountSid
        - twilioAuthToken
      title: TwilioPhoneNumber
      description: |-
        Phone number imported from a Twilio account.

        On create the backend provisions a Twilio Elastic SIP Trunk +
        credential list pointing at our SIP URI, plus the matching inbound
        / outbound routing. All Twilio resource IDs land on the response
        for auditing.
    TwilioPhoneNumberResponse:
      properties:
        id:
          type: string
          title: Id
        number:
          type: string
          title: Number
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: Folder this number belongs to, or ``null`` when unfiled.
        assistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Assistantid
        server:
          anyOf:
            - $ref: '#/components/schemas/InboundServerConfigInfo'
            - type: 'null'
        fallbackDestination:
          anyOf:
            - oneOf:
                - $ref: '#/components/schemas/FallbackNumberDestination'
                - $ref: '#/components/schemas/FallbackSipDestination'
              discriminator:
                propertyName: type
                mapping:
                  number: '#/components/schemas/FallbackNumberDestination'
                  sip: '#/components/schemas/FallbackSipDestination'
            - type: 'null'
          title: Fallbackdestination
        numberE164CheckEnabled:
          type: boolean
          title: Numbere164Checkenabled
          default: true
        status:
          type: string
          enum:
            - pending
            - active
            - failed
            - released
          title: Status
          default: active
        inboundTrunkId:
          anyOf:
            - type: string
            - type: 'null'
          title: Inboundtrunkid
        outboundTrunkId:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundtrunkid
        dispatchRuleId:
          anyOf:
            - type: string
            - type: 'null'
          title: Dispatchruleid
        sipCallerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Sipcallerid
          description: From-header override for registration-based SIP trunks.
        outboundCallerId:
          anyOf:
            - type: string
            - type: 'null'
          title: Outboundcallerid
          description: >-
            Number shown as the caller ID on outbound calls from this phone
            number. Twilio: must be a Twilio-provisioned number on the account
            or a Verified Caller ID — pre-validated against Twilio at
            create/PATCH time. BYO SIP: validated by the carrier at call time.
            Leave null to use this number's own E.164.
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        provider:
          type: string
          const: twilio
          title: Provider
          default: twilio
        twilioAccountSid:
          anyOf:
            - type: string
            - type: 'null'
          title: Twilioaccountsid
        twilioAuthToken:
          anyOf:
            - type: string
            - type: 'null'
          title: Twilioauthtoken
        twilioPhoneNumberSid:
          anyOf:
            - type: string
            - type: 'null'
          title: Twiliophonenumbersid
        twilioTrunkSid:
          anyOf:
            - type: string
            - type: 'null'
          title: Twiliotrunksid
        twilioTrunkDomain:
          anyOf:
            - type: string
            - type: 'null'
          title: Twiliotrunkdomain
        twilioCredentialListSid:
          anyOf:
            - type: string
            - type: 'null'
          title: Twiliocredentiallistsid
        twilioRegion:
          anyOf:
            - type: string
            - type: 'null'
          title: Twilioregion
          description: >-
            Twilio region the Elastic SIP Trunk lives in. ``null`` = the United
            States default (``us1``), which every number imported before
            regional selection existed uses.
      additionalProperties: false
      type: object
      required:
        - id
        - number
      title: TwilioPhoneNumberResponse
    TypingRequest:
      properties:
        durationSeconds:
          type: integer
          maximum: 25
          minimum: 1
          title: Durationseconds
          default: 5
      additionalProperties: false
      type: object
      title: TypingRequest
    UltravoxRealtime:
      properties:
        provider:
          type: string
          const: ultravox-realtime
          title: Provider
        model:
          anyOf:
            - type: string
              enum:
                - fixie-ai/ultravox
                - fixie-ai/ultravox-llama3.3-70b
                - fixie-ai/ultravox-qwen3-32b-preview
                - fixie-ai/ultravox-gemma3-27b-preview
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
            - type: 'null'
          title: Voice
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Temperature
        maxDuration:
          anyOf:
            - type: number
              maximum: 7200
              minimum: 60
            - type: 'null'
          title: Maxduration
        timeExceededMessage:
          anyOf:
            - type: string
            - type: 'null'
          title: Timeexceededmessage
      additionalProperties: false
      type: object
      required:
        - provider
      title: UltravoxRealtime
      description: Ultravox Realtime — bundles STT+LLM+TTS in one connection.
    UrlButton:
      properties:
        type:
          type: string
          const: URL
          title: Type
        text:
          type: string
          maxLength: 25
          minLength: 1
          title: Text
        url:
          type: string
          maxLength: 2000
          minLength: 1
          title: Url
        example:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Example
      additionalProperties: false
      type: object
      required:
        - type
        - text
        - url
      title: UrlButton
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
              - type: string
              - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
        - loc
        - msg
        - type
      title: ValidationError
    VersionUpdateRequest:
      properties:
        label:
          anyOf:
            - type: string
              maxLength: 200
              minLength: 1
            - type: 'null'
          title: Label
          description: >-
            New label. Omit to leave unchanged; cannot be set to an empty
            string.
        note:
          anyOf:
            - type: string
              maxLength: 2000
            - type: 'null'
          title: Note
          description: New note (empty string clears).
        pinned:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Pinned
          description: Toggle the pinned flag. Omit to leave unchanged.
      additionalProperties: false
      type: object
      title: VersionUpdateRequest
      description: |-
        Body for ``PATCH /{resource}/{id}/versions/{vId}`` — metadata only.

        Only ``label`` / ``note`` / ``pinned`` are mutable. The snapshot
        itself is immutable — to "restore" an earlier state the UI loads
        the version into the editor form and the user saves normally; the
        standard PATCH path's auto-version hook records the new state as
        the next version.
    VoicemailTool-Input:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: voicemail
          title: Type
          default: voicemail
        endCallAfterSpoken:
          type: boolean
          title: Endcallafterspoken
          default: true
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Input'
            - type: 'null'
          description: Optional custom LLM-facing description overriding the built-in one.
      additionalProperties: false
      type: object
      title: VoicemailTool
      description: |-
        Delivers a voicemail message when an answering machine is detected.

        The voicemail text itself is the ``content`` of the ``messages[]``
        entry with ``type="request-start"``. There is no separate
        ``voicemailMessage`` field — the message system is the single source
        of truth for everything the assistant says.
    VoicemailTool-Output:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
          description: Display name; also used as the function name for the LLM.
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Folder this tool belongs to. Must reference a live folder with
            ``resourceType='tool'`` in the caller's workspace. Null places the
            tool at the list root.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        type:
          type: string
          const: voicemail
          title: Type
          default: voicemail
        endCallAfterSpoken:
          type: boolean
          title: Endcallafterspoken
          default: true
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Output'
            - type: 'null'
          description: Optional custom LLM-facing description overriding the built-in one.
      additionalProperties: false
      type: object
      title: VoicemailTool
      description: |-
        Delivers a voicemail message when an answering machine is detected.

        The voicemail text itself is the ``content`` of the ``messages[]``
        entry with ``type="request-start"``. There is no separate
        ``voicemailMessage`` field — the message system is the single source
        of truth for everything the assistant says.
    VoicemailToolResponse:
      properties:
        id:
          type: string
          title: Id
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: Folder this tool belongs to, or ``null`` when unfiled.
        messages:
          items:
            $ref: '#/components/schemas/ToolMessage'
          type: array
          title: Messages
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
        createdAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Createdat
        updatedAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Updatedat
        createdBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Createdby
        updatedBy:
          anyOf:
            - type: string
            - type: 'null'
          title: Updatedby
        type:
          type: string
          const: voicemail
          title: Type
          default: voicemail
        endCallAfterSpoken:
          type: boolean
          title: Endcallafterspoken
          default: true
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Output'
            - type: 'null'
      type: object
      required:
        - id
      title: VoicemailToolResponse
    VoicemailToolUpdate:
      properties:
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        folderId:
          anyOf:
            - type: string
            - type: 'null'
          title: Folderid
          description: >-
            Move this tool to a folder. Pass an explicit ``null`` to unassign
            and return the tool to the list root. A PATCH that only changes
            ``folderId`` is treated as an organisational move — ``updatedAt`` is
            preserved.
        messages:
          anyOf:
            - items:
                $ref: '#/components/schemas/ToolMessage'
              type: array
            - type: 'null'
          title: Messages
        metadata:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Metadata
        type:
          type: string
          const: voicemail
          title: Type
          default: voicemail
        endCallAfterSpoken:
          anyOf:
            - type: boolean
            - type: 'null'
          title: Endcallafterspoken
        function:
          anyOf:
            - $ref: '#/components/schemas/FunctionDefinition-Input'
            - type: 'null'
      additionalProperties: false
      type: object
      title: VoicemailToolUpdate
    WebhookDeliveryResponse:
      properties:
        id:
          type: string
          title: Id
        organizationId:
          type: string
          title: Organizationid
        channelId:
          type: string
          title: Channelid
        eventId:
          type: string
          title: Eventid
        eventType:
          type: string
          title: Eventtype
        eventPayload:
          additionalProperties: true
          type: object
          title: Eventpayload
        webhookUrl:
          type: string
          title: Webhookurl
        status:
          $ref: '#/components/schemas/DeliveryStatus'
        attempts:
          type: integer
          title: Attempts
        maxAttempts:
          type: integer
          title: Maxattempts
        nextRetryAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Nextretryat
        deliveredAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Deliveredat
        lastResponseStatus:
          anyOf:
            - type: integer
            - type: 'null'
          title: Lastresponsestatus
        lastResponseBody:
          anyOf:
            - type: string
            - type: 'null'
          title: Lastresponsebody
        lastError:
          anyOf:
            - type: string
            - type: 'null'
          title: Lasterror
        lastAttemptAt:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Lastattemptat
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - organizationId
        - channelId
        - eventId
        - eventType
        - eventPayload
        - webhookUrl
        - status
        - attempts
        - maxAttempts
        - createdAt
        - updatedAt
      title: WebhookDeliveryResponse
    WhatsAppCategory:
      type: string
      enum:
        - marketing
        - utility
        - authentication
        - service
      title: WhatsAppCategory
      description: >-
        Meta's WhatsApp template category — analytics only (we don't bill by
        it).
    WhatsAppRegisterRequest:
      properties:
        pin:
          anyOf:
            - type: string
              pattern: ^\d{6}$
            - type: 'null'
          title: Pin
      additionalProperties: false
      type: object
      title: WhatsAppRegisterRequest
      description: |-
        Body for the WhatsApp register action.

        ``pin`` is optional — the Tech Provider manages the 2-step-verification
        PIN automatically. It's only supplied in the edge case where the number
        already had a PIN set and Meta asked us to provide it.
    WidgetAttachmentBlock:
      properties:
        fileId:
          type: string
          title: Fileid
        type:
          type: string
          enum:
            - image
            - audio
            - file
            - voice_note
          title: Type
        url:
          type: string
          title: Url
          description: Signed download URL — valid for 1 hour.
        mimeType:
          type: string
          title: Mimetype
        sizeBytes:
          type: integer
          title: Sizebytes
        filename:
          anyOf:
            - type: string
            - type: 'null'
          title: Filename
        durationSeconds:
          anyOf:
            - type: integer
            - type: 'null'
          title: Durationseconds
          description: Audio / voice notes only.
        thumbnailUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Thumbnailurl
          description: Images only.
      type: object
      required:
        - fileId
        - type
        - url
        - mimeType
        - sizeBytes
      title: WidgetAttachmentBlock
      description: An attachment on a widget visitor message.
    WidgetAuditEvent:
      properties:
        type:
          type: string
          title: Type
          description: >-
            One of the 22 catalog events: `visitor.created`,
            `visitor.identified`, `visitor.attribute.updated`,
            `contact.created`, `contact.merged`, `conversation.created`,
            `conversation.user.message`, `conversation.ai.message`,
            `conversation.closed`, `message.delivered`, `message.read`,
            `file.uploaded`, `survey.submitted`, `article.read`, `news.read`,
            `gdpr.export.completed`, `gdpr.deletion.completed`,
            `widget.webhook.dispatched`, `widget.webhook.reply_received`,
            `widget.webhook.reply_rejected`, `widget.webhook.dispatch_failed`,
            `widget.webhook.empty_reply` — plus the synthetic
            `widget.webhook.test` from the test endpoint.
        id:
          type: string
          title: Id
          description: Delivery id (`evt_…`) — dedupe key.
        createdAt:
          type: string
          title: Createdat
          description: ISO 8601.
        data:
          additionalProperties: true
          type: object
          title: Data
          description: Event-specific payload.
      type: object
      required:
        - type
        - id
        - createdAt
      title: WidgetAuditEvent
      description: Envelope for every widget audit-webhook delivery.
    WidgetCreateRequest:
      properties:
        name:
          anyOf:
            - type: string
              maxLength: 128
              minLength: 1
            - type: 'null'
          title: Name
        incomingMessageWebhookUrl:
          anyOf:
            - type: string
              maxLength: 2083
              minLength: 1
              format: uri
            - type: 'null'
          title: Incomingmessagewebhookurl
        seedStarterContent:
          type: boolean
          title: Seedstartercontent
          default: false
        primaryColor:
          anyOf:
            - type: string
              pattern: ^#[0-9a-fA-F]{6}$
            - type: 'null'
          title: Primarycolor
        backgroundColor:
          anyOf:
            - type: string
              pattern: ^#[0-9a-fA-F]{6}$
            - type: 'null'
          title: Backgroundcolor
        textColor:
          anyOf:
            - type: string
              pattern: ^#[0-9a-fA-F]{6}$
            - type: 'null'
          title: Textcolor
        linkColor:
          anyOf:
            - type: string
              pattern: ^#[0-9a-fA-F]{6}$
            - type: 'null'
          title: Linkcolor
        colorMode:
          anyOf:
            - type: string
              enum:
                - light
                - dark
                - auto
            - type: 'null'
          title: Colormode
        position:
          anyOf:
            - type: string
              enum:
                - bottom-right
                - bottom-left
                - top-right
                - top-left
                - right
                - left
            - type: 'null'
          title: Position
        launcherStyle:
          anyOf:
            - type: string
              enum:
                - icon
                - pill
                - custom
                - icon-only
                - icon-text
            - type: 'null'
          title: Launcherstyle
        launcherText:
          anyOf:
            - type: string
              maxLength: 64
            - type: 'null'
          title: Launchertext
        workspaceName:
          anyOf:
            - type: string
              maxLength: 64
            - type: 'null'
          title: Workspacename
        welcomeTitle:
          anyOf:
            - type: string
              maxLength: 128
            - type: 'null'
          title: Welcometitle
        welcomeSubtitle:
          anyOf:
            - type: string
              maxLength: 256
            - type: 'null'
          title: Welcomesubtitle
        replyTimeText:
          anyOf:
            - type: string
              maxLength: 128
            - type: 'null'
          title: Replytimetext
        awayMessage:
          anyOf:
            - type: string
              maxLength: 256
            - type: 'null'
          title: Awaymessage
        privacyPolicyUrl:
          anyOf:
            - type: string
              maxLength: 2083
              minLength: 1
              format: uri
            - type: 'null'
          title: Privacypolicyurl
        spacesEnabled:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Spacesenabled
        spacesOrder:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Spacesorder
        enabledLocales:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Enabledlocales
        defaultLocale:
          anyOf:
            - type: string
              maxLength: 8
              minLength: 2
            - type: 'null'
          title: Defaultlocale
        retentionDays:
          anyOf:
            - type: integer
              maximum: 3650
              minimum: 0
            - type: 'null'
          title: Retentiondays
        gdprMode:
          anyOf:
            - type: boolean
            - type: string
              enum:
                - soft
                - hard
            - type: 'null'
          title: Gdprmode
        allowedOrigins:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Allowedorigins
        monthlyMessageCap:
          anyOf:
            - type: integer
              minimum: 1
            - type: 'null'
          title: Monthlymessagecap
        features:
          anyOf:
            - $ref: '#/components/schemas/WidgetFeatures'
            - type: 'null'
        rateLimits:
          anyOf:
            - $ref: '#/components/schemas/WidgetRateLimits'
            - type: 'null'
        home:
          anyOf:
            - $ref: '#/components/schemas/WidgetHomeConfig'
            - type: 'null'
        voice:
          anyOf:
            - $ref: '#/components/schemas/WidgetVoiceConfig'
            - type: 'null'
      additionalProperties: false
      type: object
      title: WidgetCreateRequest
      description: |-
        Body for POST /widget/admin/widgets — minimum viable widget.

        ``name`` and ``incomingMessageWebhookUrl`` are optional at the wire
        level. When either is missing (or when ``seedStarterContent=True`` is
        set on its own for the seamless dashboard-onboarding flow), the
        backend fills them in: name auto-generates a unique
        ``"Untitled widget"`` per organization, webhook uses the reserved
        ``https://webhook.example.com/replace-me`` placeholder. The
        resulting widget starts in ``status=disabled`` so nothing goes live
        on a random visitor's site with placeholder credentials. The
        admin's setup checklist walks them through the three swaps
        (rename → real webhook → activate) on the detail page.

        API callers that want a lean widget without auto-generation still
        pass both fields explicitly + leave ``seedStarterContent=False``.
    WidgetCreateResponse:
      properties:
        id:
          type: string
          title: Id
          examples:
            - 7c7d7d4a-8d4f-412d-97b6-8c0fc172edba
        organizationId:
          type: string
          title: Organizationid
          examples:
            - 5a8c1f37-9b2d-4e64-a7c9-3f0e81d6b254
        tenantId:
          type: string
          title: Tenantid
          examples:
            - 9d2f4c81-3e57-4b6a-8f01-c5a7e92d1b43
        name:
          type: string
          title: Name
          examples:
            - Website support widget
        incomingMessageWebhookUrl:
          type: string
          title: Incomingmessagewebhookurl
          examples:
            - https://workflows.agenticflow.studio/api/v1/webhooks/Zn6Pfaohiua
        webhookReplyDeadlineSeconds:
          type: integer
          title: Webhookreplydeadlineseconds
        hasWebhookSecretRotation:
          type: boolean
          title: Haswebhooksecretrotation
          default: false
        hasIdentitySecretRotation:
          type: boolean
          title: Hasidentitysecretrotation
          default: false
        primaryColor:
          type: string
          title: Primarycolor
        backgroundColor:
          anyOf:
            - type: string
            - type: 'null'
          title: Backgroundcolor
        textColor:
          anyOf:
            - type: string
            - type: 'null'
          title: Textcolor
        linkColor:
          anyOf:
            - type: string
            - type: 'null'
          title: Linkcolor
        colorMode:
          type: string
          title: Colormode
        position:
          type: string
          title: Position
        verticalOffset:
          type: integer
          title: Verticaloffset
        horizontalOffset:
          type: integer
          title: Horizontaloffset
        zIndex:
          type: integer
          title: Zindex
        launcherStyle:
          type: string
          title: Launcherstyle
        launcherText:
          anyOf:
            - type: string
            - type: 'null'
          title: Launchertext
        launcherIconUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Launchericonurl
        workspaceLogoUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Workspacelogourl
        workspaceName:
          type: string
          title: Workspacename
        headerBackgroundType:
          type: string
          title: Headerbackgroundtype
        headerBackgroundImageUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Headerbackgroundimageurl
        welcomeTitle:
          type: string
          title: Welcometitle
        welcomeSubtitle:
          type: string
          title: Welcomesubtitle
        replyTimeText:
          type: string
          title: Replytimetext
        awayMessage:
          type: string
          title: Awaymessage
        privacyPolicyUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Privacypolicyurl
        spacesEnabled:
          items:
            type: string
          type: array
          title: Spacesenabled
        spacesOrder:
          items:
            type: string
          type: array
          title: Spacesorder
        enabledLocales:
          items:
            type: string
          type: array
          title: Enabledlocales
        defaultLocale:
          type: string
          title: Defaultlocale
        retentionDays:
          type: integer
          title: Retentiondays
        gdprMode:
          type: boolean
          title: Gdprmode
        features:
          $ref: '#/components/schemas/WidgetFeatures'
        rateLimits:
          anyOf:
            - $ref: '#/components/schemas/WidgetRateLimits'
            - type: 'null'
        allowedOrigins:
          items:
            type: string
          type: array
          title: Allowedorigins
        monthlyMessageCap:
          anyOf:
            - type: integer
            - type: 'null'
          title: Monthlymessagecap
        home:
          $ref: '#/components/schemas/WidgetHomeConfig'
        voice:
          $ref: '#/components/schemas/WidgetVoiceConfig'
        webPushVapidPublicKey:
          type: string
          title: Webpushvapidpublickey
        auditWebhookEndpoints:
          items:
            $ref: '#/components/schemas/AuditWebhookEndpointResponse'
          type: array
          title: Auditwebhookendpoints
        status:
          $ref: '#/components/schemas/WidgetStatus'
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
        identityHmacSecret:
          type: string
          title: Identityhmacsecret
        incomingMessageWebhookSecret:
          type: string
          title: Incomingmessagewebhooksecret
        installSnippet:
          type: string
          title: Installsnippet
      type: object
      required:
        - id
        - organizationId
        - tenantId
        - name
        - incomingMessageWebhookUrl
        - webhookReplyDeadlineSeconds
        - primaryColor
        - colorMode
        - position
        - verticalOffset
        - horizontalOffset
        - zIndex
        - launcherStyle
        - workspaceName
        - headerBackgroundType
        - welcomeTitle
        - welcomeSubtitle
        - replyTimeText
        - awayMessage
        - spacesEnabled
        - spacesOrder
        - enabledLocales
        - defaultLocale
        - retentionDays
        - gdprMode
        - features
        - allowedOrigins
        - home
        - voice
        - webPushVapidPublicKey
        - status
        - createdAt
        - updatedAt
        - identityHmacSecret
        - incomingMessageWebhookSecret
        - installSnippet
      title: WidgetCreateResponse
      description: |-
        Body returned by POST /widget/admin/widgets — same shape as
        WidgetResponse plus the two secrets revealed ONCE.

        Admin must capture both. After this call, the secrets are stored
        encrypted; only ``regenerate`` returns them again.
    WidgetDeletedResponse:
      properties:
        id:
          type: string
          title: Id
          description: ID of the deleted resource.
        deleted:
          type: boolean
          title: Deleted
          description: Always ``true`` — the resource is soft-deleted.
          default: true
      additionalProperties: false
      type: object
      required:
        - id
      title: WidgetDeletedResponse
      description: |-
        Tight, named response for soft-delete endpoints.

        Previously every DELETE route returned ``ApiEnvelope[dict]`` which
        rendered as an untyped object in OpenAPI. This shape replaces the
        ``dict`` so generated clients see the actual contract.
    WidgetFeatures:
      properties:
        fileAttachmentsEnabled:
          type: boolean
          title: Fileattachmentsenabled
          default: true
        imageAttachmentsEnabled:
          type: boolean
          title: Imageattachmentsenabled
          default: true
        voiceNotesEnabled:
          type: boolean
          title: Voicenotesenabled
          default: true
        emojiPickerEnabled:
          type: boolean
          title: Emojipickerenabled
          default: true
        popoutEnabled:
          type: boolean
          title: Popoutenabled
          default: true
        transcriptDownloadEnabled:
          type: boolean
          title: Transcriptdownloadenabled
          default: true
        conversationHistoryEnabled:
          type: boolean
          title: Conversationhistoryenabled
          default: true
        typingIndicatorsEnabled:
          type: boolean
          title: Typingindicatorsenabled
          default: true
        readReceiptsEnabled:
          type: boolean
          title: Readreceiptsenabled
          default: true
        csatSurveyEnabled:
          type: boolean
          title: Csatsurveyenabled
          default: true
        articlesEnabled:
          type: boolean
          title: Articlesenabled
          default: true
        newsEnabled:
          type: boolean
          title: Newsenabled
          default: true
        surveysEnabled:
          type: boolean
          title: Surveysenabled
          default: true
        pushNotificationsEnabled:
          type: boolean
          title: Pushnotificationsenabled
          default: true
      additionalProperties: false
      type: object
      title: WidgetFeatures
      description: |-
        Per-widget feature toggles.

        Admin enables or disables features at widget creation; can update
        later via PATCH. Both layers enforce:

          - Server: endpoints honour toggles (``POST /widget/files`` returns
            403 with ``feature_disabled`` if ``fileAttachmentsEnabled=False``,
            ``POST /widget/messages`` strips attachments if disabled, etc.)
          - Widget bundle: UI hides controls (paperclip not rendered if
            attachments off, voice mic icon hidden if voice notes off)

        All default ``True`` — the lean defaults match Intercom-class
        expectations. Admin disables individual features as needed.
    WidgetHistoryEntryBlock:
      properties:
        id:
          type: string
          title: Id
        senderType:
          type: string
          enum:
            - visitor
            - ai
            - operator
            - system
          title: Sendertype
        content:
          type: string
          title: Content
        attachments:
          items:
            $ref: '#/components/schemas/WidgetAttachmentBlock'
          type: array
          title: Attachments
        createdAt:
          type: string
          title: Createdat
      type: object
      required:
        - id
        - senderType
        - content
        - createdAt
      title: WidgetHistoryEntryBlock
    WidgetHomeConfig:
      properties:
        cardsEnabled:
          type: boolean
          title: Cardsenabled
          default: false
        cardsSectionLabel:
          type: string
          maxLength: 48
          title: Cardssectionlabel
          default: Contact us
        cardsPosition:
          type: string
          enum:
            - above_cta
            - below_cta
            - bottom
          title: Cardsposition
          default: below_cta
        cards:
          items:
            $ref: '#/components/schemas/HomeCard'
          type: array
          maxItems: 8
          title: Cards
      additionalProperties: false
      type: object
      title: WidgetHomeConfig
      description: |-
        Home-space configuration — currently just the "Contact us" cards row.

        ``cardsEnabled`` defaults ``False`` so every existing widget renders
        exactly as before (opt-in per widget). When enabled, the bundle
        renders a section on the Home space at ``cardsPosition`` showing every
        enabled card in ``cards`` order.
    WidgetIncomingMessageBlock:
      properties:
        id:
          type: string
          title: Id
          description: Message id.
        content:
          type: string
          title: Content
          description: The visitor's text.
        attachments:
          items:
            $ref: '#/components/schemas/WidgetAttachmentBlock'
          type: array
          title: Attachments
        createdAt:
          type: string
          title: Createdat
          description: ISO 8601.
      type: object
      required:
        - id
        - content
        - createdAt
      title: WidgetIncomingMessageBlock
    WidgetMessageIncomingEvent:
      properties:
        widgetId:
          type: string
          title: Widgetid
        sessionId:
          type: string
          title: Sessionid
          description: The conversation (chat session) id.
        message:
          $ref: '#/components/schemas/WidgetIncomingMessageBlock'
        visitor:
          $ref: '#/components/schemas/WidgetVisitorBlock'
        conversationHistory:
          items:
            $ref: '#/components/schemas/WidgetHistoryEntryBlock'
          type: array
          title: Conversationhistory
          description: Prior turns only — the current message is not included.
        prefilledContext:
          additionalProperties: true
          type: object
          title: Prefilledcontext
          description: Context your page attached to the conversation before the message.
        replyToken:
          type: string
          title: Replytoken
          description: >-
            Bearer token for POST /widget/reply — bound to this conversation; up
            to 20 reply messages.
        replyApiUrl:
          type: string
          title: Replyapiurl
          description: Absolute URL of the reply endpoint.
        replyDeadline:
          type: string
          title: Replydeadline
          description: ISO 8601 — the token stops working after this instant.
      type: object
      required:
        - widgetId
        - sessionId
        - message
        - visitor
        - replyToken
        - replyApiUrl
        - replyDeadline
      title: WidgetMessageIncomingEvent
      description: |-
        ``widget.message.incoming`` — one POST per visitor message to the
        widget's ``incomingMessageWebhookUrl``.
    WidgetMessageSyncResponse:
      properties:
        messages:
          items:
            $ref: '#/components/schemas/WidgetSyncReplyMessage'
          type: array
          maxItems: 20
          minItems: 1
          title: Messages
      type: object
      required:
        - messages
      title: WidgetMessageSyncResponse
      description: |-
        Respond ``200`` with this body to reply inline (sync mode) — or
        ``202`` with an empty body and use the reply API asynchronously.
    WidgetRateLimits:
      properties:
        authMintsPerMinutePerIp:
          type: integer
          maximum: 10000
          minimum: 1
          title: Authmintsperminuteperip
          default: 50
        authMintsPerMinutePerWidget:
          type: integer
          maximum: 100000
          minimum: 1
          title: Authmintsperminuteperwidget
          default: 200
        messagesPerMinutePerVisitor:
          type: integer
          maximum: 10000
          minimum: 1
          title: Messagesperminutepervisitor
          default: 30
        filesPerHourPerVisitor:
          type: integer
          maximum: 10000
          minimum: 1
          title: Filesperhourpervisitor
          default: 50
        pollsPerMinutePerVisitor:
          type: integer
          maximum: 600
          minimum: 1
          title: Pollsperminutepervisitor
          default: 30
        callStartsPerHourPerVisitor:
          type: integer
          maximum: 1000
          minimum: 1
          title: Callstartsperhourpervisitor
          default: 5
      additionalProperties: false
      type: object
      title: WidgetRateLimits
      description: |-
        Per-widget rate-limit overrides. Defaults match the platform
        fallbacks in ``src.widget.services.rate_limit_service``.

        Admin can tighten or loosen any limit per widget for advanced cases
        (e.g. a high-traffic public widget might need a higher mint cap,
        or an internal pilot widget might bound runaway clients with a
        tighter per-visitor message cap).
    WidgetResponse:
      properties:
        id:
          type: string
          title: Id
          examples:
            - 7c7d7d4a-8d4f-412d-97b6-8c0fc172edba
        organizationId:
          type: string
          title: Organizationid
          examples:
            - 5a8c1f37-9b2d-4e64-a7c9-3f0e81d6b254
        tenantId:
          type: string
          title: Tenantid
          examples:
            - 9d2f4c81-3e57-4b6a-8f01-c5a7e92d1b43
        name:
          type: string
          title: Name
          examples:
            - Website support widget
        incomingMessageWebhookUrl:
          type: string
          title: Incomingmessagewebhookurl
          examples:
            - https://workflows.agenticflow.studio/api/v1/webhooks/Zn6Pfaohiua
        webhookReplyDeadlineSeconds:
          type: integer
          title: Webhookreplydeadlineseconds
        hasWebhookSecretRotation:
          type: boolean
          title: Haswebhooksecretrotation
          default: false
        hasIdentitySecretRotation:
          type: boolean
          title: Hasidentitysecretrotation
          default: false
        primaryColor:
          type: string
          title: Primarycolor
        backgroundColor:
          anyOf:
            - type: string
            - type: 'null'
          title: Backgroundcolor
        textColor:
          anyOf:
            - type: string
            - type: 'null'
          title: Textcolor
        linkColor:
          anyOf:
            - type: string
            - type: 'null'
          title: Linkcolor
        colorMode:
          type: string
          title: Colormode
        position:
          type: string
          title: Position
        verticalOffset:
          type: integer
          title: Verticaloffset
        horizontalOffset:
          type: integer
          title: Horizontaloffset
        zIndex:
          type: integer
          title: Zindex
        launcherStyle:
          type: string
          title: Launcherstyle
        launcherText:
          anyOf:
            - type: string
            - type: 'null'
          title: Launchertext
        launcherIconUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Launchericonurl
        workspaceLogoUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Workspacelogourl
        workspaceName:
          type: string
          title: Workspacename
        headerBackgroundType:
          type: string
          title: Headerbackgroundtype
        headerBackgroundImageUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Headerbackgroundimageurl
        welcomeTitle:
          type: string
          title: Welcometitle
        welcomeSubtitle:
          type: string
          title: Welcomesubtitle
        replyTimeText:
          type: string
          title: Replytimetext
        awayMessage:
          type: string
          title: Awaymessage
        privacyPolicyUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Privacypolicyurl
        spacesEnabled:
          items:
            type: string
          type: array
          title: Spacesenabled
        spacesOrder:
          items:
            type: string
          type: array
          title: Spacesorder
        enabledLocales:
          items:
            type: string
          type: array
          title: Enabledlocales
        defaultLocale:
          type: string
          title: Defaultlocale
        retentionDays:
          type: integer
          title: Retentiondays
        gdprMode:
          type: boolean
          title: Gdprmode
        features:
          $ref: '#/components/schemas/WidgetFeatures'
        rateLimits:
          anyOf:
            - $ref: '#/components/schemas/WidgetRateLimits'
            - type: 'null'
        allowedOrigins:
          items:
            type: string
          type: array
          title: Allowedorigins
        monthlyMessageCap:
          anyOf:
            - type: integer
            - type: 'null'
          title: Monthlymessagecap
        home:
          $ref: '#/components/schemas/WidgetHomeConfig'
        voice:
          $ref: '#/components/schemas/WidgetVoiceConfig'
        webPushVapidPublicKey:
          type: string
          title: Webpushvapidpublickey
        auditWebhookEndpoints:
          items:
            $ref: '#/components/schemas/AuditWebhookEndpointResponse'
          type: array
          title: Auditwebhookendpoints
        status:
          $ref: '#/components/schemas/WidgetStatus'
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
        - id
        - organizationId
        - tenantId
        - name
        - incomingMessageWebhookUrl
        - webhookReplyDeadlineSeconds
        - primaryColor
        - colorMode
        - position
        - verticalOffset
        - horizontalOffset
        - zIndex
        - launcherStyle
        - workspaceName
        - headerBackgroundType
        - welcomeTitle
        - welcomeSubtitle
        - replyTimeText
        - awayMessage
        - spacesEnabled
        - spacesOrder
        - enabledLocales
        - defaultLocale
        - retentionDays
        - gdprMode
        - features
        - allowedOrigins
        - home
        - voice
        - webPushVapidPublicKey
        - status
        - createdAt
        - updatedAt
      title: WidgetResponse
      description: >-
        Body returned by GET /widget/admin/widgets/{id} and list/PATCH.


        Secrets (identityHmacSecretEncrypted +
        incomingMessageWebhookSecretEncrypted

        + Web Push private VAPID key) are NEVER included — they're revealed

        one-time on create / regenerate via a separate response shape.
    WidgetSecretRotationResponse:
      properties:
        newSecret:
          type: string
          title: Newsecret
        previousValidUntil:
          type: string
          format: date-time
          title: Previousvaliduntil
      additionalProperties: false
      type: object
      required:
        - newSecret
        - previousValidUntil
      title: WidgetSecretRotationResponse
      description: |-
        Body returned by POST .../rotate-identity-secret and
        .../rotate-webhook-secret. New secret revealed ONCE; old stays
        valid until ``previousValidUntil``.
    WidgetStatus:
      type: string
      enum:
        - active
        - disabled
        - deleted
      title: WidgetStatus
      description: Widget lifecycle.
    WidgetSyncReplyMessage:
      properties:
        content:
          type: string
          maxLength: 32000
          title: Content
        type:
          type: string
          enum:
            - interstitial
            - final
          title: Type
          description: '`interstitial` keeps the widget polling; `final` closes the turn.'
          default: final
        richContent:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Richcontent
          description: Optional structured content (cards, buttons).
      type: object
      required:
        - content
      title: WidgetSyncReplyMessage
    WidgetUpdateRequest:
      properties:
        name:
          anyOf:
            - type: string
              maxLength: 128
              minLength: 1
            - type: 'null'
          title: Name
        incomingMessageWebhookUrl:
          anyOf:
            - type: string
              maxLength: 2083
              minLength: 1
              format: uri
            - type: 'null'
          title: Incomingmessagewebhookurl
        webhookReplyDeadlineSeconds:
          anyOf:
            - type: integer
              maximum: 604800
              minimum: 60
            - type: 'null'
          title: Webhookreplydeadlineseconds
        primaryColor:
          anyOf:
            - type: string
              pattern: ^#[0-9a-fA-F]{6}$
            - type: 'null'
          title: Primarycolor
        backgroundColor:
          anyOf:
            - type: string
              pattern: ^#[0-9a-fA-F]{6}$
            - type: 'null'
          title: Backgroundcolor
        textColor:
          anyOf:
            - type: string
              pattern: ^#[0-9a-fA-F]{6}$
            - type: 'null'
          title: Textcolor
        linkColor:
          anyOf:
            - type: string
              pattern: ^#[0-9a-fA-F]{6}$
            - type: 'null'
          title: Linkcolor
        colorMode:
          anyOf:
            - type: string
              enum:
                - light
                - dark
                - auto
            - type: 'null'
          title: Colormode
        position:
          anyOf:
            - type: string
              enum:
                - bottom-right
                - bottom-left
                - top-right
                - top-left
                - right
                - left
            - type: 'null'
          title: Position
        verticalOffset:
          anyOf:
            - type: integer
              maximum: 400
              minimum: 0
            - type: 'null'
          title: Verticaloffset
        horizontalOffset:
          anyOf:
            - type: integer
              maximum: 400
              minimum: 0
            - type: 'null'
          title: Horizontaloffset
        zIndex:
          anyOf:
            - type: integer
              maximum: 2147483647
              minimum: 0
            - type: 'null'
          title: Zindex
        launcherStyle:
          anyOf:
            - type: string
              enum:
                - icon
                - pill
                - custom
                - icon-only
                - icon-text
            - type: 'null'
          title: Launcherstyle
        launcherText:
          anyOf:
            - type: string
              maxLength: 64
            - type: 'null'
          title: Launchertext
        launcherIconUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Launchericonurl
        workspaceLogoUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Workspacelogourl
        workspaceName:
          anyOf:
            - type: string
              maxLength: 64
            - type: 'null'
          title: Workspacename
        headerBackgroundType:
          anyOf:
            - type: string
              enum:
                - solid
                - gradient
                - image
            - type: 'null'
          title: Headerbackgroundtype
        headerBackgroundImageUrl:
          anyOf:
            - type: string
            - type: 'null'
          title: Headerbackgroundimageurl
        welcomeTitle:
          anyOf:
            - type: string
              maxLength: 128
            - type: 'null'
          title: Welcometitle
        welcomeSubtitle:
          anyOf:
            - type: string
              maxLength: 256
            - type: 'null'
          title: Welcomesubtitle
        replyTimeText:
          anyOf:
            - type: string
              maxLength: 128
            - type: 'null'
          title: Replytimetext
        awayMessage:
          anyOf:
            - type: string
              maxLength: 256
            - type: 'null'
          title: Awaymessage
        privacyPolicyUrl:
          anyOf:
            - type: string
              maxLength: 2083
              minLength: 1
              format: uri
            - type: 'null'
          title: Privacypolicyurl
        spacesEnabled:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Spacesenabled
        spacesOrder:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Spacesorder
        enabledLocales:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Enabledlocales
        defaultLocale:
          anyOf:
            - type: string
              maxLength: 8
              minLength: 2
            - type: 'null'
          title: Defaultlocale
        retentionDays:
          anyOf:
            - type: integer
              maximum: 3650
              minimum: 0
            - type: 'null'
          title: Retentiondays
        gdprMode:
          anyOf:
            - type: boolean
            - type: string
              enum:
                - soft
                - hard
            - type: 'null'
          title: Gdprmode
        allowedOrigins:
          anyOf:
            - items:
                type: string
              type: array
            - type: 'null'
          title: Allowedorigins
        monthlyMessageCap:
          anyOf:
            - type: integer
              minimum: 1
            - type: 'null'
          title: Monthlymessagecap
        features:
          anyOf:
            - $ref: '#/components/schemas/WidgetFeatures'
            - type: 'null'
        rateLimits:
          anyOf:
            - $ref: '#/components/schemas/WidgetRateLimits'
            - type: 'null'
        home:
          anyOf:
            - $ref: '#/components/schemas/WidgetHomeConfig'
            - type: 'null'
        voice:
          anyOf:
            - $ref: '#/components/schemas/WidgetVoiceConfig'
            - type: 'null'
        status:
          anyOf:
            - type: string
              enum:
                - active
                - disabled
            - type: 'null'
          title: Status
      additionalProperties: false
      type: object
      title: WidgetUpdateRequest
      description: Body for PATCH /widget/admin/widgets/{id} — any subset.
    WidgetVisitorBlock:
      properties:
        id:
          type: string
          title: Id
          description: Visitor id (stable per browser).
        identifier:
          anyOf:
            - type: string
            - type: 'null'
          title: Identifier
          description: Your user id — set only for HMAC-verified identities.
        verified:
          type: boolean
          title: Verified
          description: True only when the identifier passed HMAC verification.
        email:
          anyOf:
            - type: string
            - type: 'null'
          title: Email
        name:
          anyOf:
            - type: string
            - type: 'null'
          title: Name
        customAttributes:
          additionalProperties: true
          type: object
          title: Customattributes
      type: object
      required:
        - id
        - verified
      title: WidgetVisitorBlock
    WidgetVoiceConfig:
      properties:
        enabled:
          type: boolean
          title: Enabled
          default: false
        voiceAssistantId:
          anyOf:
            - type: string
            - type: 'null'
          title: Voiceassistantid
        buttonLabel:
          type: string
          maxLength: 48
          minLength: 1
          title: Buttonlabel
          default: Call us
      additionalProperties: false
      type: object
      title: WidgetVoiceConfig
      description: |-
        Voice-calling configuration — in-browser WebRTC calls to a
        configured voice assistant.

        ``enabled`` defaults ``False`` so every existing widget renders
        exactly as before (opt-in per widget). ``voiceAssistantId`` MUST
        reference an Assistant in the widget's own organisation — validated
        at the admin create/update layer (this schema has no DB access).

        The assistant id is admin-facing configuration only: it is NEVER
        exposed on the public brand payload (visitors get just ``enabled``
        + ``buttonLabel``); ``POST /widget/call`` resolves it server-side.
    Xai:
      properties:
        provider:
          type: string
          const: xai
          title: Provider
        model:
          type: string
          enum:
            - grok-4.20-0309-non-reasoning
            - grok-4.20-0309-reasoning
            - grok-4.20-multi-agent-0309
            - grok-4-1-fast-non-reasoning
            - grok-4-1-fast-reasoning
          title: Model
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        temperature:
          anyOf:
            - type: number
              maximum: 2
              minimum: 0
            - type: 'null'
          title: Temperature
        maxTokens:
          anyOf:
            - type: integer
              maximum: 16384
              minimum: 1
            - type: 'null'
          title: Maxtokens
        topP:
          anyOf:
            - type: number
              maximum: 1
              minimum: 0
            - type: 'null'
          title: Topp
        frequencyPenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Frequencypenalty
        presencePenalty:
          anyOf:
            - type: number
              maximum: 2
              minimum: -2
            - type: 'null'
          title: Presencepenalty
      additionalProperties: false
      type: object
      required:
        - provider
        - model
      title: Xai
      description: xAI Grok LLM (pipeline mode, OpenAI-compatible).
    XaiRealtime:
      properties:
        provider:
          type: string
          const: xai-realtime
          title: Provider
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        voice:
          anyOf:
            - type: string
              enum:
                - Ara
                - Eve
                - Leo
                - Rex
                - Sal
            - type: 'null'
          title: Voice
        systemPrompt:
          anyOf:
            - type: string
            - type: 'null'
          title: Systemprompt
        turnDetection:
          anyOf:
            - {}
            - type: 'null'
          title: Turndetection
        maxSessionDuration:
          anyOf:
            - type: number
              maximum: 1800
              minimum: 60
            - type: 'null'
          title: Maxsessionduration
      additionalProperties: false
      type: object
      required:
        - provider
      title: XaiRealtime
      description: |-
        xAI Grok Voice Agent — 5 static voices, model hardcoded by plugin.

        The xAI ``RealtimeModel`` constructor only accepts ``voice``,
        ``api_key``, ``turn_detection`` and ``max_session_duration`` — no
        ``temperature`` or ``language`` knob. Those fields are intentionally
        absent from this variant so the API edge rejects them rather than
        silently dropping them. ``systemPrompt`` is still accepted because
        the agent layer (``DynamicAssistant``) wires it as the agent's
        instructions independently of the plugin.
    XaiTTS:
      properties:
        provider:
          type: string
          const: xai-tts
          title: Provider
        voiceId:
          type: string
          enum:
            - Ara
            - Eve
            - Leo
            - Rex
            - Sal
          title: Voiceid
        model:
          anyOf:
            - type: string
            - type: 'null'
          title: Model
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
      additionalProperties: false
      type: object
      required:
        - provider
        - voiceId
      title: XaiTTS
      description: xAI Grok TTS — 5 static voices.
    src__api__schemas__assistant__ServerConfigInfo:
      properties:
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
        secret:
          anyOf:
            - type: string
            - type: 'null'
          title: Secret
        timeoutSeconds:
          type: integer
          title: Timeoutseconds
          default: 20
      type: object
      title: ServerConfigInfo
      description: >-
        Webhook server config — response shape.


        Deliberately NOT validated: see
        ``ServerConfigSchema._no_internal_target``.
  securitySchemes:
    apiKeyAuth:
      type: apiKey
      name: X-Api-Key
      in: header
      description: >-
        Workspace API key. Generate one from the dashboard's **API Keys** page
        (Workspace → Settings → API Keys). Send it as the ``X-Api-Key`` header
        on every request. Keys are scoped to a single workspace; rotate or
        revoke them at any time without affecting other workspaces.
security:
  - apiKeyAuth: []
servers:
  - url: https://api.agenticflow.studio
    description: Production
